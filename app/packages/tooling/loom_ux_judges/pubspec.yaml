name: loom_ux_judges
description: UX gate judge tools for Loom Communities build phases.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  archive: ^4.0.9
  image: ^4.7.2
  loom_workflow_engine:
    path: ../../core/loom_workflow_engine

dev_dependencies:
  test: ^1.25.0
