# feature_creator_launch Charter

## Scope

Creator launch and audience re-acquisition workflow: announcement templates, capture links, link-in-bio preview, QR payload, verified external account context, and simulated cross-post status.

## Allowed contracts

- Creator Announcement API
- Fan Follow Capture API
- External Account Link API
- Cross-Posting API
- Creator Metadata API

## Owned screens

Defined in Phase 11.

## Forbidden imports

Other features, `loom_fake_backend`, `loom_local_store`, and `loom_seed_data`.

Phase 12 owns fan capture landing and starter-pack onboarding UX.
