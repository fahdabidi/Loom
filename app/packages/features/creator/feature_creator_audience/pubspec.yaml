name: feature_creator_audience
description: Creator audience feature skeleton.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  loom_api_contracts:
    path: ../../../core/loom_api_contracts
  loom_app_shell:
    path: ../../../core/loom_app_shell
  loom_design_system:
    path: ../../../core/loom_design_system
