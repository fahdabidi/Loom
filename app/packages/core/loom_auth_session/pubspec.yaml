name: loom_auth_session
description: Persistent Keycloak token-session core for Loom Flutter clients.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  flutter_web_auth_2: ^5.1.0
  http: ^1.6.0
  openid_client: ^0.4.10+1
  web: ^1.1.1

dev_dependencies:
  test: ^1.25.0
  webdriver: ^3.1.0
