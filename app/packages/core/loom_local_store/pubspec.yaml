name: loom_local_store
description: Local demo persistence boundary for Loom fake backends.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  drift: ^2.28.2
  loom_seed_data:
    path: ../loom_seed_data

dev_dependencies:
  build_runner: ^2.6.0
  drift_dev: ^2.28.2
