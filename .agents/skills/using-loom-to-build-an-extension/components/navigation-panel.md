# Navigation Panel

Use `NavigationPanelProps` to preserve Loom-required navigation.

## Extension Use

- Always expose Messages and Connections.
- Add extension routes below shell-owned required entries.
- Keep navigation labels stable for workflow tests.

## Validation

- `vt_navigation-panel_messages-connections` proves required entries exist.
