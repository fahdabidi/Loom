# Connections Shell

Use `ConnectionsShellProps` for invite and block-aware connection UX.

## Extension Use

- Respect blocked passport ids.
- Route invite decisions through Loom connections contracts.
- Keep Messages and Connections reachable from navigation.

## Validation

- `vt_connections-shell_invite-blocked` proves blocked invite behavior.
