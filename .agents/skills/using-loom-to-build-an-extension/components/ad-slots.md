# Ad Slots

Use `AdSlotProps` for shell and stream ad surfaces.

## Extension Use

- Preserve required top banner and stream ad surfaces.
- Render fill, no-fill, and sensitive no-fill states.
- Do not let extensions remove shell-owned ad slots.

## Validation

- `vt_app-shell_ad-slots` proves the top banner contract.
