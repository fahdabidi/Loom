# Phase B13 - API Review

B13 adds no new Loom backend API.

The Garden Club UI maps to existing Loom workflow surfaces:

- Events API semantics for Garden event RSVP.
- Forms/workflows semantics for plant exchange submission.
- Export metadata semantics for `garden_event` and `plant_exchange` schemas.

The evidence manifest records the UI assertions and screenshot paths for each workflow.
