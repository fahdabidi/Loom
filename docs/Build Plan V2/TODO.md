# Live TODO — cross-effort rollup

**This is an index, not a memory.** One line per open item, newest/most-relevant first, each linking to its
full entry (context, source, exact wording) in the owning tracker's own `## 8. Live TODO / Next Steps Queue`
section (`docs/Build Plan V2/Tools/reference-tracker-template.md` defines that section's shape and the fixed
tag taxonomy used everywhere below). Never write item detail directly here — if you're about to write more
than one line for an item, that content belongs in the tracker, not here.

**How this file gets updated:** propose-then-promote, never a dispatched agent editing this file directly.
An Implementation Agent proposes next steps in its own STATUS.md (`## Proposed next steps`); during your own
independent-verification step, you review that proposal, write the confirmed items into the owning tracker's
§8, and add/remove the matching one-line rollup here. Every `call_*.sh` dispatch script prints a reminder
banner at completion so this step is never silently skipped — see `docs/Build Plan V2/Tools/README.md`'s core
pipeline step 5.5.

**Use the Root Cause Agent (`data/call_root_cause_agent.sh`) for two jobs, across every tracker this
file rolls up, not only the one it's traditionally been used for.** (1) To scope a non-trivial change
*before* writing an implementation ticket — what it actually touches, the real mechanism, what it would
break — rather than writing a ticket from an assumed mechanism and finding out it was wrong a full
implementation round later. (2) Its standing job: anything resisting normal investigation — a stubborn
defect, a stalled walkthrough, an unclear suite failure — goes to it before spending more implementation
rounds guessing. Never write a ticket that asserts a mechanism nobody has actually traced.

**Verification sweep 2026-08-24.** Every checkable claim below was re-measured against the working tree
rather than carried forward, and nine items closed on measurement alone — they had been fixed by later work
and never struck. Suite baselines quoted inside older tracker entries are stale by construction; the current
numbers are judges **432**, app shell **271**, engine **281 (+3 skipped)**, demo **156**, zero failures on
Windows.

**Backend-integration sweep 2026-08-26/27.** The app now runs on the real backends by default and the
document library exists end to end. Current baselines: judges **434**, app shell **307**, engine **299
(+4 skipped)**, workflow service **75 (+5)**, demo **160**, app-access **49**.

**A pattern this sweep kept finding: declared and never read.** Seven constants, fields or code paths
existed and nothing consumed them — `sharingGrantable`, a state `readGuard` under a non-`guarded`
default, `explicitReaderFanIds` (`writableBy: None`), the renderer contract's `calendar`
surface-family, `SurfaceQuery.dateWindowStart/End`, the engine's `workflowType == 'notification'`
delivery branch, and `dueNotifications` itself. None failed anything. **A grep for a declaration
proves it exists, not that anything reads it** — check the consumer.

**A measurement caveat worth carrying.** Two sweeps run that day were wrong in the same way: they read an
artifact instead of the code path that consumes it. `grep actorIdentities` over the packages returned zero
and was read as "packages declare no identities" — they are *derived* from `roles[]` at load. A role
cross-check that ignored `_roleIdsForB25Role`'s synonym fallbacks reported 11 blocked rows when the real
number is 5. Both had been "validated", but only against cases of one kind. **Validate a sweep against a
known answer for every kind of thing it claims to find, not just one.**

**Restructured 2026-09-12.** This file had drifted into the opposite of its own stated design:
87 closed rows against 36 open ones, multi-paragraph entries where the header demands one line,
and two tags (`needs-spec-decision`, `needs-user-decision`) outside the fixed taxonomy. The order
below is deliberate — **what unblocks the production bar first**. Nothing was deleted: every open
row's full text is in [TODO-open-detail.md](TODO-open-detail.md), and every closed row is in
[TODO-closed.md](TODO-closed.md).

**The bar, re-measured 2026-09-12 rather than recalled** (`check_b25_status.sh`): **72 real rows ·
walkthrough half 10 · judge half 57 · both halves 10**. So 47 rows already hold a judge artifact and
lack only a live walkthrough — class 4 below is the single biggest lever, and nothing in class 3 or 5
moves the bar until those walkthroughs run.

**Off-taxonomy tags retired.** `needs-spec-decision` and `needs-user-decision` were never in the
template's fixed set. Both are now `blocked` — the template's own tag for "can't proceed until a named
external thing resolves" — with the decision stated as one answerable question in class 2, so they can
be batched rather than rediscovered one at a time.

## Open

### 1. Release blockers

- [ ] `blocked` — **THE "BAR" NUMBER IS COVERAGE, NOT PASSES, AND ITS JUDGE HALF RESTS ON JULY ARTIFACTS THAT MOSTLY FAILED. I have been quoting it without that caveat all session; this row corrects that.** Audited 2026-09-12 by reading every artifact `check_b25_status.sh` counts. **The tool says so itself** in its own output — *"this counts rows with a judge artifact NAMING the workflow. It does not yet check that artifact's verdict for that row, so treat 57 and 44 as coverage, not as passes."* I read that line repeatedly and kept reporting the headline number anyway. **CORRECTED an hour later, and my first framing was wrong in a way worth recording.** I counted artifact statuses (10 fail, 5 pass) and implied the judge half was failing. **It is not failing — it is STALE**, and counting artifacts rather than taking the latest is the same "a count measures the thing counted" error this repo already records. Built `Tools/code/check_b25_judge_verdicts.py` to measure it properly, and it took **three iterations, the first two of which each produced the number I already believed**: v1 asked "is there SOME all-pass artifact" and returned 66/66 (useless — across 41 artifacts from a long iteration campaign nearly every row has a lucky pass); v2 used latest-wins and returned 66 pass / 0 fail, which looked like a bug and was not. **The truth:** the July campaign genuinely ended on `pass-39` — **204 screens, all pass** — and the failing artifacts are earlier iterations of that same campaign. **So every one of the 66 rows' latest verdict is a PASS, all from that ONE artifact, dated 2026-07-02 against `appCommitSha b3c7991`. Current HEAD is `dc4aa325`.** Since that judgement the packages have been regenerated, the app shell and engine have changed repeatedly (twice today alone), three workflow-service images have shipped, and the real row set moved 79 -> 77 -> 72. **The disqualifier is age, not verdict — and the whole judge half hangs on a single artifact from a single day.** The walkthrough half already solves exactly this: every manifest records `skillVersion` + `sha256` so a regeneration invalidates it. The judge half records `appCommitSha` and **nothing checks it** — until now. **The figure worth quoting is verdict-checked AND current**, which today is **zero**. Superseded detail below. ~~What the 27 counted artifacts actually contain:~~

  | | count |
  |---|---:|
  | `status: fail` | **10** |
  | `status: pass` | 5 |
  | no status field | 6 |
  | unparsed JSON | 5 |
  | `production-ux-review-complete` | 1 |

**Every artifact carrying a date is from 2026-07-01 to 2026-07-03** — two and a half months old. They predate this session's two app-shell fixes, the two engine fixes, three workflow-service deploys, and every package regeneration since. `pass-32` and `pass-33` each reviewed 195 screen rows and **both say `fail`**. **So the honest statement of today's result is:** 45 rows have a verified live write (each one confirmed by me in Postgres), and 44 of those are *named somewhere* in a judge artifact. **The number of rows with a live write AND a PASSING, CURRENT judge verdict is unknown and is certainly lower.** **Why this was invisible:** the walkthrough half is re-derived constantly — every row I banked today was checked against the database — while the judge half is a join against files nobody re-reads. A denominator nobody re-derives is exactly the failure this repo has already recorded twice, and this is the same shape one layer up. **What would fix it, in order:** (1) make `check_b25_status.sh` read each artifact's **verdict for that row**, not merely its presence, and refuse to print a combined figure it cannot verdict-check — the tool already refuses to print a combined figure in another case, so the precedent exists; (2) treat artifact **age** as disqualifying, since a July judgement says nothing about a September build — the walkthrough half already carries `skillVersion` + `sha256` for exactly this reason and the judge half carries nothing equivalent; (3) only then re-run judges, which is blocked on the capture-host question filed above. **Do not quote a completion figure without stating which half it measures.**
- [ ] `blocked` — **The B25 capture pipeline cannot run from the VM: `flutter drive` never connects to the device VM service. This is a regression from the WSL2 → VirtualBox migration that has been invisible because no canonical capture has been attempted since.** Diagnosed 2026-09-12 by running a minimal `targeted-precheck` rather than assuming — which is exactly what that mode is for. **Everything works except the last hop:** the build succeeds, the APK installs, and the app runs on the emulator (`pidof` returned 11630). `flutter drive` then retries the VM Service connection forever — *"Connecting to the VM Service is taking longer than expected... This was attempt #100"*. **VERBATIM EVIDENCE, which settles it beyond inference:** the driver's own exception names the address it is dialling — `SocketException: Connection refused (OS Error: Connection refused, errno = 111), address = 127.0.0.1, port = 54090`. **`127.0.0.1` is the VM's own loopback**, where nothing listens. And the port **changes between retries** (54090, then 53070), because each attempt re-forwards with a fresh random host port — which is also why a static tunnel alone would not be enough without `--host-vmservice-port` pinning it first. **The mechanism, and it is structural rather than flaky:** `adb forward` creates its listener on the machine running the adb **server**, which here is Windows — `adb forward --list` shows `emulator-5554 tcp:61978 tcp:44129`, a listener on **192.168.56.1**. `flutter drive`, running on the **VM**, then tries to reach that port on its *own* `localhost`. The adb bridge carries commands; it does not carry the forwarded port. **Why it used to work and quietly stopped:** every successful `full-b25` capture ran from **WSL2** — the logs record `workingDirectory=/mnt/c/Users/.../Loom/app/apps/loom_communities_demo`. **WSL2 shares its network namespace with Windows**, so a listener on Windows *was* reachable at `localhost`. The VirtualBox VM has a separate namespace, so it is not. Nothing in the migration notes flagged this, and nothing failed, because the only capture attempted since (2026-08-25, `targeted-precheck`, `evidenceRoot: C:\LoomWin`) ran from Windows and failed for its own reasons. **Blast radius: 10 B25 rows** — the 9 both-halves rows that are not parity-blocked, plus `chess-club-night`, which needs only a judge but whose frames are gone. That is every row whose remaining gap is the judge half. **THE OBVIOUS FIX WAS TESTED AND DOES NOT WORK — 2026-09-12. Read this before writing that ticket.** I proposed pinning the host port with `flutter drive --host-vmservice-port` and bridging it. **Two useful things came out of testing it rather than ticketing it.**

**Good news that narrows the problem:** the VM **can** reach the Windows host directly on the host-only network — `192.168.56.1:5037` and the live adb-forwarded port `192.168.56.1:61978` both accept connections from the VM. **So no reverse tunnel is needed and this was never a reachability problem.** A ~60-line local relay is sufficient in principle; `Tools/code/vmservice_bridge.py` is committed and works (it binds `127.0.0.1:<port>` and forwards to the host).

**Bad news that kills the proposed fix:** `--host-vmservice-port=61999` **did not pin anything**. With the bridge listening on 61999, `flutter drive` dialled `127.0.0.1:33190`, then `:34006`, then `:43366` — fresh random ports each retry — and the bridge logged **zero** incoming connections. Worse, those ports do not correspond to the adb forwards either: the Windows adb server had allocated `tcp:61978` and `tcp:51497` at the same moment. **So the port flutter drive dials is neither the pinned one nor the forwarded one**, and a static bridge cannot be made to intercept it by pinning.

**What that means for the ticket:** do not write "pass `--host-vmservice-port` from the capture tool" — it is disproven. The next question is *why* the dialled port diverges from the adb forward at all, which is a flutter-tool behaviour question and a good Root Cause Agent brief. **Until that is understood, the honest options are (a) run captures from a host that shares the emulator's network — which is how every successful capture was actually produced, under WSL2 — or (b) understand and then work with flutter's port selection rather than against it.**

**THE MOST PROMISING UNTESTED LEAD, recorded so it is not lost.** `flutter drive` accepts **`--use-application-binary=<path/to/app.apk>`** (confirmed in its own `--help`). The VM builds APKs reliably — proven three times today, including the 193MB debug build installed to the emulator. So the *build* could stay on the VM while `flutter drive` runs on **Windows**, the machine that shares the emulator's network. That sidesteps both problems at once: the Windows symlink restriction which blocks `flutter build apk` there, and the port divergence which blocks driving from the VM. **The thing to verify first** is whether a pre-built binary is compatible with a custom `--target` integration test, since for `flutter drive` the target test is normally compiled *into* the app — meaning the APK would have to be the instrumented one the capture pipeline already builds, not a plain debug APK. **Also unverified:** whether Flutter on Windows can run `drive` at all without a buildable project. Check both before committing to this path. **Cost of finding this out: about twenty minutes.** Cost of not finding it out: an implementation dispatch, a code change to a tool that gates the evidence standard, and a fix that would have failed in exactly the same way.

**✅ THE LEAD WORKED — THE CAPTURE BLOCKER IS SOLVED, 2026-09-12. Everything above this line is now history; read this first.** The `--use-application-binary` path is **proven end to end**, and it took two runs to prove because the first one was deliberately designed to fail informatively.

**Run 1 — a plain debug APK, to test the path without spending a build.** Built on the VM, driven from Windows. It got **past the build stage** (no symlink error — the Windows restriction never applies, because nothing is built there), installed, and launched: the log carries real `LOOM_BINDING service=workflow-engine mode=remote endpoint=http://192.168.56.10:30083/` lines and live `authentication_required` reminder-sweep messages. It then failed at *"Flutter Driver extension is taking a long time to become available… calls enableFlutterDriverExtension()"* — **exactly the predicted failure for a non-instrumented binary**, which is what made it worth running. Crucially, **no `Connection refused` on `127.0.0.1` appeared**, and that is the failure that blocks driving from the VM.

**Run 2 — the instrumented APK.** Built on the VM with `flutter build apk --debug --target=integration_test/workflow_ui_evidence_test.dart` plus the capture defines (`LOOM_PRELOAD_EXAMPLE_COMMUNITIES`, `LOOM_EVIDENCE_EXTERNAL_ANDROID_SCREENSHOTS`): **233,789,377 bytes**, exit 0, ~502s. Transferred to Windows at a byte-identical size and driven against `emulator-5554`. **The driver extension connected and the walkthrough executed on the device.** No `errno = 111`, no port retries, no random-port divergence.

**So the division of labour is settled, and it is the one this repo already reached for the APK build:** the VM builds (Linux has no symlink restriction), Windows drives (it shares the emulator's network namespace). Neither host can do both, and neither has to. **The `--host-vmservice-port` + bridge idea below is superseded and should not be built** — not because it was disproven a second time, but because the port problem no longer arises when `flutter drive` runs on the machine the adb listener is already on.

**AND IT IMMEDIATELY EXPOSED A SECOND, DEEPER BLOCKER THAT WAS HIDING BEHIND THE FIRST.** The walkthrough failed on its very first assertion, before capturing a single screenshot:

    Expected: an object with length of <79>
       Which: has length of <77>
    #4 _assertB25AssetCoversTargets (integration_test/workflow_ui_evidence_test.dart:1244:3)
    #5 main.<anonymous closure>.runWalkthrough (integration_test/workflow_ui_evidence_test.dart:74:7)
    WORKFLOW_EVIDENCE_RESULT status=fail walkthroughStatus=fail screenshotStatus=missing workflows=0 screenshots=0/0

`workflow_ui_evidence_test.dart:1244` hardcodes `expect(catalog.models, hasLength(79))`. **Verified against the asset with my own count, not the test's report:** `b25_semantic_interaction_models.json` declares `"rowCount": 77`, contains 77 `workflowId` entries, of which **5 are `wf_`-prefixed** across 3 distinct ids — so 77 − 5 = **72**, exactly the bar this repo already reconciled.

**Why it stayed invisible is the sharpest part, and it is a pattern worth keeping.** The same count is asserted in **four** places. Three of them already say 77 and pass today — `b25_product_doc_action_vocabulary_test.dart:25`, `b25_interaction_model_asset_conformance_test.dart:24`, `b25_product_doc_interaction_models_test.dart:15`. Only the integration test says 79, and it is **the one file the five routine suites never run**, because it needs a device and device capture has been blocked since the WSL2 → VirtualBox migration. **The number was updated everywhere anyone could see it and missed in the one place nobody could reach.** Every suite stayed green while the capture entry point was hard-broken — a green-for-the-wrong-reason of exactly the shape this project keeps recording, one layer further out: not a check that passes when it should fail, but a check nobody could execute at all.

**Dispatched 2026-09-12** (`/tmp/ticket_b25_rowcount.md`, `--fresh`). The ticket asks for the durable fix rather than the one-character one: **a single exported constant** in `loom_ux_judges/lib/b25_product_doc_interaction_models.dart` — which all four sites already import — used at all four, with a doc comment recording that 5 of the 77 rows are `wf_` test-harness ids and the real bar is 72. A value written out four times has now drifted once; fixing only the number leaves the mechanism that produced it. The ticket also opens with a **Task 0** telling the agent to count the asset itself and to stop and report if it does not find 77, so the fix is not built on my arithmetic.

**Still to prove after that lands:** that the walkthrough proceeds past this assertion and actually banks frames. Driving it is my own job, from Windows — the agent is explicitly told not to attempt it.

**✅ THE FIX LANDED (`e2eca4ee`) AND THE WALKTHROUGH NOW RUNS — and it exposed a THIRD blocker underneath the first two.** Rebuilt the instrumented APK (233,785,725 bytes, exit 0) and drove it from Windows. It **passed the row-count assertion**, signed in, captured `B13_ext_garden_club_garden-event-rsvp_member_start`, and reported `completedWorkflows: 1, totalWorkflows: 80` before stalling. So the capture path works and the assertion is genuinely fixed; what remains is a different problem.

**The stall, verbatim:**

    WalkthroughStallFailure: a step could not proceed within its bounded wait (3m 0s elapsed, limit 3m 0s).
      Last completed step: screenshot B13_ext_garden_club_garden-event-rsvp_member_start captured
      Attempted step: continuing the walkthrough after B13_ext_garden_club_garden-event-rsvp_member_start

Reading the code: `primaryCandidates` was **non-empty**, so the walkthrough called `_waitForShippedWorkflowAction`. The transition is therefore **enumerated but never rendered** — the button does not appear within three minutes.

**TWO CHECKS THAT STOPPED ME SHIPPING A WRONG DIAGNOSIS, both worth recording because each nearly went the other way.**

*First, I nearly re-fixed something already fixed.* The stall matches the defect recorded in `CLAUDE.md` today — the calendar RSVP card asking the remote engine about a response row that cannot exist — and Garden is exactly the shape that triggers it (Garden stores RSVPs as a **separate `garden-event-rsvp-response` workflow**, Masjid uses in-instance lists). I was about to file a row and dispatch a fix. **`58e9e122` already fixes it**, and `git merge-base --is-ancestor 58e9e122 HEAD` confirms it is in the very build I drove. Not the cause. The lesson this repo already carries — *search the captured knowledge before presenting a finding as new* — applies to **fixes**, not only to findings.

*Second, an empty log nearly read as evidence.* I checked workflow-service logs for the predicted `workflow_instance_not_found` 404s and found none — then ran the control in the same breath: **0 log lines of any kind in the window**, because the service has no request-level logging. Without that control, "no 404s" would have counted as evidence *against* the hypothesis rather than as a query that cannot answer the question.

**THE ACTUAL FINDING: the capture harness selects an identity, it does not authenticate — and the app it drives is server-authoritative.** `workflow_ui_test_harness.dart:477`'s `signInEvidenceAccount` taps `actor-identity-picker-button` → `actor-identity-sign-in-specific-person` → a `ListTile` matching the display name. No browser, no Keycloak, no token. Meanwhile the APK's own telemetry says `LOOM_BINDING service=workflow-engine mode=remote endpoint=http://192.168.56.10:30083/ … error=authentication_required`, and *"A bearer session is required before calling the workflow service."* So guarded actions cannot render and the walkthrough waits for a button that cannot appear.

**The timeline is the damning part.** The remote-engine default shipped **2026-09-03**; every successful full-b25 artifact is dated **2026-07-01..03**, from the local-engine era whose identity model the picker *does* match; and captures have been networking-blocked since the VM migration. So if this broke on 2026-09-03, **no run could have revealed it** — which is the same shape as the stale `79`: a thing that breaks silently in the one place nobody can execute.

**🔍 Root Cause Agent dispatched** (session key `b25-capture-auth`, `gpt-6-astra`/high, read-only). Briefed with the device telemetry, the stall text and the harness source, and asked to answer **Question 0 — is this chain even right?** before proposing a fix. The reason for scoping rather than ticketing: the cheap fix is `--dart-define=LOOM_ENV=local`, which would make the walkthrough pass by measuring the **local** engine — and the production bar would then prove nothing about the shipped remote path. That is a worse outcome than the current honest failure, so the decision needs to be made deliberately rather than discovered after 72 rows are banked against the wrong adapter.

**🔍 THE ROOT CAUSE AGENT KILLED THAT PREMISE, AND IT WAS RIGHT TO.** Session `b25-capture-auth` found that `workflow_ui_evidence_test.dart:190` does `tester.pumpWidget(const LoomCommunitiesDemoApp())` and **never calls the demo app's `main()`** — so `configureLoomRemoteServicesFromEnvironment()` and the production engine factory never run. **The walkthrough is local-engine**, the demo identity picker is exactly the right identity model for it, and no token is needed. I verified this myself rather than accepting it: the test's only `main()` is its own at `:58`, it contains zero calls to the remote-services configurator, and `main.dart` having one is the control proving the grep works.

**My error is the reusable part: I carried telemetry across two different runs.** The `mode=remote … authentication_required` lines were real — they came from the **plain debug APK**, which does run `main()`. The instrumented capture APK is a different build with a different entrypoint. I also caught that my first attempt to check this was itself invalid: comparing grep counts between the two outputs, when one was a `tail -45` and startup lines are precisely what truncation removes.

**What survives, narrower and still worth acting on:** the **judge** half's frames depict the local engine, while the product ships the remote one. The **walkthrough** half is unaffected — `check_b25_status.sh:50` counts *live-write* manifests, produced by the live-verification agent against the real backend. Since this repo already records cards that render differently on the remote path, a judge over local frames can pass a screen that is broken in the shipped app. Filed as its own concern; it is not a reason to stop capturing.

**THREE REAL DEFECTS FIXED IN SEQUENCE, each one only visible once the previous was gone.** This is what the capture path had accumulated while it was unrunnable:

| # | Defect | Commit | Effect |
|---|---|---|---|
| 1 | `hasLength(79)` against a 77-row asset, the walkthrough's first assertion | `e2eca4ee` | died before any screenshot |
| 2 | inner wait and stall watchdog both defaulting to 3m, watchdog's clock starting earlier | `9c25054f` | `primary_action_unavailable` unreachable; one row aborted all 80 |
| 3 | `tester.tap` missing silently, failing later as "widget not found" | `7ba98707` | reported a healthy widget key as missing |

**Measured progress across those fixes: `completedWorkflows` 1 → 4, screenshots 0/4 → 0/12, `WalkthroughStallFailure` 0.** Each fix was scoped by population rather than by the line that failed (4 wait sites, 7 tap sites), and each regression test was **proven able to fail** by neutralising only the fix and confirming the specific expected failure, with the source restored and SHA-256 verified identical.

**Defect 3 is the one worth remembering, because it lied about itself.** The run said *"Found 0 widgets with key `actor-identity-picker-dialog`"* — which reads as a missing or renamed widget key, and would have sent the next reader hunting through the app shell. **The key was never missing**; it is alive at `part01_local_extension_screen.dart:1059`. The real event was three lines earlier, and `tester.tap` reports a missed hit test only as a *warning*. The fix is therefore diagnostic first: `tapWhenVisible` hit-tests the target and, when it misses, names the target and **distinguishes off-screen from obscured** rather than guessing — because `ensureVisible` addresses only one of the three causes the framework's own warning lists, and a fix that silently handled one of three would have failed identically with the same misleading message.

**Also found, recorded not fixed:** `v3_milestone_phasee_purchase_proposal_test.dart:117` taps `community-tab-home` at `Offset(-141.0, 562.0)`, outside its own `800×600` bounds, and **passes anyway** — a second silent missed tap, in a test that reports success over an action that never landed.

**A DESIGN QUESTION FOR THE CAPTURE-TOOL CHANGE, NOW SETTLED BY MEASUREMENT — 2026-09-13.** The tool passes `LOOM_EVIDENCE_PHASE_FILTER` as a `--dart-define`, read via `String.fromEnvironment`, i.e. **compile-time**, and it runs **one `flutter drive` per phase**. That appeared to force a choice for the `--use-application-binary` path: either build **nine** phase-specific APKs (~8 min each) or change the tool's structure. **Neither is needed.** An unfiltered drive walks the whole range — its test is literally named `wf_full-ui-screenshot-evidence-b12-b20` — and a run today produced evidence directories for **all nine phases, B12 through B20**, from a single APK and a single drive. So the tool needs a prebuilt-binary passthrough and one drive, not nine builds. **This was checked rather than reasoned about**, which is the only reason it can be relied on: the compile-time-define argument was sound and led to the wrong conclusion, because it assumed the phase filter was *required* rather than *optional*.

**THE FULL CHAIN AS OF 2026-09-13 — seven `fix(b25)` commits, and the shape of it is the lesson.** Each defect was invisible until the previous one was fixed, because each was downstream of a failure that stopped the run earlier:

| # | Defect | Commit | Why it was invisible before |
|---|---|---|---|
| 1 | `hasLength(79)` against a 77-row asset — the walkthrough's **first** assertion | `e2eca4ee` | died before any screenshot; and the one file the five suites never run |
| 2 | inner wait and stall watchdog sharing one 3m deadline, watchdog's clock starting earlier | `9c25054f` | made `primary_action_unavailable` **unreachable**, so one row aborted all 80 |
| 3 | `tester.tap` reporting a missed hit test only as a warning | `7ba98707` | reported a *healthy* widget key as missing, three lines later |
| 4 | hit-testing once, immediately, during a route transition | `43168718` | `IgnorePointer`/`AnimatedOpacity`/`Offstage` made a healthy control briefly un-hittable |
| 5 | probing for actions **by tapping**, with no surface assertion | `51bb1cb3` | the probe opened a dialog, then the finder matched controls behind its barrier |
| 6 | the new surface assertion sampling readiness **once** | `b5bc8880` | same bug as #4, in code written one commit later |
| 7 | that assertion demanding a tappable picker the entry-gate path never uses | `e5c26a57` | failed a precondition `selectActorIdentity` does not have |

**Measured progress:** `completedWorkflows` 1 → 4 → (1, from #6's regression) → 1 **with the row now actually starting**; `stalls` and `tap-missed` both reached 0; `never-interactable` and `mismatch` both 0 after #7.

**Two of those seven were regressions I introduced myself** (#6 and #7, both from `51bb1cb3`), and both were caught within one device run because the fixes were built to **fail loudly and name what they found**. That is the argument for spending effort on diagnostics rather than on the fix alone: #3's message identified #4, #4's identified #5, and #6's elapsed-time field is the only reason #7 could be distinguished from "we never waited".

**The current failure is the most interesting and is NOT a harness bug.** Garden's `spring-workshop` event is **at capacity**, so `respond-going` renders **disabled** — the engine correctly withholds the transition (`goingCount >= capacity`) and the UI correctly greys the control. The harness polled it for 2m45s and called it a stall. **A disabled control is a product answer, not a hang**, and the walkthrough already has the right branch for it (`primary_action_unavailable`). Dispatched 2026-09-13 to split "not tappable" into *present-but-disabled* (record immediately — waiting cannot help, and 2m45s per such row would add hours to a full run) versus *enabled-but-covered* (keep polling; stall only if it never clears).

**This also confirms a root cause agent prediction made two dispatches earlier**: the selector enumerates `respond-going` because it checks `instanceDataEquals` and **does not evaluate the formula guard**, so an enumerated candidate can be one the engine legitimately withholds. That selector-fidelity gap is real and is deliberately **out of scope** for the harness fix — the harness should *handle* the condition; changing what the selector enumerates is a separate question.

**PROGRESS 2026-09-13, and the shape of the remaining work is now clear.** After eleven `fix(b25)` commits the walkthrough reached **`completedWorkflows: 4`, screenshots 0/14** (from 1 and 0/5). Garden's `garden-event-rsvp` — the row that consumed six ticks and **three wrong hypotheses of mine** — resolved, along with `plant-exchange-submission` and `garden-tool-loan`.

**Those three wrong hypotheses are worth recording, because the pattern in how each was killed is the reusable part:**

| my hypothesis | what killed it |
|---|---|
| the control is **disabled** | the per-candidate reason added one commit earlier said **absent** |
| the surface is **legitimately actionless** | a shipped test (`b41_garden_engine_migration_test.dart`) taps `respond-waitlist` on that exact instance as that exact role |
| the **selector** drops the transition | `allowViewerResponse` makes `_transitionAccountId` return null, which the account filter accepts — it was there all along |

None was killed by more reasoning. Each was killed by a diagnostic built one commit earlier, or by an artifact already in the repo. **The expensive part was not being wrong; it was the runs spent before going to look for something that already worked.**

**The real cause was an asymmetry in my own fix:** `ec3a7f0c` added `ensureVisible` to the **primary** readiness poll and not to the fallback "any other tappable" check. Nothing went red, because a fallback only runs once the primary path has come up empty — so the gap stayed invisible until a row arrived whose primary action was *legitimately* absent (Garden's event is full at 2/2, correctly withholding `respond-going`). Then no scrolling happened at all and valid alternatives sat below the fold.

**A terminology finding that constrains the bar itself, and must not be lost:** the B25 row definition lists Going/RSVP/attendance as **primary** and Maybe/decline/cancel as **alternates** — and **"Join waitlist" is in neither set**. So waitlist must not silently substitute for the required primary. **Garden's `garden-event-rsvp` row keeps its incomplete action-proof status**; proving attendance needs a seed with a place available, or an explicit product-doc decision. Both are Skill-authored; no `*.jsonc` was touched.

**Now blocked on the marketplace analogue of the same class**, dispatched 2026-09-13: `garden-tool-giveaway` / `terracotta-pots-giveaway` reports `claim-giveaway: absent`, because marketplace actions live in a **detail dialog** (`marketplace-listing-tap-<id>` → `marketplace-detail-dialog-<id>`) whose **controls omit the instance id**, so the instance-qualified finder structurally cannot see them. `ec3a7f0c` scoped preparation to `tabId == 'calendar'` deliberately; this is the other half, with owned open/close and the primary/fallback symmetry applied up front rather than rediscovered.

**⛔ A PART OF THE B25 BAR IS GATED ON A HELD PACKAGE REGENERATION, WHICH IS ITSELF GATED ON A BACKEND THAT DOES NOT EXIST — found 2026-09-13 when the walkthrough reached Book Club.** This is worth stating plainly because it bounds what the campaign can reach at all, and no amount of harness work changes it.

The walkthrough now clears Garden and stalls on Book Club's `book-nomination` / `nom-draft-1`, waiting for a card that will never render. The cause is **already documented** — `solved-patterns.md` states it outright: *"any `renderBindings` entry whose `states` include the pre-stamp state must not use `audience: \"actor\"` … a creator can be fully permitted to read their own draft and still never be shown it"*, because `role_resolver.dart` takes the **first** `actorEqualsField` guard and `break`s, and its `createdByFanId` fallback runs **only when no such guard exists at all**. `nominatorFanId` is stamped only on submission, so a `draft` has no rendering identity.

**The chain that makes this immovable right now:**

1. The B25 row for `book-nomination` needs the draft to render for its creator.
2. That needs **create-time identity in the PACKAGE** — scoped 2026-09-08 (`draft-binding-invisibility`), which explicitly rejected the engine-side `createdByFanId` fallback as the wrong fix.
3. Community JSON is Skill-authored only, so that is a Skill regeneration.
4. ~~**Book Club's regeneration is HELD by user decision** (row-247): `c0e0355b` removed its per-member `send-reminder`, shared-library queue/custody fields and reading-material access lists; a verified restore sits uninstalled, held **until the listing/loan backend exists** so the community is regenerated ONCE against the new API.~~ **HOLD RELEASED 2026-09-25 by user decision — the condition was MET, and two premises of the hold were false.** The listing/loan backend exists: all four paths in `listing-loan-api.openapi.yaml` are served by the workflow service over `item_queue_repository.dart`, the client is `LoomItemQueueClient` (`part46`), and the shell routes by action verb (`part36:543-551`, dispatched at `:838`). **The queue fields are not a loss to restore but LEGACY and superseded** (`_isLegacyQueueField`), and **"zero effects on join-queue/leave-queue" is correct by design**, not a defect — Book Club still declares both transitions and `c0e0355b` never removed them. So the real remaining loss is only the reading-material access lists; the reminder removal was that commit's deliberate and correct purpose. Ticketed, **not dispatched**: [SKILL-bookclub-regeneration-release-hold.md](Tickets/SKILL-bookclub-regeneration-release-hold.md). **I had reported this backend as absent, with a control, and was wrong twice over** — I grepped `listing-loan`/`ListingLoan` when the implementation is named *item queue*, and my `documentLibrary` control mostly matched a `cardSurfaceFamily` string rather than a service client, so it passed for the wrong reason. A control that can pass on the wrong mechanism is not a control. **SCOPE WIDENED 2026-09-20 — row-247's create-time-identity defect affects FOUR workflows, not the two it names, and the mechanism is sharper than recorded.** `role_resolver.dart:22-31` walks the transitions, takes the **first** one declaring `actorEqualsField`, and **`break`s** — `createdByFanId` is used **only when no transition declares one at all**. So declaring `actorEqualsField` anywhere in a workflow **disables the creator fallback in every state**, including states where that field is still unset. An `audience: "actor"`-only binding on such a state therefore renders for **nobody, including the author**, and the card is usually the only route to the transition that would populate the field — a closed loop. **This corrects my own earlier sweep**, which classified workflows declaring `actorEqualsField` as *immune*; that is backwards for early states. **Re-swept with the corrected model, validated against two known cases and predicting two more:** `book-nomination` (`draft`/`nominatorFanId`) is row-247's recorded defect, `book-search-ai-digest` (`draft`/`submitterFanId`) was **proven live on the device today** ([manifest](Evidence/B25/neighborhood-book-club-book-search-ai-digest-live-write-2026-09-20.md)), and newly **`book-shared-library-item` (`draft`/`ownerFanId`) and `book-vote-response` (`pending`/`voterFanId`)** have the identical shape and have never been driven. **`book-shared-library-item` is a bar row**, sitting in the "neither half" bucket — so it will fail this way whenever it is captured, and that is worth knowing before a capture campaign rather than after. **The control runs in the other direction too and passes:** every workflow whose create action **prefills** the field (Camera Club `critique-submission`, Mosque `mosque-care-request`, Garden `plant-exchange-submission`, Social Space `platform-connection` and `platform-message-thread`) is correctly separated out as rescued — which is the documented pattern where a `prefill` is the only thing making an otherwise self-blocking guard satisfiable. **This is NOT the defect fixed today** by `8d33a8af`: that one was the remote projection omitting `createdByFanId`, and it only ever helped workflows declaring **no** `actorEqualsField`. These four were never reachable by that fix and still are not. **Method note, because two of my own sweeps were broken today and both were caught only by controls:** the first was right per-workflow and wrong per-**state**; the second returned a clean `0` because I matched `"actorEqualsField": "name"` when the real shape is the nested `"actorEqualsField": { "key": "name" }`, so nothing matched and every workflow was skipped. A zero that agrees with "nothing to worry about" is exactly the result that needs a control.
5. Dispatching now would regenerate from the damaged HEAD and **bake that loss in permanently** — and any diff against HEAD would report the loss as faithfully preserved, which is why the tracker says to verify Book Club output against `c0e0355b^`, never HEAD.

**So: I did not dispatch a Skill fix, and no future tick should without the user lifting that hold.** The same Sept 8 review list names `book-vote-response`, `book-shared-library-item` and `book-search-ai-digest` as the same family, so this is plausibly **four Book Club rows** blocked behind one held regeneration, not one.

**What is still worth doing, and was dispatched:** the harness's own actor selection disagrees with the renderer — B25 falls back to a seed's creator when actor fields are absent, while `deriveInstanceRoles` does not. That is a harness defect independent of the package, and fixing it converts a 2m45s wait-for-a-card-that-cannot-exist into an immediate, explicit audience diagnostic naming the missing `actorEqualsField`. **It does not make the row provable** — only the package fix can — but it stops one blocked row from costing the run three minutes and hiding its own cause.

**STRATEGY CHANGE 2026-09-13, and the reasoning matters more than the change.** After fifteen `fix(b25)` commits the walkthrough reaches `workflows=7` with **zero stalls**, and a blocked row no longer aborts the run — `book-nomination` is recorded blocked and the run continues past it. But it then died on a **different** terminal failure one row later (`book-vote` "could not derive an actionable instance, actorIdentity, and tab" — a *setup* failure, not an audience one, in a workflow that has a seed and `audience: "any"` bindings).

**Row-by-row investigation has stopped being the right tool.** Each cycle is a dispatch, a build and a device run — roughly forty minutes — and three facts make that untenable: Book Club is **knowingly damaged and held** (row-247) with an unknown number of affected rows; the walkthrough **has never got past community two**, so eight communities are entirely unmeasured; and each fix so far has revealed the next defect only after landing, so the sequence has no visible end.

**So the next dispatch makes a row that cannot be SET UP recorded-and-skipped, the same way an unresolvable audience already is.** The goal is a single traversal that inventories the whole bar: how much is blocked on the held package, how much on seeding, how much on harness gaps still unfound. **One complete blocked-list is worth more than N investigations**, because it replaces a sequence of unknown length with one measurement.

**The risk is explicit and contained, because this is exactly how a harness starts hiding its own defects.** Three properties are mandatory and were written into the ticket as such: a blocked row is **never** counted proven and sits in its own bucket; its reason is captured **verbatim** rather than summarised into a category, so "the package has no seed for this state" stays distinguishable from "the harness could not find a tab"; and the end-of-run summary **lists every blocked row** — workflow, community, role, reason — not merely a count, since a count alone is precisely the artifact that lets blocked work disappear. The ticket also says that if all three cannot hold, report it rather than ship a partial version: **a tolerant run without a legible blocked list is worse than the current abort, because it looks like progress.** Unexpected exceptions from outside the two named paths must still abort.

**✅ THE WALKTHROUGH RAN TO COMPLETION, 2026-09-13 — first full pass since the capture path broke.** After twenty `fix(b25)` commits, a 29-minute drive ended with `run-complete` and `All tests passed!` — no exception, no abort, `stalls: 0`. The structured inventory this campaign was built to produce now exists:

| outcome | count |
|---|---:|
| **proven** (`b25Proven`) | **9 / 49** |
| completed workflows | 21 |
| **row execution failed** | **20** |
| blocked by audience | 3 |
| action succeeded, result unverified | 3 |
| blocked by selector setup | 2 |
| blocked by prerequisite | 1 |
| total workflows in the bar | 82 |

50 of 82 attempted; every non-proven row carries a category and a verbatim reason. **`completed` is not `proven`**: a completed row ran to a recorded outcome, a proven row exercised its required action. Quote the 9, not the 21.

**What this does and does not mean.** It means the harness can finally *measure* the bar instead of dying before it. It does **not** move the bar: `screenshots=0/88` and **0 PNGs written**, because frames are captured by the **tool** via `adb` in response to the test's `B25_CAPTURE_PROGRESS` events, and a hand-driven `flutter drive` emits those events with nobody listening. 19 manifest files were written; not one is evidence.

**The three biggest remaining items, in the order they gate each other:**

1. **`rowExecutionFailed: 20`** — the largest bucket and entirely unexamined. Each carries a recorded reason; reading them is cheap and is the obvious next move.
2. **The capture tool needs its `--use-application-binary` passthrough** before any of this is bankable. Its one genuine design fork is already settled by measurement: **one unfiltered APK, one drive, all nine phases B12–B20** — verified from the evidence directories, not argued from the compile-time `--dart-define` reasoning that pointed the other way.
3. **Rows that cannot prove regardless of harness work** — ~~Book Club's four behind a **held** package regeneration (row-247)~~ **Book Club's four are no longer held: the hold was released 2026-09-25 and the regeneration is ticketed (undispatched) — see row-247** — Garden's two behind seeding (a full event; an owner who cannot claim their own giveaway). These need decisions outside the harness and must never be counted as progress.

**THE RUN PRODUCES PRODUCT FINDINGS, NOT JUST COUNTS — 2026-09-13.** The manifests carry a `productFindings` array per row. **39 distinct findings** from the first complete pass, in plain language, naming the community, the row, the role, the documented actions expected and what was actually offered. Two shapes dominate: **11** rows where *"no documented primary package action was exercised"* (each listing the terms it looked for), and four distinct `primary_action_unavailable` reasons:

- `claim-giveaway unavailable: the actor owns this giveaway (ownerFanId == $actor), so the transition's guard formula denies it`
- `Going unavailable: Spring Workshop: Pollinator-Friendly Planting is full, 2/2. Other available actions include Add reminder…`
- `no primary action candidates were selected for this workflow row`
- `Request loan unavailable. Other available actions include Report issue. The documented primary action was not exercised`

**THE FOURTH INDEPENDENTLY REPRODUCES ROW-259 FROM THE DEVICE.** That row records that a Garden owner cannot withdraw their own listing once it is `onLoan` — `delist` and `pause-listing` both require `availabilityState == "available"`, every transition that clears `onLoan` is borrower-guarded, and the owner's only remaining action is `report-issue`, which declares `"to": null` and changes no state. The walkthrough found precisely that, unprompted: *"Request loan unavailable. Other available actions include Report issue."* A blocker previously established by reading the package is now **confirmed by what the app actually offers a real persona**, which is a materially stronger form of the same claim.

**Note `recordedRowStatus: "pass"` on rows whose primary action was unavailable.** That field means *the row was recorded successfully*, not *the row proved its action* — the same completed-versus-proven distinction as above, in a third place. Anyone computing a bar figure from `recordedRowStatus` would get 21-style numbers rather than the true 9.

**Still true and still the reason the tool must change at all:** every direct `flutter drive` reports `screenshots=0/N`. The frames are captured by the **tool**, which shells out to `adb` in response to the test's `B25_CAPTURE_PROGRESS` events. Driving `flutter drive` by hand — which is how all of today's debugging was done — can never produce evidence frames, only walkthrough outcomes. So the tool change is required before any of this becomes bankable B25 evidence, and the walkthrough must be completing first for that change to be worth making.

**SUPERSEDED — the paragraph below proposed `--host-vmservice-port` + a reverse tunnel. Disproven 2026-09-12 (pinning does not pin), and now moot: driving from Windows removes the problem entirely. Kept only so the reasoning is not re-derived.**

**A fix is identifiable and I stopped short of building it deliberately:** `flutter drive` accepts `--host-vmservice-port`, which pins the host-side port instead of choosing a random one. With it pinned, a reverse tunnel from Windows (`ssh -N -R <port>:localhost:<port> loom-vm`) would make the VM's `localhost:<port>` reach the Windows listener, completing the chain. **But the capture tool builds its `flutter drive` command line internally and passes no such flag**, so this needs a change to `b25_capture_workflow_screenshots.dart` — application code, and therefore an implementation dispatch rather than a hand-edit. **Worth confirming the intended host before building that:** captures may simply be meant to run from a machine that shares the emulator's network, in which case the answer is operational rather than a code change. **Do not work around this by using `--mode targeted-precheck`** — its own error text says that output must not be committed as canonical B25 evidence, and canonical mode refuses to filter communities, phases or shards by design.
- [x] **PARTIALLY CLOSED — the seeds are in and correct, but they do NOT make the rows provable; see the selector row above. CLOSED 2026-09-19 (`1b5c9f70`) — Garden's unprovable rows are seeded, by ADDING seven instances and mutating none.** The class turned out to be **five rows, not the two this row named** — the Skill dispatch, told to report rather than silently fix, found `steel-wheelbarrow`, `club-hand-tool-set` and `mulch-delivery-shift` with the same shape. Added: `autumn-work-day` + 2 responses (RSVP Going, 1 going of capacity 2), `cedar-compost-bin-giveaway` (coordinator-owned), `compact-tiller` (coordinator-owned, available), `soaker-hose-set` (onLoan to the member), `trellis-building-shift` (empty `signedUpFanIds`). Zero removed lines; all twelve original seeds intact. **Three attempts, two reverted, and the reverts are the durable output** — both are now `solved-patterns.md` §26's two amendments. (1) Raising `spring-workshop`'s capacity, exactly as this row prescribed, broke `b41`: `respond-waitlist` renders ONLY while the event is full, so one event cannot demonstrate both. (2) Adding a second event — a pure addition — broke `b41` anyway, because it was dated earlier and stole the calendar's default selection. (3) Worked, because the constraint was **derived from source** rather than guessed: `part28_engine_native_calendar_surface.dart:687` sorts by date and `:620` defaults to `entries.first`, so the new event is dated after `spring-workshop`. **All three attempts validated `pass` with 0 errors** — the third even ran the unmodified package as a control — so nothing but the demo-app suite could ever have caught this. Suites: demo 240 + the held `b43`, app shell 421 (+2), engine 339 (+5), judges 525; workflow service deliberately not run and not claimed (seed-only change, zero definition-level lines differ, seeds never reach Postgres). Provenance manifest regenerated — its test failing was the gate working. **CORRECTION 2026-09-19, same day: `ev34` proved the seeds insufficient.** B13 came back with byte-for-byte the same 13 frames and the same three `primary_action_unavailable` outcomes. The seeds are right and are worth keeping — they are the reachable instances the harness will select once it can — but the blocker was never the package. The B25 selector picks by declaration, not reachability, and ignores `formula` guards entirely. **The five rows remain unprovable and this row must not be read as having delivered them.**
- [ ] `new-ticket` — Regeneration is three steps and step 3 (install) is missing — JSON and deriver both correct, live rows stale since 2026-09-02; fix + process gate — [detail](TODO-open-detail.md#row-356)
- [x] **CLOSED 2026-09-17 — FIXED AND VERIFIED ON THE LIVE CLUSTER. `check_permission_parity.sh` now exits 0: *"every compared community's live grants equal its derived sets, by id."*** Ran `installCommunityPackage` for the three drifted communities through `apply_app_access_provisioning --apply` with a provisioning-principal token (the `loom-app-access-provisioner` client already existed and already held the realm role — nothing was created). Result: `removedRoleIds: []` for all three, no failures, and `masjid-nur-admin` went **28 → 5**. book-member 26→31, book-organizer 31→35, soccer-guardian 20→21, soccer-owner 8→9; **soccer-coach stayed at 18 while its wrong `document_library.upload` was replaced by the derived `edit`** — which is exactly why the gate compares id sets and not counts. No code and no JSON changed: the policy came from the community JSON, as it should. Process fix in [row-356](TODO-open-detail.md#row-356). Original finding below. ~~**✅ CAUSE FOUND 2026-09-15 — this is NOT a JSON defect and NOT a deriver defect, and the remaining decision is narrower than the text below implies.** Layer-by-layer: the packages declare the right actions (read directly), App Access's own `CommunityPermissionDeriver` returns the right sets (the gate's expected side calls it live), and the **live rows are stale** — Book Club and Youth Soccer roles last written 2026-09-02, both packages regenerated 2026-09-05. Masjid's 23 extras came from a hand-written SQL recipe, not an install. `installCommunityPackage` replaces both directions and is pre-checked safe for all three groups. **All that is owed by the user now is permission to read two k8s secrets** for a provisioning token; the rest is [row-356](TODO-open-detail.md#row-356). Superseded framing below. ~~**The P4 parity gate WORKS, nobody has acted on what it reports, and it is reporting six live violations right now — including the Masjid over-grant the whole P-series was built for.** Measured by me 2026-09-12 by running `check_permission_parity.sh` against the live cluster. **P3 and P4 built the *prevention* (`role_kind`, the exact-set gate) and are correctly closed; the contaminated *data* was never cleaned.** That is this project's recorded "a capability has to be true at every layer" pattern — the gap moved layers rather than closing, and each layer's row reads as locally complete. **Violation 1, rule 1 (governance set), security-shaped:** `masjid-nur-admin` holds **28** permissions where all 14 other community admins hold exactly **5** — a perfect outlier. The 23 extras are the `owner` domain role's archetype permissions (`discussion_thread.*`, `document_library.*`, `event_rsvp.*`, `form_entry.*`, `search_ai_answer.*`, `approval_queue_item.*`), copied in by the 2026-09-02 SQL recipe whose own mapping said "preserve all 28 grants" and whose anomaly note flagged them "for user decision" — never resolved. **State the blast radius precisely, because I got this wrong once before:** the violation is real at the **App Access** layer, where `checkAccess` grants those domain allowances to anyone holding the admin role; it is **absent at the engine layer**, because `guard_evaluator.dart:29-37` intersects real role ids and never permissions, so the over-granted admin still fails the four `byRoleIds: ["owner"]` transitions. It is a standing privilege violation of `permissions.md:522`, not a live path to owner actions. **Violations 2-6, rule 2 (package-derived sets), and these are FUNCTIONAL, not cosmetic — they sit directly in the campaign's path:** `book-member` and `book-organizer` are each missing several derived permissions (`document_library.download`, `equipment_loan.*`, `event_rsvp.withdraw_response`); `soccer-owner` is missing `export_wizard.download`; `soccer-guardian` is missing `document_library.download`; and `soccer-coach` holds `document_library.upload` while the package derives `document_library.edit` — a mismatch, not just a gap. **Why this matters now:** Book Club (7 rows) and Youth Soccer (6 rows) are the next two clusters in the walkthrough queue, and a missing derived permission is exactly what produced the live `403` on Cedar's `hoa-facility-reservation` earlier in this campaign. `soccer-export-metadata` needs `export_wizard.download`; `soccer-waiver-document` is the one the `edit`/`upload` mismatch would hit. **Expect 403s on those rows unless this is fixed first.** **Why I did not just fix it:** every item here is a live authorization change to a running cluster — removing 23 grants from a real role, and adding grants to five others. I over-reached on exactly this kind of change once before (granting `app-access-provisioner` to the production workflow-service client unprompted) and the right move was to escalate rather than work around the block. **My recommendation, in order:** (1) remove the 23 non-derived grants from `masjid-nur-admin` via `setRolePermissions`, leaving the five `community.*` — surgical, and avoids `installCommunityPackage`'s sweep; (2) backfill the five package-derived gaps the same way, before Book Club and Youth Soccer are walked; (3) add `check_permission_parity.sh` to the post-deploy audit so its output is *read* rather than merely produced — a gate nobody runs is a gate that does not exist, which is precisely what happened here.
- [ ] `new-ticket` — Live cluster holds state that exists nowhere in git (`test-fan-alice`/`test-fan-bob` and their `fanId` claims) — [detail](TODO-open-detail.md#row-197)
- [ ] `new-ticket` — 68 seeded accounts share one password (`LoomTest123!`) — doubled since first written — [detail](TODO-open-detail.md#row-199)
- [ ] `new-ticket` — Rotate three exposed secrets before GA — Google client secret, Facebook app secret, DeepSeek API key — [detail](TODO-open-detail.md#row-298)

### 2. Decisions owed by the user

- [ ] `blocked` — **✅ THE DECISION WAS MADE 2026-09-15 AND IT DISSOLVES THE QUESTION RATHER THAN ANSWERING IT. Read this before re-reading anything below.** The user chose neither first-match nor all-match: *"There should be a backend 'cancel event' API … it should not cascade the property to each 'members' record … removing that event removes all entitlements."* So cancellation stops being expressed as a cascade at all, and the `effects.md`/`event-rsvp.md` contradiction becomes moot for RSVPs — there are no response rows left to sweep. Masjid Nur already ships this model. What survives is (a) the `{id}` filter bug, now [row-355](TODO-open-detail.md#row-355), which still matters for Ad-Free's refund chain and Cedar's withdraw, and (b) the new capability itself, [row-354](TODO-open-detail.md#row-354), scoping dispatched. **This row stays open only until row-354's scoping says whether `event-rsvp.md`'s "must sweep EVERY non-terminal state" needs to change** — that is a hard-locked doc, so it is reported, never edited here. Superseded analysis below. ~~**`transitionRelated` cannot deliver the cancellation sweep `event-rsvp.md` requires, and the two hard-locked reference docs contradict each other. Needs a spec decision.**~~ Found 2026-09-12 by a Garden Club walkthrough, root-caused (session key `transition-related-silent-failure`), and the contradiction verified in both docs by me. **Observed live:** `cancel-event` declares five `transitionRelated` effects to release respondents; the call returned **200 and moved nothing** — the event is `cancelled` while its response row is still `going`, so a cancelled event advertises a live RSVP (`community_garden_club_garden-event-rsvp_gwrvhmw1ztbk` / `...-response_g45yq8qnsj95`). **The conflict:** `effects.md` §`transitionRelated` defines the mechanism as single-row best-effort — `limit` "currently only `1` is defined", applied to "the **first** matching row after sorting", with guard failure an explicit "silent-no-op" — and says outright that the gap it closes is *waitlist promotion*. `archetypes/event-rsvp.md` requires the opposite: "Cancelling **must sweep the rows** … the sweep must cover **EVERY** non-terminal state … Leaving those behind means a cancelled event still has rows claiming a live answer." The package did exactly what the archetype doc instructs (one effect per source state) and still failed. **Even with linkage fixed, `matches.first` strands every respondent after the first**, so any multi-person event under-applies silently. **This corrected my own premise:** I assumed the parent transition had already committed, making rollback impossible; parent and target effects actually run in the SAME transaction, and it is the swallowed guard refusal — silence — that permits a false success. **The decision, stated as one question:** should `transitionRelated` gain an explicit all-match, refusal-propagating mode for required cascades (keeping today's first-match best-effort semantics for promotion, which are separately specified and correct), or should the archetype stop expressing cancellation through this mechanism and get a different one? Not implementable either way without choosing — both docs are hard-locked, and this is the "never specify an effect without its mechanism" shape CLAUDE.md already records. **CONFIRMED BY CONTROLLED EXPERIMENT, 2026-09-12. This is the decisive evidence and no further measurement is needed to justify the fix.** Ad-Free's `cancel-subscription` was driven live specifically to test this, because it is the only site in the corpus where the same token is **written by a mechanism already proven correct** and then **read back by the mechanism under suspicion**, with **identical guards on both ends**.

| Fact | Value, read by me from Postgres |
|---|---|
| checkout instance | `community_ad_free_community_ad-off-member-checkout_o1otolp4pkda`, state **`cancelled`** |
| entitlement's stored `checkoutInstanceId` | `community_ad_free_community_ad-off-member-checkout_o1otolp4pkda` — **exact match** |
| entitlement state after the cascade | **`active`** — unmoved |

The effect declares `filter: { "checkoutInstanceId": "{id}", "$state": "active" }` -> `member-deactivate`. It had a real row to match, holding the correct value, in the required state. **It matched nothing.**

**Every competing explanation is eliminated by the design of the test, not by argument:**
- *Guard refusal* — `cancel-subscription` and `member-deactivate` are **both** `ad-off-member` + `actorEqualsField: memberFanId`, fired by the same fan. This is what Garden's failure could not rule out.
- *Missing or wrong-state target* — the row existed, in `active`, which the filter requires.
- *Bad stored value* — `checkoutInstanceId` is byte-identical to the checkout's id, written by `{id}` in an effect's `fields`, a context measured correct five times today.
- *Failed parent* — the checkout reached `cancelled`, so the transition itself ran.

**So the defect is established: `{id}` does not resolve inside a `transitionRelated` filter, while resolving correctly everywhere else it is used.** Taken with Cedar's live *working* cascade on a `{dataField}` token, the isolation is complete from both directions.

**What this means for the decision you are being asked to make.** Item 1 (the resolver inconsistency) is no longer a hypothesis needing investigation — it is a reproduced defect with a known-good reference implementation in the same codebase, affecting **33 filter sites across 6 packages**. It can be ticketed immediately and needs no spec decision. **Only item 2 remains yours:** first-match/best-effort versus all-match with refusal propagation, where `event-rsvp.md` and `effects.md` genuinely contradict each other and both are hard-locked.

**ALL THREE INTERPOLATION CONTEXTS ARE NOW MEASURED LIVE, and exactly one is broken.** Nothing here is inferred — each row is a value I read out of Postgres on 2026-09-12:

| context | example | result |
|---|---|---|
| effect `fields` | `sourceInstanceId: "{id}"` | **WORKS** — spawned `export-notification` carried the parent id |
| `scope: "instance"` create prefill | `sourceTransferInstanceId: "{context.id}"` | **WORKS** — spawned rollback carried `..._export-transfer-verification_tqt9zpajt8b4` |
| `transitionRelated` filter, data-field token | `"requestInstanceId": "{requestInstanceId}"` | **WORKS** — Cedar cascaded a row to terminal `withdrawn` |
| `transitionRelated` filter, `{id}` | `"eventId": "{id}"` | **BROKEN** — Garden returned 200 and moved nothing |

**So the defect is one token in one position, and the other three contexts prove the mechanism is otherwise sound.** That is as narrow as this can be made without reading the resolver, and it removes any remaining doubt that the fix is a consistency repair rather than a design question.

**MECHANISM NOW ISOLATED, 2026-09-12, with a live WORKING control and a live FAILING case on the same engine on the same day. The filter resolver does not special-case `{id}`, and that single fact explains the whole failure.**

| | Garden Club (FAILS) | Cedar Commons HOA (WORKS) |
|---|---|---|
| filter token | `"eventId": "{id}"` (5 sites) | `"requestInstanceId": "{requestInstanceId}"` (a real data field) |
| observed live | `cancel-event` returned **200 and moved nothing** | `withdraw-request` **cascaded the spawned row to terminal `withdrawn`** |
| evidence | the Garden walkthrough that opened this row | `community_cedar_commons_hoa_hoa-committee-decision_m3z1jsw245x4`, `current_state = withdrawn`, `requestInstanceId` = the parent `..._tvevsywi9ei9` — **read by me out of Postgres** |

**Why this is the answer.** `{requestInstanceId}` names a field that genuinely exists on the parent instance, so an ordinary data-field lookup resolves it and the filter matches. `{id}` names a field that does **not** exist in `instanceData` — the instance id is not a data field — so the same lookup yields nothing, the filter matches no rows, and the sweep silently no-ops. That is exactly the asymmetry the root cause agent read from source, now confirmed from both directions by live behaviour rather than by reading.

**Blast radius — 33 filter sites across 6 packages are silently dead today:** Ad-Free 11, Camera Club 6, Book Club 5, Garden Club 5, Youth Soccer 5, Cedar 1. The 22 sites that use a real data-field token (Cedar 11, Ad-Free 8, Youth Soccer 3) work. **Note Cedar is the only package that mostly uses the working form, which is why its cascade is the one that succeeded — this is not a Cedar-specific feature, it is a token-choice difference.**

**This changes what is actually being decided.** There are now two separable items, and only the second is a spec question:
1. **A plain bug, no spec decision needed:** the filter resolver should receive the instance id for `{id}` the way effect-field interpolation already does (measured working, same day). That is a consistency repair against a working reference implementation in the same codebase, and it un-breaks 33 sites.
2. **The genuine spec question, still yours:** first-match/best-effort versus all-match with refusal propagation. Even with `{id}` fixed, `limit: 1` plus `matches.first` still strands every respondent after the first, so Garden's five-effect sweep would move exactly one row per state.

**Honest limit on item 1's evidence.** I verified the Cedar row and its stored `requestInstanceId` myself, and the package declares exactly the four `transitionRelated` effects that would produce it; the walkthrough attributed the move to the cascade. I did **not** independently rule out that the agent drove `owner-withdraw` directly on the committee-decision card — it is `hoa-member`-guarded and it was acting as `fan-hoa-member-1`. **The Ad-Free experiment recorded in the run plan closes that gap** and should still be run: it is `{id}`-based, both ends carry identical guards so a swallowed refusal cannot confound it, and the write side uses the already-proven effect-field path.

**A STRUCTURAL FACT FOUND 2026-09-12 THAT MAY RESHAPE THIS QUESTION — read it before choosing between the two options below, because it suggests a third.** The `event-rsvp` archetype is expressed **two structurally different ways** across the shipped packages, and the "must sweep" requirement is only meaningful for one of them. I verified this by reading both packages:

| | Garden Club | Masjid Nur |
|---|---|---|
| RSVP storage | a **separate `garden-event-rsvp-response` workflow**, one instance per respondent | **lists on the event instance itself** — `goingFanIds`, `maybeFanIds`, `notGoingFanIds`, `waitlistFanIds` (15 occurrences) |
| `goingFanIds` in package | **0** | **15** |
| `transitionRelated` effects | **5**, on `cancel-event`, to release respondents | **0 — the package contains none at all** |
| `cancel-event` | declares the sweep, and the sweep **silently moves nothing** | plain `cancel`, no sweep declared |

**Masjid needs no sweep, and its absence is not a defect.** `event-rsvp.md`'s stated concern is that *"a cancelled event still has rows claiming a live answer"* — Masjid has **no such rows**. Cancelling the event row cancels the RSVPs because the RSVPs live *on* that row. So the archetype doc's cancellation requirement is written for the separate-response-row model and is vacuous for the in-instance-list model, while the doc itself does not say which representation it governs.

**Corpus context:** 6 of 10 packages use `transitionRelated` at all (Ad-Free 19, Cedar 12, Youth Soccer 8, Camera 6, Book Club 5, Garden 5); **4 use none** (Masjid, Member Social Space, Data Portability, Chess).

**So there is a third option to weigh alongside the two below:** the divergence may not be in `transitionRelated` at all, but in the archetype shipping **two representations** with a cancellation rule written for only one. If the in-instance-list model is the intended one, Garden's separate-response workflow is the thing to change and the mechanism gap becomes moot; if the separate-row model is intended, then Masjid is under-modelled and the sweep mechanism genuinely must work. **I am not choosing** — both docs are hard-locked and this is exactly the spec call that belongs to you. But choosing between "all-match mode" and "stop using `transitionRelated`" without knowing which representation the archetype is *for* would settle the mechanism while leaving the real inconsistency in place.

**The `{id}` half of this is now HALF-MEASURED, live, 2026-09-12 — and the measured half came back GOOD, which narrows the remaining work considerably.** I had a Data Portability walkthrough verify it as a side-measurement rather than reasoning about it. `export-protected-redaction`'s `preview-protected-redaction` transition carries a cross-type `createInstance` effect whose `fields` include `sourceInstanceId: "{id}"`. Driving it live produced `community_data_portability_export-notification_qniobzc2oaqc` holding `sourceInstanceId = community_data_portability_export-protected-redaction_o45jkphkf10j` — **exactly the parent instance's id.** I read that row out of Postgres myself. So **`{id}` interpolation in an effect's `fields` demonstrably works**, and the asymmetry this row describes is real rather than suspected: the same token behaves correctly in effect interpolation and (per the root-cause reading) as an ordinary data-field lookup inside a `transitionRelated` **filter**. **Why that matters for the decision you are being asked to make:** the filter-side fix is now "make the filter resolver receive the instance id the way effect interpolation already does", a consistency repair against a working reference implementation — not a new mechanism to design. That is a materially smaller and safer change than this row originally implied, and it is *independent* of the larger first-match-versus-all-match question above, which still needs your call. **Still unmeasured:** the filter path itself, which needs a live `transitionRelated` whose filter contains `{id}` — worth taking as a side-measurement on a future walkthrough the same way this one was, rather than as its own dispatch.
- [ ] `blocked` — `deriveInstanceRoles` picks the FIRST `actorEqualsField` guard — array order is not a business-party selector — [detail](TODO-open-detail.md#row-248)
- [ ] `blocked` — A payment-service stub needs a service-backed transition binding specified first — grammar gap, stops and asks — [detail](TODO-open-detail.md#row-251)
- [ ] `blocked` — Field-label humanization disagrees with itself across 4 sites in `part18`/`part26`/`part28` — [detail](TODO-open-detail.md#row-256)
- [x] **CLOSED 2026-09-18** — archetype now requires an owner exit in every state (`delist` → `delisted-pending-return` from `onLoan`), and the validator catches the class as `destructive_exit_blocked_by_counterparty` (`2bd210d7`, ux judges 520, A/B-proven able to fail). Garden's package repair is a Skill dispatch, tracked with the other Garden fixtures in [row-357](TODO-open-detail.md#row-357) — [detail](TODO-open-detail.md#row-259)
- [ ] `blocked` — `queryInstances` has no date-window parameter, so every calendar tab pages the whole instance set — [detail](TODO-open-detail.md#row-275)

### 3. Ready to ticket

- [x] **CLOSED 2026-09-12 — fixed (`3f6812b3`), deployed (`loom-workflow-service:1.0.7`, manifest bump committed as loom-backend `f1f3d45`), and PROVEN AGAINST THE RUNNING SERVICE.** **I predicted the correct outcome from the data before testing**, which is what makes this a test rather than an observation: the suppression row's checkout is `..._o1otolp4pkda`, and entitlement `..._jam3e7fa6bqx` joins to that same checkout in state `active` (a second entitlement belongs to a different checkout and must NOT match). So `linkedEntitlements` resolves to one row, `entitlementStateCounts` to `{active: 1}`, `isSuppressionActive` to **true**, and `!isSuppressionActive` to **false** — meaning the correct result is a clean **denial**. **Observed:** `GET .../available-transitions` returns **HTTP 200** with `review-again` present and **`request-restoration` correctly absent**. **And the logs separate the three outcomes:** zero `FormulaEvaluationException` (so the chain resolved) and zero `workflow_guard_evaluation_failure` (so it was a *denial*, not an *unavailable*). **That distinguishes all three possibilities at once** — before the fix this threw; a careless fix defaulting the unknown to `false` would have made the negation `true` and **granted** restoration; the correct fix computes a real answer and denies. Original finding below. ~~ROOT-CAUSED 2026-09-12 by the Root Cause Agent (session key `query-source-guard-asymmetry`), and it corrected two of the three fixes I had proposed. Ready to ticket; the analysis below is the ticket's substance.** **Confirmed:** the read path hydrates query sources (`local_workflow_engine_api.dart:473`) and the mutation guard path computes directly from stored JSON without hydrating (`:1203`-`:1215`), so the same expression renders correctly and fails during authorization. **Three things I would have missed, each verified by me afterwards in source:** (1) **deferral is implemented by OMISSION** (`:2471`-`:2493`) — the formula is skipped, so nothing downstream learns the value was unavailable; (2) **`:2399` assigns a query source only `if (matches.isNotEmpty)`**, so a legitimate zero-match query is indistinguishable from a failed lookup and **hydration alone would not fix this**, with the `catch (_)` at `:2402` discarding the cause; (3) **`_storageOnly` (`:1795`) strips formula fields but NOT source fields**, so feeding hydrated data into persistence would silently store query snapshots — a stale-data trap inside the obvious fix. **The safety point, and it reverses one of my suggestions:** *"Defaulting an unknown boolean to false can authorize its negation."* Here the guard is `!isSuppressionActive`, so a `false` default yields `true` and **grants** restoration on unresolvable data. **Unavailable must remain a third outcome**, excluding the transition with a diagnostic — distinct from an ordinary `false`, and distinct from the per-transition exception isolation already shipped in `03b93f1c`. **Explicitly rejected by the analysis:** changing boolean coercion, and refusing this grammar shape at validation — *"neither restores the missing authoritative data"*. **Also flagged:** creation/edit guards have the same preparation gap (`_passesGuard` receives raw data at `:1600`/`:1739`); `_readAllInstancesOfType` fetches community rows before filtering by type (`:771`-`:776`), so authoritative hydration adds that cost; and the parent row lock does not lock matching related rows, so this does not establish serializable cross-row authorization. **A six-case regression matrix is specified in the dispatch log**, including asserting BOTH guard polarities on unresolved data so a default-false implementation cannot pass unnoticed. Original finding below. ~~A formula over a QUERY-SOURCED field evaluates on the render path and yields `null` on the server guard path. Now observable in production logs rather than silent.** This is the *second* half of the `ad-off-ad-suppression` investigation; the first (one throwing guard aborting all enumeration) is fixed and deployed, and deliberately did **not** touch this. **The chain, verified in the package:** `linkedEntitlements` is a **query-sourced** field (`query(ad-off-entitlement-status where checkoutInstanceId == checkoutInstanceId)`); `entitlementStateCounts` is `groupCount(linkedEntitlements, '$state')` over it; `isSuppressionActive` is a comparison over *that*; and `request-restoration` guards on `formula: "!isSuppressionActive"`. **On the render path the whole chain resolves** — the card displays *"Ads suppressed now: Yes"*. **On the server guard path `isSuppressionActive` is `null`, and `!null` throws** `FormulaEvaluationException: Expected bool, got null` at `formula_evaluator.dart:408`. **Live evidence, 2026-09-12, from the deployed `loom-workflow-service:1.0.6` pod:** `{"event":"workflow_guard_evaluation_failure", "workflowType":"ad-off-ad-suppression", "transitionId":"request-restoration", "errorType":"FormulaEvaluationException"}`. **Consequence today:** `request-restoration` is unreachable, so a member who is no longer suppressed cannot request restoration through the product. The other two transitions now work, so the workflow is no longer dead — this is a missing capability, not a dead row. **Why it is worth fixing rather than tolerating:** the render/guard asymmetry is a *general* property of query-sourced fields, not specific to this package. Any guard formula reading one will behave the same way, and until now it presented as an inexplicably immovable workflow. **Not a package defect** — the declarations are well-formed and the same chain renders correctly. Start by establishing why the query source populates for rendering and not for guard evaluation; that answer probably decides the fix.
- [x] **CLOSED 2026-09-12 — fixed (`03b93f1c`), deployed (`loom-workflow-service:1.0.6`, manifest bump committed as loom-backend `ed6f898`), and PROVEN ON A DEVICE.** A walkthrough on the deployed image tapped "Mark reviewed" on the row that had been immovable for four days; it is now **`reviewed`**, and `updated_at != created_at` for the first time. The older 09-08 row remains `unreviewed` and untouched, as it should — nobody drove it. **The observability requirement fired in production, which is the part I would not have believed without seeing:** exactly one structured record in the live pod's logs — `{"event":"workflow_guard_evaluation_failure", "transitionId":"request-restoration", "errorType":"FormulaEvaluationException", "error":"Expected bool, got null", "stackTrace":"...evaluateGuard..."}` — carrying community, workflow and instance ids and the original stack. So the throwing guard was **excluded and reported**, not swallowed, and the satisfiable transition proceeded. The naive `catch (_) { return false; }` would have passed every test and produced a silently missing button instead. Original finding below. ~~`ad-off-ad-suppression` is TERMINAL AT BIRTH: one guard's formula throws server-side, and because `availableTransitions` evaluates every guard eagerly, it kills all three transitions — including two that are perfectly satisfiable.** Found by a live walkthrough 2026-09-12, mechanism traced from the service's own stack trace, and **independently verified by me in Postgres.** **The failure:** signed in as the fan named in the row's own `memberFanId`, holding `ad-off-member` — exactly what `acknowledge-proof` requires — tapping "Mark reviewed" returns *"Could not save this change"*. `workflow-service` logs `FormulaEvaluationException: Expected bool, got null` at `formula_evaluator.dart:408` via `evaluateGuard` (`guard_evaluator.dart:64`) and `availableTransitions` (`transition_evaluator.dart:27`). **The mechanism:** the package declares `linkedEntitlements` as a **query-sourced** field, `entitlementStateCounts` as `groupCount(linkedEntitlements,'$state')` over it, and `isSuppressionActive` as a comparison over *that*; `request-restoration` guards on `formula: "!isSuppressionActive"`. **On the render path the chain evaluates** — the card displays "Ads suppressed now: Yes". **On the server guard path it resolves to `null`, and `!null` throws.** **The amplifier is the real defect and it is generic:** `_resolveTransition` calls `availableTransitions`, which evaluates **every** transition's guard eagerly inside a `.where(...)`. One throwing guard aborts the whole enumeration, so the request dies before `acknowledge-proof` — whose guard is `ad-off-member` + `actorEqualsField: memberFanId` and **was satisfied** — is ever considered. **So a single unevaluable guard makes an entire workflow immovable, not just its own transition.** That generalises well past this package. **My own verification, not the agent's:** **both** `ad-off-ad-suppression` rows in the database — `..._bessl9tachgu` (2026-09-12) and `..._2d0t7n4botql` (2026-09-08) — sit at `unreviewed` with `updated_at` **exactly equal** to `created_at`, i.e. never mutated, four days apart. **Control:** the two sibling rows spawned by the *same* `record-payment-confirmed`, advanced by the *same* fan in the *same* session, both show `updated_at != created_at`. Same community, same actor, same spawn — siblings move, this one never has. That eliminates authentication, membership, connectivity and node load by construction. **Two things to fix, and they are separable:** (1) the eager-guard amplifier — a guard that throws should disqualify *its own* transition, not abort enumeration for all of them; (2) the render/guard asymmetry — a formula over a query-sourced field resolves in one context and yields `null` in the other, which is the deeper bug and the one that will recur elsewhere. **Fixing (1) alone already un-blocks this row**, since `acknowledge-proof` and `review-again` are independently satisfiable. **Not a package defect** — the declarations are well-formed, and the affordance renders; this is the "valid package, unreachable as deployed" shape CLAUDE.md records. **Evidence:** `Evidence/B25/ad-free-community-ad-off-ad-suppression-live-write-2026-09-12.md`, an honest BLOCKED manifest correctly not counted by the bar.
- [x] **CLOSED 2026-09-12 — fixed (`f869e81e`) AND PROVEN ON A DEVICE.** A walkthrough on the rebuilt APK pressed the now-rendering "Request rollback" button, created `community_data_portability_export-transfer-rollback_8dmyaeanrm0d` and drove it `available → running → complete` (terminal). I verified the row in Postgres myself; the bar moved 22 → 23. **That device run is the proof, not the suites** — every one of the five suites passed *before* the fix too, which is exactly the gap this defect lived in. The dispatcher now computes authorized instance-scoped create actions ONCE (the filter went from 3 copies to 1) and forwards them to all eight card-construction sites through a shared render widget, so the three families with no declaration today (`equipment-loan`, `documentLibrary`, `searchAiAnswer`) are covered too. **Verified rather than accepted:** the regression tests fail 0/2 against unfixed code and again 0/2 with only the forwarding arguments neutralised, verbatim output recorded, source `cmp`-verified byte-identical on restore, then 2/2; all **five** suites run (the dispatch ran four and correctly reported that my ticket omitted `loom_workflow_service` — run afterwards with both credential sets: 164 passed, 1 skipped, and that skip count of 1 is how I know the PostgreSQL-backed tests actually ran); the one app-shell failure was a 30s timeout under load 42 that passes in **1 second** isolated on a quiet box, proven not assumed; no pod restarted during the run; and the diff touches no assertion counts. Original finding below. ~~`export-transfer-rollback` is UNCREATABLE: the binding dispatcher drops instance-scoped create actions for four `cardSurfaceFamily` cases. Found by a live walkthrough 2026-09-12, root cause verified by me in source, scope bounded by a corpus sweep.** **Symptom:** with a `export-transfer-verification` transfer sitting in `transferring` — the exact state the button is declared for — the Admin-tab card rendered its three transitions ("Send for provider verification", "Record transfer error", "Cancel transfer") and stopped. **"Request rollback" never appeared**, so the workflow cannot be created through any UI path and no row exists. **This is NOT "the package forgot a create action":** the package declares it twice, well-formed — `kind: create`, `workflowType: export-transfer-rollback`, `byRoleIds: ["portability-owner"]`, `scope: "instance"`, `presentation: "button"`, prefilling `sourceTransferInstanceId: "{context.id}"` — on bindings for `["transferring","awaiting-provider","failed"]` and `["verified"]`. The declaration simply cannot reach the screen. **Root cause, verified in `part27_engine_native_binding_dispatcher.dart`:** the file switches on `cardSurfaceFamily`, and the instance-scoped create filter (`action.kind == 'create' && action.scope == 'instance' && action.presentation == 'button' && action.byRoleIds?.contains(roleId) == true`) appears at only **three** sites — lines 364, 411 and 490 — which fall inside `event-rsvp`, `votePoll` and `default`. **Four cases construct their card without forwarding them at all: `equipment-loan` (424), `documentLibrary` (437), `searchAiAnswer` (449), `exportWizard` (461).** **Scope, bounded by sweeping all ten packages rather than guessing:** there are exactly **2** instance-scoped create actions in the entire corpus, and both are this same "Request rollback" on `exportWizard`. **So today's blast radius is one workflow, not four families** — I nearly over-scoped this ticket before running that sweep, and the walkthrough's own report described `exportWizard` as uniquely affected when in fact four cases share the defect and only one currently has a declaration to lose. **Fix, and prefer the general form:** rather than copying the filter into four more cases — a rule duplicated seven times is a rule that will be missed an eighth — hoist it so every family receives instance-scoped create actions by construction, with the card deciding whether to render them. That closes the latent hazard for `equipment-loan`, `documentLibrary` and `searchAiAnswer` too, where a future package declaration would today die exactly as silently. **Value:** unblocks one B25 row (`export-transfer-rollback`), the only row in the campaign blocked by app code rather than by provisioning or a spec gap. **Evidence:** `Evidence/B25/data-portability-community-export-transfer-rollback-live-write-2026-09-12.md` (an honest BLOCKED manifest, correctly not counted by the bar).
- [x] `new-ticket` — DONE 2026-09-12 (`a8017525`): the four affected Masjid tables now carry a note after each block naming the five `wf_*` rows as test-harness ids. Deliberately NOT in the first column, which is a parsed contract — the note says so, since annotating it directly broke the asset conformance test once. Verified: bar still 72, both doc-parsing tests pass — [detail](TODO-open-detail.md#row-158)
- [x] `new-ticket` — CLOSED 2026-09-12 as not-actionable: the forward fix already landed — `make_b25_brief.sh` requires every future run to record the driven package's `skillVersion` AND `sha256`. The 11 pre-2026-09-08 manifests are unidentifiable **by construction** and cannot be retrofitted with an identity they never recorded; treat any of them as evidence only while its package asset is unchanged — [detail](TODO-open-detail.md#row-160)
- [ ] `new-ticket` — pass-42 blockers — `c14` vision review is ~97% scaffold (7 of 204 screens carry real evidence) — [detail](TODO-open-detail.md#row-161)
- [x] `new-ticket` — DONE 2026-09-12 (`f26dac6e`): typed `fanId`/`fanId[]` pickers in all four builders, backed by a real member directory, all four spellings including the nullable forms. Verified by me: app shell **414 passed / 2 skipped** (was 408+2), analyze clean, zero removed assertions, and the regression test proven discriminating by neutralising the fix — [detail](TODO-open-detail.md#row-269)
  - [ ] `blocked` — ↳ NOT closed by the above, as scoped: a directory of real members still contains a moderator-only fan, and no schema field declares role eligibility. Needs a grammar/product decision on how a `fanId` field states which roles are eligible — [detail](TODO-open-detail.md#row-210)
  - [x] `new-ticket` — ↳ CLOSED by the above: role ids can no longer reach a `fanId[]` field. The contaminated Chess row is preserved and visibly flagged rather than silently repaired — [detail](TODO-open-detail.md#row-268)
- [ ] `new-ticket` — `{id}` in a `transitionRelated` filter resolves to nothing — 33 dead cascade sites; re-count after row-354 — [detail](TODO-open-detail.md#row-355)
- [ ] `new-ticket` — Backlog of deferred polish findings (ISO-8601 humanization, `maxLines` truncation, contradictory chip pairs) — [detail](TODO-open-detail.md#row-297)
- [ ] `new-ticket` — Membership-assignment call needs a fan actor, which a client-credentials token structurally cannot supply — header fixed, identity question open — [detail](TODO-open-detail.md#row-353)

### 4. Live validation — the production bar

- [ ] `new-ticket` — **Campaign RESUMED and running 2026-09-12: the bar moved 10 → 44 of 72**, the first movement since the standard was set. Proven this session: Camera Club `photo-walk-rsvp` (after fixing the defect that blocked it) and `gear-loan-request` (full six-transition loan lifecycle), Garden Club `garden-event-rsvp`, Cedar `hoa-facility-reservation` — the last confirming the `calendar.*` grant fix end to end, since it is the row that used to 403 for both HOA roles. **WALKTHROUGH HALF COMPLETE except for what the parity gate blocks, as of 2026-09-12.** The bar went **10 -> 44 of 72** in one session: 45 rows now hold a live write, and **every remaining walkthrough-only row (13) is Book Club or Youth Soccer, blocked on the parity drift** — see the release-blocker row. Two application defects were found, fixed, deployed and proven on device along the way: the binding dispatcher dropping instance-scoped create actions (`f869e81e`), and eager guard evaluation making any workflow with one unevaluable guard entirely immovable (`03b93f1c`, deployed as `loom-workflow-service:1.0.6`). **Remaining work is accounted for row by row in [B25-data-portability-run-plan.md](B25-data-portability-run-plan.md)**: 16 parity-blocked, 9 workable by paired capture+judge, 2 blocked by the unimplemented Messages tab, 1 needing only a judge. **Note the capture constraint before planning that phase:** canonical B25 evidence requires `--mode full-b25` across all ten communities and all nine phases — the tool rejects community or phase filtering, and its diagnostic mode says its own output must not be committed as canonical evidence. Cedar `hoa-architectural-request` was added 2026-09-12 (created live, driven to terminal `withdrawn`, verified independently in Postgres as `fan-hoa-member-1`). **42 rows still need only a walkthrough; 14 need both halves.** **Next, and it is the single biggest lever left: see [B25-data-portability-run-plan.md](B25-data-portability-run-plan.md).** All 8 remaining Data Portability rows are created by ONE role (`portability-owner`) from ONE tab (`admin`), 7 of them drivable end to end by that role alone, and `export-transfer-rollback` + `export-transfer-verification` can be banked in a SINGLE run because rollback has no create FAB and is only reachable from a transfer card. That cluster alone would move the bar 15 to 23. Pre-flight (all three seeding layers, the token fanId claim, published definitions, tab visibility) is verified in that plan -- do not re-litigate it. Drive selection from `Tools/code/list_b25_walkthrough_queue.py`, which names them — `check_b25_status.sh` reports only how many. **Two selection traps cost time here and are worth avoiding:** picking `chess-club-night` off the package rather than the queue got a real live write for a row that needs BOTH halves, so the live-write count rose and the bar did not; and a binding on `tabId: "messages"` will not render at all (AP-14, the shell ignores community bindings there), so check the tab before briefing a row — [detail](TODO-open-detail.md#row-209)
- [ ] `new-ticket` — Six fakes still back 53 distinct platform APIs while the cluster runs five real services — retiring them is unscoped — [detail](TODO-open-detail.md#row-213)
- [ ] `needs-verification` — Prove reminders live end to end — no reminder-bearing instance exists anywhere in the cluster to observe against — [detail](TODO-open-detail.md#row-245)
- [ ] `needs-live-validation` — §6 step 7 product-doc reconciliation gate — confirmed closed for Cedar Commons HOA only, 9 communities unconfirmed — [detail](TODO-open-detail.md#row-295)

- [x] **CLOSED 2026-09-19 — capture integrity: byte-distinctness now proves an action, and the frame threshold is a measurement.** Three commits: `256d22ea` (guard scoped to declared action-proof pairs), `4aa2b28c` (threshold 117 → 153, derived from a live run), `e1096b6a` (an unavailable row's frame shows its own instance). **All 26 duplicate frames in run `ev30` were manufactured by the harness; zero were product defects** — seven producer sites captured consecutive frames with nothing between them, so the guard failed itself on frames that could not differ. The fact that decided the fix's shape: Camera Club `gear-loan-request` was a FULLY PROVEN row that still failed, because `alternate_action` needed no scroll and matched `primary_result` — so reducing frames could never rescue it, and the guard had to stop enforcing global intra-row uniqueness. It now fails only a byte-identical **declared** pair (a tap sits between those two frames, so identity there is a stale write or an action that rendered nothing — the hole it exists to hold open), with a closure rule so an `attempted` row cannot dodge by declaring nothing. **The threshold was the near-miss:** `256d22ea` shipped 117 as an explicitly provisional estimate; the live run measured **157**, so a gate at 117 would have passed a run capturing 75% of the evidence and called it complete — invisibly, because a passing gate on a short run looks exactly like one on a full run. Gate is **153**, not the 150 I proposed: the binary fixture derives per-phase counts via `~/ 9`, so it must be divisible by 9. **Proven on a device, not only in tests:** run `ev31` gave `screenshotStatus=complete`, `screenshots=157/157`, `TOOL_EXIT=0`, zero duplicate findings across all nine manifests, against `partial`/exit 65/26 duplicates for `ev30`.
- [x] **CLOSED 2026-09-19 — an unavailable row's evidence shows the instance, not the top of the tab** (`e1096b6a`). Found by the first judge paired with a capture since the fix, and confirmed by reading the frames. `waitForEngineNativeWidget` polls `evaluate().isNotEmpty` and never scrolls, so `start` was captured with the instance far below the fold: the frame proved the member reached the right tab as the right identity and nothing about the row's own subject. Sharpest case, a **giveaway** row whose two visible cards were both `Mode: Loan`. 22 of 81 rows. **A scoping dispatch corrected the ticket twice and the second correction is the one that mattered:** I was about to ask for `ensureVisible` in both branches, but in the `actionWait.action == null` branch `prepareMarketplaceActionSurfaceForActionPolling` has ALREADY tapped the listing and opened the detail dialog, so the dialog IS the instance view — scrolling the instance finder there would have dragged the tile behind the modal barrier, re-creating the 2026-09-13 trap with a fix meant to improve evidence. One shared non-mutating helper, early-returning on an open dialog. **Verified on a device:** the giveaway row now frames its own subject — "Terracotta pots", `Mode: Giveaway`, `Owner: Garden Member`, `Transfer: Awaiting claim` — with the dialog in front of a dimmed barrier, which is the guard working. `start` was deliberately NOT moved: it is the baseline frame for all 81 rows. **Residual, stated so later verdicts are not misread:** this puts the instance in frame; it does not guarantee the frame shows a REASON, which is a per-archetype product property. Some of the 22 may now fail a judge on a genuine product gap. That is the correct failure to surface.
- [ ] `needs-verification` — **The app-bar community title clips to a single `(` in every captured frame — do NOT ticket it as a product defect yet.** Reported by the B13 judge, confirmed by me reading the frame. But the `(` is not the title text: Garden Club's `displayName` is exactly `"Garden Club"`, so the glyph is the left arc of a clipped "G". Measured off the native 1080px frame, the title slot is ~132px while the `LOCAL ENGINE` badge takes ~330px — and that badge is a **warning label** emitted only when a binding is local or unconfigured (`part52_service_binding_report.dart:278`), so the shipped remote app does not show it. Without it the slot is ~460px, which fits. **Not settled:** two of the four action icons are conditional, so production action width varies per community. **The check that settles it: read the app bar from a capture on a remote-engine build.** Same shape as "an APK containing the production app does not necessarily run the production app" — evidence about the build under capture, not about the product.
- [x] **FIXED 2026-09-19 — my own preamble fix was itself mis-scoped: it told the UX judge and live-verification agents they were implementation agents.** `data/dispatch_preamble.md` is shared by **all three** dispatchers (`call_implementation_agent.sh`, `call_ux_judge_agent.sh`, `call_live_verification_agent.sh`), and the role block I added earlier today opened with "You are the dispatched implementation agent" and closed with "Report what you changed, what you ran" — false for a reviewer, and the wrong deliverable for one. **No observable harm:** the judge and both live runs behaved correctly, because their own briefs are specific and overrode it. But a shared prefix asserting the wrong identity is exactly the kind of thing that misleads the one run whose brief happens to be thinner. **Rewritten to state the shared truth instead** — "You are a dispatched agent… the task itself says which kind you are" — keeping the part that actually mattered (you are NOT the orchestrator; do not dispatch, arm Monitors, or set project priority), which is true for all three. Synced to the VM and md5-verified, because the file is untracked and the two machines can silently disagree. **The lesson worth keeping: when you fix a shared artifact, check who else consumes it.** I found this only because I went to add a judge-format requirement and noticed the judge script sourcing the same preamble — not because anything failed.
- [ ] `new-ticket` — **A judge verdict is only counted if it uses the literal `**Workflow:** \`<id>\`` form, and nothing tells an author that — my two verdicts were invisible to the bar for hours.** `check_b25_status.sh:130` globs `*ux-judge*.md` and extracts workflow ids with `grep -oE '\*\*Workflow:\*\* \`[a-z0-9-]+\`'`. My B13 and B15 verdicts named their rows in prose headings (`## Chess Club — chess-export-package, owner`), so **nine judged rows counted as zero**. Judge coverage went 57 → 59 once an index was added (`ebacd828`); the two newly-visible rows are `garden-tool-giveaway` and `chess-export-package`. **The tell was there and I misread it:** the judge count did not move when I committed the B15 verdict, and I concluded those rows were already covered rather than that my artifact was invisible. **Scoped by population rather than assumed systemic:** all three older verdicts (2026-09-09) DO use the convention correctly, so the gap was mine alone — the live-verification agent follows the manifests' format, and my hand-written judge briefs never specified it. **This is the tool's own recorded failure recurring one layer down.** Its header comment describes a 2026-09-09 judge PASS the tool would have ignored because the verdict was markdown outside the JSON glob, "the measurement could not see the work that had just been done for it". Scanning markdown fixed that; the residue is that scanning only works for verdicts that happen to use the convention. **Two candidate fixes, and prefer the first:** make `data/call_ux_judge_agent.sh`'s prompt (or the brief template) require a `**Workflow:**` line per judged row, so the artifact is born countable; or have the tool also accept the heading form, which is looser and risks counting a workflow merely *mentioned* rather than judged. **A convention that is mandatory but undocumented will be missed again.**
- [ ] `needs-skill-dispatch` — **Youth Soccer's package has TWO severances that make FOUR bar rows uncreatable, both found live on a device 2026-09-19 and both invisible to the validator.** Every declaration is well-formed; the defects live in the reachability graph. **Severance A — the response fan-out is rejected by its own schema.** `soccer-practice-rsvp-response` declares **11** `required: true` fields (verified: its own block, lines 743–857) while the platform's response-row fan-out supplies **two**, so the engine rejects every fan-out with `Validation error on "eventTitle": Required field is missing or null` (reproduced twice on device). The parent `soccer-practice-schedule` still creates — it sits at `upcoming` in Postgres — so the failure does not roll the parent back; it just leaves it with no response rows. That makes **`soccer-reminder-notification` uncreatable**, since its only writer is a `createInstance` on `soccer-practice-rsvp-response.send-reminder`. **Severance B — a guard whose inputs have no reachable writer.** `soccer-minor-redaction` declares **no create action at all** (verified: 0 in its block, lines 436–518); its sole creator is a `createInstance` on `soccer-guardian-join-approval.approve-request`, guarded `waiverCheckpoint == 'acknowledged' && paymentCheckpoint == 'paid'`. Both fields are `writableBy: "effect"` with exactly one writer each, in different workflows, via `relatedInstance`, and both writers are unreachable — the payment one being the already-recorded "guarded role cannot see the instance" defect (`record-offline-payment` is `soccer-coach`-guarded; the payment renders only on a `soccer-guardian`-visible tab). **That same effect block also creates `soccer-team-roster`**, row 2 of Riverside's eight, likewise at **0** rows. **So one repair unblocks two rows, and the ticket must be scoped to the chain rather than to the row that surfaced it.** **Corroboration worth keeping:** the two-party `soccer-guardian-join-approval` walkthrough ([manifest](Evidence/B25/riverside-youth-soccer-guardian-join-approval-live-write-2026-09-19.md)) stopped at `submitted`, observed the coach's card genuinely not rendering "Approve and add to roster", and flagged the waiver leg as unexercised rather than blaming payment alone — both legs turn out to be severed, so that caution was correct. **Fix is Skill-authored** (required-field set must match what the fan-out supplies; the checkpoint writers need reachable surfaces), and per `solved-patterns.md` §26's amendments, changing an existing seed or adding one both carry hazards — check what depends on ordering, selection and counts first. **RE-VERIFIED BY ME 2026-09-20, independently and with controls, before this row is used to write a Skill brief — and unlike most rows I re-tested this week, every claim holds exactly.** `soccer-practice-rsvp-response` (lines 743–856) declares **11** `required: true` fields, control **109** file-wide so the query works, and the list includes `eventTitle` — the precise field the engine's rejection names. `soccer-minor-redaction` (436–517) declares **0** create actions against a control of **5** elsewhere in the same file, so the absence is real rather than a broken pattern; its only creator is the `createInstance` at line **221**, inside `soccer-guardian-join-approval` (94–314), and that transition's guard is verbatim `"formula": "waiverCheckpoint == 'acknowledged' && paymentCheckpoint == 'paid'"`. **Recording the positive result deliberately:** this project's habit is to re-test a blocker and find it rotten — three did rot today — so a row that survives its own audit intact is worth marking as trustworthy, otherwise the next reader re-derives it a fourth time. ~~**This row is now safe to write a ticket from.**~~ **OVERSTATED, AND CORRECTED THE SAME DAY — one of this row's claims is FALSE, and my audit could not have caught it.** I verified the *structural* claims (field counts, absent create action, guard text) because those are greppable, and then wrote "every claim holds exactly", which covered a claim I had never tested: that **both** checkpoint writers are unreachable. A root cause agent (`soccer-severances`) traced both end to end and **the waiver leg is reachable.** `acknowledge-latest-version` is guarded `allowedRoleIds: ["soccer-guardian"]` + `actorEqualsField: guardianFanId`, which the guardian passes as an ordinary role guard; the guardian can read the waiver three ways over (they are its `createdByFanId`, they are in `authorizedGuardianFanIds`, and `documentLibrary` is `ownerAndShared`); and the `documents` tab is visible to **both** guardian and coach. **Only the payment leg is severed.** **How the false claim got in, which is the reusable part:** a walkthrough stopped at `submitted` and reported the waiver leg **unexercised**; this row promoted that to **unreachable**. That is precisely the documented "we found nothing" versus "the system offers nothing" confusion — only the second is evidence — and it survived because my audit checked the claims that could be grepped and inherited the one that could not. **A count is greppable; reachability is not. Verifying the former and reporting the whole row as verified is how an unfalsified claim acquires false authority.** Sequencing note: the fix is Skill-authored, the product doc is upstream of the JSON, and the three docs under `docs/references/communities` are `chmod 444` — unlock only for the legitimate dispatch, then re-lock. Per standing rule the root cause agent scopes this **before** the brief is written.
- [x] **ITEM 3 FULLY CLOSED 2026-09-19 — the sweep is proven through the app's own Cancel button** ([manifest](Evidence/B25/event-cancel-response-sweep-ui-path-proof-2026-09-19.md), `a0273ec3`), closing the UI-path claim left open by the service-boundary proof. **Every write was a device tap:** coordinator created the event and tapped Maybe, member tapped Going, coordinator tapped **Cancel event**; all three rows → `cancelled`. Verified in Postgres by me — both response rows share `updated_at` `1789865913782`, the parent at `…772`, a 10 ms gap consistent with the sweep running after the parent's own write in one transaction. **The run's own controls were stronger than the brief required:** tap→write gaps measured (312/428/221 ms), so a tap that returned was never conflated with a transition that happened; both Chrome and app data cleared before each identity switch, *proven* by each sign-in re-showing an empty Keycloak form rather than a silent "You're signed in"; all eight typed form values settled against `instance_data` rather than the screen (the `input text` truncation trap); and an unprompted role-guard control — as the member, "Cancel event" was **absent**, matching `allowedRoleIds: ["garden-coordinator"]`. **It struck one of its own claims:** a drafted "1/10 going, 9 seats left" before-picture was removed because that frame was never captured, and the manifest says so. **It also verified its artifact does not inflate the bar** (`check_b25_status.sh` still 44 — I re-ran it). **Finding kept deliberately separate:** the sweep observed is the ENGINE HOOK, not the package's declared `transitionRelated` cascade, whose `{"eventId": "{id}"}` filter still matches nothing — so this manifest must not be read as evidence the declarative path works. **Infrastructure note that unblocked this:** `adb -H 192.168.56.1 -P 5037` does NOT work (port 5037 is closed to the VM; adb binds to localhost). The mechanism is an **ssh reverse tunnel** Windows`:5037` → VM`:5037`, and a VM-local adb server squatting on that port blocks it from binding — `adb kill-server` on the VM first, then establish the tunnel.
- [x] **CLOSED 2026-09-19 at the service/engine boundary — the event-cancel response-row sweep is PROVEN live** ([manifest](Evidence/B25/event-cancel-response-sweep-live-proof-2026-09-19.md), `7066f25a`). The hook (`2e29b792`) was committed 2026-09-18; the deployed image was built **2026-09-12**, so the running service never contained it — established by comparing image creation date to commit date rather than trusting the row. Built `1.0.8`, imported, deployed, three-source audit clean (and it caught an uncommitted manifest bump), service verified **answering** rather than merely `Running`. Then proved it: a fresh Garden event, two response rows driven to **different** non-terminal states (`going` by `fan-garden-member-1`, `maybe` by `fan-garden-coordinator-1`), coordinator cancels, both rows → `cancelled`. **Verified by my own Postgres query, not the agent's report.** **The load-bearing evidence is the timestamp, not the states:** all three `updated_at` values are byte-identical at `1789862946807`, the observable signature of the sweep running inside the parent's existing transaction — mechanism evidence rather than outcome evidence. Two different states was the point; a sweep handling one state would look correct against two identical rows. **Both negative controls hold, re-read by me:** the pre-fix pair from 2026-09-12 is *still stale* (`g45yq8qnsj95` at `going`, `updated_at` unchanged), so the sweep neither touched history nor swept broadly; and an uninvolved event created minutes earlier is still `open` with `created_at == updated_at`. **REMAINING, and stated as its own claim: the UI path.** The cancel went through the deployed HTTP API with real Keycloak tokens, not a tap — the agent flagged this itself, citing that an API proof "proves the service boundary, not that any UI path reaches it". The emulator was unreachable from that dispatch, which is **fixable and was my omission**: a VM-hosted agent reaches the Windows emulator via `adb -H 192.168.56.1 -P 5037`, and my brief did not say so. **BOTH HALVES OF THAT SENTENCE ARE NOW CORRECTED, 2026-09-19.** **(a) The UI-path claim is CLOSED, incidentally, by the Book Club walkthrough** ([manifest](Evidence/B25/neighborhood-book-club-book-meeting-rsvp-live-write-2026-09-19.md)): `cancel-meeting` was fired as a **real device tap** — the manifest's own UI table records "all actions gone" as the observed on-screen effect — and the related `book-meeting-rsvp-response` row swept to `cancelled` with an `updated_at` **byte-identical** to the parent's, `1789882252919`. Verified by my own Postgres query, not the agent's report; the response row carries `eventId` pointing at the parent, and the total moved 93 → 95 with exactly those two rows. So the sweep is now proven from the UI as well as from the API, and item 3 needs nothing further. **(b) The `adb -H` recipe above is WRONG and cost two dispatches before it was caught.** Windows' adb server binds **127.0.0.1 only**, so that host:port is unreachable from the VM. Measured with a control: a TCP probe from the VM to `192.168.56.1:5037` was refused while `192.168.56.10:30083` on the same network succeeded. The real mechanism is an **ssh reverse tunnel opened from Windows**, and a VM reboot both kills it and leaves the VM's own adb server squatting the port, so the squatter must be killed before the tunnel can re-bind. Full recipe now in `CLAUDE.md` (`c33e15d6`); the Book Club agent hit the same wall independently and reported that `ADB_SERVER_SOCKET=tcp:192.168.56.1:5037` simply hangs, which is a second, unprompted confirmation. **One honest limit on (a), stated because the alarming reading is not the supported one:** this run does **not** isolate *which* code path swept the row. The package's own `cancel-meeting` declares five `transitionRelated` clauses filtered on `{"eventId": "{id}", "$state": …}`, and those five cover **every** non-terminal state of the response workflow (`pending`, `going`, `maybe`, `declined`, `waitlisted`), so the package sweep and the platform hook converge on the same transition, on the same row, in the same state — indistinguishable from the database. The service logs nothing at that granularity either (436 lines in the window, zero naming the instance, with the control confirming the query works). **So the mechanism claim rests on row 368's earlier two-state API proof, and this run adds the UI path only.** Note also that the hook's existence now **masks** the recorded "`{id}` silently matches nothing" defect through any package whose sweep covers all states — that claim can no longer be tested this way, which is worth knowing before anyone cites it as still-live.
- [ ] `needs-live-validation` — **Item 3's device proof: the hook is DEPLOYED (`1.0.8`) and the pre-fix defect is still visible in live data, which gives the run a concrete before/after reference.** Queried Postgres 2026-09-19 after deploying: `community_garden_club_garden-event-rsvp_gwrvhmw1ztbk` is `cancelled` while `community_garden_club_garden-event-rsvp-response_g45yq8qnsj95` is still `going` — **the exact pair CLAUDE.md records from 2026-09-12**, a cancelled event whose response row still advertises a live RSVP. It is stale because that cancellation predates the hook, so **this pair is evidence of the bug, not a test of the fix, and must not be reused as one.** **What the proof still needs, and why it is not a five-minute job:** the live database holds exactly **one** Garden event and **one** response row, so there is no instance with two members in different non-terminal states. Remote communities start empty by design — package `workflowInstances` seed only the local engine — so the rows must be *created*, by two distinct authenticated fans, before a cancellation can demonstrate a multi-state sweep. **Recipe for the next run:** (1) obtain tokens for two seeded Garden fans (Keycloak `loom-<slug>`, fan id `fan-<slug>`, shared password recorded in the Access Control tracker — do NOT reset a credential, and clear BOTH the app data and Chrome between identities or a stale SSO cookie silently re-authenticates the previous fan); (2) create a fresh `garden-event-rsvp`; (3) have the two fans reach **different** non-terminal response states, e.g. `going` and `maybe`, since a single-state sweep would not discriminate; (4) cancel the event; (5) settle against Postgres — both response rows must leave their non-terminal states, and `instance_id` is the key column, not `id`. **Verify with a control**: confirm the two rows were in the expected states immediately before the cancel, so a sweep that did nothing cannot be mistaken for a sweep that had nothing to do.
- [x] **DEPLOYED 2026-09-19 — `workflow-service:1.0.8` carries the event-cancel response-row sweep, unblocking item 3's device proof.** The hook landed in the engine on 2026-09-18 (`2e29b792`); the deployed `1.0.7` image was built **2026-09-12**, so the running service genuinely did not contain it — verified by comparing image creation date to commit date rather than assuming the tracker row was still accurate. Built with `IMAGE_TAG=1.0.8` (the script takes the tag from the environment precisely so built and deployed cannot drift), after confirming the VM checkout actually contains `_sweepEventRsvpResponseRows`. Imported into k3s, manifest bumped, rolled out clean. **Post-deploy audit run rather than trusting the green rollout, and it caught a dirty tree** — cluster and manifest both `1.0.8`, six pods `1/1`, and the service ANSWERS (HTTP 404 at `/` = routing works, app replied), which is the check that separates serving from merely `Running` after a heavy build. The manifest bump is committed (`loom-backend` `7d99489`); that is the step this project has skipped twice, leaving the repo claiming one version while the cluster ran another. Build load peaked at 7.29 on 8 cores with no probe failures. **Remaining for item 3:** the device proof itself — cancel a Garden event with two members in different states and settle against Postgres. Backend is up and serving; k3s had been `inactive` (it is `disabled` and nothing starts it after a VM restart) and was started this session.
- [ ] `needs-verification` — **`b25Proven` from a capture run is NOT the production bar, and I conflated them repeatedly on 2026-09-19.** The capture tool reports `b25Proven=21/81`; `check_b25_status.sh` reports **44/72 with both halves** (45 walkthrough, 57 judge coverage). They measure different things: `b25Proven` is the **local-engine capture harness's** own walkthrough result, while the bar counts **live-write manifests** produced by the live-verification agent driving a real device against the real backend. A capture run cannot move the walkthrough half at all. **Why this matters beyond my error:** the two numbers are similar in shape, both are called "proven", both appear in B25 tooling output, and the smaller one appears in every capture summary — so it is the one a reader meets first. **This is the third instance of the same trap in one session**, after the frame threshold measured in file counts rather than `verifiedScreenshotCount`, and `screenshotCount` meaning two different things one level down. **Fix mechanically, not by note:** rename the capture field to something that cannot be mistaken for the bar — `captureRowsWithActionProof`, say — or have the capture summary print an explicit disclaimer line naming `check_b25_status.sh` as the authority. A metric whose name invites the wrong reading will be misread again, and I am the existence proof.
- [ ] `new-ticket` — **A two-party row captured under the local-engine identity model can pass while proving a SELF-transaction — the evidence looks complete and is about the wrong interaction.** Found 2026-09-19 by the B15 UX judge (`018cb47d`), unprompted, on Camera Club `gear-loan-request`: the member is offered **"Approve request" and "Decline request" on their own request**, and the detail dialog reads `Owner: Camera Club Member` / `Requested by: Camera Club Member`. **Cause is already documented** — the app shell has no member directory, derives one actor identity per ROLE, and aliases `fanId` to `roleId`, so owner and requester collapse into one person (same root as the Chess row holding `["chess-member", "chess-organizer", "fan-chess-member-1"]`). **What is NEW is the consequence for the bar:** such a row can reach `attempted` with a clean five-frame before/action/after sequence and a legible state change, and still be evidence of a member lending to themselves rather than of the documented two-party loan. **Nothing in the capture, the outcome name, or the frame set distinguishes the two.** The judge caught it only by reading the dialog text and noticing both names were the same. **Scope by the population:** every row whose documented interaction needs two distinct parties is exposed — loan/borrow, giveaway/claim, request/approve, invite/accept, and the `join-queue`/`leave-queue` family. This is the same class as Member Social Space's `connected` being unreachable with one `member` holder, except here it is worse: that row failed visibly, and this one **passes**. **The fix is not more seeded backend users** — the shell cannot see them; it is either a member-directory capability (a real feature, already noted as unscoped) or an explicit rule that a two-party row cannot be proven from local-engine frames and must rest on a live-write walkthrough. **Until then, treat a two-party row's judge verdict as evidence about rendering only, never about the interaction.** **AMENDED 2026-09-19 — the half of this row that asked whether a two-party row can be proven AT ALL is now answered YES, by demonstration, and it is the live-write walkthrough that answers it.** Garden's `garden-tool-giveaway` ([manifest](Evidence/B25/garden-club-garden-tool-giveaway-live-write-2026-09-19.md)) was banked with **two genuinely distinct fans**, confirmed by my own Postgres query rather than the agent's report: the row holds `ownerFanId = fan-garden-coordinator-1` and `claimantFanId = fan-garden-member-1`, so the guard `if(ownerFanId == $actor, false, true)` was satisfied by a *different* party — the exact thing the local-engine capture cannot express. A second row in the same window, `garden-notification` created by `fan-garden-member-1`, shows the effect cascade firing under the claimant's identity too. **Two mechanisms make the remote path immune to the aliasing**: real Keycloak accounts are distinct identities rather than role-derived ones, and `signIn`'s anti-impersonation guard *refuses* a mismatch — the walkthrough proved this positively by tapping the wrong account as a control and being refused on screen with `LoomAuthException(accountNotFound)`. **So the interim rule this row proposed is not merely a fallback; it is sufficient, and it is now the standing rule:** a two-party row rests on its live-write walkthrough, and its judge verdict covers rendering only. **What remains genuinely open, and is the only part still needing a decision, is the product question** — whether to build the member-directory capability so the *local* path can depict two parties honestly, or to accept permanently that local-engine frames depict one actor per role. That is a feature call, not a blocker on the bar, and no bar row is waiting on it.

- [ ] `plan` — **What actually remains of the 72-row bar, re-derived 2026-09-20 with the tool's OWN row extraction. 52 done; the other 20 split three ways, and the split changes what work is even possible.**

  | bucket | count | rows |
  |---|---|---|
  | **NEITHER half** — capture *and* judge, and they must be PAIRED | **12** | `book-reading-material`, `book-shared-library-item`, `chess-discussion-thread`, `chess-match-meetup`, `chess-pairing-queue`, `chess-rankings-table`, `chess-rules-documents`, `hoa-document-access-request`, `mosque-discussion-thread`, `mosque-document-resource`, `platform-message-thread`, `soccer-waiver-document` |
  | **judged, not proven** — a walkthrough alone moves the bar | **7** | `book-discussion-message`, `book-nomination`, `book-search-ai-digest`, `soccer-minor-redaction`, `soccer-practice-schedule`, `soccer-reminder-notification`, `soccer-team-roster` |
  | **proven, not judged** | **1** | `chess-club-night` |

  52 + 12 + 7 + 1 = 72, so the split reconciles against the tool rather than merely looking plausible.

  **My first attempt at this list was wrong and is worth recording, because the failure is reusable.** A blanket grep for table rows across the product docs returned **81**, not 72 — it swept *every* table, including rows the 79→77 reconciliation had removed (`garden-tool-loan-giveaway`, `chess-route-home`, `chess-local-install-open`, the `platform-*` pair). Presenting that as the remaining work would have queued dispatches against workflows no package ships. The fix was to stop writing my own parser and **reuse the tool's exact `awk`**, which anchors on the literal header `| Workflow | Persona | Expected decision |` and so reads only the B25 addendum table. **When a count must match an existing tool's, derive it with that tool's own extraction rather than a fresh query that merely looks equivalent.**

  **And the "proven, not judged" row is NOT the cheap win it appears to be.** A judge run reads PNG files at explicit paths; it does not drive a device. `*.png` is gitignored, so frames never survive their run — I checked, and the only 31 PNGs on the box are today's ad-hoc Book Club walkthrough frames (`01_launch`, `02_bookclub`, …), **none for `chess-club-night`**. So judging it requires capturing it first, and canonical bar-eligible capture is **`--mode full-b25` only** — the tool refuses `--communities`, `--phases` and `--shards` in that mode by design, so there is no cheap single-row capture.

  **The strategic consequence, which re-orders the queue:** **13 of the remaining 20 rows (the 12 plus `chess-club-night`) are reachable only through a full-b25 capture campaign paired with judging in the same sitting** — a heavier and different workflow than the individual live walkthroughs that took the bar 47 → 52 today. Of the 7 walkthrough-only rows, 3 are Book Club (one in flight, one blocked behind row-247's held defect) and **4 are Youth Soccer, blocked on the two package severances** in the Skill-dispatch row above. **So individual walkthroughs are close to exhausted as a way of moving this bar**, and the next real lever is the capture campaign, not more of what worked today.

  **SUPERSEDED IN PART, 2026-09-25 — USER DECISION ON THE BAR'S VALIDITY.** The split above counts a row as judged if *any* judge artifact names its workflow. **The user has ruled that insufficient: the judge AND the live walkthrough must both be re-run per community to meet the production bar.** Every judge "pass" in the current 53 traces to **one artifact dated 2026-07-02** against `appCommitSha b3c7991`; since then the packages have been regenerated, the app shell and engine have changed repeatedly, and three workflow-service images have shipped. The tool says so itself — *"treat 59 and 53 as coverage, not as passes."* **So 53 is not 53 proven rows. Treat the judge half as unproven corpus-wide until re-run, and do not quote 53 as a completion figure.** The remaining work is therefore not "19 rows" but **a capture-and-judge campaign across all ten communities**, paired in the same sitting because `*.png` is gitignored and frames do not survive their run. The walkthrough half stays genuinely current — those manifests were driven live this month against the real backend and each was verified against Postgres — so **the re-run is primarily the judge half**, with a live walkthrough alongside wherever a row's manifest predates the package it is meant to prove.

- [ ] `plan` — **EXECUTION SEQUENCE for the five parked tickets, user-authorised 2026-09-25 to dispatch in this order.** The ordering is not arbitrary: three hard constraints and one soft one decide it, and getting it wrong wastes a regeneration or judges screens that are about to change.

  **The constraints.** (1) `SKILL-summary-orphan-primary-bindings` **must precede** `SHELL-implement-bindingkind-summary`, or five states lose reachable actions in the interval — a refund path, a resubmit path, and four digest actions. (2) `SHELL-state-label-bespoke-cards` **must precede** `SHELL-implement-bindingkind-summary`, because a `summary` binding on a terminal state is exactly where the state badge is the card's only remaining content, so the read-only rule must be written knowing the badge exists and must not suppress it. (3) The three shell tickets **cannot run in parallel** — they all edit `part18`/`part26`/`part28`/`part36`, and two dispatches in one tree is the documented way to get a dirty checkout with no record of what finished. (4) Soft but decisive: **every code and package change lands before the capture campaign**, because re-judging screens that are about to change is the whole reason the current judge half is worthless.

  | # | Ticket | Route | Notes |
  |---|---|---|---|
  | 1 | ✅ **DONE 2026-09-25 — `d87f9875`.** **Book Club, FOLDED**: [regeneration](Tickets/SKILL-bookclub-regeneration-release-hold.md) **+** the `book-search-ai-digest`/`saved` primary binding from [orphan-bindings](Tickets/SKILL-summary-orphan-primary-bindings.md) | Skill | Folded into one dispatch as required. Verified with my own oracle: `POST /validate` → `pass, 0 errors, 19 warnings` (the agent proved all 19 pre-existing by validating the untouched shipped file as a control); workflows/roles/tabs identical sets 12/2/7; legacy queue fields absent 0/0/0; reminder machinery absent 0/0/0; `join_queue`/`leave_queue` present; `saved` moved not duplicated. All three copies synced. Definitions published (85 upserted, Book Club 12/12) and verified by structural jsonb query **with a control**. `check_permission_parity.sh` **OK** — and its expected side derives from the repo package, so it saw the new file; install would be a no-op. **All five suites run**, see the result row below. The restore artifact was never read, per the pre-flight finding that it was unusable in all three directions. |
  | 2 | [orphan-bindings](Tickets/SKILL-summary-orphan-primary-bindings.md) — **Ad-Free dispatched 2026-09-25; CEDAR HELD, needs a user decision** | Skill | Book Club's share went in step 1. Gate: `check_orphan_summary_bindings.py` must report **0** before step 5. **Cedar is held in full, not half-dispatched** — splitting it forces a second Cedar regeneration, which is what the "regenerate ONCE" rule exists to prevent. See the decision row below. |
  | 3 | ✅ **DONE 2026-09-26 — `b4b691f9`.** [state-label](Tickets/SHELL-state-label-bespoke-cards.md) **+** its follow-up [calendar-icon-assertions](Tickets/SHELL-state-badge-calendar-icon-assertions.md) | Implementation | One shared `_WorkflowStateBadge` in `part08`, not five copies; inserted as the first child of each card's Column across `part28`, `part35` and `part36`'s three cards. `displayContext` threads through so a marketplace tile and its detail dialog do not collide on one `ValueKey` — that was the cause of the mid-run failures, fixed structurally rather than by relaxing an assertion. `table` stays exempt with its own row, named explicitly in the conformance test, which loops the archetype registry so a new archetype cannot ship badge-less. **All five suites green**: demo 262, app shell **435 (+2)**, judges 525, engine 345 (+1), service 168 (+1), every exit 0; `flutter analyze` clean. The +14 is accounted: 14 archetypes − 1 exempt = 13 registry cases + 1 standalone. **The first dispatch exited 0 while leaving the demo suite red at 260 +2** — caught by running the suites myself, which is the whole reason the report is not the oracle. |
  | 4 | ✅ **DONE 2026-09-26 — `f7128ba3`.** [humanizer](Tickets/SHELL-adopt-field-label-humanizer.md) | Implementation | **The ticket's population was wrong and I corrected it before dispatch (`f5acf9ba`) — there were THREE implementations, not two, and `part32` was absent from the site list entirely.** `part26`'s and `part32`'s bodies were character-identical; `part18`'s differed *behaviourally* by handling underscores, so `foo_bar` read "Foo bar" on one surface and stayed `foo_bar` on the others. All three deleted for one `_humanizeFieldName` in `part08`, taking `part18`'s superset behaviour as a deliberate decision rather than inheriting whichever body got copied. **`part32` is why the correction mattered**: it is the `table` renderer, intercepting that family before the dispatcher, so omitting it would have made table columns humanize differently from every other surface — the same file, missed for the same structural reason, that was the documented exception in step 3. All five suites green: demo 262, app shell **443 (+2)**, judges 525, engine 345 (+1), service 168 (+1); analyze clean. +8 accounted: 7 new conformance cases + 1 in the calendar e2e file. The one existing test touched is a **comment-only** repoint of a stale "mirrors `_humanizeFactField`" note. |
  | 5 | [bindingKind](Tickets/SHELL-implement-bindingkind-summary.md) | Implementation | **Last, and riskiest** — 59 bindings change what they render. Blocked on steps 2 and 3. |
  | 6 | **Rebuild the APK, then capture + judge per community** | capture/judge | The bar campaign. Everything above must have landed. **The "can capture run from Windows?" prerequisite is ALREADY SETTLED — do not spend a tick re-testing it.** It was proven end to end 2026-09-12 (recorded near line 94 of this file): the VM builds the instrumented APK, Windows drives it with `--use-application-binary`, and the driver extension connected with no `errno = 111` and no port divergence. I re-derived the mechanism from the code today and it agrees: `b25_capture_drive_plan.dart:39-41` returns early on the prebuilt path so **nothing is built on Windows**, which is why the Windows symlink restriction never applies. **One consequence to honour when building the APK:** that early return also **omits every `--dart-define`**, so the instrumented APK must be built with `LOOM_PRELOAD_EXAMPLE_COMMUNITIES` *and* `LOOM_EVIDENCE_EXTERNAL_ANDROID_SCREENSHOTS` compiled in — a prebuilt binary missing them captures nothing and looks like a tool failure. Pre-grant `POST_NOTIFICATIONS` with `adb install -r -g`, or every frame is refused. |

- [ ] `evidence` — **Regenerating Book Club and Ad-Free INVALIDATED 13 of their own live-write manifests, by design. Step 6's walkthrough half is no longer "already current" for those two communities.** Measured 2026-09-25 by comparing each manifest's recorded `sha256` against the current provenance manifest.

  | Community | Manifests recording a sha | Recorded | Current | Verdict |
  |---|---|---|---|---|
  | Ad-Free | 6 | `dd455f48…` (×5), `6f022a4f…` (×1) | `13b89460…` | **all stale** |
  | Book Club | 7 | `71e1f068…` (×7) | `23acd52b…` | **all stale** |

  `dd455f48…` is exactly the sha the Ad-Free provenance diff replaced in `a8c01fe5`, so this is not an inference — it is the same value, before and after, in two independent files.

  **This is the `skillVersion` + `sha256` requirement working, not a regression.** It was added precisely so a manifest could be checked for staleness rather than silently proving a row against a package that no longer exists. The evidence self-invalidated the moment its package changed, which is the designed behaviour and the reason to keep recording package identity in every manifest.

  **Consequence for the plan, and it widens step 6 rather than narrowing it.** The standing framing has been "the walkthrough half is current, the judge half is corpus-wide invalid, so the re-run is primarily the judge half." That remains true for the other eight communities and is **now false for these two**: both halves need re-driving for Book Club and Ad-Free. Any further package regeneration does the same to its own community, so **regenerate before capturing, never after** — which is exactly why the sequence puts every package and code change ahead of step 6.

  **Do not quote 54 as the walkthrough figure without this caveat.** `check_b25_status.sh` counts a manifest that names a workflow; it does not yet compare the recorded sha against the current package, so these 13 still count toward its total. That is a real gap in the gate, and the honest reading of today's numbers is: 54 live-write rows of which up to 13 are against superseded packages.

- [ ] `needs-user-decision` — **Cedar's `hoa-committee-decision`: the orphan ticket's instruction is WRONG for this workflow, and the ticket itself said to stop rather than guess.** Raised 2026-09-25. **Blocks the orphan gate reaching 0, and therefore blocks step 5 (`bindingKind`).**

  **The mismatch, verified by reading the package rather than the ticket.** `hoa-committee-decision`'s only `primary` binding is on the **`admin`** tab, and Cedar declares `admin` with `visibleRoleIds: ["hoa-board"]` — board only. But `changes-needed`'s only two exits, `owner-resubmitted` and `owner-withdraw`, are guarded `allowedRoleIds: ["hoa-member"]` with `actorEqualsField: requesterFanId`. **Following the ticket would move member-only actions onto a board-only tab** — nobody who can fire them could see them. That is this project's recorded "guarded role cannot see the instance" shape, the same one that killed Youth Soccer's payment row.

  **The product doc deepens the question instead of settling it.** §7's matrix says the homeowner *receives* the approved/rejected/**changes** state in this workflow, and §143's B25 row gives it persona **board**. Meanwhile the member's genuine action on `changes-needed` already exists in a *different* workflow — `hoa-architectural-request`'s `resubmit-request` — whose primary is on `requests`, a tab **both** roles can see. So the two committee-decision transitions may be redundant mirrors rather than a reachability gap.

  **The options, with my recommendation:**
  1. **Follow the ticket** — add `changes-needed` to the `admin` primary. Minimal, and leaves the actions unreachable by the only role guarded to them.
  2. **Give `requests` a primary covering `changes-needed`**, narrowing its existing all-states summary to match. Render-bindings-only, inside the ticket's scope, and puts the actions on a tab the acting role can see. **Recommended.**
  3. **Treat `changes-needed` as genuinely read-only here** and accept that `owner-resubmitted`/`owner-withdraw` duplicate `resubmit-request`. Closest to what §7's wording implies, but retiring transitions is a package change **outside this ticket's scope** ("do not alter any transition, guard or state").

  **`hoa-architectural-request` is NOT affected and its instruction is correct** — its primary is on `requests` (both roles), and `reopen-case` is board-guarded, so the board can both see and fire it. Only `hoa-committee-decision` is in question.

- [x] `test` — ✅ **DONE 2026-09-25 — `4192dbb8`. The demo app suite is GREEN for the first time: 262 passed, exit 0, zero failures, zero skipped.** Inverted the Book Club audience-block test, which asserted a defect step 1 had fixed. [SHELL-invert-bookclub-audience-block-test.md](Tickets/SHELL-invert-bookclub-audience-block-test.md).

  **Verified with my own oracle, not the dispatch's report.** The diff adds *positive* claims rather than merely dropping the negative one — per row it requires a non-null selector, that the selector is the `book-member` candidate's fan id, and that the instance carries create-time identity in its own field, keyed from the existing `expectedRows` table. That mattered: an inversion that only deleted `isBlockedByAudience` would have proven nothing and passed just as green. Scope held to exactly one file (28 insertions, 15 deletions); the six sibling cases that prove the blocking *mechanism* on synthetic instances are untouched.

  **`flutter analyze` reports 2 issues — A/B'd rather than assumed pre-existing.** Checked out HEAD without the change: 2 issues. With it: 2 issues. Identical, so neither was introduced, and the working file was restored `cmp`-verified byte-identical. Recorded in CLAUDE.md so a **third** issue is known to be new.

  **The baseline is updated, which is half the work.** This suite's row told readers for months to expect exactly one failure and identify it by its message. Leaving that in place would have made the first genuine regression look expected — the mirror of the trap the row was written to prevent.

  **The result of step 1's five-suite run, and why one red X is the best news in it.** Judges **525** exit 0, app shell **421 (+2)** exit 0, engine **345 (+1) = 346 cases** exit 0 (total identical to the 341+5 baseline — four PostgreSQL tests that previously *skipped* actually ran, so more was proven, not less), service **169 total** with three `TimeoutException`s that **all passed isolated at `--concurrency=1` in 13s/9s/7s**, proven environmental rather than assumed.

  **The demo app's long-held known failure is GONE.** `b43_book_engine_migration_test.dart`'s owner-visibility check — `Found 0 widgets with key 'generic-instance-card-nom-draft-1'` — now passes, because `book-nomination` has create-time identity so a `draft` has a rendering identity and its own author can see it. That is row-247's defect, closed and proven by the test written to detect it.

  **What is red instead asserts the defect still exists.** `b25_actor_audience_resolution_test.dart`'s *"the held Book Club package records all four audience blocks"* reads the shipped package and requires all four seeds to be blocked by an absent `actorEqualsField`; all four are repaired, so the filtered list is **empty** where it wanted four. **It is stronger evidence than the failure it replaced** — `b43` proved one workflow, this proves all four (`book-nomination`, `book-vote-response`, `book-shared-library-item`, `book-search-ai-digest`) at once. The fix is to invert its polarity, not to delete it: it is the only check covering all four against the real asset. The other six cases in that file use synthetic instances, prove the blocking *mechanism*, and must stay untouched.

  **The durable lesson, folded into CLAUDE.md: when a fix lands, the suite that goes red may be the one that was guarding the defect's presence.** Two tests pointed at the same repair from opposite directions and only one was in the baseline. The existing rule ("identify the known failure by its MESSAGE, not its name") is what made this quick; the missing half was that a *newly* red test can be a success signal. Expected end state after this ticket: **262, 0 failed** — the first fully green demo-app run this project has had.

  **Before step 6, settle one open hypothesis rather than assuming it.** The recorded blocker says the capture pipeline cannot run from the VM: `flutter drive` dials the VM's own loopback while the `adb forward` listener is on Windows, and the obvious port-pinning fix was tested and failed. **But the capture tool accepts `--use-application-binary`**, so it need not build where it runs — running it **from Windows** against a VM-built APK puts the driver on the same host as the listener. That is a cheap test and it decides whether the campaign is straightforward or needs real work. Test it before planning the campaign, not during it.

- [ ] `queue` — **Book Club's remaining bar rows, with winnability established by reading each workflow rather than assumed (2026-09-19). All five are drivable; none is blocked on a missing platform service.** Recorded so no future tick re-derives it and, more importantly, so no agent reports a deliberately-empty field as a defect. `book-meeting-rsvp` and `book-vote` are already done or in flight.

  | row | verdict | what a brief must say |
  |---|---|---|
  | `book-selection-publish` | clean | nothing special |
  | `book-export-metadata` | clean | carries **no** `NEEDS IMPLEMENTATION` annotation, despite this project's export-checksum history — do not assume one |
  | `book-search-ai-digest` | **drivable** | `draft → submit-query → open → save-digest → saved`; **no guard reads `answer`** |
  | `book-discussion-message` | core drivable | its 4 annotations are per-member thread read/unread/mute state, i.e. notification side-effects, not the message flow |
  | `book-nomination` | drivable, known defect | `nominatorFanId` is stamped only on submission, so a `draft` is invisible to its own author — **known, accepted, fix HELD** by row-247; record and move on |

  **The trap this table exists to prevent:** `book-search-ai-digest` declares `answer` as `writableBy: "platform"`, annotated *"NEEDS IMPLEMENTATION (platform service): AI answer generation… never seeded and never written by an effect"*, with `hideWhenEmpty: true`. That is the **honest** pairing this project's rules call correct, not the export-checksum shape where a field claims a writer it never had. I nearly deprioritised the row on the strength of the annotation alone, and **checking the guards is what corrected it** — the state machine never reads the field, so the workflow reaches `saved` with the AI answer simply absent. **An agent that reports that empty field as a defect has found the designed state.** Same for Book Club's five annotations generally: they are the visible surface of the four missing platform services, and no Skill dispatch can fix one.

  **Credentials for all of these:** `loom-book-organizer-1` and `loom-book-member-2`, both verified HTTP 200 on 2026-09-19. `loom-book-member-1` is genuinely dead and is never needed.

  **AMENDED 2026-09-19 — two of the five are blocked by a PLATFORM defect, not by anything in Book Club.** `book-selection-publish` was driven live and **writes fine but renders for nobody**: the instance persisted in `draft`, every call returned `200`, and no card appeared for its own creator across seven tabs and an app restart. `book-export-metadata` will fail identically. Do not dispatch either until the fix below lands.

- [ ] `in-flight` — **`audience: "actor"` bindings render for NOBODY on the remote path, because the instance projection omits creator identity.** Found 2026-09-19 by the `book-selection-publish` walkthrough ([manifest](Evidence/B25/neighborhood-book-club-book-selection-publish-live-write-2026-09-19.md)), which correctly recorded **not-proven** and declined to fire the transition through the API — that would have proved the service boundary, not the UI path. **Chain verified by me reading the code, not from the agent's report:** the remote client decodes every listed instance with `createdByFanId: ''`; `role_resolver.dart:30` falls back to that creator whenever no transition declares `actorEqualsField`; so `viewerFanId == ''` never matches a real fan and the `actor` role is never granted. **Population swept by me at STATE granularity** — my first sweep was per-workflow and reported **zero affected**, which was wrong at the wrong granularity, since a workflow can bind `any` on one state and `actor`-only on another. Correctly scoped it is **4 dead state-bindings in 2 workflows**: `book-selection-publish` (`draft`, `scheduled`) and `book-export-metadata` (`draft`, `redaction-previewed`), both B25 bar rows. **No other package is affected** — every other actor-binding workflow either declares `actorEqualsField` (9, immune) or pairs `actor` with another audience on the same state; control: 152 non-actor state-bindings found, so the sweep works. **This is a PLATFORM defect, not a package one**, and that determination is what decides the fix's layer: `render-bindings.md` promises normatively that `actor` resolves to the creator when no `actorEqualsField` exists, so the two packages are **conformant**. Regenerating them would repair 4 bindings and leave the contract broken for every future package using the documented fallback — and adding a validator rule banning `actor`-only bindings would codify a platform defect as an authoring restriction, the retired `dead_role_binding` shape. **The blocker was a comment, and it was false:** `remote_workflow_engine_api.dart:585` claims the projection "deliberately" omits creator identity. Root cause agent (`actor-audience-remote-projection`) established there is no such decision — the comment documents the spec's lean v1 shape, the original spec commit's enumerated decisions do not include it, and the **change feed already exposes `createdByFanId`** to an audience filtered by the same `readVisibleInstance` rule. Verified by me: that field occurs at `workflow-engine-api.openapi.yaml:815` **and nowhere else**, and `WorkflowInstance` declares only `instanceId`/`workflowType`/`currentState`/`instanceData`. Lesson folded into `CLAUDE.md` (`2192dbbd`). **Fix dispatched** (implementation agent, `--fresh`, sonnet): add the field to the spec as **optional** (an old service must not make a new client throw), emit it from the service's instance projections, decode it in the client. **I gave the ticket all 15 candidate service serialization sites from my own sweep** rather than the ~6 the scoping named, requiring the agent to classify each — several are membership/document/queue rows that must NOT gain the field. Three regression tests required, one per layer, each demonstrated failing first, because this defect is **invisible to local-engine tests** (the local path supplies a real creator, so everything passes today). **Remaining after the dispatch lands:** build, roll out, commit the manifest bump, then re-drive the Book Club row live — that walkthrough is the only check that closes the evidence gap. No package regeneration, definition publishing or App Access provisioning is involved.

  **FIX LANDED 2026-09-20 in `8d33a8af`** — 8 files, 158 insertions. **That commit's message is TRUNCATED mid-sentence**: a nested heredoc broke while committing over ssh, and the contents are complete and correct while the prose stops at "The remote client decoded every instance with". I deliberately did **not** amend and force-push `main` to repair a commit message — the VM tracks this branch and the content is right — so **this row is the full record of that change**, and a reader arriving at the commit should come here. **Spec:** `createdByFanId` added to `WorkflowInstance` as **optional, deliberately not `required`**, so an older deployed service cannot make a newer client throw; no backend twin exists for this API and `check_spec_parity.sh` passes 8 identical / 0 differ. **Service:** all 15 of my candidate `'instanceId':` sites classified, **5 changed**, covering all six client-facing `WorkflowInstance` endpoints one-to-one with the client's six decode sites; membership, document, queue-entry, guard-log and export-bundle maps deliberately left alone, and the change feed already carried the field. **Client:** the decode prefers the server value and keeps the caller-supplied fallback for rollout; the false `:585` comment is replaced with one stating the real contract. **Verified by my own oracle, not the agent's report:** no assertion anywhere in the diff was removed or weakened; declared-test counts are **+2 in each package with zero deletions**, which is the check that catches a deletion hiding inside a rising total; and the A/B — neutralising **only** the client decode line turns the new client test red with `Expected: 'fan-alpha' / Actual: ''` and takes the role test with it, after which restoring gives byte-identical source and 33 green. A regression test never observed failing is not a guard, and these were observed failing for exactly the reason they claim. **One reported suite figure I did NOT re-derive and am flagging rather than repeating as fact:** the engine ran 341 against a 330 baseline, and the agent attributes +2 to itself and +9 to drift from commits since the 2026-09-13 measurement. My declared-count check confirms only the +2 half; the +9 is unverified. **Deploy is NOT done and the fix is inert until it is** — and note it is a *two-artifact* deploy, which is easy to half-finish: the **service image** must be rebuilt, imported, rolled out and its **manifest bump committed**, and the **instrumented/debug APK must be rebuilt too**, because the client half of this fix ships inside the app. Re-driving the Book Club row against a new service but a stale APK would reproduce the original symptom and read as the fix having failed.

  **CLOSED 2026-09-20 — deployed, and proven on the UI path, which is the claim that matters.** `loom-workflow-service:1.0.9` rolled out; three-source audit clean (cluster `1.0.9`, manifest `1.0.9`, and the **manifest bump committed and pushed** as `681a20c` in `loom-backend` — the step this project had skipped twice). Both artifacts shipped: the APK was rebuilt, transferred **byte-identical** (md5 verified, 232,243,270 bytes) and installed `-r -g`, because the client half of the fix ships inside the app. **Service boundary proven first, by my own call:** a live `queryInstances` now returns `"createdByFanId":"fan-book-organizer-1"` on the exact draft that had rendered for nobody, with `currentState` as a control. **Then the UI path proven separately, which is the part that was actually in doubt** — and the brief said so explicitly, because treating the service check as evidence about the UI is the precise substitution this row existed to avoid. `book-selection-publish` was re-driven on the device: the same draft rendered in full for its creator (state badge, all three fields, `Publish announcement` present and enabled) and went to **`published`**. Verified by my own Postgres query: `current_state = published`, `created_by_fan_id = fan-book-organizer-1`, the authored effect `publishedAt = 2026-09-20T07:53:30.158921Z` persisted — so the engine ran the real transition rather than flipping a state — and `updated_at > created_at`. All three of that workflow's render bindings now behave, including the `audience: "any"` Home binding. **Bar 50 → 51**, re-derived by me. **Worth keeping about the deploy itself:** two of my own waiters were wrong before one was right. The first reported the image build complete while it was at Step 4/11, because it grepped the log for `ERROR|failed` — text a healthy build emits — and used a `pgrep` that can match or miss for unrelated reasons. Re-armed on the **artifact** (`docker images` listing the tag), it was correct. That is this file's own lesson arriving in my own code: a check whose signal can be produced by something other than the thing it detects is not a check, and an image tag cannot be faked by a log line. **Remaining from this defect:** `book-export-metadata`, the other affected row, is unblocked and dispatched.
- [x] `RESOLVED 2026-09-25 — ticketed, not dispatched; see the resolution at the end of this row` — ~~**The result-frame postcondition assumes every archetype renders its state label.~~ For a `bindingKind: "summary"` terminal state, nothing renders it, so the row can never be proven — and three successive fixes could not change that.** `garden-tool-giveaway` fires `claim-giveaway`, the engine **persists `given`** (the harness verifies this), and the row still records `action_succeeded_result_unverified`. **The diagnostic is now conclusive**, listing all 14 rendered forms tried — `Ownership transferred`, `Transfer: Ownership transferred`, `Claimed Ownership transferred`, and eleven more — none present. **Cause:** Garden's binding for `["given", "delisted"]` is `bindingKind: "summary"`, a compact card that does not render the fact-pill row carrying `transferSummary`. The label is not mismatched; it is **absent by design**. **What this is NOT:** not an engine defect (state persisted), not a package defect (a summary card for a terminal state is a reasonable product choice), and not a locator defect any more — `962d4700` correctly composes every `labelTemplate` form via the production renderer, and the exhaustive list above is that fix working. **The open question is what counts as visible proof when the product deliberately renders less after a terminal transition.** Three candidate answers, and this needs a decision rather than a fourth guess: (a) accept verified persistence plus a frame of the changed card as proof, dropping the label requirement for summary bindings; (b) require a different visible postcondition per binding kind — e.g. the card's *disappearance from the available list* is itself the observable result; (c) treat it as a product gap and have the summary card surface its terminal state. **Note (c) changes the product to suit the test, which this project forbids elsewhere** — flagging that so it is chosen deliberately or not at all. **History, so nobody re-walks it:** three fixes landed in sequence, each correct and each revealing the next layer — the selector picked by declaration (`b3e649b8`), then the locator sought a bare label the UI never emits (`962d4700`), and now the label is not rendered at all. `b25Proven` has held at 21/81 throughout. **SECOND INSTANCE FOUND 2026-09-19, in a different community and a different archetype — this is a platform-wide pattern, not a Garden quirk, which sharpens the decision below.** Book Club's `book-meeting-rsvp` reaches terminal `cancelled` and the calendar card **renders no cancellation notice at all**: the walkthrough recorded the card still reading `B25 Reverify Sep19 / 12 going / 12 seats left / 19:00` with only the action buttons gone, while the package declares that state `label: "Cancelled", tone: "negative"`. Same mechanism as Garden's — the `cancelled` binding is `bindingKind: "summary"`, and a summary card does not render the fact-pill row carrying the state label. **The user-facing consequence is now concrete and worse than the evidence problem:** a member opening a cancelled meeting sees a normal-looking event with a live attendee count and no indication it was called off. The information is in the model and not on the screen. **That reframes option (c).** It was recorded above as "changes the product to suit the test, which this project forbids" — but on this evidence the product has a real defect independent of any test, so surfacing the terminal state on a summary card would be fixing a user-visible gap that the harness merely happened to detect first. **This does not decide it** — whether a compact terminal card should carry a state label is still a product call, and it is the same call for both communities — but the option should no longer be weighed as test-driven. **RESOLVED 2026-09-25 BY USER REVIEW — and the diagnosis above is WRONG in its central mechanism, so read this before quoting any of it.** The user asked to see the product lines, the JSON and the resulting issue. Assembling that falsified the `bindingKind` explanation this row repeats four times. **`bindingKind` is not read by the renderer at all.** `resolveBindings` (`binding_resolver.dart:10-37`) filters on `states`, audience and `audienceMemberField` only; the card switch (`part27:364`) keys on `cardSurfaceFamily`; and there are **zero** `.bindingKind` reads in the shell's `lib/` — `part02_tab_shell.dart:996` is a hardcoded `'primary'` in an unrelated Messages fixture, a write not a branch. So a `summary` binding renders a **fully interactive card** today, and "a compact card that does not render the fact-pill row" was never the mechanism. **The real mechanism:** only `GenericWorkflowInstanceCard` (`part26:507`) and `ExportWizardArchetypeCard` (`part36:2538`) render the declared `states[].label`; five other bespoke card families never read it, in **any** state — `open` and `cancelled` are equally unlabelled, so this was never a terminal-state property either. Terminal states are merely where it becomes visible, because that is when the buttons vanish and the label is the only signal left. **The three-way decision this row posed therefore dissolves rather than being answered.** It is not an evidence-standard question: `workflow-grammar.md:279` marks `label` the **only required key** on a state with the documented meaning *"shown in the UI"*, so the packages are conformant and the shell is the defect — the same shape as the `createdByFanId` fix (`8d33a8af`). Option (c) is simply correct, and not test-driven. **Scoped by the root cause agent** (session key `state-label-bespoke-cards`), which corrected two of my premises: there is no shared dispatcher wrapper to hang the badge on (the switch returns each card directly, and a new wrapper could only stack outside the `Card` border, double-rendering for ~221 of ~250 bindings), and my "the contract is silent" claim was wrong — I had searched `docs/references/reference/` with a passing control on a different key, which proved my query worked and not that I was querying the right file. **Ticket written, deliberately NOT dispatched** (user-directed): [SHELL-state-label-bespoke-cards.md](Tickets/SHELL-state-label-bespoke-cards.md) — five per-card insertions behind one shared `_WorkflowStateBadge` helper, plus a registry-driven conformance test over `knownWorkflowArchetypeIds` as the recurrence gate. **My population was also short by one:** `table` is intercepted at `part32:132` **before** the dispatcher and renders no state either, which is the decisive argument against a dispatcher-level fix and is why the gate is a registry test rather than a wrapper. Three follow-ups split out below.

- [ ] `ticketed` — **DECIDED 2026-09-25 (user): IMPLEMENT `bindingKind`.** Two tickets, **sequenced**, neither dispatched: [SKILL-summary-orphan-primary-bindings.md](Tickets/SKILL-summary-orphan-primary-bindings.md) **ships first**, then [SHELL-implement-bindingkind-summary.md](Tickets/SHELL-implement-bindingkind-summary.md). **The sequencing is the whole finding, and it was not visible when the decision was taken.** I swept all ten packages to size the behaviour change: **115** `summary` bindings, of which **59** include a non-terminal state. That sounds alarming and mostly is not — for **115 state-coverages** a `primary` binding elsewhere also covers the state, so the primary keeps the actions and the summary correctly becomes a status view, which is `render-bindings.md`'s own documented intent. **But 5 states have NO primary binding at all, and every one has live outgoing transitions**, so read-only would remove reachable actions with no fallback: Ad-Free `ad-off-community-checkout`/`funded` loses `request-community-refund`; Book Club `book-search-ai-digest`/`saved` loses **four** (`edit-digest`, `add-citation`, `report-stale-citation`, `withdraw-query`); Cedar `hoa-architectural-request`/`approved` and `/denied` each lose `reopen-case`; Cedar `hoa-committee-decision`/`changes-needed` loses `owner-resubmitted` and `owner-withdraw`. **A refund path and a resubmit-after-changes path are not cosmetic losses**, so the packages gain primary bindings for those five states *before* the shell change lands. Exit-checked by reading every transition's `from` list rather than trusting `isTerminal`; control on the primary side returned 115, so the query matched. **Note the Book Club overlap:** `book-search-ai-digest` is also touched by the regeneration ticket, and two independent regenerations of one community is what row-247's "regenerate ONCE" instinct guarded against — fold them. ORIGINAL FINDING: **`bindingKind` is a REQUIRED grammar key that nothing in the shell reads, while the validator enforces authoring rules premised on it.** Found 2026-09-25 while assembling the state-label evidence. `render-bindings.md:33` marks it `// REQUIRED`, `:49` lists it required in the field table, and `:553-560` documents it normatively — `primary` = *"Full, interactive card — includes the action buttons"*, `summary` = *"Compact/read-only card"*. **Verified inert end to end, all three places it could act:** resolution ignores it (`binding_resolver.dart:10-37` filters on `states`, audience, `audienceMemberField`), card selection ignores it (`part27:364` keys on `cardSurfaceFamily`; `part32:132` on `cardSurfaceFamily == 'table'`), and rendering ignores it (zero `.bindingKind` reads in the shell's `lib/`; `part02:996` is a fixture write). Interactivity comes from elsewhere entirely — action buttons from transitions and guards per instance, editors from `states[].editGuard` (`part28:1634-1656`), compactness from `displayContext`/`showEditors`, which are **surface**-driven (`part32:194-199` sets `detail` for `notificationInbox`) and never binding-driven. **The only real consumers are the validator** (`workflow_validator.dart:2217,2381`, rules premised on `primary` cards carrying action rows; `:607` actively *recommends* authoring `"bindingKind": "summary"` for otherwise-invisible states) **and a test-harness ordering heuristic** (`workflow_ui_evidence_test.dart:3742,4807`). **So authors pay a real cost — a required key, plus validator rules — for a distinction the platform does not implement.** **Two options, and this needs a decision rather than a default:** implement `summary → compact/read-only`, or amend `render-bindings.md:553-560` to describe reality. **Implementing it is NOT safe to bundle with anything** — it would change live behaviour for every shipped `summary` binding on a *non-terminal* state, and Youth Soccer already declares them on `active` (`jsonc:416-417`), so it needs a sweep of all ten packages to size the change first. **A REQUIRED key that is never read is the worse steady state**, because the validator keeps enforcing rules premised on it. Same family as the `createdByFanId` comment that froze an accident into policy.

- [ ] `new-ticket` — **The `table` archetype card renders no state label, and it bypasses the card dispatcher entirely.** `WorkflowTableArchetypeCard` is selected in `part32_engine_native_list_surface.dart:132` — *before* `EngineNativeArchetypeCard` is reached — so it is invisible to any fix applied at the dispatcher. 5 shipped bindings: Chess ×2, Youth Soccer ×3. Split from [SHELL-state-label-bespoke-cards.md](Tickets/SHELL-state-label-bespoke-cards.md), whose conformance test **explicitly exempts this family with a TODO pointing here** rather than silently passing it. **Worth keeping as the reusable point:** `part27:311`'s own comment calls itself *"the single place `cardSurfaceFamily` is ever switched on for rendering purposes"* and that is false — `part32` switches on it too. A comment asserting singularity is not a census; grep for the key before trusting it.

- [ ] `new-ticket` — **`part26_generic_instance_card.dart:471` crashes on an undeclared state.** It reads `machine.states[_instance.currentState]!`, so an instance whose `currentState` is absent from the `states` map throws rather than degrading. The correct degradation is a fallback to the raw state id — unknown must remain a rendered third state, not a crash, the same principle as a tri-state guard refusing to collapse "unavailable" into false. The new `_WorkflowStateBadge` helper in the state-label ticket is written with that fallback, and that ticket deliberately does **not** change this bang so the two remain separable.
- [x] **FIXED 2026-09-19 — the dispatch preamble never said who the agent was, so two dispatches believed they were the orchestrator and did nothing.** `data/dispatch_preamble.md` (2397 bytes, prefixed to every implementation dispatch) was a *git-safety* document: locked files, how to handle an unexpected failure, do not weaken a test. It contained **zero** statements of the reader's role — no "you are the implementation agent", nothing. Handed a ticket plus a repo whose `CLAUDE.md` describes at length how the *orchestrator* dispatches agents, arms Monitors and works a loop, a capable model reasonably concluded it **was** the orchestrator. **Two dispatches died this way in one session:** the threshold fix (153→135) replied that it had "dispatched the ticket to the implementation agent" and **recursively launched a sub-agent**; the result-frame locator replied about `/loop` dynamic mode and declined to poll a Monitor. **Both exited 0 with a plausible reply — the only tell was a clean working tree.** Fixed by prepending a role block that states what the agent is, what it is not (no sub-dispatches, no Monitors/loops/wakeups, no deciding project priority), and that `CLAUDE.md`'s voice belongs to its *caller*; it names both failures so the prohibition carries its evidence. Re-dispatched immediately afterwards and the same ticket ran normally (351 KB and working, against 132 KB-and-done). **Note the file is UNTRACKED**, like the dispatchers in the row below — both copies were backed up before overwriting and the sync verified by md5, because the two machines can silently disagree. **The general shape: a prompt that omits the reader's role does not produce a confused agent, it produces a confident one with the wrong job** — and it fails in the most expensive way available, by looking like a completed run.
- [ ] `new-ticket` — **`garden-tool-loan` hits the `Instance of 'DateTime'` serialization bug — the SAME defect CLAUDE.md records for Cedar, now reachable in a second community.** Surfaced in `ev35` as `row_execution_failed` with `Converting object to an encodable object failed: Instance of 'DateTime'`. It became reachable only because the selector fix (`b3e649b8`) now picks `compact-tiller` rather than the self-owned `steel-wheelbarrow`. **The traced cause for Cedar was NOT the formula-computed date fields** — that was a wrong diagnosis recorded in this file — but `reminderAt`: an unconditional `reminder` block whose read projection inserts `WorkflowReminder.dueAtFor(...)`'s raw `DateTime` into `instanceData`. Check whether Garden's newly-selected loan instance takes the same path before assuming a new cause; a second community exhibiting a known defect is a reachability change, not a new bug. **Value: this is now blocking a B25 row, which the Cedar instance was not.**
- [ ] `new-ticket` — **`garden-tool-giveaway`: the action SUCCEEDS and persists, and the row still cannot be proven because the harness cannot locate the state label to frame the result.** `ev35` verbatim: *"persisted target state `given` after `claim-giveaway`, but B25 could not locate its declared state label \"Ownership transferred\" on source instance `cedar-compost-bin-giveaway` to position the result frame."* **Read that wording carefully before ticketing it as an engine defect — it is not one.** The harness verified persistence; the gap is purely that it could not find the label on the rendered surface to position `primary_result`. Either the label is not rendered for this instance/state combination (a product finding), or the locator looks in the wrong place for a newly-seeded instance (a harness finding). **Establish which before writing the fix** — this is the exact shape of the 2026-09-14 near-miss where a framing failure was nearly published as "the shared engine did not persist". **Value: this row is one locator away from proven.**

  > **MECHANISM FOUND 2026-09-19, and it is a HARNESS finding — the product is correct.**
  > `workflow_ui_evidence_test.dart:2541` builds `find.descendant(of: resultCard, matching:
  > find.text(stateLabel))`, and **`find.text` is an exact match**. Garden declares
  > `states.given.label = "Ownership transferred"`, but the surface renders that string through
  > `transferSummary`'s `labelTemplate: "Transfer: {value}"` — i.e. as
  > **`"Transfer: Ownership transferred"`** — which exact matching cannot find. The
  > `generic-instance-state-<id>` badge fallback at `:2538` did not match either for this terminal
  > state. **So the label IS rendered; the locator looks for a bare string the UI never emits.**
  >
  > **Do not "fix" this with `textContaining`.** That reintroduces the substring hazard this project
  > already records — `"Not attending"` contains `attend`, `"Join waitlist"` contains `wait` — and a
  > state label is exactly the kind of short vocabulary that collides. The fix must match the
  > **rendered** form: resolve the field's `labelTemplate` before searching, or assert on the state
  > badge's value rather than on free text. Say which, and why the other was rejected.
  >
  > Worth noting the symmetry: the standing rule is *never match product vocabulary by substring*,
  > and here strict equality is wrong for the mirror-image reason. Neither extreme is the answer —
  > matching the string the UI actually renders is.
- [ ] `new-ticket` — **Two different numbers are both called `screenshotCount`, and they differ by exactly the diagnostic frames.** Measured on `ev34`: phase B14's manifest-level `screenshotCount` is **57**, while the sum of that phase's per-workflow `screenshotCount` is **41** — the 16-frame gap is `diagnosticScreenshotPaths`, which `ea85244d` correctly excludes from evidence. The capture gate uses the per-workflow sum (`phaseScreenshotCount += integrity.verifiedScreenshotCount`), so **the gate is right**; but anyone reading a phase manifest, or the `screenshots=N/N` summary line (179), sees a number 36 higher than the one the gate compares. **This is the exact shape that made `fullB25MinimumScreenshotRows` wrong**: I set it from a file count while the gate used `verifiedScreenshotCount`, and ev31 consequently passed by a margin of 4 while being reported as passing by 40. That trap is now reproduced one level down. **Fix: name them differently** — the phase aggregate should be `totalFrameCount` or carry `evidenceScreenshotCount` alongside `diagnosticScreenshotCount`, so no reader can mistake a total for the evidence figure. Cheap, and it removes a whole class of misreading rather than documenting it.
- [ ] `new-ticket` — **The B25 selector picks an instance by DECLARATION, not REACHABILITY, so adding a correctly-seeded instance cannot fix an unprovable row. This supersedes the "just seed it" half of row-357.** Found 2026-09-19 by running `ev34` against the newly seeded Garden package and getting **byte-for-byte the same 13 B13 frames as before** — `garden-event-rsvp`, `garden-tool-giveaway` and `garden-tool-loan` all still `primary_action_unavailable` despite each now having a reachable instance. **Mechanism, read from source:** `workflow_ui_evidence_test.dart:3676` collects every instance of the workflow type and the loop at `:3871` returns the **first** whose transitions pass `_transitionCanBeSelectedForRole` (`:4156`). That predicate checks `allowedRoleIds`, `actorEqualsField`/`actorInList` and `instanceDataEquals` — and contains **zero** handling of `formula` guards. Garden's `claim-giveaway` guards on all three *plus* `"formula": "if(ownerFanId == $actor, false, true)"`, so both the member-owned and the coordinator-owned giveaway look equally selectable; the member-owned one is first, is selected, and the formula then refuses at render time. The reachable instance is never reached. **So this is a harness defect, not a package defect, and fixing it fixes the class across every community rather than Garden's five rows.** Two candidate shapes: evaluate `formula` guards inside `_transitionCanBeSelectedForRole` (needs the formula evaluator available to the harness), or make the selector fall through to the next candidate instance when the chosen one's primary action turns out unavailable — the second is weaker but needs no evaluator and matches the existing `b25FallbackSelector` structure at `:3866`. **Do not "fix" this by reordering seeds so the reachable one comes first** — that is fragile, and it collides head-on with the calendar's date-ordered default selection (`solved-patterns.md` §26, second amendment).
- [ ] `new-ticket` — **The `data/*.sh` dispatchers are UNTRACKED, so each machine keeps its own copy and they drift silently — the VM ran a Skill dispatch on the wrong engine on 2026-09-19.** `git ls-files --error-unmatch data/call_skill_authoring_agent.sh` errors: the file is not in git. Windows and the VM were at the **same commit** with **clean trees** and **different file contents** — Windows on `claude -p` / sonnet (the 2026-09-18 user-directed engine), the VM still on `codex exec -p gpt5_6_sol_medium`. A Garden regeneration was dispatched and ran for minutes on the superseded engine before it was caught. **What caught it was the dispatch banner**, which prints the engine in one line (`Profile: gpt5_6_sol_medium`) and contradicted the script I had just read — not any check. **This is the second instance of this exact shape**, after the 2026-09-14 UX judge case where the VM ran `fable` under a header claiming Sonnet; two instances make it a pattern, not an incident. **Swept the others at the same time and they match** (`call_implementation_agent.sh`, `call_root_cause_agent.sh`, `call_ux_judge_agent.sh` all md5-identical), so the drift was isolated — but nothing prevents the next one. **The fix is mechanical, not another note:** either track these files, or add a pre-dispatch md5 comparison against a committed manifest that refuses to run on a mismatch. Until then the standing rule is the one already in memory and which I failed to follow: **verify presence AND engine before any dispatch**, and read the banner rather than trusting the script you just read. The VM's superseded copy is preserved at `~/call_skill_authoring_agent.sh.bak-codex` rather than discarded.
- [x] **CLOSED 2026-09-19 (`ea85244d`) — a row that captures frames and then fails no longer discards them.** `_recordB25RowScopedFailure` hardcoded `screenshotNames: const <String>[]` and the failure object had no field to carry them, so 18 rows x 2 frames = **36 orphaned files per run**, in both `ev31` and `ev32`. The accounting closes exactly across all nine phases, which is how the finding is known complete rather than merely right about the cases checked. `action_succeeded_result_unverified` was the sharpest: the action SUCCEEDED and its frames were dropped. **The obvious fix would have been wrong** — adding them to `screenshotNames` feeds `verifiedScreenshotCount`, which the gate compares, letting rows nobody completed inflate the bar. They go to `diagnosticScreenshotNames`, and the separation is enforced rather than asserted: a row with 2 diagnostics and 0 evidence frames reports `screenshotCount: 0`, byte-identical diagnostics are never flagged and never counted, and the three `blocked_*` branches refuse to carry frames even if passed some (they capture none, so an empty list is the truthful record). Diagnostics still get the same staleness check. **Verified on a device, not only in tests:** `ev33`'s `book-reading-material` records `evidence=0, diagnostics=2`. Eleven new tests, each demonstrated failing for the reason it claims.
- [x] **CLOSED 2026-09-19 (`c191d5d3`) — the frame threshold is now set in the gate's own units: 135.** `4aa2b28c` set 153 from `screenshots=157/157`, a **file** count, while the gate compares the summed manifest `verifiedScreenshotCount` — I measured the wrong quantity, one tick after quoting the rule against exactly that. Measured: `ev31` files 157 / gate **121** (so it passed its 117 threshold by a margin of **4**, not the 40 reported at the time); `ev32` and `ev33` files 179 / gate **143**. 143 is confirmed on two consecutive runs, and it correctly did not move across the orphan fix because diagnostics are excluded by design. **135 = 15 x 9**; divisibility is forced by `b25_capture_prebuilt_binary_test.dart`'s `_fakeScreenshotsPerPhase = threshold ~/ 9` plus an exact-equality assertion, verified in source before being relied on. The 8-frame margin is deliberately tight: as blocked rows get fixed the count should drift UP, so a downward move means regression and should fail. The derivation — especially that the gate compares `verifiedScreenshotCount` and NOT the `screenshots=N/N` summary line — is written into the constant's comment, because the next reader will reach for the same summary line.
- [ ] `new-ticket` — **`loom_ux_judges.dart` has mixed CRLF/LF line endings, and every edit to it produces a spurious ~1500-line diff.** Hit twice (2026-09-19, both threshold changes): the editor normalises endings and the real 25-line change arrives buried in 1501 lines of churn. Both times it was caught only because the ticket carried an explicit warning, and recovered by reverting and reapplying byte-level. **A hazard that must be re-warned in every future ticket is a candidate for a mechanical fix** — normalise the file's endings once in a dedicated commit that changes nothing else, so the diff is reviewable, or add a check that fails on ending churn. Prefer the gate to the note.
- [x] **CLOSED 2026-09-19 — latest-spec-only audit: all ten packages ARE on the latest specification.** The user's directive ("no older specifications allowed, only the latest") is satisfied at the layer it names: **`specVersion: 4` on all ten**, which is the current spec per `skill-versioning.md`'s own frontmatter. The apparent gap was `skillVersion`: six at `3.6.0` (current), four at `3.3.0` — Camera Club, Member Social Space, Youth Soccer, Chess Club. **Reading each intervening version's own migration text rather than treating "3 versions behind" as the finding, the owed work collapsed to almost nothing:** `3.6.0` says "Migration: none required" (documentation only); `3.5.0` asks to re-check booking/scheduling workflows modelled as `event-rsvp`, and only Camera Club and Youth Soccer use `event-rsvp` at all — both bind genuine attendance RSVPs (`photo-walk-response`, `soccer-practice-rsvp-response`), not facility bookings, so it is a no-op; `3.4.0`'s migration is literally "re-validate". **Re-validated 2026-09-19 against a FRESHLY STARTED validator** (it had not been running — it never restarts itself — so there was no stale-grammar risk): all four `status: pass`, **0 errors** (warnings 11/17/8/2). Garden Club (`3.6.0`) as control also passed with 11 warnings, so the warning counts are not a 3.3.0 signal and the validator was discriminating, not rubber-stamping. **The stamp lag is cosmetic; the packages are conformant.** Two limits recorded rather than glossed: a validator pass proves grammar conformance, not that a package received every *convention* a later version implies (`specVersion`/`skillVersion`/provenance hash prove three different things); and deletion is invisible in a validator run, so this is not evidence that nothing was lost — only that nothing is malformed. **A `skillVersion` bump is a Skill dispatch, not a hand-edit**, so re-stamping the four is queued with the other Skill work, not done here.


### 5. Unscoped milestones

- [ ] `new-ticket` — Platform phases A, A.1, B, C, D, E, G.4 remain — [detail](TODO-open-detail.md#row-215)
- [ ] `new-ticket` — Outstanding Skill dispatches: DataPortability (6 prefill), AdFree (4 orphan), Camera (rejected), Book Club (held) — [detail](TODO-open-detail.md#row-216)
- [ ] `new-milestone` — Document ACL — `sharedWith` is one flat `fanId` list, so group sharing is impossible. Sequenced AFTER Phase F — [detail](TODO-open-detail.md#row-239)
- [ ] `new-milestone` — Spec-version compatibility hardening — Layer 4 (`requiresCapabilities`) needs a spec decision — [detail](TODO-open-detail.md#row-240)
- [ ] `new-milestone` — Archetype-owned bookkeeping unimplemented, so `grant_access`/`share` can never populate the field a grant reads — [detail](TODO-open-detail.md#row-246)
- [ ] `new-milestone` — Ending a record must revoke its dependents (platform API, not a per-member cascade) — user-decided 2026-09-15, 4 measured cases, scoping dispatched — [detail](TODO-open-detail.md#row-354)
- [ ] `needs-skill-dispatch` — Book Club regeneration is HELD by user decision — dispatching now would bake in a known loss permanently — [detail](TODO-open-detail.md#row-247)
- [ ] `needs-skill-dispatch` — Ad-Free product-doc enrichment needs redoing under hard rule 14b — the shipped doc is fine; only the rejected dispatch output was not — [detail](TODO-open-detail.md#row-292)
- [ ] `new-milestone` — §7 step 8 — legacy Dart catalog + bespoke-widget removal, now unblocked and far more tractable than 'unscoped' suggests — [detail](TODO-open-detail.md#row-296)

### 6. Needs the Root Cause Agent

- [ ] `needs-debug-agent` — CJM.16 Messages-tab fix, landing with Phase F rather than as its own dispatch — [detail](TODO-open-detail.md#row-288)

### 7. Delegated — detail lives in the owning tracker

- [ ] `new-ticket` — Never derive the community key from the group id — underscored vs hyphenated — [detail](TODO-open-detail.md#row-138)
- [ ] `new-ticket` — Staleness sweep of the migrated blocks — 54 unadjudicated checkboxes, 12 adjudicated so far — [detail](TODO-open-detail.md#row-195)
- [ ] `new-ticket` — The `chmod 444` guard on community JSON is not durable — [detail](TODO-open-detail.md#row-230)

## Closed on evidence, 2026-09-12

Re-measured rather than carried forward, in the sweep that restructured this file:

- [x] Six "dead" queue transitions in Book Club, Camera and Garden — CLOSED 2026-09-12 on this row's own finding — its text already reads `NOT dead; that framing was a premise error, corrected 2026-09-08`. All six transitions route correctly through the shared `EquipmentLoanArchetypeCard`.
- [x] App Access policy endpoints had no caller authorization — Superseded and CLOSED 2026-09-12 by P1 (`loom-backend f134935`), deployed as `app-access 0.3.11` and verified live: a plain fan token on `installCommunityPackage` now returns `403 provisioning_principal_required`. The exposure this row describes is closed in the running cluster, not just in source.
- [x] Masjid over-grant root cause — the platform had no notion of role KIND — Superseded and CLOSED 2026-09-12 by P3 (`loom-backend 1b9b320`, `role_kind` + `RoleGrantCompatibilityValidator`) and P5 (`4668822`, actor-attributed policy audit), both deployed in `app-access 0.3.11`. The platform now has the notion of role KIND whose absence this row identified as the root cause.
- [x] Remote fixture upload for `hoa-member-document` instances — CLOSED 2026-09-12 on this row's own finding — its text already reads `OBSOLETE AS FRAMED, 2026-09-10`: the fixture step it describes cannot be performed because package seed data populates only the local engine, which is the 2026-09-07 decision, not a defect.

## Closed

Moved verbatim to [TODO-closed.md](TODO-closed.md) — 87 rows, kept rather than deleted. The
template's rule is that a closed row's rollup line leaves this index once closed; it had never
been applied, which is why the open queue was buried.

### B25 non-proven rows, by cause — measured 2026-09-14 from a full capture run

**This is the artifact the campaign existed to produce.** 95/95 frames, `b25Proven=10/49`, and every
non-proven row categorised. The 19 `row_execution_failed` rows are **not** 19 problems:

| cause | rows |
|---|---:|
| **no actor identity that can represent the B25 product-doc role** (`ext_mosque` 10, `ext_neighborhood_book_club` 2) | **12** |
| `mosque-announcement` postcondition not observed (wording corrected in `efd7ae80`) | 2 |
| walkthrough stalled at its bounded wait | 1 |
| `hoa-export-evidence` changed source instance data keys | 1 |
| `book-discussion-message` ran a visible orthogonal package action | 1 |
| `No element` | 1 |
| **`Converting object to an encodable object failed: Instance of 'DateTime'`** | 1 |

~~**Twelve of nineteen share one cause, and it is a SEEDING question, not a harness defect.**~~
**CORRECTED 2026-09-14 — this was wrong, and it is not a seeding gap.** The roles exist; the resolver
matched only `roleId`, while B25 rows name personas by label or generic term. Fixed in `f22e311d`
(label + qualifier forms) and `e78ac101` (`roleLabel` + roleId suffix). What remains is a **doc
defect in three hard-locked product docs**: their B25 persona column says `owner` where each doc's
own persona table and its package say Board (Cedar, 4 workflows), Organizer (Book Club, 2) and
Coordinator (Garden, 1). Chess declares both `chess-owner` and `chess-organizer`, which is why
`owner` cannot be mapped onto those roles by rule. ~~Needs a user decision to edit the docs.~~
**STRUCK 2026-09-19 — ALREADY FIXED, AND FOUR DAYS BEFORE I LAST REPEATED IT.** `979be9df`
(2026-09-15, "B25 persona column names the persona each doc already declares") corrected all three
docs; the row was never struck, so it kept being surfaced to the user as a decision they owed.
**Re-measured rather than inferred from the commit message:** the persona columns now read Cedar
`board` (8) / `member` (7), Garden `member` (7) / `coordinator` (2), Book Club `member` (14) /
`organizer` (4), with **zero** `owner` in any of the three. **Control passed** — the identical regex
returns 4 on `ad-free-community-product-experience.md`, so the empty result means absent rather than
a broken query, which matters because an empty result that agrees with you gets less scrutiny than
one that does not. **And `owner` in the other docs is correct, not residue:** Ad-Free, Chess and
Data Portability genuinely declare owner roles (`ad-off-owner`, `chess-owner`, `portability-owner`),
so a sweep for the string alone would have produced a false finding against three healthy docs.
**No doc edit is needed and no user decision is owed.** Corroborating it from the other end: of the
workflows this row named, Cedar's and Garden's are already proven, so the doc wording was not
gating them.

**The `DateTime` row independently reproduces a documented defect.** `CLAUDE.md` records Cedar's
listing failing with `JsonUnsupportedObjectError: Instance of 'DateTime'`, traced to `reminderAt`:
an unconditional `reminder` block whose read projection inserts `WorkflowReminder.dueAtFor(...)`'s
raw `DateTime` into `instanceData`. **The walkthrough hit it on a device without being told to look
for it** — the second unprompted reproduction of a known blocker in one day, after row-259's Garden
strand.

**What this changes about priorities.** Further harness work has sharply diminishing returns: the
remaining code-side causes are five distinct one-row issues. The leverage is in ~~**seeding** (12 rows)~~
~~the **three hard-locked doc corrections** (7 workflows, see the correction above) and in the **held Book~~
~~Club regeneration** (row-247). Neither is fixable by another dispatch, and both are decisions rather than~~
~~defects.~~ **REWRITTEN 2026-09-19 — both halves of that priority claim are now wrong, in the same
direction: each named a decision the user owed, and neither was owed.** The doc corrections were
**already made** on 2026-09-15 by `979be9df` (struck above, re-measured with a passing control). And
Book Club's blocker was **not** its held regeneration but a credential, which had itself been closed
on 2026-09-08 — `loom-book-member-2` and `loom-book-organizer-1` both return HTTP 200, verified live.
**So the leverage was never in decisions; it was in walkthroughs nobody was dispatching.** Acting on
that the same day took the bar 47 → 50 (`garden-tool-giveaway`, `book-meeting-rsvp`, `book-vote`),
and established that all five remaining Book Club rows are drivable with no missing platform service
gating any of them. **The pattern worth naming, because it produced three stale items in one day:
every one of them was a claim that made work look blocked, and every one was refuted by a single
live check that took under a minute.** A blocker withholds work from the queue while looking
responsible, which is exactly why it is the row class that must be re-tested before it is repeated —
and the held Book Club regeneration (row-247) remains genuinely open, so it should not be swept up
in this correction.
