# Live TODO — cross-effort rollup

**This is an index, not a memory.** One line per open item, newest/most-relevant first, each linking to its
full entry (context, source, exact wording) in the owning tracker's own `## 8. Live TODO / Next Steps Queue`
section (`docs/Build Plan V2/Tools/reference-tracker-template.md` defines that section's shape and the fixed
tag taxonomy used everywhere below). Never write item detail directly here — if you're about to write more
than one line for an item, that content belongs in the tracker, not here.

**How this file gets updated:** propose-then-promote, never a dispatched agent editing this file directly.
An Implementation Agent proposes next steps in its own STATUS.md (`## Proposed next steps`); during your own
independent-verification step, you review that proposal, write the confirmed items into the owning tracker's
§8, and add/remove the matching one-line rollup here. Every `call_*.sh` dispatch script prints a reminder
banner at completion so this step is never silently skipped — see `docs/Build Plan V2/Tools/README.md`'s core
pipeline step 5.5.

**Use the Root Cause Agent (`data/call_root_cause_agent.sh`) for two jobs, across every tracker this
file rolls up, not only the one it's traditionally been used for.** (1) To scope a non-trivial change
*before* writing an implementation ticket — what it actually touches, the real mechanism, what it would
break — rather than writing a ticket from an assumed mechanism and finding out it was wrong a full
implementation round later. (2) Its standing job: anything resisting normal investigation — a stubborn
defect, a stalled walkthrough, an unclear suite failure — goes to it before spending more implementation
rounds guessing. Never write a ticket that asserts a mechanism nobody has actually traced.

**Verification sweep 2026-08-24.** Every checkable claim below was re-measured against the working tree
rather than carried forward, and nine items closed on measurement alone — they had been fixed by later work
and never struck. Suite baselines quoted inside older tracker entries are stale by construction; the current
numbers are judges **432**, app shell **271**, engine **281 (+3 skipped)**, demo **156**, zero failures on
Windows.

**Backend-integration sweep 2026-08-26/27.** The app now runs on the real backends by default and the
document library exists end to end. Current baselines: judges **434**, app shell **307**, engine **299
(+4 skipped)**, workflow service **75 (+5)**, demo **160**, app-access **49**.

**A pattern this sweep kept finding: declared and never read.** Seven constants, fields or code paths
existed and nothing consumed them — `sharingGrantable`, a state `readGuard` under a non-`guarded`
default, `explicitReaderFanIds` (`writableBy: None`), the renderer contract's `calendar`
surface-family, `SurfaceQuery.dateWindowStart/End`, the engine's `workflowType == 'notification'`
delivery branch, and `dueNotifications` itself. None failed anything. **A grep for a declaration
proves it exists, not that anything reads it** — check the consumer.

**A measurement caveat worth carrying.** Two sweeps run that day were wrong in the same way: they read an
artifact instead of the code path that consumes it. `grep actorIdentities` over the packages returned zero
and was read as "packages declare no identities" — they are *derived* from `roles[]` at load. A role
cross-check that ignored `_roleIdsForB25Role`'s synonym fallbacks reported 11 blocked rows when the real
number is 5. Both had been "validated", but only against cases of one kind. **Validate a sweep against a
known answer for every kind of thing it claims to find, not just one.**

## Open

### WHERE THIS ACTUALLY STANDS — 2026-09-01

Written because "BACKEND SERVICES BUILD-OUT — COMPLETE" is true and reads as far more finished than
the project is. The backend was one workstream.

**71 open items in the four §8 queues** — ACWS 24, Build 39, Community JSON 7, TabId 1 — plus **93**
checkboxes still open inside the migrated §9 bodies. The body count is falling as the staleness sweep
works through it (121 → 93 so far); a checkbox in a §9 block is a record of what was true the day it
was written, not current state.

#### Proven live, and re-verified rather than recalled

| | |
| --- | --- |
| B1–B8 | built, deployed, load-bearing, exercised against the running stack |
| Android sign-in | **works end to end on `emulator-5554`** — Keycloak page, token stored, gate error changed to one only an authenticated caller reaches |
| Packages | 10/10 shipped carry `experience.notifications`; all on `specVersion: 4` |
| Accounts | 35 across 11 communities, every one through `requestGroupMembership` → `decideGroupMembership` |
| Security | **the whole confirmed privilege-escalation class closed and proven live** — group endpoints (0.3.5) and app-level setAppAccess/revokeAppAccess (0.3.6), each verified by re-running the exact exploit: member 403, was 200 |
| Resilience | Postgres-restart recovery proven by deleting `postgres-0` — same pod, 24s |
| Suites | all five re-measured 2026-09-01: judges **485**, app shell **371** (+2), engine **316** (+1 credentialed), workflow service **148** (+1 credentialed), demo **160** |

#### The critical-path blocker — CLOSED 2026-09-08

**Permission catalog publishing is hardened, deployed, and proven. This section is kept, rather than
deleted, because it was quoted as an open blocker for over a week after parts of it stopped being
true.** Ticket with the full evidence: [GAP-permission-catalog-publishing](Tickets/GAP-permission-catalog-publishing.md).

Deploying the image **is** publishing the catalog now: `app-access` reconciles the live `permission`
table from its bundled, generated `permissions-vocabulary.json` on startup — upsert-only, never
deleting, and a true no-op when nothing changed.

- **Part A** (`ea18fb23`) — `ArchetypeResolver` carries typed `ArchetypeAction` records including a
  `governanceActions` set, so `community.*` is *generated* rather than hand-added.
- **Part B** (`80bc61f`) — the startup reconciler plus a `build.sh` parity gate that refuses Maven on
  a drifted vocabulary twin. 72 tests, 0 failures/errors/**skips** against the real test database.
- **Part C** (`047e0be`, `loom/app-access:0.3.10`) — deployed and proven in the live DB.

**Measured after the deploy, against the before-snapshot committed in the ticket:** `permission`
127 → **137** (predicted 137), `role_permission` **425 → 425, unchanged**, admin roles holding exactly
five `community.*` **11 → 11**, `permission_catalog_version` `2026-08-26.2` → the generated hash. All
nine `calendar.*` ids are present with real display names; `community.*` is still 5, so the
hand-granted governance rows were never at risk. A second restart wrote nothing —
`catalog_published_at` is byte-identical across it.

**The calendar blocker is closed structurally, not just observed once.** `ensurePermissionsExist`
rejects an install only when a *derived* permission is absent from the catalog, and the deriver can
only emit ids its bundled vocabulary names — so vocabulary ⊆ catalog settles it: 116 vocabulary ids,
**116 present, 0 missing**, with the 137−116 = 21 remainder being exactly the retired set.

**What is still genuinely open, and is a different problem:** `installCommunityPackage` replaces each
declared role's permissions and deletes every group-scoped role the package does not declare. It
would destroy all 11 community admin roles unrecoverably, because their `community.*` grants are not
archetype-derived and a re-install cannot restore them. That is the **role deletion** decision listed
below, and it is what still gates a live end-to-end community install. The catalog work deliberately
did not run that path.

#### Decisions waiting on the user

- **role deletion** — A declare admin in packages / B reserved naming / C provenance tracking
- **fan-passport identity** — decided (seeding mints the passport first); the **re-keying** step is
  what still needs a go-ahead, since it rewrites memberships
- **B25 table unit** — are Masjid's 3 persona rows B25 rows at all, and are Data Portability's 9
  identical rows nine proofs or one?
- **`community.surface.navigation.*` vs `community.manage_settings`** — the fourth spec decision,
  which gates Phase E

#### Measurement, and why the production bar number is not yet meaningful

`c14` and `c16` are both **diagnosed**: `c16` was never an app defect — all ten findings were
evidence-shape problems, and the judge now says so. `c14`'s artifact is ~97% scaffold. The denominator
is wrong too: 12 of 79 rows name a workflow their package does not ship, and **9 of those 12 are not
community workflows at all**. Repairing measurement precedes measuring.

### BACKEND SERVICES BUILD-OUT — complete 2026-08-31

B1–B8 are built, deployed and load-bearing, verified end-to-end against the running stack.
**The full record moved to `Access Control and Workflow Service Tracker` §9** on 2026-08-31 — what
was built, what was measured, the four-state vocabulary, the group-to-community mapping, and the
corrections made along the way. Checkbox status was re-verified against the repository during that
move rather than carried over on trust.

Still open, as rows in that tracker's **§8**:

- [x] server-initiated push: **DECIDED 2026-09-05 — stays a placeholder, not now.** Scoped: the spec already answers the design questions (provider-agnostic contract, must report per-device failure not swallow it, dead-token pruning distinct from transient failure); the one remaining call was which provider(s) to integrate (FCM alone vs. FCM+APNs), which needs real infrastructure (a Firebase project or Apple certs, credentials in the cluster). User declined to commit to that now — the three open-app delivery paths (in-app inbox, local device notification, reminder sweep) already work. Revisit by asking the same provider question when this is prioritized again. → ACWS §8
- [x] delivery failures: **CLOSED `46193031`** — `LoomReminderSweeper.sweep()` now logs both failure paths (debugPrint, the existing convention) and exposes `deliveryFailureCount`, with zero change to its never-throw/retry-on-failure resilience contract. Verified: my own re-run of app_shell +376 ~2 All tests passed. A separate catch in local_workflow_engine_api.dart:1549 was confirmed dead code (fires only for a workflowType no shipped community declares) and correctly left alone; `_hydrateSourceFields`'s own silent catch is a different, data-hydration concern, reported but not fixed here. → ACWS §8
- [x] change feed: **CLOSED, was stale — it IS deployed.** Proven by live request against `loom-workflow-service:1.0.3` (deployed == manifest, no drift): `GET /v1/communities/{id}/changes` returns **401 authentication_required** (route exists, wants auth) while a nonexistent collection under the identical valid `X-Loom-Correlation-Id` returns **404 route_not_found** — a working control, so the 401 means present, not missing. Note the first attempt used `/v1/changes` and 404d; the real shape is `/v1/communities/{id}/changes` (`_matchesCollection`, workflow_service.dart:255). Verified 2026-09-04. → ACWS §8
- [x] **CLOSED 2026-09-09 — this row is stale in every particular; there is no decision left to make.** It claimed `external_resource_*` was NULL for **all 24** groups. Live query today: there are **13** groups, and **10 have a correct `{type: community, id: community_*}` ref** — every one of the ten shipped communities. The only three without are exactly the known non-communities: `loom_communities_cedar_commons_hoa` (the UNDERSCORED duplicate, pending its own deletion decision), `loom_communities_tabletop-club` (a test fixture, never a shipped community), and `loom_communities_zz-install-probe` (my own 2026-09-09 install-safety probe, whose roles were deleted afterwards; App Access exposes no `deleteGroup`, so the empty group is inert residue). So the gap closed on its own once installs began passing `communityId`, which the install request treats as optional for backward compatibility — omit it and no `externalRef` is set, which is exactly how the original NULLs arose. **The data also demonstrates why the spec forbids deriving one identifier from the other:** `loom_communities_masjid-nur` maps to `community_mosque`, so nothing about the group id predicts the community id. Verified with a control — the query returned populated refs for ten rows, so an empty result would have meant absence rather than a broken query. → ACWS §8
- [ ] never derive the community key from the group id — underscored vs hyphenated → ACWS §8
- [x] fan → community: **CLOSED, was stale** — `GET /v1/fans/{fanId}/communities` (`listFanCommunities`) ships in the app-access spec and the app adopted it (part37/part39/part40). Verified 2026-09-04. → ACWS §8

### Cross-cutting — the record moved to its owning trackers, 2026-08-31

Thirty-six dated entries from 2026-08-29 → 2026-08-31 were migrated out of this file, each to the
tracker that owns the work. Checkbox status was not carried over on trust. Where to find them:

| What | Now lives in |
|---|---|
| Backend build-out as it happened — B4 minting, checksums, change feed, B5 proofs; the security escalation and its fix; the two-hour outage and the resilience defect; admin-role provisioning | `Access Control and Workflow Service Tracker` §9 |
| Production bar and device history — the membership blocker, the device runs, the legacy-fallback render, the Android sign-in dead end, and the three ways the B25 measurement is unreliable | `Build Tracker` §9 |
| Package regeneration — Book Club, the stale marker, B8 across eleven communities, the absent-block default | `Community JSON Migration Tracker` §9 |

Open items from those entries are rows in the same trackers' **§8** queues, not here:

- [x] community isolation: **CLOSED, was stale** — no longer a bare WHERE clause; `FORCE ROW LEVEL SECURITY` + the `community_isolation` policy ship in postgres_connection.dart and were proven enforcing live 2026-09-03. Verified 2026-09-04. → ACWS §8
- [x] `1bc87122` idempotency unified — item_queue.join was the last hand-rolled copy; document/export_bundle already used runIdempotent. Verified against live PG with BOTH credential sets (153 +1 skipped); admin-only skipped the one test covering the changed class. → ACWS §8
- [x] **CLOSED 2026-09-09 — stale; every community has members.** Live count of `active` rows in `group_membership`: ad-free 5, camera 5, cedar-commons-hoa 5, chess 7, data-portability 7, garden 5, masjid-nur 5, member-social-space 5, neighborhood-book-club 5, riverside-youth-soccer 7, tabletop 5 — **twelve groups, none empty** (the twelfth being the underscored `cedar_commons_hoa` duplicate at 2, which is pending its own deletion decision). Closed by the 2026-09-08 seeding round plus the Masjid `owner` holders added 2026-09-09, all provisioned through the real authorization flow — fan requests, admin approves and grants — never a database insert. → Build Tracker §8
- [x] authenticated walkthrough — **sign-in PROVEN on device**, and the follow-on 401 fixed: `JWT_ISSUER` now matches the advertised host, app-access returns 200 with real membership → Build Tracker §8
- [ ] **STILL TRUE 2026-09-09, but the defect is an INTERNAL CONTRADICTION in the doc, not an unknown id — and I have to correct my own first write-up of this row.** I initially recorded that I had diagnosed the five Masjid rows as test-harness identifiers, framing it as sharper than "workflows that do not exist". **The product doc already says exactly that, and says it better.** Its own header note reads: the three ids are *"**not real workflows at all** — they are literal Dart `testWidgets` names for this repo's B18/B19/B20 integration tests"*, names the three test files, records that they use Masjid Nur as their test-subject community, states that no `LoomWorkflowDefinition` with those ids exists anywhere, and instructs *"**Do not author engine-native JSON for any of these three**"* — citing the same category of mistake already corrected in Chess Club's doc. So the diagnosis was captured before I looked, which is the second time today I have presented an already-recorded finding as new. **The actual open defect, stated precisely:** the doc's header disclaims the three ids **while its own §6/§7/§9 and card-surface-registry tables still list all five row instances as B25 rows** (lines 96–100, 116, …). A reader or a counting script taking the tables at face value inflates the 79 denominator by five, because nothing in the tables themselves marks those rows as non-workflows — the disclaimer is 75 lines earlier and structurally disconnected from the rows it disclaims. **The fix is to strike the five rows or annotate them in place**, so the tables agree with the header. That is a documentation-consistency correction, needs no package change, and the package is already correct (it ships `mosque-*` only and no JSON was ever authored for these ids). → Build Tracker §8
- [x] `new-ticket` — **CLOSED 2026-09-10 — the "unreconciled 2-row gap" this row left open is now fully reconciled, and 79 was never wrong, only stale.** The row said the gap between the counted 77 and the quoted 79 "needs tracing before 79 is quoted again". Traced: the generated asset at `b8d019e2` (2026-08-24, the commit that first recorded 79) declares `rowCount: 79`; today's declares `rowCount: 77` with `len(rows) == 77`. Diffing the two assets' `workflowId` sets accounts for the difference exactly — **7 removed** (`chess-local-install-open`, `chess-route-home`, `garden-tool-loan-giveaway`, `platform-connection-invite`, `platform-connections-entry`, `platform-message-stream`, `platform-messages-entry`), **5 added** (`garden-tool-loan`, `garden-tool-giveaway`, `hoa-document-access-request`, `platform-connection`, `platform-message-thread`), net **−2**. Those seven removed are precisely the rows the 2026-08-24 reachability sweep flagged as naming a workflow no package ships, so the gap is that sweep's own findings landing: Garden's fused `garden-tool-loan-giveaway` split into the two workflows the package actually ships, the four `platform-*` rows consolidated into two real ones, and Chess's two route/install rows dropped as never having been workflows. **The 72 stands, independently re-derived:** 3 distinct `wf_` harness ids span 5 rows (two of them appear twice under different personas), 77 − 5 = **72**. **Method note worth keeping:** the first pickaxe for the figure's origin returned a 2026-06-30 commit that matched only because `79` appears inside its commit *hash* — read the hit, not just the date. And the right move for a stale-looking number is to re-derive it at the commit that recorded it (`git show <commit>:<asset>`), not to re-count at HEAD and call the difference a mystery. ORIGINAL: **The B25 denominator is 72 real rows, not 79 — measured 2026-09-09, and the quoted figure is wrong in two independent ways.** Counted the B25 addendum table (`| Workflow | Persona | Expected decision | ... |`) across all ten shipped community product docs: ad-free 6, camera 3, cedar 8, chess 8, data-portability 9, garden 5, masjid 15, social 6, book club 9, soccer 8 — **77 rows present**, against a canonical **79** that is quoted throughout this tracker and in `CLAUDE.md`. **Of those 77, five are not workflows**: Masjid's addendum carries 10 real `mosque-*` rows plus exactly the 5 `wf_` test-harness rows its own header already disclaims, which reconciles with that header's "5 row instances". **So the honest bar is 72.** The remaining 2-row gap between 77 and the quoted 79 is **unreconciled** — it may predate rows added or removed since the figure was set, and I am not guessing at it; it needs tracing before 79 is quoted again. **Method note, because my first count was wrong and a control caught it:** the initial pass returned 87 because the separator-skip pattern `/^\|---/` does not match `| --- | --- |` (there is a space after the pipe), so every community was inflated by exactly one. Verified by listing Camera Club's rows by eye — 3, not the 4 counted — then re-running. **A denominator is exactly the kind of number nobody re-derives**, which is how a figure can be quoted for weeks while the tables under it say something else. **Consequence:** every "N of 79" statement in this project is measured against a denominator that is 7 too large, and that error compounds with the two other B25 accuracy defects re-confirmed today — a numerator whose manifests carry no package identity before 2026-09-09, and a "proven-against-fakes" share computed from 4 fakes when 53 APIs are fake-backed. **Do not quote a completion fraction until all three are corrected in the same number.**
- [ ] **STILL TRUE, confirmed 2026-09-09 — and now fixed forward.** Unlike the six rows re-tested alongside it, this one is **not** stale: all **11** of the 2026-09-08 B25 evidence manifests return **zero** matches for `skillVersion`, provenance, or any package hash. They record the community, the workflow, the device and the APK install time — everything except which *version of the community package* was exercised. So regenerating a package silently turns every earlier manifest into a claim about a file that no longer exists, with nothing in the evidence to reveal it. **Fixed forward in the brief generator** (`Tools/code/make_b25_brief.sh`, mirrored to `data/`): every future run must record the driven package's `skillVersion` **and** its `sha256`, gathered on the VM and pasted verbatim. The brief says why both are required rather than either — per `specVersion`/`skillVersion`/provenance-hash reasoning already in CLAUDE.md, a package can match a recorded `skillVersion` and still differ in content, while a hash proves byte identity without saying which authoring convention produced it. **Not retroactive:** the 11 existing manifests stay unidentifiable, so treat any of them as evidence about a package only when its asset is unchanged since 2026-09-08. **Verified by rendering a real brief**, not by grepping the script — the first insert landed mid-sentence and split the deliverable paragraph, which the grep count reported as success. → Build Tracker §8
- [ ] pass-42 blockers — `c14` vision review is ~97% scaffold (7 of 204 screens carry real evidence); **`c16` judge FIXED `ab8720f0`** — it now reports zero capability failures and names all ten as evidence-shape problems, so what remains is regenerating the artifact in the documented schema → Build Tracker §8

### 2026-08-30 — this file stopped following its own header — RESOLVED 2026-08-31

The header has said "this is an index, not a memory" since it was written. By 2026-08-30 the file was
**2,633 lines** of narrative — findings, corrections, dated session entries — and the per-tracker §8
queues it points at had gone quiet, because detail was being written here instead of there.

**Resolved by the A1 migration on 2026-08-31** (`3054337c`, `4fe6230f`, `326d3a6b`): the record moved
to the tracker that owns each piece of work, open items became §8 rows, and this file went back to
being an index — 2,633 lines to under 300.

Two things made the drift easy to miss. **`Build Tracker` had no §8 at all**, so the one tracker that
owns the production bar had nowhere to put an open item and everything about it landed here by
default. And a narrative entry is genuinely useful when written — the cost only appears later, when
the same finding is unfindable because it sits in a chronological stream instead of beside the work
it concerns.

### PRODUCTION READINESS, the bar, and the platform phases — moved 2026-08-31

The production-readiness measurement, the 2026-08-25 resequencing decision, the bar's definition and
the platform-phase state moved to **`Build Tracker` §9**. The writer-declaration pass and the
tab-audience work moved to **`Community JSON Migration Tracker` §10**; the in-memory-engine finding
to **`Access Control and Workflow Service Tracker` §9**.

**Those blocks arrived carrying 54 open checkboxes that were not re-adjudicated during the move**, and
several are already known false — the checksum service *was* proven live on 2026-08-30, the five rows
"blocked on a missing owner/admin identity" were unblocked when eleven admin roles were created, and
the "7 rows" unshippable count was superseded by the 12-row finding. A staleness sweep is owed and is
itself a `needs-verification` row in each tracker's §8. Do not treat a checkbox inside a §9/§10 block
as current state.

The live threads, one line each:

- [ ] staleness sweep of the migrated blocks — 54 unadjudicated checkboxes. **SWEEP STARTED 2026-09-09 — 12 rows adjudicated, and the row\x27s own prediction held.** Prompted by a user challenge ("have you confirmed these are not already captured patterns we already know how to solve?") after I had reported two items as awaiting a decision for many consecutive ticks. **Outcomes: 5 plainly stale and closed** (Garden regeneration "not installed"; `app_group.external_resource_*` "NULL for all 24 groups"; the `permissions.md` §4/§6 contradiction "blocking `.create`"; "ten of eleven communities have no members"; the Garden walkthrough stall). **1 superseded by knowledge already captured** (Youth Soccer\x27s payment surface — answered by `solved-patterns.md` §18 plus a scoping already in this tracker). **1 half-true with the wrong mechanism attached** (the `calendar.*` catalog gap: all 9 ids are deployed, 0 are granted, but `roleHasPermission` is package-derived and never reads `role_permission`, so "blocks creation" was never the mechanism). **4 confirmed STILL TRUE and re-measured rather than re-read** (evidence carries no package identity — now fixed forward in `make_b25_brief.sh`; hand-made cluster state — now partly closed by a seeded-identity manifest; the B25 denominator; 53 fake-backed APIs, still exactly 53). **1 NEW defect found while sweeping: the denominator is 72 real rows, not the long-quoted 79** — 77 present across the ten product docs, minus Masjid\x27s 5 disclaimed test-harness rows, with a 2-row gap still unreconciled. **The supersession chain this row already noticed continues:** "7 rows name a missing workflow" → superseded by the 12-row finding → and today the 12-row finding is itself refined, since 5 of them are `wf_` test-harness ids the product doc already disclaims in its own header. Each count is still recorded somewhere. **Three of my own conclusions were corrected mid-sweep**, and **three queries returned clean, plausible, wrong numbers** caught only by controls — including a row count inflated by one per community because a separator pattern did not match `| --- | --- |`. **Remaining: the pre-GA credential debt, and the balance of the migrated checkboxes.** The method that worked: re-run the blocker\x27s own evidence, not the row; check `solved-patterns.md`/`keypatterns.md`/the product doc before recording anything as new; and when a blocker turns out half-true, change the question rather than splitting the row. → all three trackers §8
- [x] **CLOSED 2026-09-09 — stale; there is no contradiction and the `.create` vocabulary is live.** Checked after a user challenge to stop carrying items as blocked without re-testing them. **§4 and §6 agree:** §4 documents a `.create` id per bespoke archetype (`event_rsvp.create`, `equipment_loan.create`, `document_library.create`, `export_wizard.create`, `vote_poll.create`, `search_ai_answer.create`, `calendar.create`) and §5 gives the generic families theirs (`form_entry.create` and siblings); **§6 step 6 is the derivation rule that produces them** — *"For each `create` action's `byRoleIds`, add the archetype's `create` permission"* — which is a rule, not a rival claim. **Live evidence, not a doc read:** the deployed catalog holds **12** `.create` ids among **137** permissions (`approval_queue_item`, `calendar`, `discussion_thread`, `document_library`, `equipment_loan`, `event_rsvp`, `export_wizard`, `form_entry`, `notification_inbox`, `payment_checkout`, `search_ai_answer`, `status_timeline`), and **72 `role_permission` rows actually grant one**. A vocabulary with 72 live grants is not blocked. The row most likely predates the permission-catalog work that took the catalog from 127 to 137 ids. Control: the same query returned the full 137-row catalog, so an empty `.create` result would have meant genuine absence rather than a broken query. → Build §8
- [ ] **STILL TRUE, and LARGER than when written — partially closed 2026-09-09.** Re-tested during the blocker audit. Not stale: `test-fan-alice`/`test-fan-bob` with their `fanId` attributes and the realm's `loom-test-client` remain hand-made and unreconstructed. **And my own work this week enlarged the surface**, which is worth stating plainly rather than quietly: the 2026-09-08 seeding round, the Masjid `owner` role created via `createRole`, and its two seeded holders are all live cluster state that existed nowhere in git. The cluster now holds **63 role grants and 68 fan passports**, none of them reconstructible from the repository. **Partially closed:** [`Tools/seed/seeded-identities-2026-09-09.md`](Tools/seed/seeded-identities-2026-09-09.md) now records every group/role/fan grant, generated from the live cluster, so a rebuild has an answer to "what should exist". It is **a record, not a fixture** — nothing reads it at runtime — and it is replayed with the already-checked-in `Tools/code/seed_role_holder.sh`, which provisions through the real authorization flow (fan requests, admin approves and grants). The manifest says explicitly that these must **never** be restored by database insert: the three identity layers each have to be created by the mechanism that owns them, and a direct insert yields rows that look right and cannot sign in — the exact failure that made 23 seeded fans unusable on 2026-09-08. **CORRECTION, same day — I overstated what remains open.** I first wrote that "the Keycloak side is still undocumented and unautomated". That is wrong, and I checked the script rather than leaving it standing. `seed_role_holder.sh` **step 1 creates the Keycloak account itself**, with the `fanId` attribute and the password, and is idempotent (it accepts `409 already exists`). So all three identity layers are automated by one replay: Keycloak account, `fan_passport`, then membership and role grant through the real request/approve flow. The manifest plus that script therefore reconstructs **every seeded fan listed in it**, Keycloak included. **What genuinely remains** is narrower: the realm's `loom-test-client` OAuth client and the legacy `test-fan-alice`/`test-fan-bob` accounts, which predate the `loom-<slug>`/`fan-<slug>` convention, are absent from the manifest, and are created by nothing. A realm rebuild still needs those done by hand before any replay can authenticate.
 → Build §8
- [ ] **STILL TRUE and ROUGHLY DOUBLED, re-measured 2026-09-09 — and my own work is why.** The row was written against **35** accounts sharing one password. Live today: **68 `fan_passport` rows** and **63 distinct fans with a group membership**, and `seed_role_holder.sh:32` still hardcodes `PASS="LoomTest123!"` for every account it creates. So the shared-credential blast radius has roughly doubled, and the 2026-09-08 seeding round plus the Masjid `owner` holders are the reason. **Stating that plainly because the seeding was reported as a clean win** — it closed a real capability gap (two-party interactions were unreachable with one holder per role) while enlarging two debts recorded elsewhere in this tracker: this one and the hand-made-cluster-state row. A change can be correct and still make a known problem worse, and the report that mentions only the first half is the more flattering one. **The other two halves of this row are unchanged:** test credentials are still documented in plaintext in the Access Control tracker (which is *deliberate* and load-bearing — a session that could not find them once began resetting a live Keycloak credential before a classifier stopped it, so removing them without a replacement lookup path would trade one risk for a worse one), and the cleartext-JWT concern is untouched. **This is genuinely pre-GA debt, not a defect:** every one of these accounts is a seeded test identity in a dev cluster, the convention is documented, and nothing here is a production credential. The action it needs is a decision about what GA requires — per-account credentials, a secret store, or an explicit "test realm only, never promoted" boundary — not a fix. → Build §8


- [x] **RESOLVED 2026-09-09 — the bar is MEASURED for the first time: 7 of 72 rows have both halves.** This row previously said the numerator was uncomputable because the two halves used incompatible keys. **That was my error and I am striking it in place rather than adding a third version.** What is actually true: **the judge artifacts carry a `workflowId` field** — 95 of them do, with genuine workflow types, alongside a `persona`. That is the bar's own key. `screenRowId` is a **screen**, roughly three per row (entry/action/result), and it is *derived from* `workflowId`, which is precisely why its embedded slug resembles a bar key without being one; 204 screens over 72 rows is the ~3× that gives it away. **Measured by `Tools/code/check_b25_status.sh`:** walkthrough half **9**, judge half **55**, **both halves 7 of 72**. **Caveat kept rather than rounded away:** the judge column counts rows with an artifact *naming* the workflow, not that artifact's verdict for that row, so 55 and 7 are **coverage, not passes**, and 7 is an upper bound until the per-row verdict is checked. **How I got it wrong, recorded because the trap is real:** I read this question three ways — joinable via the slug, then uncomputable, then joinable via `workflowId` — and shipped a tool asserting the wrong middle answer. Twice I reasoned from filenames and id shapes; what settled it was reading the **producer** (`loom_ux_judges.dart`, where `screenRowId` is built as `b25-v4-row-NNN-${_slug(workflowId)}`) and then grepping the artifacts for the field it names. **When evidence is keyed opaquely, read the code that writes it before inferring from what it looks like.** The nomenclature trap is now documented inside the script itself.



- [x] `needs-deploy` — **CLOSED 2026-09-10 — DEPLOYED AND VERIFIED LIVE.** `1.0.5` rolled out, manifest bumped and committed in `loom-backend` (`72c57f0`), three-source audit clean (deployed=manifest=`1.0.5`, tree clean). Live verification, not pod status: `GET .../community_cedar_commons_hoa/instances` unfiltered → **200** (was 500), `?workflowType=hoa-dues-payment` → **200** (was 500), `reminderAt` now serializes as `"2026-09-27T17:00:00.000Z"`. Chess control re-run and still 200. **FIX COMMITTED, BUILT AS `1.0.5`.** `50533d41` converts `DateTime` to ISO-8601 UTC at the HTTP response boundary. New PostgreSQL integration test was RED before the change, green after; all five suites clean; the two engine tests asserting `reminderAt` stays a `DateTime` internally are untouched (the contract lives in the engine, the leak was in the serialisation, not the fix location). Image `loom-workflow-service:1.0.5` is built and sitting on the VM. Deploy per the runbook — `IMAGE_TAG=1.0.5` explicit, `docker save | k3s ctr images import -`, bump+commit the manifest in `loom-backend`, three-source audit, then the live Cedar query returning 200 is the actual verification, not pod status. Original finding and root-cause trail follow.

- [x] `new-ticket` — **CLOSED 2026-09-10 — duplicate of row 206 above, which carries the fix and the live-verified closure (`1.0.5` deployed, `GET .../instances` now 200, `reminderAt` serializes as ISO-8601).** This row is the original root-cause trail (the `reminderAt`/DateTime-leak investigation, including the formula-field misdiagnosis and its correction) kept below for that history. ORIGINAL: **Cedar Commons HOA's instance listing returns HTTP 500 for every workflow type, so no instance renders anywhere in that community. Reproduced by me 2026-09-09, independently of the walkthrough that found it.** Authenticated as `loom-hoa-member-1` against the deployed service: `GET /v1/communities/community_cedar_commons_hoa/instances` → **500** `workflow_service_error` "Workflow instances could not be queried"; the same call filtered to `?workflowType=hoa-dues-payment` → **500**. The walkthrough isolated it further: `hoa-facility-reservation` → 200, `hoa-owner-notification` → 200, `hoa-dues-payment` → 500, unfiltered → 500 — **one type poisons the community-wide query**, matching the server's `JsonUnsupportedObjectError: Instance of 'DateTime'` with correlation ids that line up. **Control: Chess Club's listing returns 200**, so this is not a general service outage. **The walkthrough's proposed cause does NOT survive my check, and I am recording that rather than repeating it.** It reported that a package sweep found `date`/`time`-typed formula fields "in exactly one community, matching the observed blast radius". In fact **nine** shipped packages carry `date`/`time`-typed fields — Ad-Free 7, Book Club 6, Camera 3, Cedar 10, Chess 6, Data Portability 1, Garden 8, Masjid 6, Youth Soccer 8 — and **Chess has six and lists fine**. So "has date/time fields" is not the trigger; something narrower and still unidentified is, and the next step is to find what `hoa-dues-payment` does that Chess's types do not (a computed/formula field producing a real `DateTime` rather than a stored string is the obvious hypothesis, and it is a hypothesis). **Severity is high and easy to understate:** every instance in Cedar is invisible in the app, which reads as an empty community rather than as an error, and Cedar is one of the ten shipped communities. **Note this is a serialisation defect in the query path, not a data defect** — the rows are intact and readable by `psql`, which is how the walkthrough measured its baseline of 25 while the API returned nothing. **CORRECTION, same day — I misread the walkthrough's claim and wrongly said it did not survive checking. Its sweep was right; my check tested a weaker claim.** Above I wrote that it "reported that a package sweep found `date`/`time`-typed formula fields in exactly one community" and that nine packages carry such fields, so the trigger did not hold. **Re-reading its words, it said "the only listed type with `date`/`time`-typed FORMULA fields".** I dropped "formula", swept for plain `date`/`time` fields, found nine communities, and declared the claim disproved. **Re-run with the word restored: exactly ONE package has formula-computed date/time fields — Cedar — and it has exactly two, `eventDate` (`"type": "date", "formula": "dueDate"`) and `eventTime` (`"type": "time", "formula": "dueTime"`), both in `hoa-dues-payment`, which is precisely the type that 500s.** Two independent greps agree. So the discrimination is perfect: nine communities have stored `date`/`time` fields and list fine; the one community with **computed** ones fails, on the one type that carries them. **The cause is therefore specific and well-supported: a formula evaluating to a real `DateTime` reaches JSON serialisation, where a stored date is a string and does not.** That is still short of proof — nobody has traced the serialiser — but it is a strong, discriminating correlation rather than the hypothesis I downgraded it to. **The lesson is mine: I paraphrased an agent's precise claim, tested the paraphrase, and published the agent as wrong.** A one-word elision inverted the finding. When checking someone else's claim, check the words they wrote, not the version you remember. **CAUSE CORRECTED 2026-09-09 — my formula-field hypothesis was WRONG, and the implementation agent refused to build against it rather than producing a fix that would have changed nothing.** I dispatched a ticket asserting the leak came from `hoa-dues-payment`'s formula-computed `eventDate`/`eventTime`. **It does not.** Those formulas resolve to the stored values, and the two live dues rows hold ordinary strings (`"2026-09-30"`, `"17:00"`), so the formulas evaluate to strings. **The actual leak is `reminderAt`.** The deployed dues definition carries an **unconditional `reminder` block**; on the read projection `_withComputedFields` (`local_workflow_engine_api.dart:2410`) calls `WorkflowReminder.dueAtFor`, which calls `combineDateAndTimeValues` and receives a **`DateTime`**, then inserts it raw: `computed['reminderAt'] = dueAt` (`:2425`). **I verified both lines myself.** **And the type is codified by existing tests** — `declared_reminder_test.dart:136` asserts `instanceData['reminderAt']` equals `DateTime.utc(2026, 3, 9, 18)`, with the same expectation in `v3_milestone_1_1_formula_test.dart:246` — so this is a deliberate internal representation that escapes to `jsonEncode`, not an accident of one field. **Why Cedar's own controls stayed healthy, which is the detail that breaks my correlation:** `hoa-facility-reservation` has a reminder **gated by `reminderEnabled`**, and its live rows do not enable it, so `reminderAt` is simply absent. Dues has no gate, so every visible dues row leaks. **The lesson: my discrimination was perfect and my mechanism was wrong.** Nine packages have stored `date`/`time` fields and list fine; exactly one has formula-computed ones and it is the one that fails. That is a flawless correlation across ten packages — and Cedar happened to be the only community carrying **both** computed date fields and an ungated reminder. A perfectly discriminating variable can still be a passenger. **The corrected fix is a contract decision, not a one-line patch:** either `reminderAt` stays a `DateTime` internally and is serialised at the response boundary, or it becomes an ISO string throughout and those two tests change **because the contract changed** — which is legitimate, unlike weakening them to pass. `subtractHours(...)` and any other helper that can return a `DateTime` into `instanceData` need the same audit.
- [ ] `new-ticket` — **The B25 campaign is WALKTHROUGH-BOUND, not judge-bound: 48 rows need only a live walkthrough to reach the bar. Measured 2026-09-09.** Current state from `Tools/code/check_b25_status.sh`: **72** real rows, **57** with judge coverage, **9** with a live walkthrough, **9 with both** — and the overlap means **48 rows already have a judge artifact and lack only the walkthrough half.** That is the whole remaining shape of the campaign, and it was not visible before the two halves could be counted against the same key. **What it costs:** a walkthrough is ~25 minutes of device time (measured today: Garden 22 min, Social ~25), needs the Windows-hosted emulator reachable via `adb -H 192.168.56.1 -P 5037`, and produces gitignored frames that must be judged before cleanup — so walkthrough and judge should be dispatched as a pair, as they were for the two rows completed today. **48 × ~25 min is roughly 20 hours of device time**, which is the honest estimate for taking the bar from 9 to 57; the remaining 15 rows need both halves. **Two rows on that list are known-blocked and should not be attempted as-is:** `book-nomination` is the workflow whose `draft` state renders to nobody (the `audience: "actor"` defect, fix queued to Book Club's held regeneration), and Book Club's earlier attempt is the BLOCKED manifest already in the evidence set. **Sequencing note:** every one of these should be driven against a current APK. Today's build carries app-shell fixes that changed on-device behaviour for at least one row — the owner is no longer offered "Join queue" — so a walkthrough against a stale APK banks evidence for a UI that no longer exists.
- [ ] `new-ticket` — **A `platform-connection` invite can be addressed to someone who can never answer it, because the Invitee field is free text with no check that the target holds `member`.** Found by the Member Social Space walkthrough 2026-09-09 and **verified by me directly in `workflow_instances`**: two rows exist, and the older one sits in `invited` with `inviteeFanId = fan-social-moderator-1` — a fan who holds only `moderator`. Both `accept-invite` and `decline-invite` are guarded `allowedRoleIds: ["member"]`, so that invitee **cannot respond at all**, and the row has been stuck for 29 hours. The instance is not fully dead — the inviter can still cancel or block — but the invitee's side is unreachable by design of the guard, and nothing prevented the invite being addressed there in the first place. **This is an input-validation gap, not a package defect, and the fix is NOT to loosen the guard:** the guard is correct (only members connect), and widening it would let non-members into the connection graph. The fix is that the create/invite surface should either constrain the Invitee field to fans holding `member` in that community, or the transition should refuse an invitee who does not — a check the platform can make and a package cannot express. **Note this is a different shape from the four unreachability cases already recorded:** those were a missing role, a role nobody held, a precondition nobody could satisfy, and an unpublished effect target. This one is **a valid workflow addressed to an invalid party** — every declaration is correct and the *data* makes it unanswerable. **Also verified: the second row reached `connected`** with `inviteeFanId = fan-social-member-2`, so the two-party path works once both parties hold `member`; the 2026-09-08 "can never reach connected" defect is closed by the seeding, not by any package change.
- [ ] `new-ticket` — **Two rows are one judge pass away from the bar: `garden-tool-loan` and `platform-connection`.** Measured 2026-09-09 by `Tools/code/check_b25_status.sh`. Both are **walkthrough-proven** — their live-write manifests carry the "Both halves of the proof standard were met" phrase, with a real OAuth sign-in, a workflow created from nothing through the UI, a terminal state reached, and the Postgres row read back — but **neither appears in any judge artifact's `workflowId`**. So the expensive half is already done and the cheap half is missing, which makes these the two lowest-cost rows on the board: completing them takes the bar from **7 of 72 to 9 of 72**. **The seven currently complete:** `ad-off-member-checkout`, `chess-match-result`, `critique-submission`, `export-full-bundle`, `hoa-dues-payment`, `mosque-donation-payment`, `soccer-registration-payment`. **What a judge pass needs, and why it is not free:** UX judging runs on Claude Sonnet via `data/call_ux_judge_agent.sh` and **requires images** — the DeepSeek gateway is text-only and refuses them — and `*.png` is gitignored, so the screenshots from the 2026-09-08 walkthroughs are gone. A judge pass therefore needs a fresh capture run against the Windows-hosted emulator, not just a re-read of existing evidence. **Sequencing caveat from `CLAUDE.md`:** the judge and the walkthrough are end-of-line polish, run once the backend is wired — which it now is for these two, since both were driven live against the deployed cluster.
- [ ] `new-ticket` — **The B25 numerator has never been computed, and the evidence inventory says it is small. Measured 2026-09-09.** With the denominator now corrected to **72 real rows**, I inventoried what exists to put over it. **The bar requires BOTH halves per row — a live walkthrough AND a UX judge pass.** Evidence directory holds **438** files: **15** `*-live-write-*` manifests (of which one, `root-cause-postgres-outage-*`, is not a workflow at all, leaving ~14) and **46** judge/UX-review artifacts. The live-write manifests cover roughly **12–13 distinct workflows** — ad-free member checkout, book club nomination, camera critique, cedar dues, chess dispute, chess match result, data-portability export, garden tool loan, masjid donation payment, social platform connection, book-club nomination, soccer registration payment. **Against 72 rows, each naming a distinct workflow.** **What cannot be stated yet, and why:** no one has ever *paired* the two halves per row. A live-write manifest proves a workflow was driven; a judge artifact scores a surface; the bar needs both **for the same row**, and the manifests are named by community-and-workflow slug while the rows are keyed by workflow type and persona. Until that join is done there is no defensible "N of 72", and quoting one would repeat the error this sweep just corrected in the denominator. **Two things make the join harder than it looks:** the manifest filenames are slugs (`garden-club-tool-loan`) not workflow types (`garden-tool-loan`), so the mapping is by hand or by a rule nobody has written; and **manifests written before 2026-09-09 carry no package identity**, so even a matched pair only proves the row against whatever the package was that day. **The honest current statement is therefore: roughly 12 of 72 rows have the walkthrough half, the judge half is uncounted, and the paired figure is unknown.** That is a smaller and more truthful picture than any number previously quoted. Note this is a different quantity from the "roughly 22 rows can only ever be proven against fakes" accounting, which is about the ceiling, not the progress — do not conflate them.
- [ ] **STILL TRUE, re-measured 2026-09-09 — unchanged in every particular.** Fourth negative result of the blocker audit; this row has not rotted. **Both halves re-measured today rather than re-read.** (a) `find app -iname "*community*fake*.dart" | xargs grep -ho "implements [A-Za-z]*Api" | sort -u | wc -l` → **53**, exactly the 2026-08-26 figure, across the same six files in `loom_fake_backend` (`community_{economic,engine,experience,foundation,ops,registry}_fake.dart`). (b) The deployed set is still five services plus object storage — `app-access`, `fan-passport`, `keycloak`, `workflow-service`, `postgres`, `minio` — so the number of fake-backed APIs with a real counterpart has not grown. The gap has therefore neither widened nor narrowed. **`LocalAuthApi` also still exists** (`part30_local_auth_api.dart:10`, `class LocalAuthApi implements LoomAuthApi`) and is still constructed by the remote path at `part39_remote_auth_api.dart:20`, so "retiring it means *building* an auth service" stands as written. **What this row actually needs is arithmetic, not a decision, and that arithmetic is still owed:** four fakes stay fake by an existing user decision (payment, id generation, external search/AI, checksum), and the "roughly 22 of 79 B25 rows are proven-against-fakes" accounting was computed from those four. With 53 fake-backed APIs the honest figure is higher and **still unrecomputed**, so no row should be called production-proven against the 79 bar until it is. Note this compounds with the two other B25 accuracy defects re-confirmed today — the denominator inflated by five non-workflow rows, and a numerator whose manifests carry no package identity before 2026-09-09. **All three affect the same number, and none of them has been corrected in it.** → Build §8 **RECOMPUTED 2026-09-09 — and the recomputation says the 53 figure does NOT inflate the B25 fakes-share, because those fakes are not in the path B25 drives.** The count is accurate (re-measured: exactly 53 distinct `implements *Api` across the six `loom_fake_backend` files, unchanged since 2026-08-26). What it does not establish is *exposure*. Traced every import: **16** in `apps/loom_demo/test`, **6** inside the fake package itself, **2** in `apps/loom_communities_demo/test`, **1** in `loom_lints`, and **1** in `apps/loom_demo/lib/main.dart`. **Zero references in `apps/loom_communities_demo/lib` and zero in `loom_communities_app_shell/lib`** — so the fakes back **tests and the legacy `loom_demo` app**, not the shipped one. B25 walkthroughs drive `loom_communities_demo` (the APK built in `CLAUDE.md`'s own recipe), which runs against the deployed cluster — app-access, fan-passport, keycloak, workflow-service, postgres. **So this row conflates two different things, and only one of them limits B25:** (1) `loom_fake_backend`'s 53 APIs are test and legacy-app scaffolding with no presence in the live path, and therefore do **not** make any B25 row "proven against fakes"; (2) the four platform services that stay fake by user decision — payment, id generation, external search/AI, checksum — are *missing services*, and workflows depending on them genuinely cannot be fully proven. **The "roughly 22 of 79" accounting was built on (2), and (2) has not changed**, so that figure needs restating against the corrected denominator of 72 but not inflating toward 53. **The row's premise — "the fake-backed share of the production bar is much larger than the four services already agreed" — does not survive the import trace.** Note the `pubspec.yaml` dependency on `loom_fake_backend` in `loom_communities_demo` is what makes this look worse than it is: a dev-dependency-shaped edge with no `lib` import behind it. Counting `implements *Api` measures the fakes; it does not measure what the app calls. → Build §8
- [x] **CLOSED 2026-09-09 — stale; Garden completed a full live walkthrough eight days after this row was written.** The row is dated 2026-08-31 and says "unfixed, reproducing on both hosts, fails in ~4 min". Evidence to the contrary: [garden-club-tool-loan-live-write-2026-09-08.md](Evidence/B25/garden-club-tool-loan-live-write-2026-09-08.md) records a completed session on `emulator-5554` — signed in for real as `loom-garden-member-1` through the in-app OAuth flow against Keycloak, created a `garden-tool-loan` **from nothing** through the real UI to `published`, drove `delist` to the **terminal state**, and read the Postgres row back after each step, with the UI evidence and the database row agreeing. Its own words: **"No escalation was needed."** It also supersedes an earlier reopened Garden claim that had no corresponding database row. The `garden-tool-loan-giveaway` doc/package id mismatch named here as the trigger was itself resolved earlier. Note the run needed `adb -H 192.168.56.1 -P 5037` because the VM has no AVD and its local adb server saw zero devices — the host-adb route, not a stall. → Build §8
- [ ] platform phases A, A.1, B, C, D, E, G.4 → Build §8
- [ ] outstanding Skill dispatches: DataPortability, AdFree, Camera (rejected), Book Club (held), Garden (INSTALLED — that row was stale, corrected 2026-09-08 by diffing the shipped asset against the Skill output: byte-identical apart from the added `skillVersion` stamp) → CJM §8
- [ ] queue transitions — **NOT dead; that framing was a premise error, corrected 2026-09-08.** All six route correctly through the shared `EquipmentLoanArchetypeCard`, which Home and Marketplace both use. Four real defects were found instead: **(A) and (D) are FIXED and verified** (`70c1f5fa`) — the button now evaluates its guard, which needed the viewer `roleId` threaded in, and a 403 refusal no longer renders as an outage. **(C) OPEN** — Book Club declares `libraryActivityAudit` appends the service queue path never executes; needs the SERVICE to record them, not an extra generic transition (two stores, partial-success window). **(B) IN FLIGHT** — a wrapped engine defeats `is RemoteWorkflowEngineApi` and silently selects the legacy path on every tab; dormant only because the offline-replica directory defaults to empty. → CJM §8

- [x] `needs-deploy` — **DONE 2026-09-09.** The workflow-service transaction fix (`7a71ed92`) is committed but NOT DEPLOYED, and the cluster is therefore still committing failed requests' writes.** Three-source audit run 2026-09-09 and **clean before this change**: deployed image, `loom-backend/deploy/k8s/*.yaml` manifest and the backend tree all agree at `loom-workflow-service:1.0.3`, with no uncommitted drift. So the gap is not drift — it is simply that the fix has never been built. **Deliberately batched:** queue defect (C) is also a workflow-service change, and an image build on this 8-core box previously stalled the whole node hard enough to fail every pod's probes at once, so building twice for two commits is a cost worth avoiding. Plan is to land (C), then build `1.0.4` once, import, bump the manifest, deploy, and **commit the manifest bump** — that last step is what has been forgotten twice before, leaving the repo claiming one version while the cluster ran another. After deploying, re-run a real request against the stack rather than trusting pod status, since a heavy build can leave a service `1/1 Running` while its long-lived DB connection is dead. **Until this deploys, the committed fix protects nobody** — the defect is live in the cluster exactly as before. **DEPLOYED AND VERIFIED 2026-09-09 — `loom-workflow-service:1.0.4` carries both fixes and they are now live.** Built with `IMAGE_TAG=1.0.4` passed explicitly (the script defaults to `0.1.0`, a tag nobody deploys — the drift its own comment records), imported with `docker save … | sudo k3s ctr images import -` since `docker build` alone never reaches k3s, manifest bumped in `loom-backend/deploy/k8s/workflow-service.yaml`, rolled out, **and the manifest bump COMMITTED (`13729a2`)** — the step forgotten twice before, which leaves the repo denying the version the cluster runs. **Three-source audit after the deploy: deployed `1.0.4`, manifest `1.0.4`, `loom-backend` tree clean.** **The stack was proven to still SERVE, not merely to be `Running`:** an authenticated `GET /v1/communities/community_chess_club/instances` as `loom-chess-member-1` returned **200 with real instance rows read from PostgreSQL**. That check matters because the documented post-build failure here is a service that stays `1/1 Running` holding a dead long-lived DB connection — pod status, a green rollout and even a well-formed 401 all look healthy without touching the database. Intermediate evidence on the way: unauthenticated GETs returned structured `authentication_required` JSON echoing my supplied correlation ids, proving routing and header handling before auth was added. **The build was gentler than the historical incident** — all six pods stayed `1/1`, `postgres-0` restart count unchanged at 10, and the new `workflow-service` pod shows 0 restarts. **Post-deploy parity audit, all three gates green:** spec twins and generated artifacts in parity, 85 published definitions with 0 declared-but-unpublished, and package-declared roles matching provisioned roles.
**DEPLOY RUNBOOK for `1.0.4`, written 2026-09-09 before the build so it is mechanical rather than improvised.** Read from `app/packages/core/loom_workflow_service/build.sh` and its Dockerfile header, not from memory.

1. **`IMAGE_TAG` is mandatory.** The script reads `IMAGE_TAG="${IMAGE_TAG:-0.1.0}"`, so omitting it builds **`0.1.0`** — a tag nobody deploys. The script's own comment records that this already happened once (`0.1.0 -> 0.2.1` bumped by hand), which is why the value moved to the environment. Run `IMAGE_TAG=1.0.4 bash app/packages/core/loom_workflow_service/build.sh`.
2. **Bootstrap first.** The build stages a *pre-resolved* workspace: it `rsync`s `~/.pub-cache` and `app/` into a scratch dir rather than resolving inside Docker, so `melos bootstrap` must have run or the image compiles against a stale package config.
3. **Do not clean `/tmp/tmp.*` while it runs** — that is the staging directory holding the copied pub cache and workspace.
4. **Import explicitly.** `docker build` alone does not reach k3s: `docker save loom-workflow-service:1.0.4 | sudo k3s ctr images import -`.
5. **Bump the manifest in `~/loom-backend/deploy/k8s/*.yaml`, apply, and COMMIT THE BUMP.** The commit is the step forgotten twice before; the cluster then runs a version the repo denies. Treat the deploy as unfinished until `git status` in `loom-backend` is clean.
6. **Watch the box, not just the rollout.** An image build on this 8-core node previously failed every pod's probe at once and restarted `postgres-0`. Check `cut -d' ' -f1-3 /proc/loadavg` before starting, and afterwards **re-run a real request against the stack** rather than trusting `1/1 Running` — a heavy build can leave a service running with a dead long-lived DB connection.
7. **Re-audit all three sources afterwards** (`kubectl get deploy ... -o jsonpath` for the live image, the manifest tags, and a clean `loom-backend` tree). They agreed at `1.0.3` before this change, so any disagreement afterwards is this deploy's doing.
- [x] `new-ticket` — **CLOSED 2026-09-10 — verified still landed: `_CommunityTransactionAbort` and `postWriteFailureInjectorForTest` both present in `workflow_service.dart` and the rollback integration test.** Already fixed and A/B-proven per its own text below (2026-09-09); this tick only re-confirmed the fix is still in source before striking the row. ORIGINAL: **The transaction-boundary defect is NOT specific to the item queue — the mechanism is service-wide, and this is the most consequential thing found this session.** Verified 2026-09-09 by reading both ends. `WorkflowService._handle:123` wraps **the entire request** in `_communityTransactionRunner(communityId, () => _handleUnscoped(request))`. Every `catch` in the handlers below it therefore sits **inside** that transaction, and each one converts an exception into an HTTP error `Response`. **A returned Response is normal completion to the transaction runner, so PostgreSQL commits whatever the handler already wrote.** `workflow_service.dart` contains **20+** such catch blocks. **Scope, stated precisely — the mechanism is confirmed, the exploitability is per-path:** a catch that can only fire *before* any write is harmless, and many are exactly that (`FormatException` on input parsing; the change-feed query at `:1052` is read-only). The defect bites wherever a write can precede the throw. **Two confirmed structurally:** the item-queue handler (`_withItemQueueRequest:1892` — an exception after `repository.join` inserts leaves the member queued while the client is told the request failed) and the create-instance handler (`:815–845`, catching `WorkflowValidationError` → 400, `StateError` → 404/500, and a bare catch → 500, all after the engine may have written). **`_database.transaction` is not a mitigation** — `database.dart:884` executes the callback directly when an outer executor exists and creates **no nested rollback boundary**, so wrapping a sub-section in it adds nothing while looking like it adds safety. **The fix is structural, not per-handler:** either map errors to responses *outside* the community transaction, or throw a typed abort carrying the intended response and unwrap it after rollback. Fixing individual catches in place cannot work — the problem is that the boundary and the error mapping are on the wrong sides of each other. **This is a prerequisite for queue defect (C)**, since composing effects into the same transaction is unsafe while failures cannot escape it, and it is worth fixing on its own regardless of (C). **Not yet dispatched as of this entry; the per-path audit of which of the 20+ catches can follow a write still needs doing and should be part of the ticket, not assumed.** **MY INDEPENDENT COUNT, RECORDED BEFORE THE DISPATCH REPORTS (2026-09-09) so its audit can be checked rather than believed.** `workflow_service.dart` contains **43** catch blocks, not the "20+" I estimated earlier — that estimate came from a `head`-truncated listing, which is the same truncation trap this file warns about, and I corrected it by counting rather than re-reading the first screen. The writes a catch could follow, from my own grep, are **at least eight sites across six handler families**: `_createInstance` (`:577`), `_createInstances` (`:771`), the item-queue `repository.join` (`:1590`) and three `repository.remove` sites (`:1651`, `:1685`, `:1809`), the export bundle `insertIdempotently` (`:2172`), the document `insertIdempotently`/`insert` (`:2800`/`:2805`), and `engine.applyTransition` (`:4105`). **So the defective population should be materially larger than the two handlers I confirmed by hand** (item queue, create instance). **If the dispatch's audit reports only those two, or reports a number without naming the write each catch can follow, that is a reason to distrust the audit rather than to accept a small answer** — an audit that finds only what it was handed is not an audit. Conversely a much larger count is not automatically right either: a catch is only defective if a write can actually precede it on that path, and many of the 43 are input validation that fires first (`FormatException` on parsing) or sit in read-only handlers (the change feed at `:1052`). **FIXED 2026-09-09, and PROVEN by A/B against real PostgreSQL — the proof the dispatch honestly reported it could not obtain.** **The fix is better than the one I specified.** Rather than classifying all 43 catches, it makes the **response status** the abort signal: `_handle` now wraps the community transaction so that any response with `statusCode >= 400` throws a typed `_CommunityTransactionAbort` **carrying the already-built response**, which propagates out of the transaction (so PostgreSQL rolls back) and is unwrapped immediately outside the boundary to return the identical `Response` — same status, headers, body, code, message and correlation id, with every `_logUnexpectedError` call untouched. That covers **every** error path including any the audit might have missed, which is a stronger guarantee than per-site edits, and it required no `_database.transaction` (correctly, since that creates no nested rollback boundary). **The risk that generality introduces was checked, not assumed:** a handler that deliberately writes and *then* returns 4xx would now lose that write. None exists — both idempotent-replay paths (export bundle `:2172`, document `:2800`/`:2805`) return **200** on their not-created branch, so a replay commits as before. **THE A/B, run by me because the dispatch could not.** It reported honestly that PostgreSQL credentials were absent, so its new rollback test **skipped** (0 passed / 1 skipped) instead of failing pre-fix, and that its suite numbers were the no-credentials profiles (`service 142 +13`, `engine 309 +5`) in which the PostgreSQL tests never ran. With both credential sets and a port-forward: **with the fix the test passes**; with **only the abort-throw neutralised** (source restored afterwards and verified byte-identical) it **fails** with `Expected: empty / Actual: [Instance of 'StoredItemQueueEntry']` — i.e. the queue entry **was committed despite the 500**. The defect is therefore demonstrated live rather than argued from code shape, and the fix demonstrably removes it. **Its audit also matched the population I pre-registered before seeing it:** write-reachable catches are generic definition replacement, create-instance (3 branches), batch-create (3 branches), item-queue, export, document, field-update (2) and transition (2) — far more than the two handlers I had confirmed by hand, which is what an audit that actually audited should look like. **One deliberate non-change it called out:** the active-membership lookup catches can run after an event-RSVP insert but return a fail-closed **boolean** rather than an HTTP error, so they are not a "failed request commits writes" path and were left alone. **A test-only seam was added to production code** — `postWriteFailureInjectorForTest`, called at exactly two points (after `repository.join`, after `createInstance`) — because forcing a post-write failure against real PostgreSQL has no other honest route. It is inert unless supplied.
- [ ] the `chmod 444` guard is not durable → CJM §8
- [x] `WorkflowDatabase.memory()` is the app's only engine database → **ANSWERED, not a gap: the real app defaults to the remote, Postgres-backed engine (`LOOM_ENV=dev` by default); in-memory only runs under the explicit `LOOM_ENV=local` dev opt-out. See CLAUDE.md "The demo app IS wired to the deployed backend by default" and ACWS §8.**

### Spec decisions that block other work

The two `needs-spec-decision` items that were listed here — `deliver_reminder`'s 2-of-4 applicability
and the two permission vocabularies — are stated once under **Blocked on the user** above, with the
other two. A decision recorded in more than one place is what inflated the backlog to 17.

- [ ] `new-milestone` — **Document ACL**: `sharedWith` is one flat `fanId` list, so there is exactly one permission level ("shared" = "can read") and sharing with a group is impossible. Sequenced AFTER Phase F — see [Document ACL Spec Proposal.md](Document%20ACL%20Spec%20Proposal.md)
- [ ] `new-milestone` — **Spec-version compatibility hardening**: Layers 1–3 land with the v4-only cut; Layer 4 (packages declaring `requiresCapabilities`) needs a spec decision — see [Spec Version Compatibility.md](Spec%20Version%20Compatibility.md)

### Known defects and debt

- [x] `new-milestone` — **CLOSED ON DISCOVERY 2026-09-06: this was already built the SAME DAY it was queued, and the row was never updated.** `79da87b1` ("feat(workflow-service): export bundles with a checksum over the bytes served") is dated **2026-08-27** — the exact date this row calls the work "NEXT UP" — and `836100b8` (2026-09-03, the idempotency unification) explicitly lists export-bundle as one of the three flows it hardened. Read the implementation directly rather than trusting either date: `workflow_service.dart` computes a real `sha256.convert(payload.bytes)` at generation (not a placeholder), stores it via `ExportBundleRepository`/`DocumentObjectStore`, writes `checksum`/`checksumAlgorithm` into the instance via the same declaration-driven `platformSource` mechanism as `opaqueId` (spelling-agnostic, keyed off the schema), and a real `POST .../export-bundles/{id}/verification` endpoint **re-computes the hash from stored bytes** (never compares the record to itself) and writes `checksumVerified` only when the instance's schema declares that field. All three routes (`POST .../instances/{id}/export-bundle`, `GET .../export-bundles/{id}`, `POST .../export-bundles/{id}/verification`) confirmed **live on the deployed cluster** today — probed directly, 401 not 404 on all three. Spot-checked `platformSource: "checksum"` declarations across all 6 communities named in this row (Cedar, Chess, Data Portability ×3, Garden, Neighborhood Book Club, Riverside Youth Soccer) — present and consistent in every one. 9 existing tests in `export_bundle_endpoints_test.dart` already cover it. The `migration-export-api.openapi.yaml` reference in the original row was a red herring — that spec describes an unrelated, unimplemented ecosystem-wide portability API; the real contract lives entirely in the already-deployed workflow-service, exactly matching the row's own "same shape as the document library" note. Payment note (4 markers, deferred, already-offline-path) is unaffected and still accurate.
- [ ] `needs-verification` — **PARTIALLY STALE, re-checked 2026-09-06: the deploy already happened, for an unrelated reason, and carried the fix forward.** This row's premise (`0.3.0` predates `dueNotifications`) no longer matches the cluster — the deployed image is now `loom-workflow-service:1.0.3` (built 2026-09-03 for the RLS restricted-role work, ACWS Thread 1), and `bf1acf6d` (2026-08-27, the commit that added `dueNotifications`) is an ancestor of the commit that image was built from. Verified live, not inferred: port-forwarded straight to `svc/workflow-service` and called `GET /v1/communities/{id}/notifications/due` with a real UUID correlation id — **401 `authentication_required`, not 404** — the route is registered and enforcing auth. **What's still genuinely open:** "prove reminders live" end-to-end has NOT been done — queried `workflow_instances` directly (bypassing RLS as the admin role) for any row with a `dueAt` key at all, past or future, across every community, and got **zero rows**. There is currently no reminder-bearing instance anywhere in the cluster to observe delivery against; this is an absence of test data, not a service defect. Next step is a live walkthrough that creates a real calendar/reminder instance with a near-past `dueAt`, then confirms the sweep/endpoint surfaces it and `deliver_reminder` fires — not another deploy.
- [ ] `new-milestone` — **archetype-owned bookkeeping is unimplemented**, so `grant_access`/`share` cannot populate the shared-with field a grant reads; blocks Cedar's `explicitReaderFanIds` ever being filled — see [document-library.md §6](../references/archetypes/document-library.md)
- [ ] `needs-skill-dispatch` — **HELD, NOT DISPATCHED — fold into Book Club's pending single regeneration.** All four affected workflows are Book Club's, and Book Club is **knowingly damaged and held by user decision** (`c0e0355b` removed its per-member `send-reminder`, shared-library queue/custody fields and reading-material access lists; a verified restore sits uninstalled at `~/.codex-skill-authoring-scratch/bookclub-restore-r2/`, held until the listing/loan backend exists so the community is regenerated ONCE against the new API). Dispatching this fix now would regenerate from the damaged HEAD and bake that loss in permanently — and the tracker's own warning applies: **verify any Book Club output against `c0e0355b^`, not against HEAD**, because HEAD is the damaged state and a diff against it reports the loss as faithfully preserved. **So this fix is queued to that regeneration, not dispatched separately.** SCOPED 2026-09-08: the fix is create-time identity in the PACKAGE, not a creator fallback in the engine.** (Root cause agent, session key `draft-binding-invisibility`.) **The fix I would have reached for is wrong, and that is the main result.** Falling back to `createdByFanId` when the actor field is null would break an intentional design: the docs deliberately give the **business party precedence over the creator** — justified with a charge created by a board member for another payer (`render-bindings.md`, `guards.md:392`) — so a generic creator fallback changes semantics rather than repairing missing initialization. **Verified by me in `role_resolver.dart:20-31`:** the loop takes the FIRST `actorEqualsField` guard and `break`s, and `createdByFanId` is used **only when no guard exists at all** (`if (!foundActorEqualsField)`), so a null field yields no actor and nobody matches. **The scoped fix (Skill dispatch, Book Club):** add `"prefill": {"nominatorFanId": "$actor"}` to the "Nominate a book" create action; change `nominatorFanId` to `"writableBy": "platform"` and required; **remove** the `nominatorFanId = $actor` effect from `submit-nomination` (ownership should survive submission and revision) while keeping the timestamp effect; and add a draft `creationGuard` requiring the member role plus `actorEqualsField: nominatorFanId`, so mismatched ownership fails creation including via direct API. **Three siblings confirmed by static inspection** (not live): `book-vote-response` (`voterFanId` on `confirm-vote`), `book-shared-library-item` (`ownerFanId` on `publish-listing`), `book-search-ai-digest` (`submitterFanId` on `submit-query`). **My 19-binding count was an over-count** — "the raw audience count is not the defect count"; other candidates already prefill their identity, and the audit should be by workflow/state/creation-path, not 19 mechanical replacements. **Existing invisible rows need a separate repair:** prefill only affects future creation, so the one verified draft (`..._il5zplsant0a`) needs `nominatorFanId` backfilled from its `created_by_fan_id`, applied ONLY to established self-owned workflow/state cases — creator-to-business-party copying is not valid generically.
- [ ] `needs-spec-decision` — **`deriveInstanceRoles` picking the FIRST `actorEqualsField` guard is a real defect wherever the keys differ, and JSON array order is not a defensible business-party selector.** Separate from the row above, which it does not cause (Book Club's two guards both name `nominatorFanId`, so reordering there is harmless). **Concrete counterexample in the same package:** `book-shared-library-item`'s owner transitions use `ownerFanId` while `cancel-loan-request` uses `pendingBorrowerFanId` — moving that transition earlier changes who renders as actor, and with a null pending borrower even a correctly-initialized owner loses actor status. Neither "first non-null field" nor "union of matching fields" fixes the meaning: the first changes ownership with transient data, the second treats owner and borrower as the same rendering relationship. **Proposed:** an explicit workflow-level rendering-actor selector (e.g. `actorField`); infer it only when all guards name one distinct key; keep creator fallback when there are no such keys; **require** the selector where keys differ and emit a validation error rather than choosing by order; and a selected-but-null identity resolves to **no actor**, never implicitly to the creator. Touches model parsing/serialization, grammar validation, the shared resolver, reference docs, the authoring bundle and affected packages (`workflow_models.dart:1308` has no selector today). Do not silently switch existing ambiguous packages to a new inference rule without migrating them.
- [ ] `new-ticket` — **The Skill's own guidance would reproduce this defect, so the teaching material needs the missing prerequisite.** `solved-patterns.md` presents delayed identity stamping (setting the actor field on the first transition) as equally valid **without checking render audiences** — so a documentation-driven author writing a new community would author exactly the invisible-draft shape. Per this repo's own rule, fix the Skill with a worked example rather than a prose rule: add the requirement shape (initial state binds `audience: "actor"`), the plausible-but-wrong JSON (identity stamped on the first transition), the verified-correct JSON (create-action `$actor` prefill + `writableBy: "platform"` + creation guard), and cite Book Club once repaired as the live example.
 **THE 19 CANDIDATES ARE RESOLVED 2026-09-08 — the defect is Book Club's alone, and the other four communities are CLEAN.** I checked all ten non-Book-Club `audience: "actor"` bindings individually (Camera Club 4, Member Social Space 3, Garden Club 2, Masjid Nur 1) by finding each binding's enclosing workflow, its `actorEqualsField` guard key, and whether that key is populated at creation or only by a transition leaving the bound state. **Every one of the four communities stamps its actor field with `$actor` in the create action's `prefill`:** Masjid's `mosque-care-request` sets `requesterFanId`, Garden's `plant-exchange-submission` sets `ownerFanId`, Camera's `critique-submission` sets `authorFanId`, and Member Social Space's `platform-message-thread` sets `participantAFanId` (in **both** of its create actions). So the actor resolves in `draft` and the cards render. That also means **the held Book Club regeneration is the complete fix for this defect class**, not the first of five. **Two methodology notes, because I nearly filed two false defects here.** (1) My first sweep used `grep -o '"prefill": {[^}]*}'`, which matches only **single-line** prefill blocks — Camera's and Social's are multi-line, so both came back empty and both looked like the Book Club shape. Caught only by reading the surrounding lines rather than trusting the empty result; a control (`grep -c '"prefill"'` on the same file returned 6) would have exposed the broken query immediately. (2) Member Social Space looked briefly like a **worse** defect than Book Club: `start-conversation` is `from: ["draft"] → "open"` with `guard: {actorEqualsField: participantAFanId}` and an effect that *sets* `participantAFanId` — a guard depending on a field only its own effect writes, which `guard_evaluator.dart:52` (`if (fanId != instanceData[key]) return false;`) can never satisfy against a null. That would have made starting a conversation impossible for anyone. It is **not** a defect only because the create action prefills the field first — but the shape is worth naming: **a transition whose guard depends on a field that only that same transition writes is self-blocking unless something upstream populates it**, and the prefill is what saves it here.- [ ] `new-ticket` — **A `draft` book-nomination renders to NOBODY, including the member who just created it, so it can never be submitted.** Found by the Book Club B25 walkthrough 2026-09-08 (manifest `87d7b706`) and **verified independently by me in the shipped package.** **Mechanism:** the `draft` binding is `"audience": "actor"`; `deriveInstanceRoles` resolves the actor from the first `actorEqualsField` guard it finds, which is `revise-nomination`'s `{"key": "nominatorFanId"}`; and `nominatorFanId` is written by the **`submit-nomination` effect** — `{"op": "set", "key": "nominatorFanId", "value": "$actor"}` on the transition `from: ["draft"] → "submitted"`. So the field is populated **on the way out of `draft`** and is null while in it: nobody earns `actor`, and the card matches no one. The `createdByFanId` fallback that would rescue it is skipped **precisely because** such a guard exists. **The instance is real and persisted** (`..._il5zplsant0a`, `created_by_fan_id = fan-book-member-2`, verified in Postgres) — it is simply invisible, which is worse than a failed create because nothing signals it. **The walkthrough isolated it with a positive control rather than reasoning from JSON:** a `book-discussion-message` created moments later by the same fan in the same community rendered immediately with its state chip and all five transition buttons, and again after a restart, with `LOOM_BINDING … mode=remote outcome=ok status=200` throughout. So card rendering, role resolution and the remote engine all work; this is specific to the binding shape. **Scale — candidates, not confirmed defects:** 19 `audience: "actor"` bindings exist across 5 communities (Book Club 9, Camera Club 4, Member Social Space 3, Garden 2, Masjid Nur 1; control: all ten declare `audience: "any"` somewhere). A binding is only broken where the actor-resolving field is null in that state, so this set needs checking rather than assuming. **Three siblings named by the walkthrough and NOT verified live:** `book-vote-response`, `book-shared-library-item`, `book-search-ai-digest`. **Fix direction needs a decision, not a guess:** either the actor derivation should fall back to `createdByFanId` when the guard field is null, or the initial-state binding should not use `audience: "actor"`, or the creating transition should set the actor field. That is app-shell derivation logic versus package authoring, and picking wrong moves the bug rather than fixing it — scope it before dispatching.
- [ ] `needs-spec-decision` — **A local payment-service stub cannot be built as asked without first specifying a service-backed transition binding. Scoped 2026-09-08; stopping here per the standing rule that a grammar change larger than minor correctness stops and asks.** The request: build `localpaymentservice.dart` with a documented OpenAPI stub, use it to drive `soccer-registration-payment` to `paid`, and keep it as the pattern for future payment-service-completed transitions. **Why it stops:** `platformSource` can identify a value-producing mechanism for an output field, but it cannot express which transition initiates a payment, which fields supply amount/currency/obligation, which operation is requested, when the service runs, how failure prevents the state change and its effects, or how a retry binds to the original attempt. Verified absent: `workflow_models.dart:449` (`LoomWorkflowTransition.jsonKeys`) has no service binding; `effects.md:19` defines a closed effect vocabulary with no payment invocation; `paymentCheckout` has empty bookkeeping at `archetype_resolver.dart:284`; `permissions.md:95` requires `action` absent on generic archetypes; and `workflow_validator.dart:1659` rejects an invented `platformSource`. **This fills the existing "Payment processing" row in `platform-services.md`'s closed set of six — it does not need a seventh service** — but the binding needs a normative design before any package can use it. **Three findings that block or reshape the work, each verified by me in source:** (1) **"Grant no role" does NOT mean platform-only** — `guard_evaluator.dart:29` skips the role check when `allowedRoleIds` is null **or empty**, so removing the coach guard would open the transition to everyone, not reserve it for the platform (this killed my own `deliver_reminder`-shaped hypothesis). (2) **Soccer has no money to charge** — the package carries `amountLabel` (3) and `registrationFeeLabel` (5) and **zero** `amountMinor`/`currency`; a display label is not a payment amount and the service must not parse prose for money, so the Skill must introduce structured amount/currency with a truthful upstream source first. (3) **A receipt-privacy defect already exists** — the payment declares the coach as a party, `_isVisibleToFan` grants archetype visibility before the read guard, and `receiptDisplay`'s masking does not redact the stored `receiptId`. **The design that was recommended once the binding exists:** authorized initiation → typed service result → atomic completion, preserving execution provenance. The guardian initiates; the service returns `executionMode = local-stub`, `settlementStatus = not-settled` and a simulation-prefixed confirmation id; the executor commits `paid` and its effects **only** on success. Cards must visibly read "Simulated payment — no money moved", and the marker must survive the `paymentCheckpoint = "paid"` copy into the registration case (`:568`), exports, history and refunds — otherwise a demo result becomes indistinguishable from money received, which is the export-checksum failure again. `record-offline-payment` should be retired as the completion path rather than re-guarded: it models a person attesting an offline payment, which is a different operation from processing one.
- [x] `needs-user-decision` — **CLOSED 2026-09-10 — CONFIRMED ON A REAL COMMUNITY, not just the probe.** Re-installed Cedar Commons HOA for real (user-authorized) to backfill its missing `calendar.*` grants. `removedRoleIds: []`, `cedar-commons-hoa-admin` still holds exactly 5 after, `hoa-member`/`hoa-board` grew from 20/34 to 20/35 permissions with `event_rsvp.*` correctly replaced by `calendar.*` for `hoa-facility-reservation`, and all 5 memberships survived untouched. The rule the probe predicted held on live production data, not just a throwaway handle. **The "install destroys the community admin roles" blocker may have been retired by today's permission-catalog work — worth re-testing before it is treated as permanent.** Found 2026-09-08 while looking for a safe way to provision Masjid Nur's missing `owner` role. **What I read (code, not assumption):** `AppAccessService.installCommunityPackage` deletes a group-scoped role only when it is neither `PLATFORM_ADMIN_ROLE_ID`, nor the derived `systemAdminRole`, nor declared in the package. `CommunityPermissionDeriver.resolveSystemAdminRole()` builds that role by substituting the community handle into the vocabulary's `adminRole.idTemplate`, which is **`"<communityHandle>-admin"`** — so `masjid-nur-admin` **is** the systemAdminRole and is spared. Install additionally **`setRolePermissions` on it from the template**, whose `grantedPermissions` are exactly the five `community.*` ids. **Why this changes the picture:** the 2026-09-01 warning ("would destroy all 11 community admin roles, unrecoverably, since their `community.*` grants are not archetype-derived") was true when written. Part A made those grants derivable — `governanceActions` in `ArchetypeResolver` → the vocabulary's `adminRole` block → the live catalog, deployed today as `0.3.10`. The premise the warning rests on no longer holds. **What I deliberately did NOT do:** run install to find out. A code read is exactly the evidence CLAUDE.md says is insufficient for this ("a dry run shows what the client sends, not what the server does with it"), and being wrong destroys 11 admin roles unrecoverably. **The cheap way to settle it:** install into a throwaway community handle with a seeded admin role and check that the admin survives with its five grants intact — no live community touched, and it converts a code read into evidence. If it holds, install becomes the correct way to provision declared-but-missing roles (Masjid's `owner`) **with properly derived permissions**, instead of hand-picking grants, and the long-standing role-deletion decision can close. **SETTLED 2026-09-09 BY A LIVE THROWAWAY PROBE — the decision is closed, and the answer is "half safe", which is why a code read was never enough.** Ran the experiment this row proposed, against a disposable handle (`zz-install-probe`) so no live community was touched. **Phase 1** — installed a minimal package (one role `zz-member`, empty `workflows`): the group was created and `zz-install-probe-admin` was generated **with exactly the 5 `community.*` permissions**, as `resolveSystemAdminRole` predicted. **Phase 2** — created an UNDECLARED group-scoped role `zz-canary` holding `community.view`, as the canary. **Phase 3** — re-installed the *same* package, which does not declare `zz-canary`. **Result, from the install's own response body: `{"rolesRegistered":["zz-member"],"removedRoleIds":["zz-canary"]}`, and a follow-up query confirmed `zz-install-probe-admin` still holds exactly 5.** **So both halves are now evidence rather than inference:** (a) **the generated admin IS spared and re-granted** — this retires the 2026-09-01 warning that an apply "would destroy all 11 community admin roles, unrecoverably", which was true when written and is not true now that Part A made those grants derivable; and (b) **the sweep still deletes every group-scoped role the package does not declare**, holders or not, bypassing `deleteRole`'s protections. **The practical rule: `installCommunityPackage` is safe exactly when the package declares every role that ought to exist in that group, and destructive otherwise.** Masjid's `owner` is declared, so a future install would keep it; the risk was never the admin, it was any hand-provisioned role a package has forgotten. **Control: all 11 live community admin roles were re-queried afterwards and are untouched.** **Cleanup done and verified** — both probe roles deleted (204 each), 0 roles remain in the probe group, and `check_role_parity.sh` re-run clean at 38 live rows, so this experiment left no drift behind. The empty group itself remains because App Access exposes no `deleteGroup` operation; it holds no roles and no members, and the parity gate compares roles, so it is inert. **Caveat on scope:** the probe used an empty `workflows` array, so it proves the role-sparing and role-deletion behaviour, not permission derivation from a real package's workflows. That was the blocker's actual question.
- [x] `new-ticket` — **NOT A DEFECT, checked 2026-09-08 before filing one.** A cross-layer sweep found `portability-member` is the only community role in the live `role_permission` table with **zero** permission grants (control: every other role has 3+, e.g. `ad-off-member` 3). That looks exactly like a provisioning gap, and it is not. Reading the package: all six mentions of `portability-member` are its `roles[]` declaration plus **five `readGuard`s** — it appears in **zero transition guards**, so it can read and never act, and zero derived permissions is therefore the **correct** derivation rather than a missing grant. **Recorded so the next sweep does not re-file it.** The real question underneath is product-level, not provisioning: Data Portability's member can perform no action at all, which the 2026-09-08 walkthrough independently hit from the other side — all ten transitions are `portability-owner`-guarded and the create FAB sits on the owner-only `admin` tab, so the nominated member could not drive the workflow and the run legitimately escalated to the owner. Whether a portability *member* should be able to do anything is a product decision for that community's doc, and it belongs with the owner-vocabulary row rather than here.
- [x] `new-ticket` — **CLOSED 2026-09-10 — re-verified live, not just recalled: `group_membership_role` for `loom_communities_member-social-space` shows `member: 2` holders (also `moderator: 2`).** Already resolved by seeding per its own text (2026-09-08); this tick confirmed the state still holds. ORIGINAL: **Member Social Space can never reach `connected`: only one fan holds the `member` role, and all three invite transitions require it.** Found by the final B25 walkthrough, 2026-09-08 (manifest `89933ca6`), and **confirmed by me in `group_membership_role`**: the community provisions exactly `member → fan-social-member-1`, `moderator → fan-social-moderator-1`, and the generated `member-social-space-admin` — so **exactly one `member` exists**, while the deployed definition guards `send-invite`, `accept-invite` *and* `decline-invite` with `allowedRoleIds: ["member"]`. There is therefore no pair in which both parties are members, and an invitee can neither accept nor decline. The walkthrough confirmed this live rather than reasoning from JSON: as the invitee the card rendered **zero action buttons**, against a control where the inviter's card in the same `approvalQueueItem` surface rendered two. **The definition is coherent; the provisioning is short** — the fix is a second `member` fan, not a grammar change. **This is the fourth community whose intended outcome no account can reach** (after Masjid Nur's unprovisioned `owner`, Youth Soccer's coach-guarded-but-guardian-visible payment, and Garden Club's borrower-stranded owner) — and the second caused purely by *who was provisioned* rather than by what was authored. **RESOLVED 2026-09-08 — seeded. Every package-declared domain role in every live community now has at least two holders (24 of 37 role rows, up from 1), verified by my own query. The 13 still single are all generated `*-admin` governance roles plus the underscored duplicate `cedar_commons_hoa` group, deliberately skipped pending its deletion decision. Admin roles left single on purpose: a second admin changes governance rather than enabling an interaction. Seeded through the real authorization flow (`Tools/code/seed_role_holder.sh`) — fan requests, admin approves and grants — never a database insert. ORIGINAL MEASUREMENT:** of 37 role assignments in `group_membership_role`, **36 are held by exactly one fan**. The only exception is `cedar-commons-hoa:hoa-board`, with 2. So **any workflow requiring two distinct holders of the same role is unreachable in nine of ten communities** — Member Social Space's `connected` is simply the instance a walkthrough happened to drive. The same sweep found that `masjid-nur :: owner` is the **only** declared role with zero holders anywhere, so that gap is genuinely a one-off while this one is the corpus-wide default. **RESOLVED BY STANDING POLICY 2026-09-08:** the user's locked decision is to seed SEVERAL backend users per role for every community, so this is a seeding task, not a decision — and it clears the whole class at once, not just this row. Do NOT edit any package to route around a single-holder shortage.
- [x] `new-ticket` — **FIXED 2026-09-08: re-published, and a parity gate now exists so it cannot recur silently.** All three types are now in the deployed catalog (85 definitions, up from 82), verified by my own query with a control, and `Tools/code/check_published_definitions.sh` reports `declared-but-unpublished: 0`. The gate was proven in BOTH directions before being trusted: clean → exit 0; a scratch copy with one renamed `workflowType` → names it and exits 1. ORIGINAL: **Three workflow types are declared in their packages but absent from the deployed catalog, so every `createInstance` effect naming them fires silently and does nothing.** Surfaced one community at a time by the B25 walkthroughs (Ad-Free, then Data Portability), then **generalised by me across all ten communities with a control**: comparing every package's declared `workflowType`s against the 82 rows in `workflow_definitions`, exactly three are missing — **`ad-off-notification`** (Ad-Free), **`export-notification`** (Data Portability), and **`hoa-document-access-request`** (Cedar Commons HOA). Control: `hoa-dues-payment` is present, so the comparison works. **Note the third one breaks the obvious hypothesis** — it is not a notification workflow, so this is "some definitions were never published", not "notifications are handled differently"; I checked rather than assumed. **Severity differs sharply between them.** Ad-Free's case is two orphaned effects. **Data Portability's is the whole mechanism**: all **7** `createInstance` effects across **6** workflows target `export-notification`, and none of them can ever fire — the community has no working notification path at all. Confirmed live by the walkthrough: `complete-full-bundle` succeeded, the instance total stayed at 18, and nothing errored (control: 13 other published definitions do contain `createInstance`). Cedar's `hoa-document-access-request` needs its own look, since a document-access request is a member-facing capability rather than a background effect. **This is exactly CLAUDE.md's "publishing definitions is not a one-time step" hazard, and it is silent in every direction** — no test fails, no route errors, no probe changes colour, and the effect returns success. Fix: re-publish, then confirm by querying the stored definitions for each type **with a control**. Worth adding a declared-vs-published parity check alongside the spec-twin and vocabulary-twin checks, since all three are the same failure and only this one currently has no gate.
- [ ] `new-ticket` — **Raw camelCase field keys are reaching users as labels when no `labelTemplate` is declared.** Two instances found by live walkthroughs, 2026-09-08: `memberNotice` renders literally as "memberNotice" on Data Portability's Home card, and Camera Club's submitted tile renders `photoImage` instead of its value. `_isVisibleField` falls back to the field key as its own label template. Decide the intended fallback — humanise the key, omit the label, or require `labelTemplate` and fail validation without it — before editing, since `labelTemplate` works correctly elsewhere and a careless change risks regressing it (same caution as the `{value}` edit-label row). A validator rule would catch the whole class — and that is the recommendation, for a reason I proved on myself below. **Scale, measured 2026-09-08: at least 38 displayed fields across 5 communities declare no `labelTemplate`** — YouthSoccer 13, MemberSocialSpace 11, DataPortability 9, Cedar 4, BookClub 1 — while AdFree, CameraClub, ChessClub, GardenClub and Mosque have **zero**, which is the control showing that declaring one is the established norm. **That 38 is a floor, not a total, and my method is why:** I counted with a regex that only matches single-line field declarations, and it missed `photoImage` itself, which is declared across multiple lines — one of the two cases that started this ticket. So an ad-hoc grep demonstrably under-reports this class, which is precisely the argument for a mechanical rule rather than a sweep. **Renderer inconsistency worth deciding on first:** `part18_marketplace_rendering.dart:514,629` and `part26_generic_instance_card.dart:394,426` fall back to the raw key (`labelTemplate ?? key`), but `part28_engine_native_calendar_surface.dart:2475` falls back to an **empty string** (`?? `). So the codebase already contains both behaviours, and the calendar surface is the one that does not leak a key — an in-repo precedent, not a hypothetical.
- [x] `new-ticket` — **CLOSED 2026-09-10 — re-verified live: `ad-off-notification` is now present in `workflow_definitions`** alongside Ad-Free's other 6 types (queried `workflow_type ilike '%ad-off%'`, all 7 present). Landed via a later re-publish during other work this session, not a dedicated dispatch for this row. ORIGINAL: **`ad-off-notification` is declared in the Ad-Free package but was never published, so two `createInstance` effects can never fire.** Found during the Ad-Free B25 re-verification, 2026-09-08 (manifest `d4489091`), and **confirmed by me directly against `workflow_definitions` with a control**: Ad-Free has **6** published definitions while its package declares **7**, and the missing one is `ad-off-notification`; the control — five other communities *do* publish their notification workflows (`book-notification`, `garden-notification`, `hoa-owner-notification`, `mosque-neutral-notification`, `soccer-reminder-notification`) — confirms the query works and the absence is real (82 definitions published in total). **Consequence:** two effects target it, including `record-payment-failed` on `ad-off-member-checkout`, so a failed payment can never notify anyone. **This is exactly the hazard CLAUDE.md names — "publishing definitions is not a one-time step":** the backend holds a *copy*, nothing fails when it goes stale, no test goes red, no probe changes colour. Fix is a re-publish, then confirm by querying the stored definitions for the type with a control. Worth also asking why one type in one community was skipped, since the publisher is an upsert and the other six landed.
- [ ] `new-ticket` — **`paymentConfirmationId` is an orphan field in the Ad-Free package: nothing anywhere writes it.** Found 2026-09-08 in the same run, with a control (the same query finds 8 writers for `submittedAt`). It is `hideWhenEmpty`, which is why it never looked wrong on screen — the UI simply omits it. **This is the export-checksum shape the autonomous-mode rules open with: "a field that claims a writer it never had", which stayed invisible for weeks precisely because an empty field reads as "not yet set" rather than "unwritable".** **Independently confirmed by me 2026-09-08 with a working control:** the field declares no `writableBy` at all (only `type`, `displayIcon`, `labelTemplate`, `hideWhenEmpty`, `displayContexts`), and enumerating every effect key in the package yields **36 distinct written keys, none of them `paymentConfirmationId`** — the 36 are the control proving the query finds real writers. So it is unwritable by any path, not merely unwritten. **Note on method, because my first attempt was wrong:** my initial control returned 0 for a field I expected to be written, which meant my query was broken rather than the field being orphaned — I had chosen a control field from a different community. Enumerating the package's own keys is what settled it. Decide whether the field should be written (and by which effect) or removed; do not leave it declared and unwritten. Skill-side change. **This is almost certainly already queued work, not a new finding.** The CJM tracker's outstanding-Skill-dispatches row (`needs-skill-dispatch`, 2026-08-31) lists "AdFree (4 orphan)" as never dispatched — so `paymentConfirmationId` is likely one of those four, and three more probably exist in the same package. **I tried twice to enumerate them and failed both times, each caught by a control** (a block-matching awk and a single-line regex both returned counts that were obviously too low against a control that should have matched dozens). Rather than publish a number I cannot stand behind, the count stays unverified: dispatch the queued AdFree orphan pass and let the Skill find them. **Same recommendation as the `labelTemplate` row above: a validator rule catches this whole class**, and both are the same shape — something declared in a package that nothing can ever satisfy.
- [ ] `new-ticket` — **A Garden Club owner cannot withdraw their own property once it is `onLoan`: a silent borrower strands the listing permanently.** Found during the Garden Club B25 re-verification, 2026-09-08 (manifest `78d10488`), and **confirmed by me directly in the shipped package**: `delist` and `pause-listing` are both `actorEqualsField: ownerFanId` **and** require `availabilityState == "available"`, while `return-item`, `mark-damaged` and `report-lost` — the transitions that could clear `onLoan` — are all `actorEqualsField: borrowerFanId`. So while an item is on loan the owner's only exits are precondition-blocked and every state-clearing action belongs to the borrower. The owner retains only `report-issue`, which changes no state, and there is **no coordinator or admin override**. **Every guard is individually well-formed, which is precisely why no validator run can see this** — it is a property of the guard *graph*, not of any one declaration. This is the inverse of the hazard `keypatterns.md` already records ("a destructive transition's guard must come from the specific obligation it ends… owner-only delist can strand an active borrower"): here the borrower-protecting guards strand the *owner*. Needs a decision on the escape hatch — a coordinator override, an owner-initiated recall from `onLoan`, or an overdue-based path — before any edit; and it belongs to the Skill, not application code.
- [x] `needs-verification` — **CLOSED 2026-09-10 — ALREADY FIXED in `70c1f5fa`, ~17 hours after the walkthrough that reported it. Not an authoring slip; an app-shell guard bypass. No package change needed, and none should be made.** Root cause agent (session key `garden-queue-affordance`, fresh) traced it; every load-bearing claim re-verified by me against git and the test suite, not taken from its report. **The mechanism:** `_queueToggle` selects the queue action through `_declaredActionFor`, which at `70c1f5fa^` matched only `candidate.action` and `candidate.from.contains(currentState)` — **no `evaluateGuard` call at all**, so owner identity and availability were never consulted. `request-loan` was correctly hidden because it comes through `_actions`, the engine-filtered list — the same asymmetry the row noticed, now explained. The tap then POSTed to the item-queue endpoint and a **catch-all cleared `_queue` and set `_queueUnavailable` for any exception including an authorization refusal**, which is the "relabels itself to Queue unavailable with no error" symptom (the buttons disappeared and the status line changed; no button was relabelled). `70c1f5fa` (2026-09-08 22:02:06 -0700, *"evaluate the queue transition guard, and stop rendering a refusal as an outage"*) added exactly that `evaluateGuard(candidate.guard, widget.fanId, _instance.instanceData, roleId: widget.roleId)` check plus 403-vs-outage separation, with 159 lines of regression tests. **Timing confirmed from the evidence manifest:** APK installed 2026-09-08 00:17:13, listing exercised 05:15 local — the observation predates the fix. **Verified by my own run, not the commit message:** `flutter test test/item_queue_integration_test.dart` → **10/10 passed**, including *"an owner of an available Garden-shaped tool loan is not offered Join queue"*, the non-owner reserved/on-loan positive cases, and *"a 403 queue refusal keeps the queue state and is not rendered as unavailable"*. **Two corrections from the dispatch worth keeping.** (1) **The `leave-queue` permissive guard is NOT implicated** — I had flagged it as a suspect; at queue position zero the renderer never selects it, and adding join-style restrictions to it could trap existing membership, so it is not an authoring error. (2) **Unchanged `instance_data` did not prove the service refused the join** — queue membership lives in a separate table and the walkthrough never captured the HTTP response, so that piece of the original evidence proved less than it appeared to. **Residue:** re-run the owner/available check on device against a build containing `70c1f5fa`. That is walkthrough work and belongs with the B25 backlog row, which **already recorded this same fix's consequence** ("today's build carries app-shell fixes that changed on-device behaviour for at least one row — the owner is no longer offered 'Join queue'") while this row stayed open describing the defect — the recurring shape where a correction lands in one row and the original is never struck. ORIGINAL: **Garden Club's "Join queue" renders for the owner on an available item although its guard denies it twice over, and fails silently.** Found 2026-09-08 in the same run: the engine correctly refused the write (`instance_data` unchanged, so no data harm), but the control is offered, then relabels itself to "Queue unavailable" with **no error** — an affordance that invites an action it will never permit. `request-loan` carries the same formula shape and *is* correctly hidden, so the correct behaviour already exists next to it in the same package; this reads as an authoring slip rather than a renderer bug, but confirm which before editing. UI-only, no data integrity impact.
 **CORRECTION 2026-09-09 — the Skill did NOT author this wrong, and a future session must not "fix" the package.** I described this in conversation as "the guardian has no pay action". **That is false**, and it is a careless compression of this row's actual (correct) claim, which is narrower: *the only transition **into `paid`** is coach-guarded*. Read from the shipped package: the guardian holds **`start-checkout` ("Pay registration", `unpaid → processing`)**, **`retry-payment` (`failed → processing`)**, **`view-receipt`**, `confirm-payment-authorization` and `cancel-checkout` — all guarded `soccer-guardian` + `actorEqualsField: guardianFanId`. The product doc's payment row names exactly three actions, **"pay, retry, view receipt"**, and **all three are present and guardian-held**. The package is faithful to its doc. **Why the validator is right to pass it, which is the question worth recording:** nothing is structurally wrong. Every state is reachable including `paid`; `processing` is not a dead end (it exits via `record-offline-payment` → `paid`, `mark-payment-failed` → `failed`, and `cancel-checkout` → `unpaid`); every guard is well-formed; every declared role exists. **The workflow is modelled correctly for a world in which a payment service exists** — the guardian initiates and the service completes `processing → paid`. The missing actor is the service, and **the grammar cannot declare a service-completed transition at all**, so there is no absent declaration for any rule to detect. A validator cannot flag the absence of something the grammar has no way to express. This is the class already named in CLAUDE.md: every individual guard is well-formed and the defect lives in the graph, so no per-declaration check can find it. **The real fault is that `record-offline-payment` — a human attesting an offline payment, the intended fallback — silently became the PRIMARY completion path** because the service it was a fallback for was never built. That is why the recorded fix is to retire it as the completion path rather than re-guard it, and why the genuinely open item is the service-binding spec decision, not a tab or guard change.- [ ] `new-ticket` — **Youth Soccer's registration fee can never be marked `paid` by anyone: the only transition to `paid` is coach-guarded, and the only tab that renders the payment excludes coaches.** Found during the Youth Soccer B25 re-verification, 2026-09-08 (manifest `5c45baf2`), and **confirmed by me in the shipped package**: `record-offline-payment` declares `"to": "paid"` with `"guard": {"allowedRoleIds": ["soccer-coach"]}`, while the `giving` ("Payments") tab — the payment's only primary render binding — declares `"visibleRoleIds": ["soccer-guardian"]`, and the `home` summary binding covers only `paid`/`refunded`, i.e. states that transition alone can produce. **The tab immediately above `giving` declares `["soccer-guardian", "soccer-coach"]`, so including the coach is the established authoring pattern here and `giving` simply omits it** — which is what makes this look like an authoring slip rather than a deliberate access decision. The walkthrough escalated to the seeded `loom-soccer-coach-1` (named, and correctly *not* the generated admin role) and confirmed on-device across the coach's full tab bar, Home, and "Coach & Owner" that **no coach-visible surface renders the instance**. Consequences: no registration fee can ever be verified, `receiptStatus` is stuck at `not-issued`, `refund-payment` and `view-receipt` are dead transitively, and the coach's card shows a permanently stale "Payment: Unpaid" checkpoint. **The guard is arguably correct; the binding is wrong** — a package authoring gap, no application code implied, so the fix goes through the Skill. **CORRECTION 2026-09-08 (later, from the payment scoping dispatch): my privacy analysis below was incomplete.** Coaches **already** have read access to this instance — the payment declares `"fields": {"parties": ["guardianFanId", {"role": "soccer-coach"}]}`, and `_isVisibleToFan` grants archetype visibility before consulting the read guard, while the masked `receiptDisplay` formula does not redact the stored `receiptId`. So the `giving` tab was hiding the UI, not protecting the data, and a receipt-confidentiality defect exists **today** independent of which fix is chosen. Do not treat "leave the tab alone" as privacy-preserving. ORIGINAL (still true as far as it goes): **the obvious one-key fix is NOT safe, checked 2026-09-08 before dispatching it:** adding `soccer-coach` to the `giving` tab's `visibleRoleIds` would expose that whole tab to coaches, and the tab's own description is "Registration checkout, payment status, and **private receipt access**" — so it would hand every coach every guardian's private receipts. That is a worse defect than the one being fixed, and this is a **youth community with minors and explicit redaction workflows**, where over-exposure is the more serious failure. A better candidate exists in the same package: the **"Coach & Owner"** (`admin`) tab, which coaches already see legitimately — surface the payment there with only the fields a coach needs to verify it, rather than widening a guardian-private surface. **Decide between those two before authoring**, since the naive version passes every validator and silently leaks. **Pattern worth noting across the campaign:** this is the *second consecutive community* whose payment/completion flow no account can finish, after Masjid Nur's unprovisioned `owner` role — different causes, same shape, and **neither is visible in a validator run** because both are about who can *see* or *hold* a role rather than what is declared. Worth a dedicated check: for every transition into a terminal state, is there a surface visible to the guarded role that renders the instance?
- [x] **CLOSED 2026-09-08 — role-provisioning drift now has a gate.** `Tools/code/check_role_parity.sh` (mirrored to `data/`) diffs the roles every shipped package DECLARES — derived through the client-side plan deriver, plus the generated `<handle>-admin` — against the roles actually provisioned in `app_role`, and exits 1 on drift in **either** direction. This closes the gap this campaign kept hitting: a `roleId` rename is a migration, not an edit, and install is not idempotent across one (the sweep only deletes roles the package no longer declares, so the stale role survives holding its permissions and keeps working while the new name has no row — nothing fails, nothing goes red). **Current state: `OK` — 33 declared rows, 38 live, and "declared but not provisioned" is now EMPTY** after the Masjid `owner` repair, so every package-declared role in every community is provisioned. The 5 live-only rows are allowlisted with reasons, not silently ignored: the underscored `cedar_commons_hoa` duplicate group (pending its deletion decision) and `tabletop-club` (a test fixture, never shipped). **Proven to fail, not just to pass** — a gate whose output never changes is not a gate: with the allowlist disabled it reports exactly those 5 rows and exits 1, and with a deliberately bogus control row it refuses to report at all ("this query proves nothing about the rest") and exits 1. The first attempt at that proof was itself invalid — the variants were run from `/tmp`, which broke `REPO_ROOT`, so both exited 1 for the wrong reason and the exit codes looked right; re-run from inside the repo they fail for the intended reasons. The `missing` branch was not independently exercised in the script, though it is the same `comm`/report path and the equivalent ad-hoc query is what found `masjid-nur :: owner` this morning.
- [ ] `needs-user-decision` — **APP ACCESS POLICY ENDPOINTS HAVE NO CALLER AUTHORIZATION — found 2026-09-10 by the root cause agent (session `b25-provisioning`) while tracing the Masjid over-grant, verified by me against the code and the cluster.** `JwtSecurityConfiguration` is `anyRequest().authenticated()` and nothing more; `AppAccessController`'s `createRole`, `setRolePermissions` and `installCommunityPackage` call straight into the service with no actor check, while the membership endpoints (`setGroupMembership:668`, `issueInvite:710`, `decideGroupMembership:920`, `removeMember:1000`) all call `requireGroupAdministrator`. `app-access` is a **NodePort (30080)** reachable from the host network. **So any holder of any valid Keycloak token for the issuer — every seeded fan — can rewrite any role's permission set or install a package into any community.** This is the literal "a normal user gets admin permissions" vector and it is live, independent of the Masjid history. The agent also noted `requireGroupAdministrator` keys on `community.manage_members`, so an unchecked `setRolePermissions` can mint the very permission the membership checks trust, and that authorization must run before idempotency replay (`IdempotencyService:59` can return a stored response without re-running a check placed only inside the mutation). **Not exploited as far as any record shows** (all 230 `idempotency_record` rows are from operator sessions), but nothing prevents it. **Decision needed:** (a) gate the three policy endpoints on a trusted provisioning principal (a Keycloak client role / service account, not a fan), (b) additionally take app-access off NodePort so only cluster-internal callers reach it, or (c) both. Ticket should also cover `deleteRole` and any catalog-mutation route. Recorded in `keypatterns.md` 2026-09-10 and CLAUDE.md.
- [ ] `needs-user-decision` — **ROOT-CAUSED 2026-09-10 (user asked "is it the JSON? if not, why, and is there an architectural weakness") — NOT the JSON; two operator steps the platform accepted because it has no notion of role KIND.** Reconstructed from `idempotency_record` (window continuous 2026-08-13→09-10, 230 rows), `app_role.created_at`, and the Access Control tracker's own record (lines ~1815-1836, 1896-1911, 2149-2176): **(1) 2026-08-31 02:32** an earlier session needed a governance admin per community, saw the package DOMAIN role `masjid-admin` (label "Masjid Admin", 23 archetype perms) and classified it as the governance role *by its name* — `PUT /roles/masjid-admin/permissions` with 23 + 5 (API record, 200). That is the moment a domain role acquired `community.*`. **(2) 2026-09-02 09:33:34-35** the documented SQL copy-and-delete recipe renamed all eleven hand-named admins to `<handle>-admin` in one second with zero API records; its mapping said `masjid-admin → masjid-nur-admin (preserve all 28 grants)`, copying the domain role's label, grants and holder into the governance id and deleting the domain role. The plan's own anomaly note flagged the 23 extras "for user decision" and it was never resolved. **(3) 2026-09-05** package regenerated with `masjid-admin → owner`; nothing to re-provision. **Correction to my earlier framing, from the agent and verified in `guard_evaluator.dart:29-37`:** the 28-grant admin does NOT pass the four `byRoleIds: ["owner"]` transition guards — guards intersect real role ids, never permissions — so "every Masjid admin can fire the owner transitions" was wrong. The violation is at the App Access layer (`checkAccess` returns domain allowances for the admin, e.g. it can pass the workflow-service `create` permission gate for domain families), not at the engine guards. **Blast radius measured live:** every other `<handle>-admin` holds exactly 5; no non-admin role in any live group holds any `community.*` (only hit: `cedar_commons_hoa_admin` with 1 in the orphaned underscored group). **Architectural weaknesses, ranked by the agent and confirmed by me:** W0 the missing caller authorization (own row above); W1 no persisted role kind — `app_role` cannot tell a package domain role from the generated governance role, and `installCommunityPackage:498-575` silently reuses a package role whose id equals `<handle>-admin`, overwriting its domain grants with the 5 governance ids and thereby elevating its existing holders (latent, verified by reading the code); W2 `permissions.md:522` states "only the admin role holds `community.*`" and nothing enforces it — `createRole`/`setRolePermissions` only check `ensurePermissionsExist`; `check_role_parity.sh` compares role existence only and passes today; W3 direct SQL against the auth DB is an accepted, unaudited operational path (the tracker documents the recipe); W5 a package role rename is a migration nothing propagates. The agent rejected a blanket ban on `-admin`/`_admin` ids or "Admin" labels (would reject legitimate domain terminology; security must not depend on avoiding a word) and said `setRolePermissions`' replace semantics are fine — the missing piece is validation, not merge. It also found a reference-doc conflict (`identity-types.md:63-78` ties community setup/admission to the package's reserved `owner`; `permissions.md:510` assigns governance to the generated admin) that contributed to the 09-05 rename. **Proposed fix set for your call:** (A) persist `role_kind` (`package_domain` / `community_system_admin`) set only by the service, with rules in `createRole`/`setRolePermissions`/install — domain roles may hold no governance ids, the system admin must equal the group's resolved id and hold exactly the governance set; reject a package role id equal to the resolved admin id before any write; DB uniqueness of one system admin per group; backfill kinds from package declarations, not from current (contaminated) grants; (B) a permission-SET parity gate (extend `check_role_parity.sh` or a sibling): exact ids, not counts, expected sets exported from the Java deriver rather than re-implemented, with a positive control and a negative test that makes it exit 1; (C) retire the SQL recipe: renames go through `createRole` + assignment moves + `deleteRole`; domain→governance is never a rename; add an actor-attributed audit event for policy writes; (D) a validator error for a package `roleId` matching the governance id template, mirroring the backend rejection; (E) reconcile `identity-types.md` vs `permissions.md` on ownership vs governance (my edit, reference docs). **Live repair, unchanged and still awaiting your go-ahead:** `setRolePermissions("masjid-nur-admin", [the 5 community.* ids])`, leave `owner` 23 and `community-member` 21 as they are, do NOT reinstall; and the agent recommends `fan-masjid-admin` ALSO be granted `owner` to restore the domain assignment it held before the 09-02 conversion (it held `masjid-admin` from 2026-08-31) — via `setGroupMembership`, which REPLACES the role set, so pass both ids. Verification after repair: the governance-only identity fails an owner-only transition; an `owner` identity passes. B25 note: the 09-03 walkthrough used `masjid-admin-20`, not `fan-masjid-admin`, and the 09-08 manifest already reports the owner transitions unreachable for the admin — no evidence of unauthorized owner actions, and no proof of their absence either. ORIGINAL ROW FOLLOWS. `new-ticket` — **Masjid Nur's `owner` role was never provisioned, so its donation flow can never reach `paid` — a live production gap, not a test-data gap.** Found during the Masjid B25 re-verification, 2026-09-08 (manifest `54d92d9e`), then **verified by me directly against `app_role` with a control**: `loom_communities_masjid-nur` holds exactly two roles, `community-member` and the generated `masjid-nur-admin`, while every peer community has its package role provisioned (`chess-owner`, `soccer-owner`, `portability-owner`, `ad-off-owner`) — so the query works and the absence is real. The package's *other* role, `community-member`, was provisioned verbatim; **only `owner` is missing**, which points at provisioning rather than the package. **Consequence, confirmed in the shipped JSON:** four transitions are gated by `"byRoleIds": ["owner"]` alone (two more share it with `community-member`), and **no existing account can reach any of them**. So a Masjid Nur donation can never be recorded as `paid`, no receipt can be issued, `mosque-donor-visibility`'s only writer (`confirm-offline-donation`'s `createInstance`) can never fire, and the `paid`-only home-tab binding can never render. `cancelled` is the only reachable terminal state, and `pending` is the furthest any real account can take the workflow. **The walkthrough correctly refused to substitute `masjid-nur-admin`** — that is generated community governance, not the package's domain `owner` role; conflating them is exactly the distinction `keypatterns.md` records. **I answered the obvious follow-up rather than leaving it open: I diffed every community package's declared `roleId`s against the live `app_role` table, and `masjid-nur :: owner` is the ONLY missing declared role across all 10 communities** (control: `chess-club :: chess-owner` present, so the comparison works). So this is a single isolated provisioning miss, not a systemic one. **RESOLVED BY STANDING POLICY 2026-09-08: seed several backend users holding `owner` for `loom_communities_masjid-nur`.** This is a seeding task, not a decision, and NOT a reason to re-guard the four transitions or to substitute `masjid-nur-admin` (generated platform governance, a different role from the package's domain `owner`). Worth noting nothing detects this class of drift automatically — it surfaced only because a walkthrough tried to use the role; a parity check between declared package roles and provisioned `app_role` rows would catch it cheaply. **REFRAMED 2026-09-08 — the permissions did not go missing, they went to the wrong role, and that changes what the fix is.** Provisioning `owner` by hand was about to be a permission-guessing exercise, because the client-side plan deriver emits only `{"roleId": "owner", "label": "Masjid Admin"}` — no permissions at all; the server's `CommunityPermissionDeriver` computes them on receipt. So `createRole` alone would create a permission-less `owner`. Checking what a correct owner role *should* hold surfaced the real anomaly: **every generated `<handle>-admin` role in all ten communities holds exactly 5 permissions — the `community.*` set — except `masjid-nur-admin`, which holds 28.** Verified with a control: `chess-club-admin`'s full list is exactly those 5 and nothing else, so the query is sound and the outlier is real. The 23 extra ids on `masjid-nur-admin` are archetype-derived (`approval_queue_item.*`, `discussion_thread.*`, `document_library.*`, `event_rsvp.*`, `form_entry.*`, `search_ai_answer.*`) — precisely the kind a package DOMAIN role receives, and exactly what the absent `owner` should hold. **Note the package's `owner` role is LABELLED "Masjid Admin"**, which is the obvious candidate mechanism (a collapse keyed on label rather than role id) and is currently a hypothesis, not a finding. **Root cause agent dispatched 2026-09-08** (session key `b25-provisioning`, `gpt-6-astra`/high, read-only) to settle the mechanism from `installCommunityPackage`/`CommunityPermissionDeriver` before any fix is written — specifically to confirm or kill the label hypothesis, to say whether the same code would repeat this for any other community declaring an admin-labelled role, and to derive the INTENDED permission split from the deriver's rules rather than copying the observed damage. **Do not seed `owner` holders until that split is known** — seeding fans onto a permission-less role would satisfy the workflow `byRoleIds` guards while leaving App Access wrong, which is the same cross-layer trap as the fan-passport miss. `community-member`'s 21 permissions are also unverified against the deriver's rules and may be part of the same mixing. **ROOT-CAUSED 2026-09-08 — and it is not a provisioning miss at all. It is an unpropagated package rename plus a direct database write that merged a domain role into the governance role.** The root cause agent (session key `b25-provisioning`, `gpt-6-astra`/high, read-only) **killed my label-collapse hypothesis by reading the code**: `CommunityPermissionDeriver:43` keys the permission map on `role.getRoleId()` and `installCommunityPackage:498` builds `declaredRoles` by role id — labels are display text only and never participate; `resolveSystemAdminRole():343` derives the admin id from the handle template alone; and the installer's final step *replaces* the admin's grants with the vocabulary's five (`setRolePermissions:1129` deletes before inserting). **So the current installer cannot produce the observed 28-permission admin, and attributing it to the installer would have been a false diagnosis.** It also derived the intended sets independently from the shipped package rather than from the observed rows: `owner` 23, `community-member` 21, `masjid-nur-admin` 5. **I verified that independently against live data with my own query: `community-member`'s 21 actual ids are byte-identical to the derived set (diff empty), and the admin's 23 *extra* ids match the derived `owner` set exactly.** So precisely two rows are wrong. **The actual history, from `idempotency_record` (the audit window is continuous 2026-08-13 → 2026-09-09, so absences in it are meaningful):** the 2026-08-26 install **succeeded** — `{"rolesRegistered": ["community-member", "masjid-admin"], "permissionsGranted": 44}`, and 44 = 21 + 23 exactly. At that time the package declared its domain role as **`masjid-admin`**, not `owner`. Then on **2026-09-02** `masjid-nur-admin` was created carrying **`display_name` "Masjid Admin" — the package's domain-role label** — with the old role's 23 domain permissions plus the 5 governance grants, **and there is no API record for that date at all**, so it was a direct database write, not any endpoint. Finally on **2026-09-05**, commit `14d6e1a6` ("regenerate through the Skill — adopt reserved owner role") renamed the package's role `masjid-admin` → `owner`, **and App Access was never re-provisioned afterward**. Hence: no `owner` row, a governance role holding a domain role's permissions, and a display name that still betrays where those permissions came from. **Two defects, not one:** (a) a package role rename that never reached App Access — nothing detects that class of drift, which is the same gap this row already noted; (b) a direct DB write that collapsed a domain role into the generated governance role, which is a live **privilege escalation** — every Masjid community admin currently holds the four `owner`-guarded transitions. Only one account holds it (`fan-masjid-admin`), so the repair is contained. **Repair, deliberately NOT via `installCommunityPackage`** (its sweep at `AppAccessService:565` deletes undeclared group roles by invoking repository deletion at :579, bypassing `deleteRole`'s holder-protection checks): `createRole` for `owner` with the 23 derived permissions, `setRolePermissions` to reduce `masjid-nur-admin` to the five `community.*`, then seed several `owner` holders per the locked policy. **The prior 28-id admin set is recorded in this repo's git history for reversibility.** **REPAIRED 2026-09-08 — two of three steps done, one blocked and needing your call.** (1) **`owner` created via `createRole`** (HTTP 201) in `loom_communities_masjid-nur` with the 23 independently-derived permissions — deliberately NOT via `installCommunityPackage`, whose sweep deletes undeclared group roles by calling repository deletion directly and bypassing `deleteRole`'s holder protections. **Verified with my own query, not the response body:** `owner` holds 23, and a diff of its actual ids against the 23 non-`community.*` ids stranded on `masjid-nur-admin` is **empty — identical**. Three headers were required and only the first is obvious from the spec's prose: `Idempotency-Key`, `X-Loom-Actor`, and `X-Loom-Correlation-Id`, the last of which must be a **real UUID** (it 400s on any other string, with a precise `Expected type class java.util.UUID`). (2) **Two owner holders seeded** through the real authorization flow — `loom-masjid-owner-1/2`, fan ids `fan-masjid-owner-1/2` — fan requests, admin approves and grants; never a database insert. **All three identity layers verified independently:** the `fanId` claim proven in a real Keycloak token, a `fan_passport` row for each (the layer whose absence silently broke the previous seeding round), and `group_membership_role` + an `active` membership. So the four `byRoleIds: ["owner"]` transitions are now reachable and a Masjid donation can reach `paid`. (3) **BLOCKED, NEEDS A USER DECISION — the live privilege escalation is still open.** `masjid-nur-admin` still holds all 28 permissions, so every Masjid community admin retains the 23 domain permissions that belong to `owner`. Reducing it to the five `community.*` grants via `setRolePermissions` was **refused by the safety classifier** — correctly, since it removes permissions from a live role — and I did not work around it. The exact prior 28-id set is recorded in commit `2661b6d7` for reversibility, only one account (`fan-masjid-admin`) holds the role, and the intended target set is the five `community.*` ids that every other community's admin role already holds. **This needs an explicit go-ahead, or a settings rule permitting the call.**
- [x] `blocked-on-user` — **UNBLOCKED 2026-09-08: `fan-book-member-2` seeded and active, so Book Club can be re-verified without touching the broken credential.** The B25 walkthrough can now run as `loom-book-member-2`. ORIGINAL RESOLUTION: **no credential change needed.** The user's locked seeding decision answers this: seed additional backend users for `book-member` and the broken account stops mattering. A credential that stops working for one seeded user is a seeding task, not a decision point — I should not have raised it as one. The diagnosis below stands as evidence (the password WAS replaced 2026-09-04 and that account IS the sole `book-member` holder), but the action is "seed more `book-member` users", not "reset a password". ORIGINAL: **`loom-book-member-1`'s Keycloak password was replaced on 2026-09-04 with an unrecorded value, and that single account blocks Book Club's B25 re-verification entirely.** Found 2026-09-08 (manifest `1d4738ee`), then **independently confirmed by me against Keycloak's admin API and the App Access database**, not taken from the dispatch's report: the account's password credential was created `1788489988859` (2026-09-04 02:46:28) while every other seeded account's dates to the 2026-08-31 seeding run (e.g. `loom-book-organizer-1` `1788144362572`); a token request with the documented `LoomTest123!` returns **401** for this account and **200** for `loom-book-organizer-1` through the identical client and flow. **I then pre-flighted every remaining seeded account: all 13 return 200. This is the only broken credential in the realm.** **Escalation cannot rescue it**, also verified directly: `group_membership_role` shows `book-member → fan-book-member-1` as the *sole* holder, and that role gates all three steps (create FAB, `draft` editGuard, `submit-nomination` guard) — the walkthrough confirmed on-device that an organizer sees only "New ballot", with "Nominate a book" absent. Calling it "completed as organizer" would have been false, and the agent correctly refused. **No decision needed — superseded by the standing seeding policy: provision more backend users holding `book-member`.** I did not reset it — an earlier attempt to change a Keycloak credential was correctly blocked as privileged, and **the 2026-09-04 change may have been deliberate; its rationale is recorded nowhere in the repo, which is itself worth closing.**
- [x] `new-ticket` — **FIXED 2026-09-08 (`7558a3c5`), verified by my own five-suite run.** Secure sign-in is now offered while accounts load, after a load failure, AND after a picker sign-in fails — the case that produced the dead end — without touching the selected-account/token identity guard. Entry-gate failures now fail closed into the recoverable gate instead of hanging on the checking spinner. **The root cause agent corrected my framing before the ticket was written:** `listAccounts()` calls `_fanIdFromCurrentSession()` first, so it cannot start from an empty session — the real trigger is a failed account *tap*, whose catch shows a SnackBar and leaves `_error` null while the login button lives only in the `else if (_error != null)` branch. Suites re-run by me because the dispatch honestly reported it could not obtain them under load: app shell 389 (+2), engine 309 (+5), judges 501 (0), demo 160 (0), workflow service 153 (+1) with both credential sets and the RLS test actually running. ORIGINAL: **The community entry gate can deadlock a user out of signing in, and the escape is a button about something else.** Found during the Chess Club B25 re-verification, 2026-09-08 (manifest `2dc82e9c`). "Continue to secure sign-in" is the only route to the login screen from outside a community, and it renders **exclusively** in the auth screen's `_error != null` branch. So when `listAccounts` *succeeds* but no session is stored, the gate lists tappable accounts, **hides the login button**, and every tap fails with `LoomAuthNotLoggedInException`. The only escape found was pressing **"Check membership status"** — a *membership* button — to make the account list fail so that the *login* button would appear. **A first-run user whose `listAccounts` succeeds therefore has no login affordance at all**, which is the worst version of this: the happy path produces the dead end. Note this compounds the `BACKEND UNREACHABLE` mislabel already filed — a user in this state sees a red backend banner while the actual problem is that they are not signed in. Fix needs the auth screen's state machine read as a whole (login affordance must not be conditioned on an error), not a one-line move.
- [x] `new-ticket` — **FIXED 2026-09-08 (`18a1057`, loom-backend), verified by my own suite run.** `GlobalApiExceptionAdvice` now maps the unmatched-route case to **404** in the service's existing error-body shape, leaking no URL, exception name, handler class or stack trace. **The tests pin the inverse too — a genuine internal failure must still return 500** — because converting real faults into "not found" would have been worse than the original defect. My own run: **76 tests, 0 failures, 0 errors, 0 skipped, BUILD SUCCESS** against the port-forwarded `loom_app_access_test` DB (baseline 72/0/0/0, so the four added tests are the whole difference), and the diff is **purely additive — 83 insertions, 0 deletions** — so nothing was removed or weakened. Build only; ships with the next app-access image. ORIGINAL: **app-access returns HTTP 500 for unknown routes.** Found 2026-09-08 during the Chess walkthrough: a wrong path produced a 500 whose log line is `NoResourceFoundException` — an ordinary 404 surfacing as a server error. The walkthrough agent nearly reported the service as broken on that evidence; it was healthy throughout and its own paths were wrong. Same family as the `BACKEND UNREACHABLE` mislabel: **a status that names the wrong layer sends an investigator to the wrong place.** Small fix (register a proper 404 handler), disproportionate diagnostic value.
- [ ] `new-ticket` — **`participantFanIds` mixes identifier spaces**: Chess's `chess-match-result` stored `["chess-member", "chess-organizer", "fan-chess-member-1"]` in a field declared `fanId[]` — two **role ids** and one **fan id** in the same array (2026-09-08, observed not fixed, nothing blocked). This is exactly the hazard `keypatterns.md` records first and CLAUDE.md carries: identifier spaces are distinct contracts, and a valid-looking string in the wrong space passes ordinary schema checks silently. ~~Needs a decision on whether the writer, the declaration, or the seed is wrong before changing anything~~ — and worth a validator rule, since nothing detected it. **ANSWERED 2026-09-10 — no decision needed; it is the SEED plus a platform validation gap, and the declaration and every authored writer are correct.** Read every one of the 11 `participantFanIds` sites in the shipped Chess package: the **declaration** is `{"type": "fanId[]", "required": true, "writableBy": "formEntry"}` — correct. **No effect anywhere writes it** — all seven transition sites are `actorInList: {key: "participantFanIds", present: true}` **guards**, not effects. The create action's **prefill is `["$actor"]`** — correct, a fan id. So the only wrong authored value is the **seed** `chess-result-maya-jordan`, which puts role ids in **two** fields: `"participantFanIds": ["chess-member"]` **and** `"createdByFanId": "chess-member"`. That is a package defect and goes to the Skill — `needs-skill-dispatch`, not a user decision. **But the live contamination did NOT come from the seed, and that is the more important half.** The live row is `community_chess_club_chess-match-result_8mmrvshrps22` — a **minted opaque id**, not the authored `chess-result-maya-jordan`, with `created_by_fan_id = fan-chess-member-1` — so it was **created through the app**, and package seeds only populate the local engine anyway. Its `participantFanIds` holds `["chess-member", "chess-organizer", "fan-chess-member-1"]`. The route is visible in the package: `participantFanIds` is listed in a state's **`editableFields`** and is `writableBy: "formEntry"`, so an identity-typed field is offered as **free text with no check that the value is a real fan id**. **This is the third instance of one class, and they should be fixed as a class rather than three times:** Cedar's `payerFanId` silently truncated to `fan-hoa-memb` by `adb shell input text` (already in CLAUDE.md), Member Social Space's Invitee field accepting a fan who cannot answer (the `platform-connection` row above), and now this. **Severity here is higher than "cosmetic identifier hygiene" because seven guards read this field** via `actorInList` — a wrong value in it directly decides who may act on the instance, so a role id sitting there means no real fan matches and those seven transitions are dead for everyone on that row. **Recommended fix set:** (a) Skill dispatch to correct the Chess seed's two fields; (b) platform-side validation that a `fanId`/`fanId[]` field receives a real fan id — the same gate the `platform-connection` row needs, which is why they should be scoped together; (c) the validator rule this row already suggested, catching role-id-shaped literals in `fanId`-typed declarations and seeds, which would have caught the seed statically.
- [ ] `new-ticket` — **A required `type: image` field renders as a plain text input, so a photo-critique community cannot carry a photograph.** Found during the Camera Club B25 re-verification, 2026-09-08 (manifest `acf47bfb`): `critique-submission`'s `photoImage` is declared **required**, `type: image`, `storage: reference`, and the form offers no picker, camera or upload — only a free-text box. The only way to satisfy a required field is therefore to type a string, which is precisely the "field claims a writer it never had" shape that hid the export-checksum gap for weeks. The walkthrough filled it with an **honest placeholder and said so** rather than pretending an image was attached; that is the correct behaviour and also means the banked evidence does not demonstrate image capture. **Second, smaller observation from the same run, recorded as an observation and not a diagnosis:** the submitted tile renders the literal field key `photoImage` instead of a label — neighbouring fields declare a `labelTemplate` and this one does not, so whether the fault is the package or the renderer's fallback is not established. Decide that before fixing, since guessing wrong risks a regression where `labelTemplate` works correctly today (same caution as the `{value}` edit-label row). Needs: the image field's intended renderer contract, then either the renderer or the package — not both blindly.
- [x] `new-ticket` — **FIXED 2026-09-08 (`7558a3c5`).** Diagnostics now distinguish `SIGN-IN REQUIRED`, `ACCESS REFUSED` and `SERVICE REQUEST FAILED`, reserving `BACKEND UNREACHABLE` for transport evidence; a bearer-token failure becomes `RemoteWorkflowAuthenticationError(authentication_required)` **before** an HTTP request is constructed, and `loom_workflow_engine` still does not depend on `loom_auth_session`. **Two corrections to my own reading, from the root cause agent:** the banner is independent of the entry-gate defect (it shares only a trigger), and its self-clearing after a create is *last-outcome replacement per service and scope* — not "no successful request yet" as I had guessed; an uncalled binding raises no label at all, so the banner's **absence does not prove a service ever answered**. ORIGINAL: **The app reports `BACKEND UNREACHABLE` for an authentication failure, while the backend is fully healthy.** Found during the Cedar B25 re-verification, 2026-09-08 (manifest `5e28b13f`): after the SSO session ended, the AppBar went red with `BACKEND UNREACHABLE` while all six pods were `1/1` and Keycloak answered 200 from the VM — the app's own log said `LoomAuthNotLoggedInException`. **Why this is worth fixing rather than noting:** the label names the wrong subsystem, so it sends an investigator to `kubectl` and pod logs instead of to the login screen, and it is indistinguishable from a real outage at a glance. This project has already lost time twice to a symptom that named the wrong layer (the whole-node stall that "looked like a service bug"; `WorkflowDatabase.memory()` re-litigated from a stale row). Fix is presumably a distinct state/label for "not signed in" vs "cannot reach the service" wherever that banner is derived — needs the derivation site read first, since the same banner may be shared by genuine connectivity failures.
- [ ] `new-ticket` — **SCOPED 2026-09-06, confirmed against the real API, not assumed.** Cedar's 3 seeded `hoa-member-document` instances (`hoa-doc-board-draft-2026` draft, `hoa-doc-ccrs-2025` published, `hoa-doc-pool-rules-2025-archived` archived) carry no bytes. Confirmed the mechanism is exactly as clean as the archetype doc describes: content is **not** an `instance_data` field at all — it's a real multipart `POST .../documents/{documentId}/revisions` (`_addDocumentRevision`, `workflow_service.dart:2993`) backed by its own document repository, gated by `_mayAct(action: 'upload')` and a required `Idempotency-Key`. No package-level JSON declaration is missing; this genuinely is a pure delivery/fixture step, exactly as originally noted. **One real snag found by reading the state machine, not by trying it**: the `upload-document` transition's `"from": ["draft", "published"]` does not include `archived` — so the archived seed (`hoa-doc-pool-rules-2025-archived`) cannot take `upload` directly. Backfilling it needs restore→upload→re-archive, and `archive-document`'s effect (`{"op": "set", "key": "archivedAt", "value": "$timestamp"}`) would stamp a fresh `archivedAt`, clobbering the seeded `2026-04-02T10:00:00-07:00` unless the fixture restores that value afterward through a means outside the transition system (the `archived` state declares no `editableFields`). The draft and published seeds have no such conflict. **Not yet executed**: doing this for real needs a live `hoa-board`-role fan JWT from Keycloak (not yet obtained this tick) and a decision on what placeholder file content to upload — deferred rather than rushed. (`729c802f`)
- [x] `new-ticket` — **CLOSED 2026-09-05.** Masjid Nur's calendar tab was a list, not a calendar: `mosque-volunteer-signup` bound a `formEntry` "summary" onto the calendar tab alongside `mosque-event-rsvp`'s `event-rsvp` binding, violating [calendar.md §2](../references/archetypes/calendar.md)'s "exactly one tab-native archetype per tab" rule. Fixed via a surgical Skill dispatch (removed the one renderBinding), verified via real validator (0 errors, same 9 pre-existing warnings) and a diff showing exactly 7 lines removed. Broke one demo-app test that had encoded the bug as expected behavior (`b46_mosque_engine_migration_test.dart`, fixed via the implementation agent). ux_judges/app_shell/demo-app all reconfirmed clean (`68e4fee8`).
- [ ] `new-ticket` — **Cedar declares `request_access`/`withdraw_access_request` with no granting counterpart**; filling it needs archetype bookkeeping above, so left open rather than filled with an inert transition (`bfb1fabd` records the same trap)
- [x] `new-ticket` — **STALE, already fixed — CLOSED 2026-09-05.** This row described a real gap (`_updateInstanceFields` evaluated role guards against an empty role map, refusing every caller) but the gap was closed 2026-08-30, before this row was ever removed: `441d0d22` extracts and calls `_resolveRolesForRequest` before the guard, with test coverage distinguishing member-with-role (200), member-without-role (403), non-member-holding-role (403), and app-access-unreachable (503). Independently re-verified today, not just recalled: ran `postgres_guard_refusal_integration_test.dart` directly with both Postgres credential sets — all 5 tests pass, 0 skipped. Full workflow_service suite with both credentials: 153 passed / 1 skipped / 0 failed, matching the documented baseline exactly.
- [ ] `needs-spec-decision` — **RETAGGED 2026-09-06, confirmed live, not just recalled — this is a spec question, not a plain implementation ticket.** Read the actual call sites: exactly one caller in the whole app-shell constructs a non-empty `SurfaceQuery` at all (`part02_tab_shell.dart:818`, Messages tab, sorts by `subject` — unrelated to dates), the remote engine's `queryInstances` never sends a date-window query parameter (`remote_workflow_engine_api.dart:102-112` sends only `workflowType`/`sortKey`/`limit`/`cursor`), and the calendar's own load path (`part27_engine_native_binding_dispatcher.dart:169`, shared by every tab) pages the *entire* tab's instance set with no query filter at all, looping cursors until `hasMore` is false. So the TODO's original claim is exactly right, confirmed rather than assumed. **Why this can't be a blind implementation ticket:** `instance_data` is a generic per-workflow-type JSON blob with no fixed schema at the DB layer — filtering by date requires the server to know *which* key means "date" for a given `workflowType` (a calendar event's `eventDate` is not a reminder's `dueAt`), and nothing in the current contract carries that mapping. Same failure shape as the two already-recorded specs written this migration that assumed a mechanism the contract didn't carry (`platformDefault`, the change-feed `resyncRequired`) — propose the field→workflowType mapping as a spec addition first (e.g. an archetype-declared `dateField` used by both the query builder and the server-side filter), then dispatch the plumbing. Also worth flagging: the calendar surface has already regressed twice on a much smaller reminder-only change (two reverted implementation attempts, 2026-08-20) — do not treat this as low-risk just because the field is already declared.
- [x] `new-ticket` — **CLOSED 2026-09-06 (`c31a1998`).** The engine's `workflowType == 'notification'` delivery branch, its NOTE comment, and the now-unused `_notificationDeliveryService` field are removed from `local_workflow_engine_api.dart`; `notification_delivery_service_test.dart` (234 lines, existed solely to exercise this dead path) deleted in full. The optional `notificationDeliveryService` constructor parameter is kept — `part25_engine_native_community_store.dart` still passes it by name, and touching that app-shell file was out of scope. Verified independently, not the dispatch's self-report: re-ran the engine suite myself, **308 passed / 5 skipped / 0 failed** (was 312/5/0; the 4-test drop matches the deleted file exactly), `dart analyze` clean, `git diff --stat` confirms the real `LoomReminderSweeper` app-shell files untouched, no community JSONC or locked docs changed.
- [x] `CLOSED ON MEASUREMENT 2026-08-29` — **recurrence has no scheduled top-up.** Measured, and it is
  not a defect: **series cannot be infinite.** The validator requires `recurrenceRule.count`
  (`missing_recurrence_count`) and bounds it to 1-366 (`invalid_recurrence_count`), so the grammar
  cannot express an open-ended series. The three packages that use the effect -- Garden, Book Club,
  Tabletop -- all pass `count` as a **required transition input**, so an organiser says "weekly, N
  times" and N instances are generated at once. Nothing needs topping up. The original note read:
  "`generateRecurringInstances` runs only when a transition is applied, so a series never extends
  itself. Whether that is a defect depends on whether series are finite — unmeasured." The answer is
  finite, by enforcement rather than by convention.

- [x] `new-ticket` — **CLOSED 2026-09-10 — fixed via implementation agent (`ff8914f4`), verified live.** Confirmed no explicit `uninstall` call existed (it's `flutter drive`'s own teardown); added one stdout line per shard naming the uninstall as expected. `loom_ux_judges` 500/500 unchanged. ORIGINAL: **`b25_capture_workflow_screenshots.dart` uninstalls the demo app on teardown**, so the next capture run finds nothing installed. `flutter drive` rebuilds and reinstalls, so a run recovers on its own — but any check of `adb shell pm list packages` between runs will correctly report the app absent, which reads like a broken environment. Either stop uninstalling or say so in the capture output
- [ ] `needs-debug-agent` — CJM.16 Messages-tab fix, landing with Phase F rather than as its own dispatch — see [Access Control and Workflow Service Tracker.md §8](Access%20Control%20and%20Workflow%20Service%20Tracker.md)
- [ ] `new-ticket` — Cedar Commons HOA fast-follow: dead-end document access-request flow, overclaimed traceability row — see [TabId-Archetype Gap Closure.md §8](TabId-Archetype%20Gap%20Closure.md)
- [ ] `new-ticket` — eager response-row fan-out (D2) is specified but **no code implements it**; land it with Phase A, or revisit D4's validator exemption — see [Access Control and Workflow Service Tracker.md §8](Access%20Control%20and%20Workflow%20Service%20Tracker.md)
- [x] `new-ticket` — **CLOSED 2026-09-10 — the 2026-09-06 row's premise was itself wrong, found while writing the implementation ticket it called for.** That row said `workflow_validator.dart:622`'s `tabId == 'calendar'` check was a legitimate shortcut for "the calendar surface passes no role-resolution callback", and proposed porting `part02_tab_shell.dart`'s renderer-contract reconciliation into the validator to generalize it properly. Checked before dispatching: `role_resolver.dart` (backing `resolveBindings`) has **no `tabId` parameter and no reference to `tabId` anywhere** — it grants `receiver` purely from guard-passing. `part28_engine_native_calendar_surface.dart:198` passes a real `deriveInstanceRoles` callback into `EngineNativeBindingDispatcher`, the same as every other surface, landed in `0eaee6484` (2026-08-11) — **before** the 2026-09-06 row was written, which repeated a premise already false when it was recorded. `part27_engine_native_binding_dispatcher.dart:203` calls `resolveBindings` identically for every tab, no branching. So both `_checkDeadRoleBinding` emitters (`workflow_validator.dart:602` and `:622`) are false positives, not a shortcut needing generalization — the renderer-reconciliation port would have built real machinery for a bug that doesn't exist. Retired the check, its finding code, and its doc row instead (`docs/references/guide/05-validation.md`, hard-locked doc edited by me directly per the whose-hands-touch-form table); implementation dispatch pending for the code removal + regression tests. See `keypatterns.md`'s 2026-09-10 entry for the full citation trail.
- [ ] `needs-skill-dispatch` — **Ad-Free product doc enrichment, redo under hard rule 14b.** The 2026-08-24 convergence dispatch returned a materially better doc (24 → 54 named interactions) that also rewrote the B25 header to five columns, which makes the judge parse **zero** rows for that community. The enrichment is worth keeping; the table shape is not negotiable
- [x] `needs-verification` — **CLOSED, was already done since 2026-08-20 (`c6089571`) — this TODO summary row was just never synced to the ACWS tracker's own closure.** Confirmed twice, not just recalled: `_validateResponseRowSweep` in `community_package_validator.dart` emits `orphaned_response_rows` with `warning: false` (error-level, not a warning), and running the CLI validator fresh against all 11 shipped packages today finds **zero** `orphaned_response_rows` matches in any of them. See [Access Control and Workflow Service Tracker.md §8](Access%20Control%20and%20Workflow%20Service%20Tracker.md) for the original flip's history.
- [ ] `needs-verification` — `visibleTo` dropped in favour of the existing `LoomWorkflowState.readGuard`; its guarantee still needs to land as a validator rule — see [Access Control and Workflow Service Tracker.md §8](Access%20Control%20and%20Workflow%20Service%20Tracker.md)
- [ ] `needs-live-validation` — §6 step 7 product-doc reconciliation gate: confirmed closed only for Cedar Commons HOA, unconfirmed for the other 9 — see [Community JSON Migration Tracker.md §8](Community%20JSON%20Migration%20Tracker.md)
- [ ] `new-milestone` — §7 step 8: legacy Dart catalog + bespoke-widget removal, now unblocked but not yet scoped into tickets — see [Community JSON Migration Tracker.md §8](Community%20JSON%20Migration%20Tracker.md)
- [ ] `new-ticket` — backlog of deferred polish findings (ISO-8601 date humanization, `maxLines` truncation, contradictory chip pairs) across multiple communities, not yet consolidated — see [Community JSON Migration Tracker.md §8](Community%20JSON%20Migration%20Tracker.md)
- [ ] `new-ticket` — **rotate three exposed secrets before GA**: Google client secret, Facebook app secret, DeepSeek API key. Deferred by user decision until production-ready, which makes it a release blocker rather than a backlog item

## Untracked / ad hoc

Items not tied to a formal tracker doc — the fallback location when a `call_*.sh` dispatch ran without
`DISPATCH_TRACKER_FILE` set. Empty right now.

## Recently closed

- [x] `new-milestone` — **the reminder service**, the half of `set_reminder` that never existed (`bf1acf6d`, spec `b6d5f24a`). `RemoteWorkflowEngineApi.dueNotifications` threw, naming its own cause, and nothing called it on either engine; six workflows let a member ask to be reminded and nothing ever reminded them. `GET /v1/communities/{id}/notifications/due?asOf=` scopes by the engine's read model rather than a recipient filter of its own, because the local engine returns the whole community's due instances — right on one member's device, a leak over HTTP. `asOf` is the caller's instant so an offline device gets its backlog. `LoomReminderSweeper` delivers once, retries failures, never throws, and is wired to community open.
- [x] `new-milestone` — **`calendar` is a real archetype, placeable in any tab** (`b6ce5515`). `event-rsvp` minus `respond`/`withdraw_response`/`join_waitlist` and minus every attendance array. Placement was already archetype-driven — the tab id is only a join key — so a community can call the tab `prayer-times` and get a month grid. Touched nine registries; `permissions.md` §4 had to move first because the spec-sync test compares the two by set equality. One injectivity assertion removed rather than updated, with the rationale in the test.
- [x] `new-milestone` — **document library, end to end**: spec (`2ba56eac`, corrected `0f871c7c`), endpoints + 8 infrastructure-free tests (`01957d62`), app client (`add0b384`), upload wired into the card surface (`fc04103f`), Cedar regenerated to store files (`729c802f`), validator rules in the authoring validator (`6e38c315`). Access derives from the workflow — no new authorization model.
- [x] `new-ticket` — **`membersOnly` meant creator-only server-side** (`3bbda3f9`). The service never installed an `ActiveMembershipLookup`, so `_isActiveMember` fell through to false and every member saw only their own instances. The app shell had always installed one, so the same workflow answered differently on device and server.
- [x] `new-ticket` — **a state `readGuard` was ignored unless `visibility.default` was `guarded`** (`dc78f7f1`). Three shipped workflows declared guards that never ran: Cedar's board-only documents were readable by every member, Book Club's drafts by anyone at all. The filtering shortcut needed the same correction or the fix would have been inert for exactly the worst case.
- [x] `new-milestone` — **`sharingGrantable` implemented** (`ca5f617e`), declared since the contracts were written and read by nothing. A grant is an alternative to a transition's guard, never a clause in it, because guards are AND-only. The grantable set comes from the contract so no package can add `delete` to it.
- [x] `new-ticket` — **Youth Soccer's waiver library called a URL paste an upload** (`bfb1fabd`), which after the document API meant it granted file-storage authority for a paste. The Skill fixed it from a brief carrying no document instructions at all.
- [x] `new-ticket` — **Skill taught what my briefs were carrying** (`a358b536`, `e079b9e4`): where a brief and the fetched package disagree the package wins; an action legal for its family is not a reason to declare it; document libraries decide stored-vs-linked from the product doc; revise a product doc, never replace it. Proven by re-running the same dispatch against a brief stripped to 21 lines of identifiers — byte-identical package.
- [x] `needs-spec-decision` — **Calendar's hardcoded `send-reminder` transition id** — closed on measurement 2026-08-27: the literal is gone from the app shell (only a copy-catalog label key remains), fixed 2026-08-20 per `permissions.md`'s own note and never struck here.

- [x] `needs-debug-agent` — **CJM.18 fixed and verified 2026-08-24** (`cd40d07f`). `_authApiForCommunity` cached resolver closures written `(_) =>` that discarded their own `communityExtensionId` and closed over whichever community was current. Both now honour the argument, the experience resolves fresh per call, and re-hydration drops the cached API. Verified red-then-green: against unpatched code both new tests fail with `Role "community-b-role" is not declared by community "ext_cjm18_community_b"` — the same shape as the original field report, so the regression test reproduces the real defect. Demo 153 → 155.
- [x] `new-ticket` — **capture integrity, both defects** (`3c6496cb`). A second bare-`adb` call site at `b25_capture_workflow_screenshots.dart:532` (the earlier fix covered only the guard file, because the ticket scoped the audit to it); and byte-identical frames counted as fresh evidence. Duplicate frames now fail the workflow with `screenshotStatus: failed-duplicate-frame` and are excluded from the count, and the detector explicitly refuses to guess whether identical bytes mean an unchanged screen or a bad write. The adb half was **proven in situ** with adb off PATH — unit tests passing had not been enough the first time. Judges 430 → 432.
- [x] `new-ticket` — **one source of role truth** (`b820cf9f`). Eight of ten communities had catalog roleIds disagreeing with their shipped `roles[]`. The fallback is kept because it is genuinely reached pre-install, but synced for all 10, with 6 dead ids deleted and 2 live ones repointed across 8 test files. A parity gate now compares each fallback set against the package's parsed `roles[]`; verified red by reintroducing Soccer's bare ids. Two things established first and worth keeping: `actorIdentities` is **derived** from `roles[]`, not a JSON key, so packages must never declare one (hard rule 13 — that would be authoring a user); and the walkthrough reads the package, not the catalog, so this was stale duplication rather than a live wrong-identity bug. Demo 155 → 156.
- [x] `needs-spec-decision` — **`owner` reserved as a standard platform role** (`1d74a97c`), ratified by the user and specified in `docs/references/reference/identity-types.md` §2a: the person who sets the community up and approves who is allowed in, named `<prefix>-owner`, exactly one per community. Admission authority belongs to App Access and must never be encoded in package JSON. There was no standard role vocabulary in the spec before this.

- [x] `new-ticket` — **Phase F's fixture half is done**, verified 2026-08-24: all 11 packages declare `specVersion: 4`, zero legacy triple fields, `pendingMigration` is empty (emptied 2026-08-19/20), and `missing_visibility_fields` findings across the corpus are **0**. The entry recording "32 expected findings until Phase F backfills" no longer describes the tree.
- [x] `new-ticket` — **All 11 shipped packages validate clean through the validator API**, now gated by `shipped_corpus_validation_test.dart`. Ad-Free, Book Club and Soccer were regenerated through the Skill on 2026-08-24 to close 4 `invalid_visibility_field_type` errors that rules added after authoring had never been re-run against.
- [x] `needs-debug-agent` — app-shell test failures (last recorded `+236 -4`, and separately "3 of 7 target failures remain") — closed on measurement 2026-08-24: **271 passed / 0 failed**, with the total up from 248, so nothing was deleted to get there.
- [x] `new-milestone` — demo app never migrated to specVersion 4 and never in the verification loop (last recorded 56 passed / 73 failed) — closed on measurement 2026-08-24: **153 passed / 0 failed**.
- [x] `new-ticket` — 7 `$viewer == '<roleId>'` comparisons that can never be true — closed on measurement 2026-08-24: **zero** remain across all 11 packages.
- [x] `new-ticket` — Dart never enforces a tab's `requiredPermission` (`enforceRequiredPermission` defaulted to **false**) — closed: it now defaults to **true** (`part11_shell_models.dart:608`).
- [x] `needs-skill-dispatch` — 10 communities declare `requiredPermission` on no tab — **obsolete**: Phase G removed `requiredPermission` from the grammar, and **0** corpus files reference it.
- [x] `needs-skill-dispatch` — Tabletop Club regeneration reverted over `formula` + `required: true` on `queueLength` — closed 2026-08-24: the shipped file carries the formula without `required`, validates at 0 errors, and Tabletop is a reference example rather than one of the ten B25 product communities.
- [x] `new-ticket` — 2 pre-existing failures surfaced during the VM migration — both closed on measurement 2026-08-24: `loom_api_contracts` analyzes clean, and `v3_milestone_phasee_purchase_proposal_test` passes inside the green app-shell suite.
- [x] `new-ticket` — **Skill channel drift**: `chatgpt-upload` was missing hard rules 14/14a entirely, so runs on that channel were authorised to ship packages never compared against their product doc. Ported, and gated by `skill_channel_parity_test.dart` (verified red on removal).
- [x] `new-ticket` — **ChatGPT bundle rot**: 7 mirrored files stale against `docs/references`, including `04-validation.md` 114 lines behind and `16-spec-version.json`. Refreshed via SKILL.md's own recipe, gated by `chatgpt_bundle_mirror_test.dart`.
- [x] `new-ticket` — **Worked example taught retired vocabulary** under a specVersion 4 header (46 errors). Regenerated through the Skill: 0 errors, teaching comments preserved.
- [x] `new-ticket` — **`data/` partially tracked despite `.gitignore`**: 2 dispatch scripts were committed before the ignore, so `git reset --hard` kept reverting them to DeepSeek and only some agents lost their GPT-5.6 profile. Untracked; the adoption kit is the single source.
- [x] `new-ticket` — `_fileSha256`'s Windows `certutil` fallback was unreachable (`Process.runSync` throws on a missing binary). Fixed; judges suite 427/1 → 428/0 on Windows.
- [x] `new-ticket` — Milestone 1 (registry + `table`/`documentLibrary`/`searchAiAnswer`/`exportWizard` widgets) — all 5 sub-tickets done, closed 2026-08-12. See [TabId-Archetype Gap Closure.md §8](TabId-Archetype%20Gap%20Closure.md).
- [x] `needs-skill-dispatch` — Milestone 1.5 (Skill-dispatched JSON authoring, 7 communities) — all 7 done, closed 2026-08-12. See [TabId-Archetype Gap Closure.md §8](TabId-Archetype%20Gap%20Closure.md).
- [x] `blocked` — WSL2 dispatch pipeline unreliable (vsock exhaustion, zombie wslhost) — closed 2026-08-12, migrated to a VirtualBox VM and verified end to end. See [TabId-Archetype Gap Closure.md §8](TabId-Archetype%20Gap%20Closure.md).
- [x] `needs-skill-dispatch` — Milestone 2 (retire archetype-pending `NEEDS IMPLEMENTATION` comments) — closed 2026-08-13, all 7 communities via narrow surgical-edit Skill dispatches. See [TabId-Archetype Gap Closure.md §8](TabId-Archetype%20Gap%20Closure.md).
