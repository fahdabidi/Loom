
## Garden Club (9 findings)

- **[major] tool-loan-shift-generic-shell**: garden-tool-loan and garden-volunteer-shift render as the same generic engine shell with no real content
  - Workflows: garden-tool-loan, garden-volunteer-shift
  - Description: Six of fifteen screens (all of garden-tool-loan and all of garden-volunteer-shift) use an identical component stack: a 9-dot grid icon in a translucent circle, a hero card whose subtitle and body are two restatements of the same sentence, the chip triplet 'Details ready / Editable / State saved', a strip reading 'This view shows editable details, current progress, and the next step before saving.' with an 'Edit response' button, a 'Task details' or 'Saved details' section repeating the hero sentence a third time, and a 'Start <workflow name>' button. The action sheets for the two workflows are the same layout down to control positions; the complete screens likewise. No loan or shift data is ever rendered: no tool identity beyond the word 'broadfork' in a subtitle, no queue position, no due date, no shift date, no time window, no slot count. This is directly contrasted within the same community by the RSVP card, which shows 'Sat, Apr 18', '10:00 AM', 'Riverside Greenhouse', '18 of 24 spots', and the plant exchange card, which shows 'Sweet Genovese basil', '6 starter pots', 'Pickup Sat 1-3 PM'.
  - RequiredFix: Give both workflows domain content and a domain icon. Tool loan: tool name, availability, current holder, due-back date, queue depth and the user's position, with a 'Borrow' / 'Join queue' action. Volunteer shift: shift date, time window, location, slots filled out of total, task description, with a 'Sign up' action. Replace the 'Details ready / Editable / State saved' chips with these facts, and replace 'This view shows editable details...' with copy about the loan or shift.
- **[major] tautological-completion-copy**: Complete states say only '<workflow name> state updated', repeated three times per screen
  - Workflows: garden-export-custom-schemas, garden-tool-loan, garden-volunteer-shift
  - Description: On garden-tool-loan complete the string 'Garden tool loan state updated.' appears three times -- as the card body, as the whole 'Saved details' body, and as the 'Saved' block body. On garden-volunteer-shift complete 'Volunteer shift signup updated.' appears three times in exactly the same slots. Each is accompanied by a 'Saved details' heading, a 'Saved' block and a checked 'Saved' chip: three redundant success markers around zero information. Neither screen states what was borrowed or signed up for, for when, or how to undo it. 'Updated' is also ambiguous for a first-time action. The garden-export-custom-schemas complete screen has a milder version of the same defect, duplicating 'Garden Club export is ready with redaction preview, checksum 8F4A-PLANT, and download progress.' as both the card body and the entire 'Transfer record' body.
  - RequiredFix: Write completion copy that states the resulting fact and the next step, in the style already achieved by the RSVP complete screen ('You are going to Spring Planting Workshop. Calendar, attendee count, and reminder status stay visible.') and the plant exchange complete screen ('Basil seedlings are listed with pickup details and contact sharing limited until a member claims them.'). Collapse the triple 'Saved details' / 'Saved' / 'Saved' chip redundancy into one confirmation.
- **[major] stale-prestate-chips-and-helper-text**: 'State saved' chip and 'before saving' helper text persist unchanged across start, action and complete
  - Workflows: garden-tool-loan, garden-volunteer-shift
  - Description: The chip row 'Details ready / Editable / State saved' is byte-identical on the start, action and complete screens of both garden-tool-loan and garden-volunteer-shift. On the start screens this asserts saved state before the workflow has begun, alongside a 'Start ...' button. Conversely the strip 'This view shows editable details, current progress, and the next step before saving.' remains on the complete screens, so a single card simultaneously says 'before saving' and 'Saved'. Because the chips never change, the only pixel-level difference between the start and complete states is a small corner chip, which makes state very hard to read at a glance.
  - RequiredFix: Make the chip row and helper text state-dependent: on start show availability/openness, on action show what is being edited, on complete show the saved result. Remove 'State saved' from un-started screens and remove the 'before saving' helper from saved screens.
- **[major] export-complete-missing-download**: Export complete screen advertises 'Download ready' but offers no download action
  - Workflows: garden-export-custom-schemas
  - Description: garden-export-custom-schemas complete shows a 'Download ready' chip and body copy reading 'Garden Club export is ready with redaction preview, checksum 8F4A-PLANT, and download progress.' The only interactive controls on the screen are two 'Change scope' buttons -- one inside the scope strip and one inside the 'Transfer record' section. There is no Download, Save or Share affordance, so the terminal step of the export workflow cannot be performed from its own completion state.
  - RequiredFix: Add a primary Download (or Share/Save to Documents) action to the completed export, and give the 'Transfer record' section real audit content -- timestamp, destination, per-schema record counts, file size -- rather than duplicating the summary sentence.
- **[major] captures-clipped-mid-card**: Two start screens are captured mid-scroll with the primary card's title cut off
  - Workflows: garden-export-custom-schemas, garden-tool-loan
  - Description: On garden-export-custom-schemas start the export card's title and description are scrolled above the viewport, leaving an orphaned chip row ('Redaction preview', 'Checksum verified', 'Download ready') and a 'Generate export' button sitting directly under the 'Garden Club' app bar with no heading -- workflow identity is not determinable from the screen. On garden-tool-loan start the card above is sliced horizontally through the letterforms, so 'Tap for expanded view' and 'Data package' render as half-height text fragments beneath the app bar. Both screens also carry large empty regions lower down (below 'Local package details' and below the tool-loan card respectively), so they read as thin as well as clipped.
  - RequiredFix: Anchor start-state captures so the subject card's title and description are fully visible, and ensure preceding cards scroll cleanly under the app bar rather than being sliced mid-glyph.
- **[minor] low-contrast-secondary-actions**: Secondary actions render dark-on-dark and read as disabled with no reason given
  - Workflows: garden-event-rsvp, garden-export-custom-schemas, garden-tool-loan, garden-volunteer-shift
  - Description: 'Maybe later' on garden-event-rsvp action, 'Edit details' on both the garden-tool-loan and garden-volunteer-shift action sheets, and the lower 'Change scope' on garden-export-custom-schemas action are all rendered as dark teal or dark green text on near-black or dark green cards, with no border or background. Each sits beside a high-contrast filled primary, so the visual read is 'disabled', but nothing on any of these screens explains why they would be unavailable. On the export action sheet the ambiguity is compounded by 'Change scope' appearing twice with two different treatments -- a bordered button near the top and the faint link at the bottom.
  - RequiredFix: Raise secondary/text-button contrast to meet accessibility minimums against the dark card surfaces, and if a control genuinely is disabled, show the reason. De-duplicate the two differently styled 'Change scope' controls on the export sheet.
- **[minor] instrumentation-chrome-and-crowding**: Developer-facing 'In-focus product surface' strips and minimized-card stacks clipped by the bottom nav
  - Workflows: garden-event-rsvp, garden-tool-loan, garden-volunteer-shift, plant-exchange-submission
  - Description: The strip 'In-focus product surface' with an 'Expand' affordance appears above the main card on garden-event-rsvp start (as 'Minimized' badging), plant-exchange-submission start, garden-tool-loan start and garden-volunteer-shift start. This is harness chrome, not product copy. Separately, several home-surface screens end in a stack of minimized cards whose text is double-truncated and whose button rows are sliced by the bottom nav: 'Plant exchange subm...' on rsvp start, 'Garden volunteer shift' with 'Volunteer details include shift, availability, and prot...' and a clipped 'Start garden volunteer shift' button on plant-exchange start and complete, and 'Garden export custo...' with 'You can read the current details without changing ...' on volunteer-shift start. The 'Documents' bottom-nav label is itself clipped at the right screen edge on several screens.
  - RequiredFix: Remove or hide the 'In-focus product surface' / 'Minimized' instrumentation banners in product captures, add bottom padding so card stacks clear the nav bar instead of being sliced, and let the bottom nav scroll or shrink so the 'Documents' tab label is not clipped at the screen edge.
- **[minor] rsvp-count-not-incremented**: Attendee count stays at '18 of 24 spots' after the user RSVPs Going
  - Workflows: garden-event-rsvp
  - Description: The garden-event-rsvp action sheet states 'Your RSVP updates the attendee count and leaves a reminder in your community inbox.' and shows '18 going' / '6 spots left'. After selecting Going and confirming, the complete screen still displays '18 of 24 spots', identical to the start screen. The user's own attendance is not reflected in the figure the copy promised it would change, making the count look static.
  - RequiredFix: Increment the attendee count on the complete state (e.g. '19 of 24 spots' / '5 spots left') so it agrees with the confirmed RSVP and with the action sheet's stated behaviour.
- **[minor] tool-loan-description-conflict**: Garden tool loan is described two different ways in two places
  - Workflows: garden-export-custom-schemas, garden-tool-loan
  - Description: On its own screens the garden-tool-loan workflow is framed as borrowing shared equipment: 'Shared garden tools are available to borrow or queue for.' and 'Borrow the broadfork and manage the loan queue.' But on the garden-export-custom-schemas start screen the minimized card for that same workflow reads 'Garden tool loan -- A submitted member form is ready for action and fol...', a form-submission framing with a 'Member form' chip and an 'Open' button. The two descriptions do not describe the same thing, so the card the user taps does not match the screen they land on.
  - RequiredFix: Use one consistent description of the tool-loan workflow across its minimized card and its full screen, reflecting whichever framing is correct (borrow/queue, or member-form submission).

## Neighborhood Book Club (10 findings)

- **[critical-blocker] export-start-shows-discussion-thread**: Export-metadata start state shows the discussion thread, not the export surface
  - Workflows: book-export-metadata, book-meeting-rsvp
  - Description: The screen evidencing the export workflow's start state contains no export surface at all. The in-focus card is a purple 'Discussion thread' with 'Prompt: What does community care require?', chips 'From Jordan', 'Members only', '3 unread', 'Club scoped', and an unexplained 'Waiting' pill, under a 'Messages and connections' band. No scope selection, redaction option, destination, checksum, or generate action is visible. The meeting-RSVP start screen has the same defect in reverse — it is parked on the 'Knowledge and search' / 'Answer with sources' surface with only a sliced 'RSVP to event' button at the top edge.
  - RequiredFix: Capture each start state with that workflow's own card scrolled fully into frame — the export package card with its scope/redaction/destination controls, and the RSVP card with 'Parable discussion night', Thu Feb 15, 7:00 PM, Maya's living room and 10 of 14 spots visible.
- **[major] ad-copy-in-book-club-reading-material**: Advertising-preference copy and an 'Open ad details' CTA appear inside the reading-material workflow
  - Workflows: book-library-item, book-reading-material
  - Description: The 'Reading material' card renders body text from an advertising product — 'Ad preference and sponsored-message beh…' — with a button labelled 'Open ad details', on the reading-material start, the library-item start, and again as the filled teal primary CTA on the reading-material action sheet whose app bar reads 'Reading material'. A neighborhood book club has no ad preferences; this is content leaked in from an unrelated surface and it makes the most prominent control on the action screen contradict the screen's own title.
  - RequiredFix: Replace the ad-preference copy and 'Open ad details' action with reading-guide content — the guide's book and chapter range, author links, discussion prompts — and an 'Open reading guide' primary action.
- **[major] library-and-reading-workflows-are-scaffolding**: Shared-library and reading-material workflows show framework scaffolding instead of club content
  - Workflows: book-library-item, book-reading-material
  - Description: Across all six screens of these two workflows the only content is generic: headings 'Task details', 'Saved details', 'Activity details'; chips 'Details ready', 'Editable', 'State saved'; a CTA that names its own component, 'Start shared library item'; and completion payloads of one sentence each — 'Reading material opened.' and 'Shared library custody state updated.' — the latter using internal data-model vocabulary. No item title, holder, queue position, due date, guide name, chapter, or timestamp appears anywhere. The library-item action sheet uses the same generic sentence in its subtitle, body, and Task details card.
  - RequiredFix: Author real content for both workflows to the standard the vote and RSVP screens already meet: name the item and its custody ('Maya has the club copy until Feb 22, 2 members queued') and the guide and its scope, and replace 'Start shared library item' with 'Borrow', 'Join queue', or 'Return'.
- **[major] complete-states-triplicate-confirmation**: Every complete state prints its confirmation sentence three times
  - Workflows: book-discussion-message, book-library-item, book-meeting-rsvp, book-nomination, book-selection-publish, book-vote
  - Description: On each complete screen the same sentence appears as the hero body, again inside a record card, and a third time in the trailing confirmation strip. Examples: 'Your vote for Parable of the Sower is confirmed, status is saved, and it can be changed before the ballot closes.' (hero / 'Vote saved' / 'Vote confirmed'); 'You are going to the Parable discussion; seat count, reminder, and change response stay visible.' (hero / 'Response options' / 'RSVP confirmed'); 'Your discussion message is posted to the Parable thread with sender and member scope.' (hero / 'Conversation record' / 'Reply sent'). The secondary action is likewise duplicated ('Change vote', 'Change response', 'Archive thread', 'Edit nomination', 'Save draft'), with the second copy rendered as a low-contrast green ghost link at the card's bottom-right that is barely readable on the dark card.
  - RequiredFix: State the outcome once and let subsequent cards add new information (timestamp, resulting record, next step). Render each secondary action once, at a contrast ratio readable against the card background.
- **[major] publish-complete-offers-save-draft**: Published announcement still offers 'Save draft' and pre-publish copy
  - Workflows: book-selection-publish
  - Description: The selection-publish complete screen states 'Members received the February selection announcement with book, sender, timing, and next steps.' and shows a 'Selection published' strip, yet between them it retains the pre-send line 'Message body, audience, delivery timing, preview, and draft option stay visible before publishing.' with an active 'Save draft' button, and offers a second 'Save draft' inside 'Member inbox result'. 'Today 5:00 PM' also still reads as a scheduled future slot after delivery. Offering to draft an already-delivered announcement is a wrong-state action that could make an organizer doubt whether the send occurred.
  - RequiredFix: Remove draft controls and pre-publish instructional copy from the published state; show delivery timestamp and reach, with post-send actions such as 'View in inbox' or 'Resend'.
- **[major] top-clipping-and-nav-overflow**: Hero cards sliced mid-sentence at the top of frame and the 'Library' nav pill runs off the right edge
  - Workflows: book-discussion-message, book-export-metadata, book-library-item, book-meeting-rsvp, book-nomination, book-vote
  - Description: Six screens open on a cut-off line rather than a card top: 'cycle.' on nomination start, 'Parable of the Sower is leading the' on vote start, 'Reply to the Parable discussion with a note scoped to club members.' on discussion start, 'and claim shared items.' on library start, 'Expand | RSVP to event' on RSVP start, and two label-less button outlines on export start. Separately, the bottom navigation row overflows horizontally so the 'Library' pill is cut by the right screen edge on nomination start, vote start, discussion start and RSVP start. Trailing confirmation strips ('Nomination submitted', 'Selection published', 'RSVP confirmed', 'Reply sent', 'Book archive generated', 'Saved answer') are also each clipped mid-sentence by the bottom nav.
  - RequiredFix: Anchor each capture so the focused card's title is fully visible below the app bar, add bottom padding so trailing confirmation strips clear the nav, and fix the bottom nav so all four tabs fit within the screen width.
- **[major] unexplained-not-available-and-repeated-shells**: Repeated generic card shells with unexplained 'Not available' actions
  - Workflows: book-discussion-message, book-nomination, book-reading-material, book-search-ai-digest, book-vote
  - Description: Background cards repeat an identical shell — icon, 'Minimized' pill, 'Tap for expanded view', 'Member form', 'Expand', trailing action — differing only by title. On vote start, 'Shared library item' and 'Reading material' are visually identical and both read 'Another community role manages this item.' with a disabled 'Not available' pill that names no role and offers no path. On discussion start, 'Book nomination' and 'Book vote' both carry the same truncated description 'Member form captures labeled details, privacy c…'. A debug affordance, 'In-focus product surface … Expand', is also visible on three screens.
  - RequiredFix: Give each background card its own real summary content, replace 'Not available' with a reason naming the responsible role and any available next step, and remove the 'In-focus product surface' debug strip from production surfaces.
- **[major] cards-describe-controls-instead-of-rendering-them**: Several cards describe affordances in prose rather than rendering them
  - Workflows: book-discussion-message, book-export-metadata, book-meeting-rsvp, book-search-ai-digest, book-selection-publish
  - Description: The AI-digest action sheet's 'Answer actions' card promises 'Open cited sources, save the answer, refine the query, or share it with members.' but contains no tappable controls, and one of its bullets is written about the user in third person: 'The user sees the query, answer, citations, source visibility, and follow-up options.' The same sheet duplicates the whole 'Answer summary / Source snippets / Citation detail' block twice on one screen. Similarly, the export action sheet says 'Scope, redaction, checksum, destination, transfer status, rollback, and audit evidence are visible' with no controls in frame; the RSVP sheet says 'Going/Maybe/Not going options are visible' but renders one generic 'RSVP to event' button; the discussion sheet lists eleven options ('reply, send, accept, decline, mute, archive, and block…') but renders only 'Archive thread'; and the publish sheet promises a body preview whose card is clipped off-screen.
  - RequiredFix: Render the described controls as real controls — distinct Going/Maybe/Not going options, citation rows that open sources, export scope and redaction toggles — and delete spec-voice bullets that narrate what 'the user sees'.
- **[major] counts-unchanged-after-completion**: Counts and chips do not change between action and complete states
  - Workflows: book-discussion-message, book-export-metadata, book-meeting-rsvp, book-vote
  - Description: The vote complete screen still shows '1 member vote' after the member records a vote, and the RSVP complete screen still shows '10 of 14 spots' after confirming Going, even though the action sheet stated 'Going reserves a seat and sends a reminder.' The discussion complete screen still shows '3 unread' after the reply is posted, and the export complete screen re-renders the same five numbered steps as the action screen with none marked done. The completion text asserts a change the visible data contradicts.
  - RequiredFix: Recompute chips and counters for the completed state — increment the vote tally and seat count, clear or decrement unread, and mark completed export steps — so the data corroborates the confirmation copy.
- **[minor] internal-instructional-copy-in-ui**: Internal 'before saving' instructional text persists into saved and published states
  - Workflows: book-library-item, book-nomination, book-reading-material, book-vote
  - Description: The string 'This view shows editable details, current progress, and the next step before saving.' appears verbatim on the nomination, vote, RSVP, reading-material and library-item screens, including on complete states where the save has already happened, paired with an 'Edit response' or 'Edit nomination' button. Chip labels like 'Details ready', 'Editable', 'State saved' and 'Book title entered' / 'Author confirmed' similarly report harness status rather than club data — the latter two are redundant when the title and author are printed two lines above them.
  - RequiredFix: Remove the pre-save instructional band from completed states, and replace status-report chips with the values they refer to (the entered title, the confirmed author, the saved timestamp).

## Riverside Youth Soccer (9 findings)

- **[critical-blocker] wrong-workflow-substituted**: Waiver-document and team-discussion evidence shows the minor-redaction surface
  - Workflows: soccer-team-discussion, soccer-waiver-document
  - Description: Four screens across two workflows render the 'Youth privacy record' surface that belongs to soccer-minor-redaction. soccer-waiver-document action and soccer-team-discussion action are the same capture as soccer-minor-redaction action — title 'Youth privacy record', a 'Roster and visibility' card (Team / Schedule / Privacy), a 'Roster visibility' card repeating those rows, and 'Filter roster' / 'Hide protected fields' buttons. soccer-waiver-document complete and soccer-team-discussion complete are likewise the same 'Youth privacy record' hero with the 'Team update saved' banner. The word 'waiver' never appears on the waiver screens, and no thread, post, author or composer appears on the discussion screens. Two of eight workflows therefore have no action-state or complete-state evidence at all.
  - RequiredFix: Route each workflow to its own surface and recapture. The waiver action/complete must show the waiver document, the covered player (Leo Rivera), the signing guardian (Mia Rivera), the Saturday deadline and a signed confirmation. The discussion action/complete must show a real U10 Falcons thread with authored posts, timestamps and a posted-message confirmation.
- **[critical-blocker] reminder-sheet-shows-rsvp-body**: Reminder-notification action sheet is titled 'Guardian practice reminder' but shows the RSVP form
  - Workflows: soccer-reminder-notification
  - Description: soccer-reminder-notification action carries the title 'Guardian practice reminder' over a body identical to the practice-schedule action sheet: an 'Event details' card followed by 'Choose attendance — Event date, capacity, location, host note, and reminders are visible before choosing Going, Maybe, or Not going.' No message body, recipient list, send channel or send button is present. Relatedly, the reminder start and reminder complete captures are visually the same screen (same calendar-tile hero, same 'Event details' card, same 'Saturday 9 AM' / 'Field 3' / 'RSVP available' chips), so the workflow has no distinguishable states.
  - RequiredFix: Give the reminder workflow its own surface: a composer showing the draft message body, the guardian recipient count and inbox channel with a 'Send reminder' action, and a completion state showing the sent message, timestamp and recipient count — visibly different from the start state.
- **[major] duplicated-content-blocks-in-sheets**: Every action sheet prints the same context rows twice
  - Workflows: soccer-guardian-join-approval, soccer-minor-redaction, soccer-practice-schedule, soccer-registration-payment, soccer-reminder-notification, soccer-team-roster
  - Description: Each detail sheet renders a context card and then a second card that repeats the same rows verbatim. On soccer-practice-schedule action, 'Team — U10 Falcons, coach Jordan Patel, 12-player roster.' and 'Schedule — Saturday practice at 9:00 AM on Riverside Field 3…' appear in 'Event details' and again inside 'Choose attendance'. On soccer-guardian-join-approval action, 'Guardian — Mia Rivera, parent of Leo Rivera, requested U10 Falcons access today.', 'Safety info' and 'Decision note' all appear twice. On soccer-team-roster action, 'Featured player — Leo Rivera, jersey 14…', 'Protected fields' and 'Team context' all appear twice. On soccer-registration-payment action, 'Registration — U10 Falcons spring season registration for Leo Rivera.' and 'Receipt' appear twice. On the redaction sheet, Team / Schedule / Privacy appear twice. The pattern also shows up between hero and banner on the completion screens, where the same paragraph is printed as both subtitle and banner body.
  - RequiredFix: De-duplicate the sheet composition so each context row renders once per screen; the second card should carry only content the first does not, or be removed.
- **[major] start-states-are-mid-scroll-feed-frames**: Most 'start' captures are arbitrary scroll positions with clipped top cards
  - Workflows: soccer-export-metadata, soccer-guardian-join-approval, soccer-minor-redaction, soccer-practice-schedule, soccer-registration-payment, soccer-team-discussion, soccer-team-roster, soccer-waiver-document
  - Description: Six of the eight start rows capture the shared Home feed mid-scroll rather than a workflow entry point, with the topmost card sliced through its text. soccer-registration-payment start opens on the fragment 'form state.'; soccer-export-metadata start opens on 'Protected export includes roster metadata, redaction preview, checksum verification, and transfer progress.' with its heading cut off; soccer-waiver-document start and soccer-minor-redaction start open on bare 'Edit details' buttons; soccer-guardian-join-approval start opens on bare 'Request changes' / 'Approve request' buttons. soccer-team-discussion start and soccer-team-roster start are the same feed position submitted twice, both dominated by the 'Team discussion thre…' card, with a third of the viewport left empty below 'Local package details'. soccer-practice-schedule start devotes its lower two-thirds to the unrelated 'Season registration fee' payment card.
  - RequiredFix: Capture each start state at the top of that workflow's own surface, with its title, its not-yet-started status and its primary action all in frame, and no unrelated workflow card occupying the majority of the viewport.
- **[major] generic-shared-completion-banner**: 'Team update saved' is reused as the completion state for four unrelated workflows
  - Workflows: soccer-export-metadata, soccer-minor-redaction, soccer-practice-schedule, soccer-reminder-notification, soccer-team-discussion, soccer-waiver-document
  - Description: soccer-practice-schedule complete, soccer-reminder-notification complete, soccer-minor-redaction complete, soccer-waiver-document complete and soccer-team-discussion complete all end in the same banner: 'Team update saved — The soccer update shows RSVP choice, reminder body, checksum or saved result, inbox preview, and protected data handling.' The sentence lists five unrelated capabilities and names none of the specific outcome, and on several screens it is printed twice (once as the hero subtitle, once as the banner body). It is also clipped by the bottom navigation bar on every occurrence. By contrast the workflows that do have specific banners — 'Registration paid — Leo Rivera registration is paid…', 'Guardian approved — Mia Rivera is active for U10 Falcons…', 'Roster opened — The U10 Falcons roster shows players, guardians, waiver status…' — read correctly, which shows the specific pattern is achievable.
  - RequiredFix: Give each workflow a completion banner that names its own outcome and the resulting artifact (RSVP response and attendance count; reminder sent time and recipient count; fields redacted; waiver signed by and when; export file, size and checksum), and pad it clear of the bottom nav.
- **[major] spec-language-in-user-facing-copy**: Card descriptions read as acceptance criteria rather than product copy
  - Workflows: soccer-guardian-join-approval, soccer-minor-redaction, soccer-practice-schedule, soccer-registration-payment, soccer-team-discussion, soccer-team-roster, soccer-waiver-document
  - Description: Alongside the good seeded copy, the scaffolding text is written as requirements-doc assertions: 'Payer, amount, privacy choice, entitlement, receipt destination, and retry/refund options are visible.' (registration action), 'Member names, roles, counts, status, and protected fields are visible according to persona.' (every roster sheet), 'The saved result keeps status, history, and change actions visible for this community task.' (four completion screens, naming no task), 'Title, date, time, location, host, capacity, and current attendance are visible before responding.' (practice action), plus the truncated generic 'Member form captures labeled details, privacy c…' used as the subtitle of two different feed cards and 'Another community role manages this item.' as the entire description of the waiver library. Sentences of the form 'X, Y, and Z are visible' describe the screen to a tester rather than speaking to a parent or coach.
  - RequiredFix: Rewrite all '… are visible' scaffolding lines as product copy that states the content itself, and replace the shared 'Member form captures labeled details, privacy c…' subtitle with per-feature descriptions.
- **[major] described-actions-not-present**: Action sheets describe controls they do not render
  - Workflows: soccer-guardian-join-approval, soccer-practice-schedule, soccer-registration-payment, soccer-reminder-notification
  - Description: Several action states name actions that are nowhere on screen. soccer-practice-schedule action and soccer-reminder-notification action both say 'Going, Maybe, Not going, and Change response remain available while the event is open.' with no such buttons rendered. soccer-guardian-join-approval action says 'Coach can approve, reject, or ask for a missing waiver before activation.' with no Approve or Reject button in frame. soccer-registration-payment action shows the payment summary but no pay button anywhere, even though the feed card behind it offers 'Pay and save receipt'. Separately, every feed card carries a 'Minimized' status chip — internal jargon that means nothing to a parent — alongside both a 'Tap for expanded view' control and a redundant 'Expand' button.
  - RequiredFix: Render the actual controls each sheet describes (RSVP buttons, Approve/Reject, Pay), and drop the 'Minimized' chip and the duplicate 'Tap for expanded view' / 'Expand' pairing in favour of one clear expand affordance.
- **[minor] off-palette-blue-on-roster**: Team-roster surfaces use a slate-blue accent in a green-themed community
  - Workflows: soccer-team-roster
  - Description: soccer-team-roster action renders its app bar in slate-blue behind 'U10 Falcons roster', and soccer-team-roster complete renders the 'Roster opened' banner in the same slate-blue, while every other surface in Riverside Youth Soccer — app bar, section headers, completion banners on the guardian and schedule flows, bottom nav — uses forest green. The registration workflow separately uses purple. The result is three different accent families across one community's evidence set, with the blue reading as an unthemed default rather than an intentional category colour.
  - RequiredFix: Apply the community's green accent to the roster app bar and the 'Roster opened' banner, or document the colour-by-category scheme and apply it consistently across all eight workflows.
- **[minor] truncated-titles-and-nav-clipping**: Card titles ellipsise and content slides under the bottom nav
  - Workflows: soccer-export-metadata, soccer-guardian-join-approval, soccer-minor-redaction, soccer-registration-payment, soccer-team-roster, soccer-waiver-document
  - Description: Feed card titles are cut rather than wrapped: 'Guardian join and app…', 'Protected minor-data…', 'Waiver document libr…', 'Team discussion thre…', 'Youth soccer export …', and subtitles truncate mid-word ('Practice details include date, location, capacity, R…', 'You can read the current details without changing …'). Separately, content is clipped by the light bottom navigation bar on the majority of full-app captures — the 'Registration paid' banner, the 'Membership state' row, the 'Update roster' row, the 'Team update saved' body and the 'Team and roster view' card are each sliced horizontally. The 'Team' tab itself is cut off at the right screen edge on several frames.
  - RequiredFix: Allow card titles to wrap to two lines, add bottom padding equal to the nav bar height so no content is occluded, and fix the horizontal overflow that clips the 'Team' tab and the flush-left hero on the export complete screen.

## Cedar Commons HOA (14 findings)

- **[critical-blocker] unresolved-record-disjunction**: 'Community Rules, Room A reservation, dues receipt, or architectural case' renders as the record identity on every workflow
  - Workflows: hoa-architectural-request, hoa-committee-decision, hoa-dues-payment, hoa-export-evidence, hoa-facility-reservation
  - Description: The 'Record' row prints a four-way 'or' list of every record type in the community rather than the record actually in context. It appears verbatim on the facility-reservation action and complete screens, the export action screen, the architectural-request start/action/complete screens, the dues-payment action and complete screens, and the committee-decision action and complete screens. On screens whose own title already names the record ('Fence color request', 'Quarterly HOA dues', 'Clubhouse Room A reservation'), the disjunction directly contradicts the header. The same pattern appears in 'Change option — Cancel reservation, request changes, retry payment, or reopen where policy allows', shown identically on a document view, a payment, and a decision queue.
  - RequiredFix: Bind the Record and Change-option rows to the instance in context so each screen names one record ('Clubhouse Room A booking, Sat Aug 15') and offers only the change actions that apply to that record's current state.
- **[critical-blocker] rsvp-model-on-room-reservation**: Facility reservation action sheet asks the user to RSVP Going/Maybe/Not going to their own room booking
  - Workflows: hoa-facility-reservation
  - Description: The sheet titled 'Clubhouse Room A reservation' contains 'Choose attendance — Event date, capacity, location, host note, and reminders are visible before choosing Going, Maybe, or Not going', with a supporting row 'Attendance options: Going, Maybe, Not going, and Change response remain available while the event is open' and 'Member reminder — The saved response keeps calendar, reminder, and capacity status visible.' This is an events/RSVP interaction model rendered on a facility-booking workflow. A homeowner reserving a room selects a time slot and pays a fee; they do not respond to an invitation. The action state of this workflow therefore demonstrates the wrong interaction entirely.
  - RequiredFix: Replace the attendance card with reservation controls: date and time-slot selection, capacity/conflict check against existing bookings, deposit amount, and Confirm reservation / Change slot / Cancel actions.
- **[critical-blocker] cross-workflow-card-bleed**: Start screens foreground a card from a different workflow than the row under test
  - Workflows: hoa-committee-decision, hoa-dues-payment, hoa-facility-reservation, hoa-member-document
  - Description: Four of seven start states put a card belonging to another workflow in the visual foreground: the dues-payment start screen's dominant card is 'Facility reservation a…' with a 'Reserve and pay' button; the member-document start screen's dominant card is 'HOA export' with a download icon and 'Read only'; the facility-reservation start screen's dominant card is 'Architectural request' with 'Submit request'; and the committee-decision start screen's dominant card is 'Member-visible docu…' reading 'Waiting for the member form to be submitted.' In the last case the borrowed card's waiting state also contradicts the actionable 'Approve request' button above it. A reviewer or homeowner landing on these screens would identify the wrong workflow.
  - RequiredFix: Scope each start screen's foreground card to its own workflow, and open the screen at the top so the workflow title is the first thing rendered rather than a mid-scroll fragment.
- **[critical-blocker] approve-without-subject**: Committee decision start offers 'Approve request' with no case, requester, or requested change on screen
  - Workflows: hoa-committee-decision
  - Description: The committee-decision start screen presents a full-width 'Approve request' pill and a 'Request changes' button, but nothing in the viewport identifies what is being approved — no case title, no homeowner name, no requested change, no attachments, no deadline. The visible text is a clipped 'Homeowner sees receipt, decision, document,' fragment, the generic 'Change option' row, and 'Request queue — Approve, reject, request changes, comment, or reopen with a visible owner notification state.' Offering a consequential, owner-notifying approval action against an unnamed subject is the most serious actionability defect in this set.
  - RequiredFix: Render the case under review above the decision controls — case title, homeowner, lot, requested change, attachments, and applicable rule — and disable the approve action until a case is in context.
- **[critical-blocker] spec-prose-instead-of-values**: Descriptive copy asserts that values 'are visible' while none are rendered
  - Workflows: hoa-architectural-request, hoa-committee-decision, hoa-dues-payment, hoa-export-evidence, hoa-facility-reservation
  - Description: Section bodies across the community are written as acceptance criteria rather than product copy, and in several cases the assertion is false as rendered. 'Payer, amount, privacy choice, entitlement, receipt destination, and retry/refund options are visible' appears on a dues checkout showing no amount. 'Scope, redaction, checksum, destination, transfer status, rollback, and audit evidence are visible' appears on an export sheet showing no scope, checksum, or destination. 'Labeled fields, privacy indicators, attachments, assignee, and status are visible' appears on a request sheet with no fields, attachments, or assignee. The result is that the dues workflow never shows a dollar figure, the export workflow never shows a file, and the reservation workflow never shows a date or time.
  - RequiredFix: Replace every '… are visible' / '… remain visible' / '… match the state' sentence with the actual values: amounts, dates, file sizes, checksums, assignee names, and status chips rendered as data.
- **[major] identical-five-row-block-reused**: One five-row block is reused under six different headings across twelve screens
  - Workflows: hoa-architectural-request, hoa-committee-decision, hoa-dues-payment, hoa-export-evidence, hoa-facility-reservation, hoa-owner-notification
  - Description: The Property / Record / History / Owner inbox / Change option list with identical icons and identical body text is rendered under the headings 'Event details', 'Export steps', 'Decision queue', 'Payment summary', 'Submitted details', and 'Submitted request'. Numbering it 1–5 on the export screen turns unrelated content into fake 'Export steps' whose step 5 reads 'Cancel reservation, request changes, retry payment, or reopen where policy allows.' The committee-decision action sheet is pixel-identical to the architectural-request action sheet below the header. Two screens duplicate the block within one scroll view (facility-reservation action, owner-notification action). The effect is that six workflows look like one workflow with six titles.
  - RequiredFix: Give each workflow a purpose-built body — booking slots for reservations, line items for dues, a record picker for exports, vote and conditions for committee decisions — and remove the duplicate render of the identity block within a single scroll view.
- **[major] completion-banners-do-not-state-outcome**: Completion screens confirm that a record was saved without stating what happened
  - Workflows: hoa-architectural-request, hoa-committee-decision, hoa-dues-payment, hoa-export-evidence, hoa-facility-reservation
  - Description: Four completions share the banner body 'The homeowner record now shows owner, amount or decision, status history, and member next steps.' The reservation completion never states the booked date or fee; the dues completion never states the amount paid or the method; the export completion says 'HOA record saved' rather than confirming a file was produced, and never names a filename, size, or destination; the committee-decision completion never says whether the request was approved, rejected, or conditioned. 'amount or decision' is itself an unresolved disjunction. Several of these banners are additionally clipped by the bottom nav mid-sentence.
  - RequiredFix: Write outcome-specific confirmations that lead with the result and its key facts — amount paid and method, room/date booked with confirmation number, file generated with size and destination, decision reached with conditions — rather than a shared 'record saved' sentence.
- **[major] bottom-nav-clipping**: Content and primary controls are severed horizontally by the persistent bottom nav
  - Workflows: hoa-architectural-request, hoa-committee-decision, hoa-dues-payment, hoa-export-evidence, hoa-member-document, hoa-owner-notification
  - Description: Multiple screens render content under the Home/Board/Documents/Payments bar instead of padding above it: 'Edit notice' on the owner-notification start is cut in half; 'Download PDF' on the member-document complete is clipped; 'Change option' on the export complete is cut mid-glyph; the 'Request outcome saved' banners on both the architectural-request and committee-decision completions lose their body text; the 'After delivery' card on the owner-notification complete is truncated to its heading; and the dues-payment start ends in an illegible partial card heading. The bottom nav's rightmost item ('Messages') is also cut off at the right screen edge on several screens.
  - RequiredFix: Add bottom-inset padding equal to the nav bar height to all scroll containers so no content or control renders beneath it, and make the nav bar's item set fit the viewport width.
- **[major] unexplained-state-chips**: 'Minimized', 'Waiting', and 'Read only' render as buttons without explaining the state
  - Workflows: hoa-architectural-request, hoa-committee-decision, hoa-dues-payment, hoa-export-evidence, hoa-facility-reservation, hoa-member-document
  - Description: The recurring dark card shows a 'Minimized' chip with a target-reticle icon next to both 'Tap for expanded view' and a separate 'Expand' button — three overlapping affordances plus a state label styled identically to them. A 'Waiting' pill with a clock icon appears on the export-evidence, architectural-request, and member-document start screens with no adjacent text naming what is pending. On the member-document start screen 'Read only' with an eye icon occupies the primary action slot with no reason given for why the document cannot be edited or acknowledged there. Low-contrast translucent buttons ('Change details', 'Request changes', 'Open document' on blue cards) further blur the enabled/disabled distinction.
  - RequiredFix: Style state indicators distinctly from controls, give each pending state a subject ('Waiting on board review since Aug 4'), state why an item is read-only, collapse the three expand affordances to one, and raise disabled/enabled contrast so the two are distinguishable.
- **[major] empty-canvas-on-start-screens**: Export and member-document start screens end in a large empty region
  - Workflows: hoa-export-evidence, hoa-member-document
  - Description: Both the export-evidence start and member-document start screens place a collapsed 'Local package details' disclosure row roughly two-thirds down the screen and render nothing below it — approximately a third of a page of blank dark canvas above the bottom nav. This is exactly where the export's record list, file size, and destination, or the document's version history and acknowledgement status, would be expected. Combined with the truncated card bodies above ('Data package includes scope, protected fields, a…', 'You can read the current details without changing …'), these screens present less readable content than empty space.
  - RequiredFix: Fill the lower region with the substantive content these workflows own — record list with sizes for the export, version history and acknowledgement state for the document — and expand the truncated card bodies rather than ellipsising them.
- **[major] notice-content-duplicated-in-send-sheet**: Owner-notice send sheet renders the full notice twice and exposes no send control
  - Workflows: hoa-owner-notification
  - Description: On the 'Send owner notice' sheet, 'Sender and recipient — From Cedar Commons HOA Board to Avery Brooks, homeowner for Lot 42' and 'Message body — Your slate gray fence repaint is approved if trim remains cedar and work starts within 30 days' appear in full under 'Notice preview' and again verbatim under 'Delivery actions', with a third 'Delivery' row starting below the fold. Between the copies sit two spec sentences ('Sender, recipient or audience, timestamp, body, and delivery channel are visible', 'Reply, read/unread, archive, mute, block, draft, or resend actions match the message state'). No Send button appears anywhere in the viewport on the sheet whose title is 'Send owner notice'.
  - RequiredFix: Show the notice once, replace the capability sentences with the notice's actual channel and scheduled time as editable values, and surface Send now / Schedule / Save draft controls in the sheet.
- **[minor] debug-chrome-on-notification-start**: 'In-focus product surface' harness strip renders above product content
  - Workflows: hoa-owner-notification
  - Description: The owner-notification start screen shows a dark strip directly beneath the app bar reading 'In-focus product surface' with a target-reticle icon and an 'Expand' link on the right. This is framing language about the capture itself rather than product UI; a homeowner would never see a label describing a surface as 'in-focus'. It occupies the position where the workflow title 'Owner decision notice' should sit — that heading instead appears well below the preview card, mid-scroll.
  - RequiredFix: Remove the in-focus/minimized harness chrome from the product surface and place the 'Owner decision notice' heading above the notice preview card.
- **[minor] low-contrast-secondary-actions**: Secondary actions render at very low contrast against their backgrounds
  - Workflows: hoa-dues-payment, hoa-facility-reservation, hoa-member-document
  - Description: 'Download PDF' on the member-document action sheet is drawn in a dark teal on the dark navy panel, at a contrast level where it reads as decorative text rather than a control, and it sits at the clipped viewport edge. The same low-contrast treatment affects translucent white-on-blue buttons throughout: 'Change details' on the dues and facility-reservation starts, 'Reserve and pay' and 'Pay and save receipt' pills, 'Open document' on the member-document start, and 'Save document' on the document screens all render as pale outlines over the blue card, visually closer to disabled than to available.
  - RequiredFix: Raise secondary-action contrast to meet accessible minimums against both the navy and blue card backgrounds, and reserve the translucent style exclusively for genuinely disabled controls.
- **[minor] unlabeled-card-shell**: A bordered card containing only a spec sentence and 'Save document' has no heading
  - Workflows: hoa-member-document
  - Description: On both member-document screens, a bordered card renders with no title at all — its entire content is the sentence 'Document access, version, download, save, share, and access-request options are visible.' followed by a 'Save document' button. Without a heading the card reads as an empty shell wrapped around a caption, and it sits directly above a separate card that is titled 'Document access' and covers overlapping ground ('Open, download, acknowledge, request access, or view version history'), so two adjacent cards describe the same capability set with only one of them labeled.
  - RequiredFix: Merge the unlabeled card into the titled 'Document access' section so save, download, share, and access-request controls appear once under one heading.

## Chess Club (8 findings)

- **[critical-blocker] six-workflows-share-match-result-screens**: Six workflows' action and complete states are the same match-result screen
  - Workflows: chess-club-night, chess-discussion-thread, chess-export-package, chess-pairing-queue, chess-rankings-table, chess-rules-documents
  - Description: The action captures for chess-rankings-table, chess-club-night, chess-pairing-queue, chess-export-package, chess-rules-documents and chess-discussion-thread are byte-identical (hash e0033376…), and their complete captures are likewise byte-identical (hash 0c88ea55…). All twelve render the same content: heading 'Chess Club home', chips 'Board 1', 'Round 3', '12 players', 'Correction option', 'Players — Ava vs Liam, Board 1, friendly ladder night.', 'Result — Ava wins 1-0; standings and next pairing will update.', and the actions 'Edit score', 'Dispute result', 'Open standings'. No rankings table, calendar, queue, export artifact, document, or thread appears anywhere in the set. Six of eight workflows therefore have no evidence of their own behaviour.
  - RequiredFix: Each workflow must render and be captured showing its own subject matter: a ranked ladder for rankings-table, dated club-night entries for club-night, an ordered waiting list for pairing-queue, a named export artifact for export-package, a document list/body for rules-documents, and authored posts for discussion-thread — each with a workflow-specific title rather than 'Chess Club home'.
- **[critical-blocker] meetup-reuses-result-captures**: chess-match-meetup action and complete are the chess-match-result captures
  - Workflows: chess-match-meetup
  - Description: chess-match-meetup_action is pixel-identical to chess-match-result_action (hash 492aa15c…) and chess-match-meetup_complete is pixel-identical to chess-match-result_complete (hash 845b7a08…). Both are titled 'Board 1 match result' and show a concluded score ('Ava wins 1-0'), with the complete state banner reading 'Chess result saved'. A meetup workflow — arranging when and where a game happens — is never depicted; there is no date, time, venue, invitation or confirmation anywhere in the three meetup rows.
  - RequiredFix: Build and capture a real meetup surface: proposed opponent, date/time, board and venue, with propose/accept/reschedule actions and a completion that confirms the scheduled game rather than a saved score.
- **[critical-blocker] wrong-completion-banner-every-workflow**: Every complete state announces 'Chess result saved' regardless of workflow
  - Workflows: chess-club-night, chess-discussion-thread, chess-export-package, chess-match-meetup, chess-match-result, chess-pairing-queue, chess-rankings-table, chess-rules-documents
  - Description: All eight workflows' complete captures display the banner 'Chess result saved — Chess Club shows board, players, result, correction option, standings impact, and next pairing.' On the export-package, rules-documents and discussion-thread rows this actively misinforms: the user is told a chess score was saved when they exported a package, updated a document, or posted a message. The supporting card compounds it with 'The saved result keeps status, history, and change actions visible for this community task.', where 'this community task' is a generic stand-in for the thing the user actually did.
  - RequiredFix: Derive the completion banner from the workflow that completed and name the concrete object — e.g. 'Rankings published', 'Club night scheduled for Thu 14 Aug', 'chess-club-results.csv ready', 'Club rules v3 saved', 'Reply posted' — and drop the 'this community task' placeholder.
- **[major] record-match-result-cta-on-every-card**: 'Record match result' is the CTA on unrelated cards, and its icon contradicts its label on export
  - Workflows: chess-club-night, chess-discussion-thread, chess-export-package, chess-match-meetup, chess-match-result, chess-pairing-queue, chess-rankings-table, chess-rules-documents
  - Description: Every minimized card in this community carries the identical four-button shell — 'Tap for expanded view', 'Member form', 'Expand', 'Record match result' — regardless of the card's subject. It appears on 'Club night calendar', 'Rules documents', 'Pairing discussion' and 'Organizer pairing queue' cards, where recording a chess score is not a valid action. On chess-export-package_start the same full-width button renders a download/export glyph while still reading 'Record match result', so the icon and label describe different actions.
  - RequiredFix: Give each card the action that belongs to its own content type ('Open rules', 'Reply', 'RSVP', 'Export', 'View queue') and make the button icon match its label everywhere.
- **[major] spec-restating-and-mismatched-copy**: Body copy restates acceptance criteria and is mismatched to its cards
  - Workflows: chess-discussion-thread, chess-export-package, chess-match-meetup, chess-match-result, chess-pairing-queue, chess-rankings-table, chess-rules-documents
  - Description: Across the set, copy describes mechanisms rather than content: 'Confirm player names, round, board, result, edit option, and standings update before saving.', 'Choice and standing — Nominations, ballot options, selected title, match result, or standings are visible before saving.', 'Open result — Change vote, dispute score, view results, and next meeting or round stay available.', 'Member form captures labeled details, privacy c…', and the engine-jargon strips 'In-focus product surface' and 'Local package details'. Ballot and nomination vocabulary appears in a chess community that has no voting workflow. Worse, on chess-rankings-table_start the card titled 'Pairing discussion' is subtitled 'Ad preference and sponsored-message beh…', which is advertising-consent copy on a chess pairing card.
  - RequiredFix: Replace criteria-restating and cross-domain copy with sentences describing the actual chess content on each card, and remove internal strings like 'In-focus product surface' and 'Local package details' from user-visible surfaces.
- **[major] clipping-truncation-and-duplicate-headings**: Clipped headers, truncated titles, actions cut by the nav bar, and duplicate 'Club board' headings
  - Workflows: chess-club-night, chess-discussion-thread, chess-export-package, chess-match-meetup, chess-match-result, chess-pairing-queue, chess-rankings-table, chess-rules-documents
  - Description: Start captures are mid-scroll with the leading card header sliced by the app bar (chess-match-meetup_start, chess-match-result_start). Titles and subtitles truncate mid-word ('Organizer pairing que…', 'Member form captures labeled details, privacy c…'). On chess-rules-documents_start a 'Member tools' heading is cut to unreadable glyph tops behind the bottom nav. Every complete sheet cuts two follow-up buttons in half behind the Home/Matches/Calendar/Rankings bar, so the terminal state offers no reachable next step. Separately, chess-club-night_start, chess-pairing-queue_start, chess-export-package_start, chess-rules-documents_start and chess-discussion-thread_start each stack two sibling cards both titled 'Club board', destroying hierarchy, and the brown-on-brown sheet gives the inner card almost no edge contrast.
  - RequiredFix: Add bottom padding so sheet actions clear the navigation bar, capture start states from the top of the scroll, allow card titles to wrap to two lines instead of ellipsizing, deduplicate the sibling 'Club board' headings, and raise card-versus-sheet contrast in the brown theme.
- **[major] unexplained-not-available-state**: 'Not available' cards cite an unnamed role and still offer a form button
  - Workflows: chess-match-result
  - Description: On chess-match-result_start, the 'Live rankings table' and 'Club night calendar' cards each show a 'Not available' chip beside the identical sentence 'Another community role manages this item.' No role is named and no path to request access is offered, so the restriction is an unexplained dead end — particularly in this legacy schema where there is no identity or role display to reconcile it against. Both cards simultaneously present a 'Member form' button while marked not available, which contradicts the chip.
  - RequiredFix: Name the managing role concretely (e.g. 'Managed by the club organizer') and either hide or visibly disable the secondary buttons on cards marked 'Not available' so the affordances agree with the state.
- **[minor] nav-tab-never-reflects-workflow**: 'Home' stays the selected nav tab on all 24 screens, including the rankings and calendar workflows
  - Workflows: chess-club-night, chess-match-meetup, chess-rankings-table
  - Description: The bottom navigation offers Home, Matches, Calendar and Rankings, and the 'Home' pill is rendered as the filled/selected tab on every one of the 24 captures — including chess-rankings-table_start/action/complete and chess-club-night_start/action/complete, whose workflows correspond directly to the Rankings and Calendar tabs. On chess-match-meetup_start and chess-rankings-table_start the 'Rankings' pill is additionally clipped by the right screen edge.
  - RequiredFix: Select the nav tab that corresponds to the surface being shown, and size the tab row so the fourth pill is not clipped at the right edge.

## Camera Club (11 findings)

- **[critical-blocker] primary-actions-never-visible**: No workflow renders its primary action as a tappable control
  - Workflows: critique-submission, gear-loan-request, photo-walk-rsvp
  - Description: Across all nine content screens, every action is described in prose but none is rendered as a button. The RSVP start screen instructs 'Choose Going, Maybe, or Not going' yet shows no such buttons -- only a '← Change response' control sliced through the middle of its letterforms by the bottom nav bar. The RSVP action sheet lists 'Going, Maybe, Not going, and Change response remain available while the event is open.' as body text. The critique action sheet lists 'Edit, withdraw, approve, reject, request changes, reopen, or appeal' as text. The gear-loan screens name 'Mark returned, change pickup, or cancel request' with none rendered. The only genuine primary button in the entire set is the teal 'Start camera validation report' pill on the validation action sheet.
  - RequiredFix: Render each workflow's state-appropriate actions as real, visible, tappable controls positioned clear of the bottom nav bar, and delete the prose that merely enumerates action names.
- **[major] content-clipped-by-nav-bar**: Cards and confirmation banners are truncated mid-word by the bottom tab bar
  - Workflows: camera-validation-report, critique-submission, gear-loan-request, photo-walk-rsvp
  - Description: The Home/Walks/Critique/Gear tab bar overlays the scroll content on five screens. The RSVP start screen's '← Change response' button is cut horizontally through its glyphs. The RSVP complete screen's confirmation banner ends at 'change-response option visibl'. The critique start screen's 'Request queue' card is cut at 'Submission enters Avery's queue; you can edit' with a strip of glyph-tops below. The gear-loan start screen's 'Submission handoff' card is cut at '...contact preference, and'. The validation start screen is also clipped at the top, showing a sliced line 'Useful actions for this community.' under the app bar. In every case the clipped element is the confirmation or next-step message -- the most state-critical content on the screen.
  - RequiredFix: Add bottom padding equal to the tab bar height to the scroll view (and top padding for the app bar) so no card, banner, or button is ever bisected by system chrome.
- **[major] detail-rows-duplicated-in-action-sheets**: Action sheets render the same detail rows twice in one viewport
  - Workflows: critique-submission, gear-loan-request, photo-walk-rsvp
  - Description: The RSVP action sheet shows an 'Event details' card with Route/Host/Capacity and then repeats a sub-row also labelled 'Event details' inside 'Choose attendance', restating 'Dock 4, mural loop, riverfront pier, 75-minute walk.' and 'Avery Kim hosts and shares the rain-plan update.' verbatim. The gear-loan action sheet shows Item / Pickup and return / Contact in 'Submitted details' and then repeats all three inside 'Submitted request', with '35mm prime lens, clean condition, filter included.' appearing twice in one screenshot. The critique action sheet repeats 'Image and title' after already listing it as queue item 1.
  - RequiredFix: Render each detail row exactly once per screen; use the freed space for the action controls the sheet is supposed to provide.
- **[major] self-describing-meta-copy**: Body and confirmation copy describes the UI's own fields instead of the state
  - Workflows: camera-validation-report, critique-submission, gear-loan-request, photo-walk-rsvp
  - Description: Instead of stating outcomes, cards enumerate their own components. Examples: 'Title, date, time, location, host, capacity, and current attendance are visible before responding.' (RSVP action); 'Your photo walk attendance is confirmed with status, route, host, capacity, reminder, and change-response option visible.' (RSVP complete); 'The critique shows image, prompt, consent, reviewer queue, edit option, and comment state.' (critique complete); 'The loan request status shows item, owner, borrower queue, pickup, protected contact, and return option.' (gear complete); 'This view shows editable details, current progress, and the next step before saving.' (validation action and complete). In several cases the enumerated items are not actually present -- the critique complete screen names an 'edit option' that is not rendered, and the gear complete screen names 'borrower queue' without showing a position.
  - RequiredFix: Replace field-inventory sentences with outcome copy tied to the data ('You're going -- Sat 4:30 PM at Dock 4', 'Sent to Avery, awaiting critique', 'Awaiting Sam's approval, 1st in queue') and show the referenced values rather than naming them.
- **[major] complete-states-show-no-state-change**: Complete states do not reflect the recorded outcome
  - Workflows: camera-validation-report, photo-walk-rsvp
  - Description: The RSVP complete screen claims attendance 'is confirmed' but never names the chosen response and still displays '12 going / 4 open' -- byte-identical to the start screen -- so the RSVP appears to have changed nothing. The validation-report start, action, and complete screens all carry the same 'Details ready / Editable / State saved' chips, so the chips convey no state; 'State saved' even appears on the start screen before any action has been taken.
  - RequiredFix: Have the complete state mutate the visible data: show the selected RSVP as a status chip and an updated attendance count, and make state chips differ between start, in-progress, and complete.
- **[critical-blocker] validation-report-is-scaffolding-not-content**: camera-validation-report workflow has no community content and repeats one string three times
  - Workflows: camera-validation-report
  - Description: All three validation-report screens are engine tooling rather than club content: title 'Camera validation report', body 'Mark the Camera Club package validation complete.', chips 'Details ready / Editable / State saved', and a generic 3x3 dot-grid glyph. The complete screen prints 'Camera Club validation status updated.' three times, each inside its own bordered card shell (hero subtitle, 'Saved details', 'Saved'). The start screen leaves roughly a quarter of the viewport as empty dark canvas beneath a collapsed 'Local package details' row. The action screen renders 'Edit details' in dark teal on a dark slate card at barely-legible contrast, and the complete screen styles a '✓ Saved' status label as a bordered chip indistinguishable from the adjacent tappable chips.
  - RequiredFix: Either give this workflow real subject matter (what is being validated, by whom, which checks pass or fail, with a photography-appropriate icon) or remove it from the member-facing catalogue; fix the 'Edit details' contrast, expand or drop the empty 'Local package details' section, and state the saved fact once.
- **[major] cross-workflow-boilerplate-reuse**: Identical 'Submitted request' boilerplate shared between critique and gear-loan action sheets
  - Workflows: critique-submission, gear-loan-request
  - Description: The critique-submission and gear-loan-request action sheets contain the same three sentences word for word: 'Labeled fields, privacy handling, assignee, and status are visible before submitting or editing.', 'Submitted fields: Labeled fields, privacy indicators, attachments, assignee, and status are visible.', and 'Review path: Edit, withdraw, approve, reject, request changes, reopen, or appeal actions match the state.' Nothing in that text references a photo, a lens, Avery, or Sam. The seven-action 'Review path' list is also workflow-agnostic and never resolves to which action currently applies.
  - RequiredFix: Write per-workflow copy for the submitted-request card and show only the actions valid for the current state instead of a shared catalogue of every possible transition.
- **[minor] debug-scaffolding-strip-visible**: 'In-focus product surface / Expand' developer strip renders as product UI
  - Workflows: camera-validation-report, critique-submission, gear-loan-request
  - Description: Three screens show a dark strip at the top of the content area reading 'In-focus product surface' with an 'Expand' link at the right, above the workflow card. This is harness vocabulary, not community content, and it occupies the position a user would read as a section heading. It is absent from the complete-state screens, so its presence is inconsistent as well as inappropriate.
  - RequiredFix: Remove the 'In-focus product surface' strip from member-facing builds, or replace it with a real content heading.
- **[major] missing-photo-thumbnail-in-critique**: Photo-critique screens show an 'Image and title' row with no image
  - Workflows: critique-submission
  - Description: All three critique screens include a row labelled 'Image and title' reading 'Evening Reflection, street portrait, uploaded by Mina.' but no thumbnail or image placeholder is rendered anywhere. For a photography club's critique workflow, the photograph is the subject of the entire interaction; describing it in text only leaves the screen looking like a form record rather than a critique surface.
  - RequiredFix: Render the submitted photograph as a thumbnail (or an explicit image placeholder with its dimensions/filename) alongside the title on all three critique states.
- **[minor] sheet-accent-colors-diverge-from-community-theme**: Action sheets use purple and olive-green headers unrelated to the community's slate-blue identity
  - Workflows: critique-submission, gear-loan-request
  - Description: Every persistent Camera Club surface uses a slate-blue app bar, and the RSVP sheet correctly uses a mid-blue header. The critique-submission sheet instead uses a saturated purple header and the gear-loan sheet an olive-green one, with matching accent banners on their complete screens. Because each workflow gets a different unrelated hue, the sheets read as belonging to different applications rather than to Camera Club.
  - RequiredFix: Derive workflow accents from the community's slate-blue palette (varying tint or using a single semantic success colour) so all four workflows read as one product.
- **[minor] state-inappropriate-actions-listed**: Post-completion actions described on start states
  - Workflows: camera-validation-report, gear-loan-request, photo-walk-rsvp
  - Description: The gear-loan start screen, whose own handoff card says 'Decision pending: the lender reviews borrower name, pickup window, contact preference, and...', simultaneously lists 'Return option: Mark returned, change pickup, or cancel request.' -- 'Mark returned' presupposes a loan that has already been picked up. The RSVP start screen similarly surfaces '← Change response' before any response exists. The validation-report start screen shows a 'State saved' chip before its 'Start camera validation report' button has been pressed.
  - RequiredFix: Gate action descriptions and state chips on the current lifecycle stage so start states show only the actions actually reachable from a start state.

## Masjid Nur (9 findings)

- **[critical-blocker] spec-prose-rendered-as-product-copy**: Acceptance-criteria sentences are rendered verbatim as user-facing copy
  - Workflows: mosque-care-request, mosque-donation-payment, mosque-donor-visibility, mosque-neutral-notification, mosque-search-ai-citation, mosque-volunteer-signup
  - Description: Across at least fourteen screens the body text lists what the UI is supposed to contain instead of containing it: "This view shows editable details, current progress, and the next step before saving." (donor-visibility action/complete, volunteer-signup action/complete), "Member-visible summary, protected details, receipt or citation, and recipient preview stay visible before sending." (donor-visibility action, care-request start), "Payer, amount, privacy choice, entitlement, receipt destination, and retry/refund options are visible." (donation-payment action, neutral-notification action), "Amount, payer, recipient, method, visibility, and retry/refund path are visible before checkout.", "Edit, withdraw, approve, reject, request changes, reopen, or appeal actions match the state." (care-request action), and "The user sees the query, answer, citations, source visibility, and follow-up options." (search-ai-citation action) — the last written in third person about the user. The repeated strings "Member-facing text stays neutral and does not reveal sensitive care details." and "Private notes are visible only to the care team or donor account owner." appear on eight or more screens. In several cases the sentence is self-refuting: it claims an amount or assignee is visible while none is rendered.
  - RequiredFix: Replace every field-list/criteria sentence with the actual community value it describes — the amount, the private note, the assignee, the status, the query, the message body. Where no value exists for a slot, render a real empty state rather than a description of the slot.
- **[critical-blocker] payment-surface-without-amount**: Donation workflow shows no amount, payer, method, or receipt on any of its three states
  - Workflows: mosque-donation-payment, mosque-neutral-notification
  - Description: All three donation-payment screens are titled "Sadaqah donation receipt" and carry a card headed "Payment summary", yet no currency figure, payer name, payment method, fund, receipt number, or transaction timestamp appears on any of them. The start screen's own subtitle promises "Amount, donor visibility, receipt destination, and giving history are visible before saving." and the action sheet asserts it twice more. The complete state is unchanged from the start state except for a clipped "Masjid preference confirmed" card, which confirms a visibility preference rather than a completed gift, so there is no evidence a payment ever occurred. The same empty "Payment summary" card is also reused as the body of all three neutral-notification screens.
  - RequiredFix: Render the real transaction on all three states: amount and currency, fund/recipient, payment method, donor visibility choice, receipt destination on start/action; and on complete, a paid badge with amount, paid-at timestamp, receipt number and a view-receipt link.
- **[critical-blocker] cross-workflow-content-bleed**: Five workflows render another workflow's surface, and two share byte-identical captures
  - Workflows: mosque-care-request, mosque-discussion-thread, mosque-donation-payment, mosque-donor-visibility, mosque-event-rsvp, mosque-neutral-notification, mosque-search-ai-citation
  - Description: The event-rsvp start screen renders the knowledge/search surface ("Knowledge and search — Cited answers, source visibility, and saved digests." plus "Answer with sources" and "Iftar announcement answer") with no event details or RSVP control. The discussion-thread start screen renders the donor-visibility save card plus an ad-preference card, with no thread, post, author, or composer. The discussion-thread action and complete captures are byte-identical to the search-ai-citation action and complete captures (SHA-256 fe1bed61f8ab… and de1a59a33d14… each appear twice in the directory), so the discussion-thread workflow has no distinct render at all. The three neutral-notification screens combine a "Neutral care receipt" title, the announcement-search subtitle, and Payment summary/Checkout cards. Four unrelated complete states share the single string "Masjid preference confirmed".
  - RequiredFix: Bind each workflow row to its own surface and re-capture: RSVP start must show the event and Going/Maybe/Not going; discussion-thread must show the thread, posts and composer across all three states; neutral-notification must show a message, sender and delivery status. Replace the shared "Masjid preference confirmed" completion string with a per-workflow confirmation.
- **[major] ad-preference-card-in-mosque-community**: Sponsored-message/ad-preference card appears inside Masjid Nur
  - Workflows: mosque-discussion-thread, mosque-donor-visibility
  - Description: Both the donor-visibility start and discussion-thread start screens render a dark card titled "Community discussio…" whose body reads "Ad preference and sponsored-message beh…" with buttons "Expand" and "Open ad details". Ad preferences and sponsored messaging are not part of any of the eight workflows under review and are jarring inside a mosque community surface. Both the title and body are ellipsis-truncated, so even the leaked content is unreadable.
  - RequiredFix: Remove the ad-preference/sponsored-message card from these surfaces entirely, and give any remaining minimized cards titles short enough to render without truncation.
- **[major] clipping-and-chip-overlap**: Headers bisected by chrome, chips colliding with list rows, and text cut off at the nav bar
  - Workflows: mosque-announcement, mosque-care-request, mosque-donation-payment, mosque-donor-visibility, mosque-neutral-notification, mosque-search-ai-citation, mosque-volunteer-signup
  - Description: care-request start opens with the words "Public summary" sliced horizontally in half by the green app bar. On donation-payment start/complete and all three neutral-notification screens the chip block (Community care / Privacy checked / Receipt record / Member inbox) sits flush against the "Public summary" row with no gutter, the chip border touching the row icon, and wraps into a ragged 1/2/1 layout. Bottom-of-screen text is repeatedly cut mid-glyph by the Home/Calendar/Giving/Care bar: "Update privacy" on search-ai-citation start, "Member tools" on care-request start, "Saved answer" on search-ai-citation complete, "...clear next step" on care-request complete, "Volunteer signup confirmed" body on volunteer-signup complete, and the "Masjid preference confirmed" body on donation-payment and neutral-notification complete. Titles truncate as "Community discussio…", "Anonymous donor vis…", "Neutral care notificati…".
  - RequiredFix: Add bottom padding equal to the nav bar height on all scroll views, restore vertical spacing between the chip wrap and the following list rows, scroll start states to the top of their content so titles are visible, and widen or wrap card titles so they do not ellipsize.
- **[major] rsvp-off-palette-and-missing-response-selector**: RSVP screens use a blue palette and offer no Going/Maybe/Not going control
  - Workflows: mosque-event-rsvp
  - Description: The event-rsvp action and complete screens render in blue (blue header, blue cards, navy sheet) while the Masjid Nur app bar directly above them and every other screen in the set use the community's green accent. Separately, the action screen's card headed "Choose RSVP response" explains all three options — "Going reserves a spot; maybe keeps the event visible without taking capacity; not attending releases your seat." — but renders only a single "RSVP to event" button, so the choice cannot be made. The complete screen then never states which response was recorded, and the "86 of 120 spots" capacity chip is unchanged between the action and complete states despite a confirmed RSVP.
  - RequiredFix: Re-theme the RSVP surfaces to the community green accent, render three selectable Going / Maybe / Not going controls on the action state, show the chosen response on the complete state, and decrement the remaining-spots count after a Going response.
- **[major] duplicated-sentences-and-repeated-card-shells**: Same sentence printed two or three times per screen inside repeated generic card shells
  - Workflows: mosque-announcement, mosque-care-request, mosque-donation-payment, mosque-donor-visibility, mosque-event-rsvp, mosque-neutral-notification, mosque-search-ai-citation, mosque-volunteer-signup
  - Description: Several screens say one thing repeatedly across stacked shells. volunteer-signup complete prints "You are signed up for iftar setup with protected phone, shift time, role, and coordinator follow-up." three times (header card, "Signup record", "Volunteer signup confirmed"); event-rsvp complete prints its confirmation sentence three times; donor-visibility complete and announcement complete each print theirs twice; care-request action shows a "Public summary" heading three times. Structurally, the identical four-row "Answer with sources" card appears on six screens across three workflows and the identical five-row "Payment summary" card on six more, so distinct workflows read as one template re-skinned.
  - RequiredFix: Show each fact once, in the highest-level card that owns it, and give secondary cards distinct content (history, related records, next steps) rather than restating the headline sentence.
- **[major] complete-states-lack-status-markers**: Completion states carry no badge, timestamp, or resulting value
  - Workflows: mosque-announcement, mosque-care-request, mosque-donation-payment, mosque-donor-visibility, mosque-event-rsvp, mosque-neutral-notification, mosque-search-ai-citation
  - Description: Most complete screens can only be inferred from verb tense. announcement complete still calls itself "Announcement composer for Masjid Nur members." with no Sent badge, sent-at time, or delivery count, and offers "Preview announcement" as its only action on an already-published message. donor-visibility complete never states whether the donation ended up anonymous or attributed. donation-payment, care-request, neutral-notification and search-ai-citation complete states all share the single marker "Masjid preference confirmed", none of which matches its workflow. event-rsvp complete says "RSVP confirmed" without naming the response chosen.
  - RequiredFix: Give every complete state an explicit status badge with a timestamp and the resulting value (published at, saved as anonymous, paid, going, request #, delivered to N members), and swap pre-action CTAs for post-action ones.
- **[minor] ambiguous-disabled-secondary-actions**: Secondary buttons rendered in near-invisible teal with no disabled explanation
  - Workflows: mosque-announcement, mosque-care-request, mosque-donor-visibility, mosque-event-rsvp, mosque-volunteer-signup
  - Description: On donor-visibility action, announcement action, volunteer-signup action and event-rsvp action, a second control sits below the filled primary in a dim teal on dark-green that is barely legible — "Update privacy", "Preview announcement", "Edit availability", "Change response" respectively — and in several cases duplicates a normally-styled button already present higher on the same screen. Nothing indicates whether these are disabled or merely low-emphasis. Separately, all four start-state primaries ("Save visibility", "Publish announcement", "Submit signup", "Submit request") are unfilled outline pills on same-hue cards, reading as inert compared with the filled teal treatment the identical buttons receive on the action screens.
  - RequiredFix: Render each secondary once at an accessible contrast ratio, use the platform disabled style with an adjacent reason when a control genuinely is disabled, and give start-state primary CTAs the same filled treatment they have on the action screens.

## Member Social Space (12 findings)

- **[critical-blocker] message-stream-evidence-duplicated**: platform-message-stream reuses platform-messages-entry screenshots and shows invite content at start
  - Workflows: platform-message-stream
  - Description: The message-stream action screenshot is byte-identical to the messages-entry action screenshot (both hash b2b647169c633d36215dcec3936827bc0d27fb369c064bab2ac8dc8c535edc3f) and the message-stream complete screenshot is byte-identical to the messages-entry complete screenshot (both hash 94f3b1d3dabbde57109e81cb83ddff97927cef9596e2056108e646fad9248da5). Both show a single 'Message thread' with 'Maya Chen -> Jordan Lee' and 'Can you bring the sign-in sheets before the meetup?'. The message-stream start screenshot, meanwhile, shows connection-invite content: the heading 'Member connection invite', 'Invite body — Message: "Want to coordinate the Saturday welcome table together?"', and a 'Cancel invite' chip. No screenshot in this row group shows a stream of multiple messages.
  - RequiredFix: Capture distinct message-stream screenshots showing an actual multi-message stream (several senders, timestamps, read markers) for start, action and complete, and fix the start capture so it opens on stream content rather than the connection-invite surface.
- **[major] complete-state-confirmation-clipped**: Every complete-state screen clips its confirmation card behind the bottom nav
  - Workflows: platform-blocked-target, platform-connection-invite, platform-connections-entry, platform-in-stream-ad, platform-sensitive-no-fill, platform-top-banner-no-fill
  - Description: The confirmation that distinguishes each complete state is truncated by the Home/Calendar/Admin/Messages bar in all six complete screenshots: 'Banner no-fill recorded / No sponsored message is shown' is cut mid-line; 'Sensitive no-fill recorded' shows only its title row; the in-stream-ad confirmation is reduced to an untitled brown sliver; 'Blocked state confirmed' is cut at 'and appeal/unblock option'; 'Connection invite sent' is cut at 'state, cancel option, and inbox continuation' on both the connections-entry and connection-invite complete screens.
  - RequiredFix: Add bottom padding equal to the nav bar height (or auto-scroll the confirmation into view) so the completion card is fully readable above the navigation bar in every complete state.
- **[major] acceptance-criteria-as-ui-copy**: Requirement/acceptance-criteria prose is rendered as member-facing UI copy
  - Workflows: platform-blocked-target, platform-connection-invite, platform-connections-entry, platform-in-stream-ad, platform-messages-entry, platform-sensitive-no-fill, platform-top-banner-no-fill
  - Description: Across nearly every screen, body text enumerates what the screen contains instead of being the content: 'Sender, recipient or audience, timestamp, body, and delivery channel are visible.', 'Reply, read/unread, archive, mute, block, draft, or resend actions match the message state.', 'Show sender, protected recipient, attempted invite body, disabled send option, safety reason, appeal/unblock option, and audit timestamp.', 'Review reserved slot behavior, disclosure, no-fill reason, entitlement, receipt, and restore option.', 'Entitlement scope, renewal or expiry, receipt link, restore/manage action, and affected ad slots are visible.' Section subtitles use the same voice ('Show a clear no-sponsored-message state...', 'Show the member why ads are suppressed...').
  - RequiredFix: Replace every field-enumeration sentence with the actual data or with short member-facing copy, e.g. render the entitlement status and a receipt link instead of stating that they 'are visible'.
- **[major] described-but-missing-controls**: Disabled, appeal, dismiss and manage controls are described in text but never rendered
  - Workflows: platform-blocked-target, platform-connection-invite, platform-connections-entry, platform-in-stream-ad, platform-messages-entry, platform-sensitive-no-fill, platform-top-banner-no-fill
  - Description: Screens promise controls that do not exist in the pixels. The blocked-target screens name a 'disabled send option' and an 'appeal/unblock option' with no disabled Send button and no appeal affordance anywhere. The ad screens state 'dismiss, report, and manage-ad controls sit inside the stream' and 'restore/manage action ... are visible' with no Dismiss, Report, Manage or Restore button. The message and invite sheets say 'Reply, mute, archive, or block remain available.' and 'choose reply, mute, archive, or block' while rendering none of those controls, and never offer Accept/Decline on the invite surfaces despite showing an 'Accepted' state.
  - RequiredFix: Render each named control as a real element in its correct state — a visibly disabled Send with inline reason plus a working Appeal/Unblock on the block guard, and Dismiss/Report/Manage ad on the sponsored surfaces.
- **[major] reused-generic-thread-actions-block**: The same generic 'Thread actions' block is reused on workflows where its verbs do not apply
  - Workflows: platform-blocked-target, platform-connection-invite, platform-connections-entry, platform-messages-entry
  - Description: An identical 'Thread actions — Read sender, body, timestamp, and choose reply, mute, archive, or block.' card with the same 'Sender and receiver' and 'Conversation state' rows appears on the messages-entry, blocked-target, connections-entry and connection-invite action sheets. On the 'Connection safety guard' sheet it advertises reply and resend on a conversation whose entire premise is that sending is blocked; on the invite sheets it offers thread verbs where accept/decline are needed. Each of these sheets then re-renders its own 'Sender and recipient' and body rows a second time inside that block, duplicating the card above.
  - RequiredFix: Give each workflow an action block scoped to its own verbs (accept/decline for invites, appeal/report for blocks) and remove the duplicated sender/body restatement from the lower card.
- **[major] repeated-minimized-card-shells**: Stacked identical minimized card shells with a shared truncated subtitle
  - Workflows: platform-connection-invite, platform-connections-entry, platform-messages-entry, platform-sensitive-no-fill
  - Description: The start screens stack two and three cards that differ only by title: 'Connections', 'Connection invite' and 'Message stream rend…' each carry the same 'Minimized' chip, the same 'Tap for expanded view' and 'Community setting' chips, the same 'Expand' button, and the byte-identical truncated subtitle 'Member communication stays scoped to the com…'. The 'Message stream rend…' and 'Sensitive page ad sta…' titles are themselves truncated mid-word.
  - RequiredFix: Give each card a distinct, complete subtitle describing its own content, widen or wrap the title so it is not truncated mid-word, and replace the stacked shells with real content rows (conversations, connections, pending requests).
- **[minor] low-contrast-workflow-hero-titles**: Workflow hero titles rendered in low-contrast accent tints on the dark panel
  - Workflows: platform-in-stream-ad, platform-sensitive-no-fill, platform-top-banner-no-fill
  - Description: 'Top banner no-fill' (dark green), 'Sensitive page ad guard' (muted purple) and 'Sponsored stream message' (dark ochre) are all drawn against the navy panel at noticeably lower contrast than the white section headings directly below them, with even fainter subtitles. On each screen the workflow's own title is the least legible text on the screen and reads as disabled rather than as a heading.
  - RequiredFix: Lighten the accent tints used for hero titles and subtitles on dark panels so they meet normal on-dark contrast, keeping the accent hue for identity.
- **[major] wrong-nav-tab-highlighted**: Calendar tab highlighted on the blocked-connection guard completion screen
  - Workflows: platform-blocked-target
  - Description: On platform-blocked-target-complete the bottom navigation shows the 'Calendar' pill filled solid blue while the content is 'Blocked connection guard' / 'Blocked state confirmed'. Every other in-app screen in this set highlights 'Home', so the navigation is asserting a location that contradicts the content.
  - RequiredFix: Keep the navigation selection in sync with the displayed surface so the messaging/safety guard does not render with the Calendar tab active.
- **[major] contradictory-state-pairs**: Complete screens show mutually exclusive states side by side
  - Workflows: platform-connection-invite, platform-connections-entry, platform-message-stream, platform-messages-entry
  - Description: platform-messages-entry-complete and platform-message-stream-complete display 'Today 9:12 AM, unread until opened.' and 'Reply, mute, archive, or block remain available.' immediately above a 'Message sent' confirmation. platform-connection-invite-complete shows a check-icon 'Accepted — Accepted invites open a thread and connection history.' immediately above 'Connection invite sent'. In each case the screen simultaneously reports two states that cannot both be current.
  - RequiredFix: Update the conversation card to reflect the post-action state (read/sent, or sent-and-pending) so the confirmation and the card above it agree.
- **[major] start-screens-open-mid-scroll**: Start states open mid-scroll on clipped headings and orphaned fragments
  - Workflows: platform-connection-invite, platform-connections-entry, platform-messages-entry
  - Description: platform-messages-entry-start opens on the orphan sentence 'preserves read/unread receipt.' directly under the app bar, with an 'Archive thread' chip and 'Reply' pill belonging to an invisible thread. platform-connections-entry-start and platform-connection-invite-start both open on a 'Conversation actions' heading sheared to its descenders. In all three the user's first view is a fragment rather than the top of the workflow.
  - RequiredFix: Ensure start states render scrolled to the top of the workflow surface with its title and primary content in view.
- **[minor] empty-region-on-invite-start**: Large empty region below the collapsed 'Local package details' row
  - Workflows: platform-connection-invite
  - Description: platform-connection-invite-start ends with a collapsed 'Local package details' disclosure row followed by roughly a quarter-screen of blank navy before the bottom navigation, in a start state where an invite composer or recipient list would be expected.
  - RequiredFix: Fill the invite start state with the composer (recipient picker, message field, mutual-community context) so no large dead region remains above the nav bar.
- **[minor] messages-nav-pill-clipped**: 'Messages' bottom-nav pill sliced by the right screen edge
  - Workflows: platform-blocked-target, platform-connection-invite, platform-connections-entry, platform-in-stream-ad, platform-message-stream, platform-messages-entry, platform-sensitive-no-fill, platform-top-banner-no-fill
  - Description: On the majority of in-app screens the fourth navigation pill renders as 'Messages' with its rounded container cut off by the right edge of the viewport, unlike the fully contained Home, Calendar and Admin pills. On a small number of screens (top-banner-no-fill-complete, sensitive-no-fill-complete, in-stream-ad-complete) the row fits, indicating an inconsistent horizontal layout rather than an intentional scrollable nav.
  - RequiredFix: Constrain the navigation row to the viewport width (or make it explicitly scrollable) so no pill is clipped by the screen edge.

## Ad-Free Community (10 findings)

- **[critical-blocker] complete-state-not-evidenced-on-checkouts**: Both checkout workflows show no completion evidence; member checkout complete is identical to its start
  - Workflows: ad-off-community-checkout, ad-off-member-checkout
  - Description: ad-off-community-checkout complete and ad-off-member-checkout complete render the same hero, subtitle ('...restore/manage option before checkout.'), 'Ad-free status active' chip and 'Payer and scope' rows as their pre-purchase frames. The member checkout complete frame is visually indistinguishable from the member checkout start frame above the fold; its only differing element is a bare card edge fully hidden behind the bottom navigation. No receipt reference, charged amount confirmation, or activation timestamp is visible on either screen, even though the receipt workflow demonstrates that a real receipt ID ('ADO-1042') exists in this data.
  - RequiredFix: Render a distinct post-purchase state for both checkouts showing the confirmed charge (120.00 USD / 4.99 USD), the receipt reference, entitlement activation and renewal date, and update the hero subtitle so it no longer says 'before checkout'.
- **[major] completion-banners-clipped-by-bottom-nav**: Every completion banner is sliced by the Home/Messages navigation bar
  - Workflows: ad-off-ad-suppression, ad-off-entitlement-status, ad-off-receipt-evidence, ad-off-settlement-utility
  - Description: 'Suppression verified' (ad-suppression complete), 'Settlement recorded' (settlement complete) and 'Receipt opened' (receipt complete) all render at the extreme bottom of the viewport with their glyphs bisected by the opaque white bottom navigation bar, and their supporting body text entirely off-screen. On entitlement-status complete only a coloured card edge is visible with no readable text at all. In each case the single element that marks the state is the element that is clipped, and for the settlement workflow this hides money-movement details such as the settlement ID.
  - RequiredFix: Scroll or inset the complete states so the confirmation banner and its detail line render fully above the bottom navigation, and include the outcome specifics (ID, amount, timestamp) in the banner body.
- **[major] requirement-prose-instead-of-product-copy**: Body copy asserts what the UI shows instead of showing it
  - Workflows: ad-off-community-checkout, ad-off-entitlement-status, ad-off-member-checkout, ad-off-settlement-utility
  - Description: Across every modal the detail rows are acceptance-criteria sentences rather than product copy: 'Payment — Payment method, renewal, utility allocation, and fee details are shown.' (community checkout), 'Payment method, renewal, receipt, retry, and cancellation are shown.' (member checkout), and the status chip caption 'Entitlement, receipt, renewal, restore action, and affected ad slots stay together without payment-screen context.' which appears on seven screens. Hero subtitles follow the same pattern ('Confirm payer, amount, entitlement scope, payment method, renewal, receipt, and restore/manage option before checkout.'). The result is that payment surfaces name a card, a renewal date and a fee without ever rendering any of them, while the receipt workflow proves those values exist ('card ending 4242').
  - RequiredFix: Replace the 'X, Y and Z are shown' rows with the actual values (payment instrument, renewal date, fee breakdown) and rewrite the status chip caption as user-facing copy about the entitlement, not about screen composition.
- **[major] duplicated-card-shells-and-truncated-subtitles**: Repeated generic card shells and one truncated subtitle reused across differently-purposed cards
  - Workflows: ad-off-ad-suppression, ad-off-community-checkout, ad-off-entitlement-status, ad-off-member-checkout, ad-off-receipt-evidence, ad-off-settlement-utility
  - Description: On the start screens, 'Entitlement record', 'Receipt history', 'Ad suppression' and 'Settlement and utility…' cards all share the identical chrome — right-aligned 'Minimized' chip, 'Tap for expanded view' chip, 'Ad-free account' chip, 'Expand' button — and all carry the same truncated subtitle 'Ad preference and sponsored-message beh…', which conveys nothing distinguishing. Card titles themselves truncate mid-phrase ('Settlement and utility…'). On the action modals, an identical 'Ad-free account controls' card with byte-identical 'Ad-free account' and 'Receipt or settlement' rows is appended to the community-checkout, member-checkout, entitlement, suppression, settlement and receipt sheets, consuming the lower half of each with no workflow-specific content.
  - RequiredFix: Give each card its own summary line drawn from its own data (latest receipt, suppressed slot count, settlement period), stop truncating card titles, and remove or heavily condense the duplicated 'Ad-free account controls' block on the modals.
- **[major] start-frames-open-mid-scroll**: Start states open scrolled past their titles, some on a dangling sentence fragment
  - Workflows: ad-off-ad-suppression, ad-off-community-checkout, ad-off-entitlement-status, ad-off-settlement-utility
  - Description: community-checkout start opens with the 'Ad-free controls' heading already sheared at the viewport top; entitlement-status start does the same; ad-suppression start opens mid-sentence on 'Confirm ad suppression status, 4.99 USD plan amount…'; settlement-utility start opens on the fragment '…the entitlement that caused it.' with no settlement heading anywhere on screen. In each case the workflow hero is scrolled away, leaving the generic 'Ad-Free Community' app bar as the only context cue, and on settlement start the most prominent control on screen ('Verify ad suppression') belongs to a different workflow entirely.
  - RequiredFix: Anchor each start state at the top of its workflow so the hero title and full description are visible, and ensure the workflow's own primary action is the prominent control on its start screen.
- **[major] empty-lower-half-on-suppression-and-settlement-start**: Large empty regions below the fold on two start screens
  - Workflows: ad-off-ad-suppression, ad-off-settlement-utility
  - Description: ad-suppression start and settlement-utility start both end in a single collapsed 'Local package details' row with a chevron, below which roughly a third to 40% of the viewport is blank navy with no content. On settlement start in particular no amount, settlement ID or period appears anywhere, despite '120.00 USD community plan, 96.00 USD utility allocation' being available one tap away in the action modal.
  - RequiredFix: Populate the lower area with the real suppression slot list and settlement summary (period, gross, allocated amount, status) instead of leaving dead space under a collapsed row.
- **[minor] prohibition-glyph-on-primary-ctas**: Primary CTAs render grey with a slashed-circle glyph, reading as disabled
  - Workflows: ad-off-ad-suppression, ad-off-community-checkout, ad-off-entitlement-status, ad-off-settlement-utility
  - Description: 'Start ad-free checkout', 'Verify ad suppression', 'Manage entitlement', 'Open receipt' and 'Review settlement' are all drawn as low-contrast grey pills prefixed with a ⊘ slashed-circle icon — the same prohibition glyph used elsewhere in the community as the ad-off mark. On a primary purchase or verification button this reads as blocked or unavailable, and no explanatory text accompanies it. The result is that no start screen presents an obviously enabled primary action.
  - RequiredFix: Give enabled primary actions filled accent styling and drop the slashed-circle glyph from actionable buttons; reserve it for genuinely suppressed or disabled states, which should carry a reason.
- **[minor] inconsistent-modal-palettes-and-low-contrast-heroes**: Three different modal palettes and low-contrast hero text
  - Workflows: ad-off-ad-suppression, ad-off-entitlement-status, ad-off-receipt-evidence, ad-off-settlement-utility
  - Description: The suppression sheet uses a desaturated green header and background, the settlement sheet a khaki/olive one, while checkout, entitlement and receipt sheets stay indigo — three colour identities within one community with no explanation. Separately, on every complete state the hero title and subtitle are tinted versions of the accent drawn directly on navy ('Ad-off entitlement record' in slate-blue, 'Ad-off settlement allocation' in olive, 'Ad suppression proof' subtitle in dim green), giving noticeably weaker contrast than the white card text immediately beneath.
  - RequiredFix: Standardise modal chrome on the community accent and raise hero title/subtitle contrast against the navy background to match the legibility of the card body text.
- **[minor] instrumentation-strip-visible-in-product**: 'In-focus product surface' scaffolding strip rendered above product content
  - Workflows: ad-off-receipt-evidence
  - Description: On ad-off-receipt-evidence start a black bar reading 'In-focus product surface' with an 'Expand' link sits pinned across the top of the content area, directly above the 'Ad-off receipt history' hero. This is harness/instrumentation language rather than anything a community member would be shown.
  - RequiredFix: Remove the 'In-focus product surface' strip from rendered product screens, or hide it outside capture/debug builds.
- **[minor] unexplained-read-only-state**: 'Read only' pill shown without a reason
  - Workflows: ad-off-ad-suppression
  - Description: On ad-off-ad-suppression start the 'Settlement and utility…' card exposes a 'Read only' pill with an eye icon next to 'Expand', and its truncated subtitle 'You can read the current details without changing …' cuts off before explaining anything. The user cannot tell whether the settlement is permanently read-only, read-only because of their role, or unlockable from the settlement workflow.
  - RequiredFix: State the reason the settlement card is read-only in this context and point to where the editable view lives, rather than showing an unqualified 'Read only' pill with truncated explanatory text.

## Data Portability Community (10 findings)

- **[critical-blocker] redacted-bundle-shows-full-bundle**: All three export-redacted-bundle screenshots are the export-full-bundle screens
  - Workflows: export-redacted-bundle
  - Description: B16_ext_export_migration_export-redacted-bundle_start/action/complete.png are byte-identical (sha256 6aacc2fc..., 1e8d1f12..., f1094ba2...) to the corresponding export-full-bundle files. Every one renders the heading or modal title 'Full export bundle', and the complete frame's banner body explicitly reads 'Full export bundle shows contents, redaction, checksum, file size, download progress, and'. The word 'redacted' never appears as a heading, state label, or outcome on any of the three frames. The redacted-bundle workflow therefore has no visual evidence whatsoever, and a user on these screens would correctly conclude they are in the full-bundle workflow.
  - RequiredFix: Navigate to and capture the actual redacted-bundle surface. Each frame must self-identify as the redacted bundle and show what distinguishes it from the full bundle: which fields were stripped, the redaction count, and the resulting reduced artifact size and checksum.
- **[critical-blocker] import-replay-shows-import-preview**: export-import-replay action and complete screenshots are the import-preview screens
  - Workflows: export-import-preview, export-import-replay
  - Description: B16_ext_export_migration_export-import-replay_action.png and _complete.png are byte-identical (sha256 3a1f8873..., dc822c23...) to the export-import-preview files of the same states. Both render 'Legacy import preview' as the title/heading, and the complete frame's banner reads 'Import replay ready' -- readiness, not a completed replay. No replay execution, no post-replay row outcomes, and no replay progress appear on either frame. Only the replay start frame contains replay-specific copy, and only inside a subordinate card ('then replay the import with retry and rollback available').
  - RequiredFix: Capture the distinct replay surface for the action and complete states: replay progress against checkpoint I-118 while running, and afterwards the rows applied vs skipped, the resolved duplicate decisions, and a completion timestamp. The preview workflow's complete banner should also say the preview was completed rather than announcing replay readiness.
- **[critical-blocker] action-state-shows-static-explainer**: Every 'action' screenshot is a static explainer sheet with no controls and no in-progress state
  - Workflows: export-checksum-evidence, export-full-bundle, export-import-preview, export-import-replay, export-protected-redaction, export-redacted-bundle, export-schema-listing, export-transfer-rollback, export-transfer-verification
  - Description: All eight action frames (full-bundle, redacted-bundle, transfer-verification, protected-redaction, transfer-rollback, import-preview, import-replay, schema-listing, checksum-evidence) are full-screen modals containing the workflow's numbered 'Export steps' list -- the same list already fully visible on the corresponding start frame -- followed by the identical boilerplate block 'Export workspace / Scope, redaction, checksum, destination, transfer status, rollback, and audit evidence are visible.' with 'Export scope' and 'Verification and recovery' sub-items. Not one contains a button, spinner, progress bar, percentage, or status chip in the visible viewport, and each ends on a duplicated row that restates step 1 ('Bundle contents', 'Destination', 'Rows preview', 'Schema list', 'Digest', 'Mismatch'). The modals also replace the 'Data Portability Community' app bar with a bare 'X', removing community context. The in-progress state of every workflow in this community is therefore undocumented.
  - RequiredFix: Capture the actual in-progress surface for each workflow after the primary action is invoked -- packaging progress, hash comparison in flight, redaction being applied, rollback restoring, scope being toggled -- each with a visible progress or status indicator and at least one control (cancel/abort). Retain the community app bar or an equivalent context label inside the modal, and remove the duplicated step-1 row from the 'Export workspace' block.
- **[major] cross-community-copy-leak**: Copy from other communities (HOA, youth sports) appears in the Data Portability Community
  - Workflows: export-import-replay, export-protected-redaction, export-transfer-verification
  - Description: The transfer-verification action and complete frames name the destination as 'Provider Cloud HOA' -- a homeowners association -- in a community with no HOA context. The protected-redaction action and complete frames describe the masked data as 'Protected youth profile, minor roster phone, care, vault, payment, and private notes' with policy copy reading 'Each masked field shows guardian or coach visibility policy and the persona allowed to reveal it.' Guardians, coaches, minors, and rosters are youth-sports concepts. The import-replay start frame carries the same leak in a card summary: 'Protected youth roster profile includes minor-da...'. The 'persona allowed to reveal it' phrasing is also inconsistent with this community's legacy flat schema, which has no persona system.
  - RequiredFix: Replace the borrowed fixture copy with this community's own domain terms: a data-portability destination provider name, protected field examples drawn from this community's schema (member contact, payment, audit fields), and a visibility-policy description that does not reference guardians, coaches, or personas.
- **[major] results-described-not-rendered**: Complete states assert outcome values that the screens never display
  - Workflows: export-full-bundle, export-import-preview, export-import-replay, export-protected-redaction, export-redacted-bundle, export-schema-listing, export-transfer-verification
  - Description: Every completion banner promises concrete artifact data that no frame renders. 'Export bundle generated' claims to show 'contents, redaction, checksum, file size, download progress' but no filename, size, checksum, or download button appears. 'Transfer verified' claims 'provider, hash match, receipt, timestamp' with no hash pair, provider ID, or timestamp on screen. 'Schema scope saved' claims 'include/exclude scope' with no table list or record count. 'Redaction applied' shows no masked-field count or before/after sample despite step 3 promising 'source label, exported safe value, and redaction count'. 'Import replay ready' claims 'duplicate decisions' with none recorded. Only the checksum workflow renders real values ('9A7F-PORT', 'file count 84', '18.4 MB'), and the rollback complete frame renders real outcome rows -- proving the pattern is achievable elsewhere.
  - RequiredFix: Render the actual result values on each completion screen -- artifact name, byte size, checksum, redaction count, matched hash pair, saved table scope, per-duplicate decisions -- following the pattern the checksum and rollback screens already establish, and offer the appropriate follow-on action (download, view receipt).
- **[major] completion-banners-clipped-by-tab-bar**: Completion banners are truncated mid-sentence by the bottom tab bar on six screens
  - Workflows: export-checksum-evidence, export-full-bundle, export-import-preview, export-import-replay, export-protected-redaction, export-redacted-bundle, export-schema-listing
  - Description: The banner that carries each workflow's outcome is the element being cut off. 'Export bundle generated' ends at 'download progress, and'; 'Transfer verified' is cut immediately after its second line; 'Redaction applied' shows only its title plus a half-height sliver of body text with glyphs sliced through the middle; 'Import replay ready' ends at 'checkpoint, retry and rollback'; 'Schema scope saved' ends at 'include/exclude scope'; 'Checksum verified' ends at 'verification status, and integrity'. On the start frames the same defect hits the 'Package progress' card, which is reduced to a bare title on full-bundle, redacted-bundle, schema-listing, and checksum-evidence, hiding the start action entirely.
  - RequiredFix: Add bottom padding equal to the tab bar height to the scroll container so terminal banners and the 'Package progress' card render fully, and scroll captures so the state-carrying element is entirely within the viewport.
- **[major] truncated-card-titles-and-repeated-shells**: Card titles truncate mid-word and minimized cards repeat one identical shell
  - Workflows: export-import-replay, export-protected-redaction, export-transfer-rollback, export-transfer-verification
  - Description: Card headings clip to 'Protected field routin...', 'Exportable custom-sc...', and 'Provider transfer roll...' while a 'Minimized' chip beside them consumes roughly a third of the row width. Each such card then renders the same fixed 2x2 grid -- 'Tap for expanded view' / 'Data package' / 'Expand' / 'Generate export' -- so the feed reads as one shell stamped repeatedly. Card summaries compound this: on export-protected-redaction_start both the 'Exportable custom-sc...' card and the 'Full export bundle' card carry the identical string 'Data package includes scope, protected fields, a...', and export-transfer-rollback_start reuses that same string for its 'Provider transfer roll...' card while export-transfer-verification_start gives that same card a completely different body ('Waiting for the export package to be prepared').
  - RequiredFix: Let card titles wrap to two lines or shrink the 'Minimized' chip so full titles render, and give each card a summary describing its own content rather than a shared boilerplate string.
- **[major] empty-lower-region-and-orphaned-chip**: Transfer screens end in a large empty region, with an unattached 'Waiting' chip on the rollback start
  - Workflows: export-transfer-rollback, export-transfer-verification
  - Description: export-transfer-verification_start and export-transfer-rollback_start both terminate in a collapsed 'Local package details' disclosure row followed by roughly 400px of uniform empty dark background before the tab bar -- the lower fifth of the screen carries no content where transfer or rollback status belongs. On export-transfer-rollback_start a bare 'Waiting' chip is additionally rendered floating at the right edge in the vertical gap between the 'Package progress' card and the card below it, attached to no container and with no adjacent explanatory label, which reads as a layout escape rather than a designed status indicator.
  - RequiredFix: Fill the lower region with the transfer/rollback status panel (or expand 'Local package details' by default), and anchor the 'Waiting' chip inside the card whose state it describes with an accompanying label explaining what is being waited on.
- **[minor] harness-chrome-and-inconsistent-nav**: 'In-focus product surface' harness strip is visible, and the bottom tab bar is inconsistent across screens
  - Workflows: export-checksum-evidence, export-full-bundle, export-import-replay, export-protected-redaction, export-redacted-bundle, export-schema-listing, export-transfer-rollback, export-transfer-verification
  - Description: Four start frames render a dark strip reading 'In-focus product surface' with an 'Expand' affordance directly above the page heading -- capture-harness labelling, not product UI, occupying the position where product context belongs. Separately the bottom tab bar is inconsistent: export-full-bundle, export-redacted-bundle, export-schema-listing, export-checksum-evidence, export-protected-redaction and the import screens show four tabs (Home / Documents / Admin / Messages) with the 'Messages' label clipped at the right edge on several, while export-transfer-verification and export-transfer-rollback show only three tabs (Home / Documents / Messages) with no Admin tab at all. export-protected-redaction_start also clips a row of 'Expand' and 'Generate export' buttons horizontally at the bottom edge, and opens on the severed fragment 'actor are preserved.' cut by the app bar.
  - RequiredFix: Remove the 'In-focus product surface' harness strip from captured screens, make the tab set consistent across all workflows in this community, and size the tab bar so no label is clipped at the right edge.
- **[minor] ambiguous-repeated-generate-export**: Multiple identical 'Generate export' buttons with no scope differentiation
  - Workflows: export-import-replay, export-protected-redaction, export-transfer-rollback
  - Description: export-import-replay_start shows three visually identical 'Generate export' buttons in one viewport: one full-width button below the 'Package progress' card, and one inside each of the two minimized cards ('Protected field routin...' and 'Exportable custom-sc...'). export-protected-redaction_start and export-transfer-rollback_start repeat the pattern. Nothing labels which records each button would export, so the outcome of tapping any of them is unpredictable. Relatedly, export-transfer-rollback_complete leaves a 'Rollback' button enabled alongside a 'Rollback ready' chip on a screen whose body copy already states 'destination status is marked rolled back', with no indication whether pressing it repeats the rollback or is a no-op.
  - RequiredFix: Scope-label each export action (e.g. 'Generate full bundle', 'Generate this package') or collapse them to a single primary action, and either disable the post-completion 'Rollback' button with a reason or relabel the status chip to a completed state consistent with the body copy.