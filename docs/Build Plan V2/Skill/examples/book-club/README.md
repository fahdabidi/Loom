# Book Club Example Extension

Status: Phase B14 screenshot-backed workflow example.

This example grows during Phase B2. Phase B1a seeds a minimal local package and initialization package
for sideload/install; Phase B1b adds hosted publish/install fragments.

Covered workflows:

- Book nomination schema.
- Voting workflow.
- Meeting event.
- Discussion thread.
- Search/digest integration.
- Export metadata.

## UI Evidence

Phase B14 validates the Book Club workflows through visible Demo App UI on Android emulator
`emulator-5554` instead of only direct backend calls. Evidence is recorded in
`docs/Build Plan V2/Evidence/B14/workflow-ui-evidence.json` with 7 Book Club workflows and
start/action/completion screenshots under `docs/Build Plan V2/Evidence/B14/screenshots/`.
