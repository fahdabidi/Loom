# Payment Surface

Use `PaymentSurfaceProps` for Loom-owned checkout surfaces.

## Extension Use

- Delegate dues, donations, reservations, and ad-off checkout to the shell.
- Display shell-owned payment results from wallet contracts.
- Do not embed custom payment collection UI inside extensions.

## Validation

- `vt_payment-surface_shell-owned` proves the shell-owned payment invariant.
