# Ad Campaign Service

Use `CommunityAdCampaignApi` to define eligible sponsor campaigns for approved Loom ad slots.

## Extension Use

- Create campaigns for declared shell or stream slots only.
- Keep sponsor targeting out of protected data.
- Let the ad decision service select eligible campaigns.

## Validation

- `vt_ad-campaign_setup` proves active campaign lookup by community and slot.
