# Ticket status: ballot deadline + expiry reminder notification

## Item 1 of 1: deadline display + reminder banner
Status: blocked
If blocked: production deadline/reminder wiring is analyzed (`No issues found!`), but the two required isolated reminder widget fixtures/tests have not been added in this turn, so the whole-file `flutter test` run and its `+N -0` summary cannot yet be produced.
